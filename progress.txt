# O+V Strategic Progress Ledger
# Git-tracked operator-legible record (the Ralph pattern, Slice 202/205).
# Advisory — authority stays with the gates + the operator. Not auto-signed.

## COMPLETED (this arc — 192-205, all merged to main)
  [x] slice-192  proactive transport-hedge hierarchy (RT vs batch race)
  [x] slice-193  sovereign telemetry registry (durable mmap counters)
  [x] slice-194  race-triage + cross-model rotation (dual-arm recovery)
  [x] slice-195  adaptive horizon governor (end of the 360s magic number)
  [x] slice-197  M10 autonomous graduation contract (operator-delegated)
  [x] slice-198  sovereign ignition (cadence + protection arming)
  [x] slice-199  sovereign tooling (gh + HTTPS-token PR shipping, no host SSH)
  [x] slice-200  genesis proposal (deterministic end-to-end ship proof) -> PR #69440 MERGED
  [x] slice-201  contextual bandit routing advisor (Thompson Sampling)
  [x] slice-202  strategy bootstrapper + operator-signer (refused self-signing)
  [x] slice-203  strategy simulator (telemetry-driven proposal) -> PR #69445 MERGED
  [x] slice-204  chronos continuity matrix (non-volatile, honest uptime ledger)
  [x] slice-205  state portability (operational ledgers travel on migration)
  [x] slice-206  boot-warmup lifecycle + honest starvation reclassification (warmup_lag)
  [x] slice-207  class-level loop guard: SemanticIndex.build() self-offloads if called sync on the loop
                 (immunizes ANY caller, current/future — not a localized patch)
                 NOTE: 206/207 MISDIAGNOSED — the 25s freeze is GIL contention (CPU build in a thread),
                 not on-loop execution. Real fix = process-isolation (JARVIS_COMPUTE_ISOLATION_ENABLED);
                 helped 23s->13.5s but NOT fully eliminated (a direct cold-start caller remains). Open.
  [x] slice-208  epistemic-integrity: SemanticGuardian now detects metric-gaming DECEPTION (not just AST
                 mutation) — self-signing, counter-suppression, chronos-laundering -> HARD/APPROVAL_REQUIRED.
                 HONEST SCOPE: pattern detectors raise friction + flag; NOT "un-gameable" (obfuscation evades).

## NEXT TARGETS (operator-gated — the engine is built; these need runtime + judgment)
  [ ] runtime       leave the soak UNTOUCHED to accrue continuous unsupervised days
  [ ] host          keep Mac awake (caffeinate) + Docker autostart, OR migrate to GCP when billing on
  [ ] direction     author + sign real strategic goals (.jarvis/roadmap.yaml via strategy_signer)
  [ ] starvation    the organism's own #1 self-identified priority: eliminate control-plane starvation
  [x] slice-212  runtime attestation: image stamped (commit+dirty) at build; boot compares vs
                 operator pin -> DEPLOYMENT_INTEGRITY_MISMATCH fail-closed on drift (stale rebuild /
                 dirty tree / unstamped). Born from the LIVED 2026-06-10 failure: a dirty compose
                 blocked ff-merge and the "211 rebuild" silently ran 208 code. UNPINNED=warn-only.
  [x] slice-213  native orchestration kernel: bash launcher -> typed/tested/async Python
                 (lifecycle_kernel.py, HOST-side). Post-launch attestation verification IS the
                 launch contract (exit 0 only on ATTESTED_MATCH — phantom deploys can't exit 0).
                 REFUSED: in-container self-cycling (docker-socket = host escape) + auto-reload
                 on self-verified patches (S208 = friction not proof; operator merge stays the
                 deploy boundary).
  [x] slice-215  FrugalGPT per-round cascade: Claude RE-ENABLED as per-round rescue tier
                 (DW-primary rounds, Claude rescues the failed round -> ~99.7% round survival vs
                 0.7^n chain death). Topology defaults already encode the charter (IMMEDIATE/
                 COMPLEX cascade_to_claude; BACKGROUND/SPECULATIVE skip_and_queue = never Claude).
                 Rescue budgets 180s/120s. COST: session cap 25->10 (bounds restart-reset
                 loophole), sentinel COST_CAP_90 Discord alert armed, thinking IMMEDIATE=0,
                 response cache dedupes. Bandit state reset for the new Claude arm (cold-start).
                 L2 PROBE PROVED: goal->reader(valid)->decomposition(valid, 2 sub-goals)->
                 envelope INTO router. Remaining break: orchestration 'no_plan' = planning needs
                 reliable LLM capacity = exactly what this slice supplies.
  [x] slice-216  concurrency kernel: JARVIS_MULTI_STEP_ORCHESTRATION_ENABLED on (the no_plan
                 root cause = 4th cut wire); closed-client message-keyed recycle (_is_closed_client_error
                 -> _recycle_client instead of doomed backoff); fallback concurrency -> cap 4 (D2/12F-B
                 fast-fail confirmed live). KNOWN ISSUE deferred: tool_call_without_tool_loop on
                 IMMEDIATE rescue (loop IS wired at construction; conditional branch needs careful
                 repro — no rushed patch on the generation gatekeeper). PRD §51.11.32 documents the arc.
  [x] slice-217  THE 5TH CUT WIRE FIXED (verify-first; declined the singleton-registry refactor):
                 GLS._intake_router was NEVER assigned -> roadmap daemon got None -> multi_step emit
                 was a DRY-RUN ("router not provided") -> GOAL-001 sub-goal envelopes evaporated, WAL
                 frozen 4 days, zero dispatch. Fix = ONE guarded line in IntakeLayerService.start():
                 publish the consumed router onto the GLS the daemon reads. DECLINED: process-safe
                 singleton (incoherent cross-process; over-engineering for a missing assignment) +
                 sleep-sync queue-bypass (Chronos sleep-detect already honest; real fix = non-sleeping
                 host, not code). HOST NOTE: Mac slept overnight (chronos_sleep_event) -> laptop != soak host.
  [x] slice-218  arm the EXISTING multi-model verification matrix (verify-first: NOT a new
                 circuit). One missing flag: JARVIS_SCHEMA_DRIFT_ROTATION_ENABLED -> schema_invalid
                 (tool_call_without_tool_loop) demotes the model + rotates to next ranked. Declined
                 the duplicate schema-guard/demotion-circuit build (race_triage + schema_drift_tracker
                 + PromotionLedger Zero-Trust quarantine + cortex timeout + bandit ALREADY do it).
                 PREMISE CORRECTED: Nemotron does NOT jump into COMPLEX — the prove-it PromotionLedger
                 quarantines new models to SPECULATIVE until 10 clean ops; DeepSeek-V4-Pro/Qwen-397B
                 (proven, ranked) carry COMPLEX while Nemotron earns trust.
  [x] slice-220  Aegis session-TOKEN-expiry root cause (verify-first; declined the plan's
                 session-factory/pool-reconnect as fixing a non-existent socket bug). The real bug:
                 session token TTL default 3600=1h + single-use PSK + no /session/refresh -> after 1h
                 every Claude call = 403 psk_already_consumed -> fallback_failed -> EXHAUSTION (NOT
                 sleep, NOT sockets; HTTP session already self-heals via _ensure_http). Fix = the
                 DESIGNED knob (client.py:14-17): JARVIS_AEGIS_SESSION_TOKEN_TTL_S=7776000 (90d, covers
                 §41.6 evidence window). DURABLE follow-on = build the /session/refresh endpoint
                 (Slice-3 TODO) for truly-infinite uptime beyond any TTL.
  [x] slice-221  STRATEGIC TARGET TRACE (verify-first KILLED two phantom slices first: the
                 'topology bypass' didn't exist — immediate dw_allowed=False loads correctly; and the
                 failing IMMEDIATE ops were TestFailure sensor noise, NOT GOAL-001 which routes
                 COMPLEX/BACKGROUND). ROOT CAUSE FOUND: GOAL-001::file-00 held hostage by STALE
                 goal_decomposition_ledger.jsonl (Jun-10 dry-run era, pre-S217) recording
                 status=proposed/'emitted' for an emission that never ingested -> _mark_emitted_via_
                 goal_decomposition sees already-emitted -> never re-emits -> emit_outcomes=[] forever.
                 FIX: archive BOTH stale ledgers (multi_step + goal_decomposition) + clean-slate rebuild.
                 QoS BLUEPRINT (designed, not built): partition intake into QoS-0 (sensor/health noise,
                 throttleable) vs QoS-1 (operator-signed roadmap targets, dedicated un-starvable slots)
                 — intake_priority_queue already has priority buckets; the gap is per-CLASS worker
                 reservation so an IMMEDIATE timeout storm can't consume all execution slots. Build
                 trigger: IF post-cleanup file-00 dispatches but starves behind sensor ops.
  [x] slice-222  quiet the self-DDoS: TestFailure sensor thrashing on 3 KNOWN pre-existing
                 failing tests -> constant IMMEDIATE ops -> all 3 workers saturated -> GOAL-001
                 (priority 4) starved 40min in queue. Soak config: poll 1/day + fs-events off.
                 NOT gaming (failures documented pre-existing; changes WHEN we look). QoS build
                 remains the durable fix if starvation persists.
  [x] slice-223  headless sensor demotion (10A pattern generalized; verify-first DESCOPED the
                 plan's two-lane pool + 'cryptographic firewall'): BG/SPEC already barred from Claude
                 (topology skip_and_queue); the leak was test_failure->IMMEDIATE->Claude-direct. New
                 Priority 0.75: headless + test_failure -> STANDARD (DW primary, rescue preserved).
                 Source-discriminated (S208-safe: not a self-elevatable label). Two-lane QoS pool
                 build stays gated on the b5nai4cms watch verdict (starvation with noise quieted?).
  [x] slice-224  token-economic accounting fabric: (1) P2a prompt-prefix cache COMPLETED —
                 builder logic pre-existed (7 tests green) but NO call-site passed the sink
                 ('on_without_sink_falls_back'); wired generate() -> _assemble_codegen_prompt ->
                 fold into cached system blocks (concurrency-safe local). Tool catalog + schema
                 now ~90% cheaper on cache hits. (2) accounting_ledger.py + /spend verb + CLI:
                 day x provider(inferred) x route rollups over aegis spend WAL incl. backups.
                 (3) JARVIS_AEGIS_HOURLY_BURN_CAP_USD=2.00 pinned (rate rail). Honest: provider
                 attribution is op-id heuristic, LABELED inferred; consoles stay authoritative.
  [x] slice-225  PRD §51.11.34 (v3.27): the Soak Reliability + Token-Economic arc (217-224) +
                 the next-slices roadmap. Five-layer onion documented; audited money verdict (~$1.90
                 not $26 -> ~$24 outside audit trail); cost stack complete; BLUNT truth recorded
                 (cost solved, file-00 still won't dispatch = NON-economic autonomy blocker).
                 ROADMAP: A1 trace file-00 dispatch (milestone gate) is the #1 next action.
  [x] slice-231  Telemetry-Driven Budget Synthesizer (Dynamic Allocation Kernel). ROOT FIX for
                 the file-00 dispatch blocker: the per-route generation deadline was a STATIC
                 lookup blind to live provider state, so a priority:critical roadmap goal routed
                 IMMEDIATE (120s reflex window) and, when Claude was out of credits, the DW reroute
                 (Slice 127 P2.1) inherited a too-short deadline -> deadline_exhausted_pre_fallback
                 x23 before reaching the hardened DW agentic path. FIX: provider_availability.py
                 (read-only ProviderAvailabilitySnapshot collector over the EXISTING
                 claude_circuit_breaker + dw_surface_health state, fail-soft, no probe consumption);
                 urgency_router.synthesize_generation_timeout LIFTS an IMMEDIATE tool-loop op to the
                 COMPLEX 240s window when Claude is down (the load-bearing dispatch lever) +
                 synthesize_budget_profile / context_budget_profile for honest §7 telemetry. Wired
                 at orchestrator.py GENERATE _gen_timeout + both emit_decision stamp sites. Master
                 JARVIS_BUDGET_SYNTHESIS_ENABLED default-TRUE; OFF = byte-identical legacy (proven).
                 45 TDD tests green; zero new regressions (urgency_router/slice208/antivenom clean).
                 LIVE-SOAK VALIDATED (jarvis-dw-cortex-soak, attested a301c38941): synthesizer
                 fires correctly (reads claude=breaker_open_economic + dw=transport_degraded
                 read-only, DW-primary budgets). DW-only autonomy works (ops COMPLETE on DW w/
                 Claude unfunded). FOUND: gen_timeout 240s lift fired 0x — cold-start TIMING gap
                 (breaker boots CLOSED, trips mid-GENERATE; signal absent at pre-dispatch lift). → S232.
  [x] slice-232  Cold-start provider-funding signal (RC1 of the soak findings). The collector now
                 derives Claude funding from the AUTHORITATIVE persisted breaker state at op #1 —
                 reusing claude_circuit_breaker._read_breaker_state (TTL-aware, fail-soft), INDEPENDENT
                 of the persist ENABLE flag (provider_availability._persisted_claude_economic_down,
                 cached/non-blocking, in-memory OPEN keeps precedence, missing/stale/corrupt → graceful
                 available). AND the economic-funding RECORD write is now flag-independent too
                 (_maybe_persist_open/_maybe_clear_persisted decoupled from the flag) so the signal
                 stays fresh from cold boot — the §33.1 gate still guards the behavior-changing
                 auto-boot-OPEN restore (test_no_restore_when_disabled preserved). Closes the S231
                 timing gap: the 240s lift now fires for a GOAL-001-shaped op from op #1. 11 TDD tests
                 (cold-boot-persisted-OPEN / no-state / trip-during-generate / fail-soft / record-
                 maintenance); zero new regressions (test_snapshot_shape failure is pre-existing S147
                 drift). RC2 (DW tool-loop non-convergence) tracked separately.
  [x] slice-233  Parse-gate enforcement of convergence (RC2 of the soak findings). The soak proved
                 the 3-axis convergence machinery (Slice 85 cumulative + Slice 3E nudge + grace round)
                 FIRES but is ADVISORY — the model emits read-only nav on the grace round, the loop
                 returns the patch-less raw, op fails. FINDING (premise-correction): this loop
                 advertises tools as PROSE + parses tool_use from TEXT (generate_fn:(prompt)->str) —
                 NO structured tools= array at the boundary to strip. So the absolute invariant is
                 enforced at the PARSE gate: tool_executor._slice233_enforcement_action (pure) →
                 once _final_nudge_issued, a PURE read-only round is REJECTED (reuses
                 _READONLY_EXPLORATION_TOOLS — no new taxonomy), model gets a hard denial via the
                 proven current_prompt-append+continue path, bounded by _enforcement_rounds_cap
                 (env JARVIS_TOOL_LOOP_ENFORCEMENT_ROUNDS default 2) AND remaining budget vs
                 final_write_reserve_s. Resilient: a valid patch (non-tool response) returns upstream
                 at parse_fn=None BEFORE the gate (honored mid-enforcement); progress/mutation tools
                 → FINALIZE (not suppressed); cap/budget spent → clean fail. 11 TDD tests
                 (ENFORCE / NORMAL / cap-reached / no-budget / progress-present / env-cap / zero-cap
                 opt-out + 3 wiring AST pins). slice3e+slice85 convergence regression green; the 3
                 test_tool_loop_budget_plan failures are pre-existing (fail identically stashed).
  [x] slice-234  Runtime-aware repo-root resolution (the final GOAL-001 blocker). RE-SOAK of
                 231+232+233 PROVED the budget root cause SOLVED: GOAL-001 routed IMMEDIATE w/
                 DW-primary {1.0,0,180s} (was {0,0,0}), Slice232 persisted signal fired
                 (breaker_open_economic_persisted), DW generated the patch (outcome=ok x9),
                 REVIEW=APPROVE, GATE→OrangePR, reached APPLY — then failed ONLY at APPLY:
                 "[Errno 2] No such file: '/Users/.../semantic_index.py'" (host abs path inside a
                 /app container). ORIGIN (diagnosed, not symptom-patched): harness.from_env sourced
                 repo_path from JARVIS_REPO_PATH (host path injected via .env env_file)
                 UNCONDITIONALLY → fed the op-envelope repo_root (harness.py:2403/2805/2918) →
                 ChangeEngine priority-1 per-request write root → APPLY joined relative targets
                 against the host path. FIX: harness._resolve_runtime_repo_root — honor
                 JARVIS_REPO_PATH ONLY if it exists+plausible in THIS runtime (carries
                 backend/core/ouroboros), else derive from the code's own on-disk location
                 (container-safe, no .git needed), else .git walk-up, else FAIL LOUD. Same
                 authoritative root fixes OrangePR git discovery. 7 TDD tests (host-valid-env /
                 container-stale-env-rejected / env-not-a-repo / no-env-code-location /
                 unresolvable-fail-loud / empty-env / from_env-integration). 15 existing harness
                 tests green. gen_timeout-lift wiring (S231 lever masked by autarky) deferred to a
                 separate follow-up slice per operator.
  [x] slice-234b RE-SOAK after S234 proved it NECESSARY but not sufficient: GOAL-001 again routed
                 IMMEDIATE w/ DW-primary 180s (breaker_open_economic_persisted), generated,
                 REVIEW=APPROVE, reached APPLY — then STILL "[Errno 2] /Users/.../semantic_index.py".
                 2nd source: JARVIS_REPO_PATH is STILL the host path in the container (env_file: .env
                 leaks it) and ~8 modules read it DIRECTLY (auto_action_router.py:1193 et al), joining
                 absolute APPLY targets against it — bypassing the S234 harness resolver. FIX (config,
                 not code): docker-compose.dw-cortex-soak.yml `environment:` now sets JARVIS_REPO_PATH=
                 /app (+ sibling reactor/prime=/app), winning over env_file, so the container reflects
                 its real /app layout. roadmap.signed.yaml carries ZERO host paths (ruled out).
                 FOLLOW-UP (tracked, separate slice): promote _resolve_runtime_repo_root (S234) to a
                 SHARED governance resolver + route all ~8 direct JARVIS_REPO_PATH reads through it
                 (validate vs runtime FS, fail loud) — context-independent of the env. NOT done here.
  [x] slice-235  Adaptive diff (re-enable native 2b.1-diff) — LAYER-5 fix, FULLY WIRED.
                 AUDIT: the full 2b.1-diff machinery already exists (providers.py _SCHEMA_VERSION_DIFF
                 :219, _DIFF_CANDIDATE_KEYS :222, _apply_unified_diff :1488, validate_diff_context +
                 StaleDiffError :1343/1373) and schema_capability plumbing is complete end-to-end
                 (brain_selector cfg → GLS:2800 → routing_intent → provider:4884). ROOT CAUSE: the flag
                 is DEAD-CODED — providers.py:4884-4893 computes _force_full from schema_capability then
                 passes hardcoded force_full_content=True (:4912/:4926); doubleword_provider.py:1583
                 forces True unconditionally → DW emits 60K full_content blob → JSONDecodeError.
                 LANDED: pure keystone should_force_full_content() + _diff_schema_threshold_lines()
                 (env JARVIS_DIFF_SCHEMA_THRESHOLD_LINES default 800) in providers.py — full UNLESS
                 (schema_capability==full_content_and_diff AND file_lines>threshold). 7 TDD tests green.
                 REMAINING (mapped, own focused build): (1) wire keystone into both providers (replace
                 hardcoded True at providers.py:4912/4926 + doubleword_provider.py:1583; compute
                 target_line_count fail-soft); (2) brain_selection_policy.yaml: schema_capability:
                 full_content_and_diff for ELITE agentic brains (config, no hardcoded model in code);
                 (3) fail-safe degradation at orchestrator:6245 (diff_apply_failed/StaleDiff → flag op
                 force_full_content + retry full_content, never crash); (4) soak. OPEN: can elite models
                 (Kimi/DeepSeek-V4-Pro/GLM) do verbatim diffs where the 397B couldn't — fail-safe makes
                 it safe to test live. Branch perf/slice-235-adaptive-diff.
  [x] slice-235b WIRING COMPLETE (gen-core, careful): keystone should_force_full_content +
                 _diff_schema_threshold_lines (env JARVIS_DIFF_SCHEMA_THRESHOLD_LINES=800) + the
                 decoupled seam resolve_force_full_content / resolve_diff_capability_for_model
                 (env JARVIS_DW_DIFF_CAPABLE_FAMILIES default moonshotai,deepseek-ai,zai-org — the
                 397B EXCLUDED; reuses dw_catalog_client.parse_family) / _max_target_line_count.
                 WIRED: doubleword_provider.py:1583 (was unconditional True) + providers.py Claude
                 block (the computed-but-IGNORED _force_full now USED — both lean+full prompt builds)
                 → native 2b.1-diff emitted only when (diff-capable elite family AND file>threshold).
                 FAIL-SAFE: op_context.force_full_content_override field (on OperationContext@881,
                 carried by advance/replace) + orchestrator GENERATE_RETRY governor (diff_apply_failed/
                 stale_diff/validate_diff → set override + 'emit full file' feedback → retry degrades
                 to full_content, never crashes/loops). Elite DW models are CATALOG models (not yaml
                 brains) → capability via env families, not brain_selection_policy.yaml. 19 TDD tests;
                 slice208 + op_context replace green; pre-existing failures unrelated (urgency source-
                 table drift, sandbox_loop missing module). NOT YET SOAK-VALIDATED — open empirical Q:
                 do elite models clear the verbatim-diff wall (fail-safe makes it safe to test live).
  [x] slice-235b SOAK-VALIDATED live (image cfa9e89fa1): elite models DO clear the verbatim-diff
                 wall — `emitting 2b.1-diff` fired 14x on moonshotai/Kimi-K2.6 + deepseek-ai/
                 DeepSeek-V4-Flash. The open empirical Q is answered YES. (see slice-236)
  [x] slice-236  Capability-aware DW selection (the "sixth layer"). RC of the s235 soak: the
                 IMMEDIATE->DW reroute escaped the sealed Claude-only route but fell to the
                 UNCONDITIONAL self._model baseline (Qwen-397B, not diff-capable) -> gate forced
                 full_content -> 63K blob -> JSONDecodeError -> exhaustion. Fix in the SELECTOR (not
                 gate/allowlist): providers.py pure seams op_warrants_diff_capable_model (reuses the
                 gate's _max_target_line_count + _diff_schema_threshold_lines -> no size-signal
                 drift) + select_diff_capable_model (ranks injected catalog by diff-capable family +
                 JARVIS_DW_FAMILY_PREFERENCE; no model names in code) + capability_aware_default_model;
                 doubleword_provider._capability_aware_default wired into the self._model baseline
                 fallback ONLY (sentinel/topology overrides untouched). JARVIS_DW_CAPABILITY_AWARE_
                 SELECTION_ENABLED default-TRUE; OFF/small/no-elite/already-capable byte-identical.
                 25 TDD tests, +0 regressions. LIVE-SOAK (16min,$0.34,0 Claude): selection log 45x,
                 elites now dominate (Kimi 66 + deepseek-V4 45 vs Qwen 44), JSONDecodeError 1 (was 6).
                 OPEN: a SEVENTH layer (tool_loop_deadline_exceeded on heavy multi-file GOALs = the
                 Slice-233 convergence problem at scale) blocks clean large-GOAL state=applied -> next.
  [x] slice-226  PRD split (the 2.2MB doc exceeded GitHub's ~1MB render cap). 4 byte-conserved
                 files (only +2109b of cross-links, zero content lost): PRD.md (lean §1-§23+TOC, 322KB,
                 renders) + PRD_SPEC_DETAIL.md (§24-50) + NORTH_STAR.md (§51 + §51.11.x + active roadmap)
                 + PRD_HISTORY.md (version headers + §40/41 banners). All <770KB.
  [x] slice-237  Op-weight-scaled convergence (the seventh layer). scale_convergence_threshold +
                 scale_final_write_reserve: heavy multi-file GOALs force convergence earlier AND hold
                 a bigger final-write reserve, both scaled to target-file line count (no hardcoded
                 round count). Reuses the Slice-85/233 machinery. 27 TDD. MERGED #69476.
  [x] slice-238  Cascade consults the economic breaker (the eighth layer). DW transient hiccup no
                 longer cascades into the credit-dead Claude lane (terminal_quota poison). should_
                 cascade_to_claude + central _call_fallback guard reuse the read-only _claude_breaker
                 _open. SOAK: terminal_quota 60->0, BadRequestError 23->0. 13 TDD. MERGED #69477.
  [x] slice-239  Adaptive test-sharding (the ninth layer). TestCoverageEnforcer decouples "generate
                 tests for N files" into a SEPARATE background intake op (UnifiedIntakeRouter WAL)
                 instead of ballooning the primary patch. 239b wired the LIVE plan_runner seam (the
                 orchestrator copy was dormant). 22+1 TDD. MERGED #69478/#69479.
  [x] slice-240  Dynamic cost-vs-bandwidth shard trigger (Phase 2). Stripped the hardcoded >2-file
                 gate -> (Files x Est_Tokens_per_file) > (Velocity x Budget_Remaining); tokens from
                 ACTUAL file sizes, ratios env-tunable. Phase 1 (byte-offset stream healer) REFUSED:
                 architecturally impossible vs a stateless SSE endpoint. 31 TDD. MERGED #69480.
  [x] slice-241  T1: GENERATION_TIMEOUT classification (stop blaming DW for OUR budget). tool_loop_
                 deadline_exceeded was mislabeled LIVE_TRANSPORT -> falsely degraded DW health +
                 severed the lane. New non-transport FailureSource (weight 0.0); the ==LIVE_TRANSPORT
                 degrade/sever consumers auto-skip it. 11 TDD. MERGED #69481.
                 T2: graduated the RT-vs-batch transport hedge to default-ON (dual-stream arbitrage).
                 Race RT(fast) vs batch(stable), first-success wins + loser cancelled; RT rupture
                 swallowed -> batch returned seamlessly (rupture-pivot); prefer_fast holds batch
                 speculative so RT exploration clears the Iron Gate. Honest limit: cannot save a
                 WHOLESALE DW outage (both arms fail). 63 TDD. MERGED #69482.

  ==========================================================================================
  ||  O+V SOVEREIGN PIPELINE -- FLAG PLANTED (2026-06-14). Slices 235->241, 18 PRs merged.
  ||  ------------------------------------------------------------------------------------
  ||  Objective: make DoubleWord the reliable PRIMARY for Ouroboros+Venom (Claude is
  ||  economically dead AND more expensive) -- O+V completes real autonomous work on DW
  ||  alone. Every poison metric driven to ZERO and held across soaks:
  ||      tool_loop_deadline_exceeded -> 0   deadline_exhausted -> 0
  ||      terminal_quota -> 0                dead-Claude BadRequestError -> 0
  ||      anthropic API calls -> 0           $ spend under outage -> $0.00
  ||  Capabilities sealed: bounded 2b.1-diff emission; op-weight convergence + reserve;
  ||  cascade respects the economic breaker; cost-driven async test-sharding to the WAL
  ||  intake; honest GENERATION_TIMEOUT vs transport labels; dual-stream RT/batch hedge.
  ||  Final soak (cf2513b407): total DW outage (46 DoublewordInfraError, both transports);
  ||  hedge raced 14x + cleanly abandoned all 14; 0 poison, 0 Claude, $0.00 -- clean
  ||  degradation under total external failure.
  ||  The governance layer is structurally complete. The remaining gap to a heavy-GOAL
  ||  state=applied is DW's OWN external generation reliability (infra), NOT governance
  ||  code -- when DW was healthy (s237b window) elites served + diffs emitted + ops
  ||  progressed. Capstone observation is gated on a healthy DW window, not on more
  ||  slices. ZERO further governance slices required. O+V SOVEREIGN INFRA OPERATIONAL.
  ==========================================================================================

Slice 242 (MERGED #69484, main ca4e4a5366): Adaptive Statistical Recovery-Duration Prior + Resurrection Soak.
  RecoveryDurationPrior (dw_transport_recovery.py): bounded deque ring of dark-window durations (enter_hibernation->wake) + linear-interp quantile -> first_probe_interval(p25), fallback to static below min_samples. Pure/env-driven/thread-safe/NEVER raises. Knobs JARVIS_RECOVERY_PRIOR_{WINDOW=20,QUANTILE=0.25,MIN_SAMPLES=3,FLOOR_S=1.0}.
  HibernationProber wiring: _first_probe_delay() reads prior; _record_outage_duration() banks on wake. Master JARVIS_RECOVERY_PRIOR_ENABLED default-TRUE (=0 byte-identical static-5s). Online/training-free (NO ML/NN - exogenous vendor outage, no features): times WHEN to probe, never predicts WHEN DW returns.
  Phase 3 resurrection injection soak: deterministic inject-outage->backoff->wake->record->adaptive-next proof (live soak cannot force deterministic DW outage). NO mid-stream serialization fiction; _replay_wal untouched. 75 tests green (14 new). Reused existing prober/recovery modules - no parallel Grid Sentinel.

Slice 243 (stability gate): Adaptive Grid Stability Matrix & Micro-Streaming Flap Mitigation.
  Eradicated naive wake-on-first-ping. HibernationProber now runs a Stability Confidence Gate: on ping UP -> VERIFYING_STABILITY -> _verify_grid_stability() runs a Micro-Streaming Load Test (provider.stream_health_probe) requiring N consecutive clean streams (JARVIS_GRID_STABILITY_STREAM_CHECKS default 1). Mid-flight rupture/incomplete -> FLAPPING_GRID_DETECTED, wake ABORTED, back to RecoveryDurationPrior backoff (S242), WAL intent untouched, NO false outage banked. Master JARVIS_GRID_STABILITY_GATE_ENABLED default-TRUE (=0 byte-identical wake-on-ping). Providers w/o stream_health_probe (Prime/Claude) trust the ping (legacy).
  DoublewordProvider.stream_health_probe(): micro-stream against /chat/completions stream=true, capped max_tokens=16, ~$0; reuses session/Aegis-auth/lease/SSE-read (no duplicate transport); True iff >= JARVIS_GRID_STABILITY_MIN_TOKENS (default 2) content tokens + clean close; rupture/non-200/too-few -> False; NEVER raises. 14 new tests (8 gate + 6 DW probe), 96 green total incl. existing prober(23)/recovery/WAL/hedge. wake_from_hibernation strictly gated behind 100% stream verification -> no network thrashing.

Slice 244 (priority dispatch — AUDIT + PROOF, NOT a rebuild): premise 'WAL processed as sequential single-lane FIFO' is FALSE at every layer. Verify-first audit found priority dispatch ALREADY EXISTS:
  - INTAKE: unified_intake_router enqueues onto asyncio.PriorityQueue via _compute_priority (lower int=higher primacy); test_coverage in _PRIORITY_MAP_DEFERRED (base 99->~100, lowest tier, Slice 239 decoupled); primary GOAL sources (voice_human/backlog/roadmap/test_failure) at 0-6.
  - BACKGROUND POOL: BackgroundAgentPool._queue is ALSO asyncio.PriorityQueue keyed on provider_route (immediate=1,standard/complex=3,background=5,speculative=7); test_coverage->background(5,last), GOAL->immediate/standard(1/3,first). Decoupled pool already exists. Slice 240 should_decouple_test_gen already budget/velocity-gates test-gen.
  Building the requested float-weight matrix (1.0/0.5)+scheduler+bg-lane would DUPLICATE all of it. Instead: PROOF test (test_slice244_priority_dispatch.py, 7 tests) locking Phase 4 invariant at BOTH lanes (inject test,test,GOAL out of order -> GOAL dequeues first, shards last) + Phase 3 Iron-Gate-deferral-safety (per-op gate vs cross-op intake ordering: reordering distinct intents never touches a single op's GENERATE->VALIDATE sequence; test_coverage is a Slice-239-decoupled distinct intent, never a precondition). Fixed stale 'FIFO ordering' docstring in background_agent_pool.py (the misleading wording that seeded the false premise). NO duplicate machinery built. 2 pre-existing test_bg_readonly_cascade failures (providers.py drift + topology-skip) proven unrelated via stash.

Slice 245 (Absolute-Primacy Re-Ingest Matrix): hibernation SURVIVORS (ops that failed all_providers_exhausted during a dark window — today terminal at bg_pool) get re-ingested with absolute-max primacy on wake.
  HONEST SCOPE (2 corrections, like 242/243): (1) did NOT eradicate pool-resume — healthy QUEUED ops are preserved across pause + resume in place correctly; only the exhaustion-FAILED survivor needs re-ingest. (2) Phase 3 'carry exact partial state' = re-submit the EXACT frozen OperationContext (phase/generation.candidates/plan preserved; no completed work re-computed) NOT a reconstructed envelope (which restarts at CLASSIFY); live LLM stream cannot cross stateless boundary + doesn't need to.
  op_context.py: resurrected_from_hibernation field + with_resurrection() (dataclasses.replace + hash-chain). intake _compute_priority: resurrected=True short-circuits to _resurrection_intake_priority()=min(_PRIORITY_MAP.values())-margin (DYNAMIC, not hardcoded 0). background_agent_pool: _ROUTE_PRIORITY module const + _resurrection_pool_priority()=min(route)-margin; submit() honors resurrected flag; resubmit_resurrected(ctx) (reuses submit) + drain_exhaustion_failures() (scans _ops for failed+_RESURRECTABLE_ERROR_MARKERS, clears to prevent double-resurrect). GLS _wake_bridge now async: after resume, drain+resubmit survivors (gated JARVIS_RESURRECTION_REINGEST_ENABLED default-TRUE, fail-soft). Shared knob JARVIS_RESURRECTION_PRIMACY_MARGIN default 100.
  TDD 10 tests incl Phase 4 (blackout→3 dark-window tasks→recovery→survivor re-ingested, dequeues before all 3). 199 green across hibernation/pool/intake/242-245, zero regression (async wake_bridge compatible via _call_maybe_async, proven by test_hibernation_observability). Branched off fresh origin/main.

Slice 246 (Preemptive Interrupt Matrix & Human Override): closes the sovereignty hole S245 opened (resurrected=-100 out-ranked voice_human=0 → human could be starved).
  HONEST: the brief under-specified the PRIMARY fix. Two parts: (1) SOVEREIGN HUMAN TIER (queue guarantee) — human sources rank BELOW resurrection (Human>Resurrected>Normal), dynamic (resurrection_floor - JARVIS_SOVEREIGN_PRIMACY_MARGIN default 100), in BOTH intake _compute_priority + pool submit. Does NOT degrade resurrected (still beats all normal work). SOVEREIGN_SOURCES={voice_human,cli_emergency,human_override} in leaf intent_envelope.py (no cycle). (2) COOPERATIVE PREEMPTION (running-op backstop) — new preemption.py (request/is/clear/check_preemption + OperationPreemptedError(BaseException so it slips past broad except Exception to the pool worker, like asyncio.CancelledError) + JARVIS_HUMAN_PREEMPTION_ENABLED default-TRUE). Pool submit() = the SENTINEL: a sovereign-human submit fires request_preemption on running_resurrected_op_ids(). tool_executor round-boundary (the per_round_observer seam) calls check_preemption(op_id) → raises at coherent boundary (NO hard SIGTERM). Pool worker catches OperationPreemptedError (before generic except, mirrors _ParkRequested non-terminal pattern) → status=preempted + resubmit_resurrected (S245 reuse, micro-hibernation).
  HONEST GRANULARITY: yields at a ROUND boundary; the in-flight round restarts; completed phases in OperationContext survive (no catastrophic loss, not literally zero-compute). Multi-worker pool + sovereign tier handle the common case; preemption is the worker-saturation backstop.
  TDD 17 tests incl Phase 4 (resurrected running → human emergency → sentinel fires → checkpoint raises → re-ingest → human dequeues before survivor, payload preserved). Updated 1 S245 test (resurrected-beats-voice_human invariant INTENTIONALLY overturned → resurrected-beats-top-NON-sovereign). 162 green across pool/intake/hibernation/controller/242-246, zero regression. Pre-existing unrelated pgrep-pattern test failure (battle_test script, Slice 125) proven via stash. Branched off fresh origin/main.

Slice 247 (State-Drift Reconciliation & Dynamic Context Alignment): closes the concurrency-collision vuln from 246 preemption — a resurrected GOAL awakening to a human-drifted target file would blind-patch → AST/line corruption.
  VERIFY-FIRST: Phase 1 (capture hashes) ALREADY exists (generate_runner snapshots sha256 of target_files into ctx.generate_file_hashes, rides preserved context through preemption). Phase 2 (zero-LLM compare) ALREADY exists at APPLY (orchestrator ~8549) but was LOG-ONLY + blind-applied (the real latent bug). Phase 3 (autonomous re-align) GENUINELY NEW.
  NEW state_drift.py (pure, zero-LLM, NEVER raises): detect_drift(prior_hashes, project_root) → drifted rel-paths (skips empty-hash/new + missing); build_realignment_feedback(files) → ASCII re-read instruction (STATE=CONTEXT_DRIFTED, forces read_file before patch); state_drift_reconcile_enabled() gate (JARVIS_STATE_DRIFT_RECONCILE_ENABLED default-TRUE).
  WIRED: generate_runner GENERATE-entry — for a resumed/resurrected op carrying PRIOR generate_file_hashes, compare vs disk BEFORE the re-snapshot erases the baseline; on drift inject realignment feedback via the existing strategic_memory_prompt channel (reuses consciousness-injection pattern) → model re-reads drifted files + regenerates against NEW state. orchestrator APPLY-seam (8549) refactored to REUSE detect_drift (single source of truth). KEY INSIGHT: preemption (246) fires in the tool loop = GENERATE phase, so resurrected ops RE-RUN GENERATE → the drift validator at GENERATE entry is the correct seam (not risky APPLY→GENERATE_RETRY control-flow surgery).
  TDD 13 tests incl Phase 4 (snapshot baseline → mutate target_file.py → detect_drift catches → feedback names file + forces read_file → control: re-snapshot of new state shows aligned). 260 green across phase-runner/park/iron-gate/exploration/244-247; zero regression (1 pre-existing stale source-pin test_determinism_generate_wiring proven via stash — pins removed 'generate(ctx, deadline)' string). Branched off fresh origin/main. Companion: untracked 355 committed .pyc + forbid-pyc pre-commit guard (#69490).

Slice 248 (APPLY-time verification pass hardening the 247 re-alignment): the legacy APPLY drift check (orchestrator ~8549) was LOG-ONLY → blind-applied stale candidates (corruption: full-content overwrite=data loss, diff=line drift). NOW a deterministic zero-LLM VERIFICATION GATE that BLOCKS.
  state_drift.py: state_drift_verify_enabled() gate (JARVIS_STATE_DRIFT_VERIFY_ENABLED default-TRUE; OFF=legacy log-and-apply byte-identical) + should_block_apply(prior_hashes, root)->(block, drifted) pure decision (block iff drift AND gate on; NEVER raises) + STATE_DRIFT_UNRECONCILED='state_drift_unreconciled' telemetry token.
  orchestrator APPLY-seam: on block → record FAILED + advance POSTMORTEM(terminal_reason_code=state_drift_unreconciled) + publish_outcome + return (fail-safe, NO corruption) — REUSES the LiveWorkSensor abort pattern (8590-8606). Op re-runs fresh on next sensor trigger → regenerates against current disk = eventual re-alignment. Routing APPLY→GENERATE_RETRY in-line is unsafe (past the retry walk), so fail-safe is correct.
  Defense-in-depth: 247 re-aligns at GENERATE entry (re-read+regenerate); 248 verifies at APPLY (block if drift persists — model ignored re-read OR disk drifted again during regen). detect_drift verifies CRYPTOGRAPHICALLY (no model-trust). 8 tests + updated 1 S247 pin (orchestrator now uses should_block_apply which composes detect_drift). 208+13 green; zero regression (no test relied on log-and-apply-on-drift). Branched off fresh origin/main.
