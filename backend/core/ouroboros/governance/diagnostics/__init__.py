"""Governance diagnostics — deterministic transport and matrix probes.

Submodules are intentionally authority-light: they MUST NOT import the
orchestrator, candidate_generator, iron_gate, or mutation paths.
"""
from __future__ import annotations

__all__: tuple[str, ...] = ()
