# Production VERIFY sandbox — GOVERNANCE import-chain deps (Slice 108).
#
# Empirically derived (not guessed): `import backend.core.ouroboros.governance.*`
# eager-loads `backend.core.ouroboros.__init__` → engine, whose dependency closure
# is exactly this MEDIUM set — NO torch / whisper / transformers / numpy / scipy
# (those are vision/voice-only and belong to a separate heavier image variant).
# This image therefore runs the TRUE O+V self-development (governance/architecture)
# test suite while staying buildable in minutes, not the 308-line ML monolith.
#
# Pins mirror the host (2026-06-06) so in-container results match local results.
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-timeout==2.4.0
pytest-json-report==1.5.0
anyio==4.12.0
aiohttp==3.13.2
aiofiles==25.1.0
aiosqlite==0.19.0
cryptography==47.0.0
networkx==3.6.1
opentelemetry-api==1.39.0
opentelemetry-sdk==1.39.0
psutil==7.1.3
redis==7.4.0
PyYAML==6.0.3
typing_extensions==4.15.0
uuid6==2025.0.1
xxhash==3.7.0
