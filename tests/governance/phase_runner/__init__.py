"""Tests for the Wave 2 (5) PhaseRunner extraction.

Every concrete PhaseRunner subclass gets a parity test here that
pins byte-identical observable output against the inline orchestrator
block it replaces — the graduation evidence for the
``JARVIS_PHASE_RUNNER_<PHASE>_EXTRACTED`` flag flip.
"""
