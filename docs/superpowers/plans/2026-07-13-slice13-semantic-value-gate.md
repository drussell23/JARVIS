# Slice 13 — Semantic Value Gate (AST-proven cosmetic NO_OP) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development or superpowers:executing-plans, task-by-task.

**Goal:** Close the Run-22 op-value gap (operator verdict: the only landed commit was a duplicate requirements.txt comment — "not real code"). A candidate whose every proposed change is mathematically cosmetic (no executable-logic mutation) must terminate as a benign NO_OP immediately after GENERATE — before VALIDATE's ~14s candidate tree, before APPLY, before VERIFY — without poisoning the flag audit.

**Architecture:** New `candidate_value_gate.py` (pure, zero-LLM): per-file semantic-weight classification — Python via AST proof (parse old + new, strip docstrings via the EXISTING `epistemic_shedder._DocstringStripper`, compare `ast.dump(include_attributes=False)`; comments never reach the AST, so comment/whitespace/docstring-only edits are *provably* structure-identical), declared line-grammar formats (`requirements*.txt`, `constraints*.txt`, `*.cfg`, `*.ini`) via whole-line comment/blank stripping per the format's grammar spec, everything else INDETERMINATE. Gate wired at the single post-GENERATE / pre-VALIDATE seam in the orchestrator (both GENERATE paths rejoin there — verified; wiring pin included), mirroring the adjacent `is_noop` benign terminal (`terminal_reason_code="no_op_cosmetic"`, POSTMORTEM `root_cause="no_op_cosmetic"` — joins the audit's no-mutation completion class; no `_FAMILY_SIGNALS` reject marker anywhere in the emits).

## Operator-locked mandates (2026-07-13, verbatim intent)

1. **Root-Cause Only** — no naive regex comment-matching on code. Python classification is an AST-equality proof. For formats WITHOUT an AST (requirements/ini — the Run-22 noise class), whole-line comment/blank stripping IS the format's grammar normalization (declared per-format, never applied to code, never applied to undeclared formats). *Substrate audit (mandate 3, Slice-6 discipline):* `doc_staleness_sensor` walks docstring presence only; `opportunity_miner`'s AST use is heuristic counting; `duplication_checker._Normalizer` is DISQUALIFIED for this purpose (blanks constants + renames vars → would classify real value changes as cosmetic — a mandate-4 violation); the sanctioned reusable piece is `epistemic_shedder._DocstringStripper` (exact AST-level docstring removal, no other normalization). `state_drift.file_sha256` reused for the fast byte-identity short-circuit.
2. **Architectural Purity** — the gate sits between GENERATE and APPLY (concretely: pre-VALIDATE, saving the most spend); NO_OP terminates the FSM immediately via the existing benign-completion shape (ledger convention mirrored from the `is_noop` path).
3. **DRY** — no new diff engine: stdlib `ast` + `_DocstringStripper` + `file_sha256`. Candidate file pairs come from the EXISTING `_iter_candidate_files` shape.
4. **Bulletproof / fail-safe-FORWARD** — one byte of executable-logic change anywhere → PASS. SyntaxError on either side → INDETERMINATE → PASS. Unknown format → PASS. Missing old file (creation) → SUBSTANTIVE → PASS. Mixed multi-file → PASS. Only a candidate where EVERY file is PROVEN cosmetic terminates. Master `JARVIS_CANDIDATE_VALUE_GATE_ENABLED` default true (registered; OFF = byte-identical legacy).

## Tasks (strict TDD)

- [ ] T1 RED→GREEN: `candidate_value_gate.py` classifier — python cosmetic (comment/whitespace/docstring) → COSMETIC; any logic/constant change → SUBSTANTIVE; syntax error either side → INDETERMINATE; new file → SUBSTANTIVE; requirements comment-append (the Run-22 class) → COSMETIC; requirements version bump → SUBSTANTIVE; unknown format → INDETERMINATE; byte-identical → COSMETIC (fast path via `file_sha256`).
- [ ] T2 RED→GREEN: orchestrator seam — all-candidates-cosmetic → benign `no_op_cosmetic` terminal (no VALIDATE dispatch); any substantive/indeterminate → unchanged flow; master-off inert; wiring pin (seam sits before the VALIDATE delegation gate); audit-neutrality pin (emits carry no reject markers).
- [ ] T3: FlagRegistry seed + CLAUDE.md bullet + ledger + arc memory. Merge gate re-run. Then Run #23 (Slice 12 landing + Slice 13 gate in one proof; 6/6 attempt).
