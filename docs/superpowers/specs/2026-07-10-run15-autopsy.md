# Run #15 Autopsy — `fsm:only_noop_read_only_completions_no_mutation`

**Run**: A1 isomorphic soak, attempt 3 (session `bt-iso-1783742671`, run `iso-a1-20260710-210431`)
**Window**: 2026-07-10 21:05:31 → 22:28 PDT (full 5000s wall, rc=75 `wall_clock_cap` as designed)
**Verdict**: `proven=false`, `failure_locus=fsm:only_noop_read_only_completions_no_mutation`
**Criteria**: a1trace 5-hops ✓ · lineage ✓ · intervention-lock ✓ · fsm_classify_to_applied ✗ · autonomous_pr ✗ · twelve_flag_audit ✗
**Artifacts**: `logs/a1_runs/20260711T040418Z/iso-a1-20260710-210431/a1_verdict.json` + autopsy dir `…_20260710-223512`

(Attempts 1–2 context: attempt 1 killed by the T1 wired-but-inert budget channel — fixed by T1b, merged. Attempt 2 killed at 38 min by an external SIGTERM during the macOS memory-exhaustion event — environmental, not a run defect. Attempt 3 is the first Run #15 to complete its wall.)

## What HELD (Slice 4 causal set fully validated live)

- Zero LoopDeadman fires across 83 min (Run #13 died in minutes).
- Funded budget channel: boot `--cost-cap 2.0` (T1b), zero `SessionBudgetPreflightRefused`.
- Zero **spurious** quarantine: the one `dw_global_outage` quarantine (22:04–22:06, 12 DLQ-orphaned goals) fired under **adversary-injected provider chaos** — chaos-legitimate, not the Run #14 budget-misclassification class. T2's refusal-taxonomy path stayed clean.
- SENSE machinery (T3): event lane armed; the resolver/off-loop scoped-pytest path works (21:08:17: event → 1 scoped target → pytest 1.5s).
- Intake WAL (T4): LoopSink quiet. GENERATE/APPLY: exonerated (see L5).

## Root cause — a five-link kill chain in SENSE event delivery

The chaos red test (`leaf_predicates.py::clamp01` mutated + touched ×2 at ~21:05:4x) never became an intake signal: `test_leaf_predicates` appears **0 times** in 83 minutes of debug.log. The drop is NOT one bug but a stack:

### L1 — exposure: FS-watch warm-up latency (~2 min), not a swallow

`[FSEventBridge] Watching` logs at 21:05:31 (guard.start() return) but the first published event arrives 21:07:31. **Live repro** (`repro_dead_window.py`, same FileWatchGuard config on this repo, addendum below): `guard.start()` itself takes ~43s (schedule-plan crawl: 175 candidate roots hard-coalesced → 1); the macOS path is watchdog **PollingObserver** (native FSEvents broken per bt-2026-04-12-005521) whose delivery lag is ~one snapshot pass (~45s idle, ~120s under boot load). The repro **refuted** the strong "baseline swallows early touches" theory: an early mtime touch WAS delivered, ~47s late. A baseline-walk race (change lands after the walker visits the file during pass 1) can still swallow — but the soak log favors late delivery: the ONLY `fs.changed.modified` published in the boot window (21:07:31, id=393a6b60, the second the chain went live) is consistent with the late-delivered leaf touch (path-level attribution unavailable at DEBUG). Either branch, the touch's effect arrived (if at all) ~2 min late, into a hostile window — and the driver's run-#12 READY gate (`isomorphic_a1_local.py:42-46,97`) proves only the *subscription*, never awaits the event.

FIXED: Slice 5 T4 (d03c8f7435 "feat(sense): FSEventBridge sentinel self-verification + fs.watch.ready + WATCH ACTIVE marker (Slice 5 T4, Run #15 L1/F6)")
FIXED: Slice 5 T6 (1047c4cf25 "fix(iso): gate chaos injection on WATCH ACTIVE + verify sensor evidence post-touch with bounded retry (Slice 5 T6, Run #15 L1 driver half)")

### L2 — KILL (definite): single-slot debounce cancel-chain, fed by the worktree-churn bug

`test_failure_sensor.py::_on_fs_event` routes every `.py` event into ONE debounce slot, **cancelling the pending task each time** — last event wins, earlier paths are evicted unrecorded. At 21:07:31–21:08:15 the newly-live chain delivered a ~300-event burst of `.worktrees/ouroboros__auto__*` `.py` *created* events — because the guard's `ignore_patterns` entry `.worktrees/*` does **not match nested paths** (fnmatch one-level semantics). The burst steamrolled the leaf event's pending debounce; the sole survivor was the burst's last event, and the session's ONE scoped pytest (21:08:17) ran against a **worktree copy** of a test — green, no signal. The churn is not a side finding; it is the murder weapon.

FIXED: Slice 5 T1 (688966e950 "fix(sense): deep-glob ignore semantics — nested .worktrees/__pycache__ churn never reaches sensors (Slice 5 T1, Run #15 L2 fuel)")
FIXED: Slice 5 T2 (d0a5c2959c "fix(sense): set-accumulator debounce — burst events aggregate, never evict (Slice 5 T2, Run #15 L2 kill)")
FIXED: Slice 5 T2 (3b26d51b71 "fix(sense): re-seed drained debounce batch on early returns — no silent path loss (Slice 5 T2 review fix)")

### L3 — backup KILL (proven live at 21:25:59): stale plugin-results suppression window

`_debounced_pytest_run` skips entirely if plugin results were consumed <10s ago. At 21:07:31 — the same second the leaf event arrived — the sensor "consumed" `.jarvis/test_results.json` via its **deleted** event (parse of a missing/stale file → 0 failures) and still bumped `_last_plugin_ts`. Had the leaf debounce survived L2, its 21:07:33 fire was inside the poisoned window. The mechanism demonstrably fires in this run: 21:25:59 `skipping subprocess run — plugin results consumed 2.2s ago`, again right after a 0-failure stale consume. Consuming a stale/deleted results file must not arm the suppression window.

FIXED: Slice 5 T3 (a214ac582a "fix(sense): plugin-results staleness gate — absent/stale test_results.json never arms suppression (Slice 5 T3, Run #15 L3 kill)")

### L4 — amplifier: event-primary derate has no recovery path

Slice 4 T3 ext: with the event lane armed and `JARVIS_INTENT_POLL_WHEN_EVENT_PRIMARY` unset, `_poll_loop` **fully skips** `poll_once()` every cycle — the 600s "fallback" sleep loops back into the skip. Correct trade against Run #14's 300s SIGKILL sweep storm, but it leaves NO reconciliation: an event dropped once (L1–L3) is dropped for the session. The red test sat undetected for 83 minutes with zero re-detection attempts.

FIXED: Slice 5 T5 (b2bb90c669 "feat(sense): quiet-lane reconcile on the derate wake — zero-event window triggers one git-dirty-scoped run (Slice 5 T5, Run #15 L4)")
FIXED: Slice 5 T5 (6aa65055ce "fix(sense): reconcile anchors at repo root + poll_once serialized across trigger paths (Slice 5 T5 review fixes)")

### L5 — consequence: only no-op goals existed → verdict

With no TestFailure signal, no mutation-scoped op was ever created. The 75 `read_only_complete` terminals were roadmap seeds + proactive-exploration goals served by the adversary's canned DW batch payload (constant 1604 chars); canned candidates targeting `leaf_predicates.py` against other-scoped ops were **correctly rejected** (`file_scope_mismatch`). `is_noop=True … skipping APPLY` ×75 → FSM never reached APPLIED. This is the Grade-B ceiling (53 prior read-only sessions) isolated to SENSE event **delivery** — distinct from Run #14's resolver wedge (cured); GENERATE/APPLY behaved correctly throughout.

## Prioritized fix set (pre-Run-#16)

1. **F1 — driver: verify, don't assume (covers all of L1–L3 live).** Gate injection on *chain-live* (sentinel touch → await first-published-event marker, bounded); after the chaos touch, **await the sensor-side evidence** (scoped-targets log line naming `leaf_predicates`/`test_leaf_predicates`) with bounded retry-touch — the boot burst is transient, so a post-burst retry lands. Abort loudly on budget exhaustion. Makes run-#12's "(fires fs.changed.modified)" honest.
   FIXED: Slice 5 T4 (d03c8f7435 "feat(sense): FSEventBridge sentinel self-verification + fs.watch.ready + WATCH ACTIVE marker (Slice 5 T4, Run #15 L1/F6)")
   FIXED: Slice 5 T6 (1047c4cf25 "fix(iso): gate chaos injection on WATCH ACTIVE + verify sensor evidence post-touch with bounded retry (Slice 5 T6, Run #15 L1 driver half)")
   FIXED: Slice 5 T6 (3c7f2218cc "fix(iso): watch-gate fail-fast on NOT CONFIRMED + abort-path integration coverage (Slice 5 T6 review fixes)")
2. **F2 — sensor: coalescing debounce (L2).** Replace last-event-wins cancel with a changed-paths **set** accumulated across the debounce window; scoped resolution over the union. No event is silently evicted.
   FIXED: Slice 5 T2 (d0a5c2959c "fix(sense): set-accumulator debounce — burst events aggregate, never evict (Slice 5 T2, Run #15 L2 kill)")
   FIXED: Slice 5 T2 (3b26d51b71 "fix(sense): re-seed drained debounce batch on early returns — no silent path loss (Slice 5 T2 review fix)")
3. **F3 — guard: nested ignore patterns (L2's fuel).** `.worktrees/*` → also `*/.worktrees/*` (same for `.git`, `__pycache__`, `.ouroboros`, venvs) so worktree churn never reaches sensors.
   FIXED: Slice 5 T1 (688966e950 "fix(sense): deep-glob ignore semantics — nested .worktrees/__pycache__ churn never reaches sensors (Slice 5 T1, Run #15 L2 fuel)")
4. **F4 — sensor: honest plugin-consume timestamp (L3).** Bump `_last_plugin_ts` only on a FRESH results file with parseable content; a stale/deleted parse (0 failures by absence) must not arm the 10s suppression window.
   FIXED: Slice 5 T3 (a214ac582a "fix(sense): plugin-results staleness gate — absent/stale test_results.json never arms suppression (Slice 5 T3, Run #15 L3 kill)")
5. **F5 — sensor: derate liveness reconcile (L4).** On each 600s fallback wake while armed, if the event lane published zero events since last wake (bridge metrics delta), run ONE bounded scoped reconcile instead of skipping — preserves the T3 anti-storm property.
   FIXED: Slice 5 T5 (b2bb90c669 "feat(sense): quiet-lane reconcile on the derate wake — zero-event window triggers one git-dirty-scoped run (Slice 5 T5, Run #15 L4)")
   FIXED: Slice 5 T5 (6aa65055ce "fix(sense): reconcile anchors at repo root + poll_once serialized across trigger paths (Slice 5 T5 review fixes)")
6. **F6 — bridge observability.** WARN if `events_published==0` N seconds after "Watching" (would have made L1 a one-grep diagnosis).
   FIXED: Slice 5 T4 (d03c8f7435 "feat(sense): FSEventBridge sentinel self-verification + fs.watch.ready + WATCH ACTIVE marker (Slice 5 T4, Run #15 L1/F6)") — boot-time WARN
   FIXED: Slice 5 T5 (6aa65055ce "fix(sense): reconcile anchors at repo root + poll_once serialized across trigger paths (Slice 5 T5 review fixes)") — steady-state coverage

Sequencing: F1+F3 give Run #16 a fair shot with driver-side certainty and the churn removed; F2/F4 close the in-sensor drop classes; F5/F6 harden recovery + visibility. None touches GENERATE/APPLY.

## Addendum — repro transcript (`repro_dead_window.py`, 2026-07-10 23:3x)

```
[FileWatchGuard] schedule_budget_hard_coalesced candidate=175 scheduled=1 cap=30 …
[   43.5s] guard.start() returned ok=True   (this is when 'Watching' logs)
[   45.5s] touched FILE_A (dead-window touch): leaf_predicates.py
[   92.6s] CHAIN LIVE first event: modified …/leaf_predicates.py
[  103.4s] touched FILE_B (post-live touch): fs_event_bridge.py
[  145.0s] TARGET EVENT: modified …/fs_event_bridge.py
RESULT: dead-window FILE_A reported=True  post-live FILE_B reported=True
VERDICT: P1 (strong swallow form) REFUTED for mtime-touch — delivery is LATE (~1 polling pass), not lost
```

Interpretation: the watch pipeline delivers late rather than swallowing (idle-load); the soak's loss is therefore attributed to L2+L3 (with L1 supplying the fatal 2-minute latency that landed the event inside the burst+suppression window). A baseline-walk race remains possible under boot load but is not required to explain the outcome. Teardown note: observer teardown exceeded its 10s deadline and abandoned the emitter daemon thread (`total_abandoned=1`) — known guard property, visible in the repro, harmless to the repro verdict.

## Reconciliation with the parallel autopsy (commit ebb5b7176f, `2026-07-10-run15-attempt3-autopsy.md`)

An independently-produced autopsy landed on main at 23:20 with a different L1 mechanism and one finding this document missed. Controller-verified reconciliation:

- **L1 mechanism corrected.** The guard's observer path DOES checksum-gate (`file_watch_guard.py:1873-1937`) — but only once a baseline md5 exists (`if old_checksum and old == new`), and checksums populate lazily per-event. That resolves the repro-vs-soak contradiction: the repro's mtime touch delivered because `leaf_predicates.py` had no checksum entry yet AND won the watchdog baseline-snapshot race (walker visited it before the touch); the soak's mutation+touches (~21:05:35-45) lost that race (baseline pass 21:05:31→~21:06:31 captured the post-touch mtime) → **no leaf event was ever emitted**. The parallel autopsy's headline is right for the soak; the "late delivery" branch is what the repro path exercised. Consequences already folded into the Slice 5 plan: sentinel content NONCE per retouch (T4), driver re-fires must CHANGE CONTENT not mtime (T6), stale `test_results.json` purged at inject (T6).
- **L2/L3 reframed as dormant-but-real.** With no leaf event emitted, the debounce cancel-chain and the stale-suppression window were not this run's killers — but both are code-verified live defects (L3 demonstrably fired at 21:25:59 on another event) that WOULD kill a delivered event under a boot burst. T2/T3 stand.
- **L6 added (parallel-autopsy finding, controller-verified): the adversary batch lane cannot emit a valid candidate.** The only mutation-class op (signed-roadmap seed) was batch-routed; the canned `v1_batch_*` body carries a fixed ~1306-byte truncated payload → 8× `full_content too short (1306 vs 15183)`, 4× syntax-error, 2× scope-mismatch. `manifest["original_source"]` is the FULL pre-mutation file (revert restores byte-identical from it) — the batch builder simply never consults the manifest, while the completion-lane repair path (`build_repair_completion`) does. Without fixing this, ACT is untestable in iso mode. → Slice 5 T7.
  FIXED: Slice 5 T7 (98fc73141b "fix(iso): adversary batch lane serves manifest full-file repair for chaos-targeted ops (Slice 5 T7, Run #15 L6 — ACT untestable without it)")
- **"What held" corrected:** ControlPlaneStarvation was NOT quiet — 1,041 warnings (LoopSink 284: `file_has_test_coverage` 225 residual, ingest 38, posture 17). WAL LoopSink 4 vs Run #14's 214 stands. Also flagged there: `LEDGER_TERMINAL state=applied written=True` is stamped for read-only ops (bookkeeping trap for any "did we mutate?" check), and 14 DLQ CRITICAL orphans incl. `l2_unresolvable_awaiting_human`.
