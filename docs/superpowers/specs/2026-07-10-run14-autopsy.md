# A1 Run #14 Autopsy — session bt-iso-1783659655 (2026-07-09/10)

**Verdict: FAILED = `a1_iso_not_proven:fsm:did_not_reach_state_applied`** — but with TWO independent,
individually-sufficient blockers. Slice 3's fatal-wedge fix HELD (zero LoopDeadman fires in 83 min;
Run #13 died in minutes). Conductor: agent (per feedback_agent_conducted_soak_delegation).

Boot pid=54020 @ 22:01:01; wall cap 5004s ≥ 5000s @ 23:26:10; graceful shutdown wedged → ShutdownWatchdog
`os._exit(75)` @ 23:26:40. Git HEAD c87a877f6c (Slice 3 merged). Adversary live (failover pinned off),
ignition red injected on `leaf_predicates.py`.

## Causal chain (minimal)

1. **SENSE never fired.** TestWatcher's `_resolve_scoped_targets` (`test_failure_sensor.py:836/843`)
   was so slow (main thread still inside `pathlib.resolve → _joinrealpath` at shutdown, 83 min later)
   that every cycle fell back to running the FULL `tests/` suite: `pytest tests/ --tb=short -q` →
   TIMEOUT 180s → SIGTERM → SIGKILL, `stdout_bytes=0`, **7 times** (22:02:46 → 23:22:08, ~13min cadence).
   The injected `leaf_predicates` red never produced a parseable failure signal and never became an op.
2. **The pipeline was fed noise instead**: 1049 of 1065 pool submissions were `doc_staleness` triggers.
   6-worker pool saturated (ceilings up to 1039s); 65 distinct ops parked in intake WAL (1065 defer
   events); intake WAL sync flock + residual sync callsites starved the loop — 590 starvation events,
   peak lag 8.9s (at shutdown).
3. **Every op that reached GENERATE (15) hit a hard budget wall**: `SessionBudgetAuthority` at **$0.00**
   from boot (`budget=$0.00` @ 22:02:46) with `JARVIS_PROVIDER_CLAUDE_DISABLED=true` (autarky) → 30×
   `preflight REFUSED: est=$0.1000 > effective_remaining=$0.0000` → 26 EXHAUSTION events, all
   `cause=fallback_skipped:no_fallback_configured`. Zero tokens generated all run
   (`tokens=0 first_token_ms=-1`).
4. **Amplifier — misclassification**: budget refusals were counted as DW provider failures →
   `UPSTREAM QUARANTINE: global DW out` (22:05:00 → 23:24:37, ~79 of 83 min) → 12 goals orphaned to
   IntakeDLQ (`reason=upstream_quarantine:dw_global_outage`). A local config gate masqueraded as a
   provider outage.
5. **Shutdown wedge** (30s deadline blown): NOT WAL-drain/checkpointing (6 ops signed-suspended fine).
   Tombstone (31 threads): main thread in `test_failure_sensor.py:836 pathlib.resolve`; majority workers
   in `doc_staleness_sensor.py:447/455/468 _scan_files_sync` (rglob/read_text/ast.parse);
   `file_watch_guard.py:1024 _blocking_teardown` joining the single recursive PollingObserver folded over
   **175 watch-roots** (`runaway-watching guard: 175 candidate roots (>30)`). Uninterruptible FS scans →
   `os._exit(75)`.

## Op funnel (quantified)

| Stage | Count |
|---|---|
| Ops ingested | 207 |
| Submitted to pool | 74 |
| Picked up by worker | 58 |
| Parked in WAL (capacity) | 65 distinct / 1065 events |
| Reached GENERATE | 15 |
| GENERATE EXHAUSTION | 26 events / 18 ops |
| DLQ-orphaned | 12 |
| Reached APPLY / proof chain | **0** |

## LoopSink final histogram

| Callsite | Events | Note |
|---|---|---|
| `cross_process_jsonl.flock_append_line` sync | 214 | intake WAL (uir.py:1267 + :2173, one self-contending lock) |
| `intake.UnifiedIntakeRouter.ingest` async | ~106 | WAL append + offload-await wall-inflation |
| `target_stratification.file_has_test_coverage` sync | 91 | blocked up to 640ms — residual beyond Slice 3 T3's cache (re-diagnose: real vs GIL wall-inflation; cpu_ms tells) |
| `posture_observer.run_one_cycle` async | 12 | healthy-path = offload-await inflation |

## What was NOT a factor (do not chase)

- Provider latency: adversary stub answered in 38ms; every failure was the budget gate.
- LoopDeadman / fatal wedge: zero fires — Slice 3 validated.
- SemanticTriage kills: triage was DISABLED at boot (configured `google/gemma-4-31B-it` unavailable) —
  itself a finding (no triage protection all run), but not causal.
- Evidence-glob wedge (Run #13's killer): silent all run.

## Ranked fixes for Run #15

CONFIG/harness first (cheapest, highest leverage):
1. **Fund the session budget in iso mode** (or make adversary-stub preflight $0.00). Without this Run #15
   CANNOT pass — structurally.
2. **Scope the TestWatcher pytest** to resolved targets (fast path, no `pathlib.resolve` walk on the hot
   path; cache the affected-test map). Full-`tests/` fallback guarantees the 180s SIGKILL.
3. **Throttle/disable doc_staleness during A1 iso runs** (SensorGovernor exists for exactly this).
4. Pool headroom / shorter worker ceilings (secondary once #3 lands).

CODE:
5. **`SessionBudgetPreflightRefused` must NOT count as provider failure/outage** (distinct non-provider
   terminal; no quarantine, no immortal retry).
6. **Bound the `[Immortal]` QUEUE_ONLY loop for non-transient failures** (budget/no-fallback = fail fast).
7. **Off-loop the residual sync callsites**: intake WAL (wal.py:255 via uir.py:1267/:2173 — dominant),
   file_has_test_coverage residual, mutation_gate.append_ledger, per `.superpowers/sdd/slice4-evidence-pack.md`.
8. **Interruptible shutdown teardown**: teardown budget/cancel-and-detach for FS scanners +
   watch-root cap enforcement so graceful shutdown beats the 30s watchdog.

Artifacts: `.ouroboros/sessions/bt-iso-1783659655/{debug.log,summary.json}`,
`logs/a1_runs/20260710T050046Z/iso-a1-20260709-220055/telemetry/failure_telemetry_20260709-233157/`,
`logs/a1_ignition_20260710T050046Z.log`, shutdown tombstone `shutdown_watchdog_tombstone.txt`.
Checkpointed ops resume next ignition (signed, phase=CLASSIFY).
