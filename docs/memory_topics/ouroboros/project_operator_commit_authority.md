---
title: Project Operator Commit Authority
modules: [backend/core/ouroboros/governance/operator_commit_authority.py, tests/governance/test_operator_commit_authority.py, scripts/install_hooks.py, backend/core/ouroboros/governance/commit_authority_cli.py, backend/core/ouroboros/governance/persistent_master.py, backend/core/ouroboros/governance/commit_repl.py, backend/core/ouroboros/governance/commit_authority_archive.py, backend/core/ouroboros/governance/harness_sovereignty_pin.py, backend/core/ouroboros/governance/commit_authority_daemon.py, commit_authority_repl.py, tests/governance/test_meta_phase_runner.py, tests/governance/test_opp_miner_merkle.py]
status: open
source: project_operator_commit_authority.md
---

**Operator Commit Authority (OCA)** — long-term structural fix for the Cursor Source Control "⛔ IRON GATE — Commit blocked" error. Root cause: authorization lived in an untracked bash `.git/hooks/pre-commit` requiring the `JARVIS_AUTHORIZE_COMMIT_TOKEN` shell env var, which GUI git cannot inherit. OCA replaces the env-var seam with a signed grant file the Python verifier reads directly.

**Slices 1+2 — COMMITTED + PR OPEN 2026-05-19.** Branch `ouroboros/operator-commit-authority-slice-1` off `origin/main` (c563965bb5) in worktree `/tmp/claude-501/oca-clean`. Slice 1 = `1b3874384a` (bootstrap under legacy bash gate, operator-run `export TOKEN && git commit`). Slice 2 = `4d64bac0f5` — **end-to-end proof: committed master ON, NO `JARVIS_AUTHORIZE_COMMIT_TOKEN`, authorized by signed grant `b51ba307…` + chained integrity guardian**. Pushed; **PR #41674** (https://github.com/drussell23/JARVIS/pull/41674) → main. 72 cumulative tests green.

Slice 2 adds: `commit_authority_cli.py` (hook dispatcher + grant/revoke/status; legacy SHA-256 token check single-sourced here, bash retired), `scripts/hooks/pre-commit` (thin Python dispatcher), `scripts/hooks/pre-commit.project` (tracked integrity guardian), worktree-aware `install_hooks.py` (resolves hooks dir via `git rev-parse --git-path hooks` — fixes linked-worktree `.git`-is-a-file). Contract: master OFF = legacy token gate byte-equivalent; master ON = signed grant, no env token.

**LIVE HOOK-STATE CAVEAT (critical):** `install_hooks.py install` rewrote the **shared** `.git/hooks/pre-commit` → dispatcher importing `commit_authority_cli`, which is ABSENT on `ouroboros/predictive-resilience-s1` (operator's real Cursor checkout) → that checkout's commits failed closed. **Mitigated 2026-05-19**: restored `pre-commit.backup.20260519_021358` (bash Iron Gate, 1786B) as active `.git/hooks/pre-commit`; dispatcher + `pre-commit.project` files preserved for one-command reactivation. Real checkout commits work again via legacy token gate. Mismatch must be closed by merging OCA onto `predictive-resilience-s1` (or PR→main then realign) — OPERATOR CHOICE PENDING (Option A main-merge vs Option B local merge into predictive-resilience-s1). Post-merge: re-run `install_hooks.py install` in real checkout + CLI grant + prove + commit from Cursor.

Sandbox notes: `.git/hooks/` writes, `~/.jarvis/` secret write, and `gh` all need `dangerouslyDisableSandbox`. `!`-prompt mangles long/quoted/heredoc/multiline commands — deliver operator commands as a short `bash /tmp/claude-501/<script>.sh` (sandbox-written script, chmod 755, readable after `chmod 755` on `/tmp/claude-501`). Operator runs all `git commit` (bright-line: Claude never runs git commit / sets token / git ac).

**Slices 1+2 MERGED to main `506eb034fe` (PR #41674 squash, 2026-05-19).** Real checkout realigned via merge `eb27d1d72d` on `ouroboros/predictive-resilience-s1` (stray Slice-1 artifacts were verified byte-identical to main, dropped, then merged — predictive work untouched). Real checkout dispatcher installed (bash retired, backup `pre-commit.backup.20260519_031341`); tokenless terminal commits proven (grant `c3b95ffe…`).

**Slice 3 #0 COMMITTED + PR #41852 (2026-05-19)** — branch `ouroboros/operator-commit-authority-slice-3` off main `506eb034fe`, worktree `/tmp/claude-501/oca-s3`, HEAD `ab544b1b76` (3 commits: da08b9ba85 substrate + b778952754 CLI [Cursor-authored] + ab544b1b76 test-seed-fix). **THE Cursor-SCM fix**: `master_enabled()` = env OR HMAC-signed persistent enable record at `~/.jarvis/commit_authority/enabled` (composes existing `_sign`/`_verify`, zero new crypto; default-FALSE preserved, all 6 AST pins green). New: `persistent_enabled()`/`enable_authority()`/`disable_authority()` + CLI `enable`/`disable` + status reflection + 6th FlagRegistry seed. 88 cumulative tests green. **Persistent enable is now LIVE on the operator's machine** (`~/.jarvis/commit_authority/enabled` signed, label cursor-persistent-20260519) — every Slice-3 commit was authorized with NO token AND NO env master flag (the real Cursor SCM path). Reversible: `commit_authority_cli disable`.

UNVERIFIED until operator action (no pre-result euphoria [[feedback-no-preresult-euphoria]]): actual Cursor SCM *button* commit in the real checkout — needs a non-expired `ide` grant in `~/Documents/repos/JARVIS-AI-Agent/.jarvis/commit_authority/grants.jsonl` (phase3 grant c3b95ffe ~8h TTL from ~02:14; re-grant via CLI if expired). Operator must do the SCM-button commit and confirm no "Missing operator cryptographic authorization".

**STRUCTURAL CHANNEL-RESOLUTION FIX — PR #42212 MERGED to main (d7a4d21d19, 2026-05-19).** Root cause of the autonomous-commit incident (a Cursor *Agent* auto-committed WIP mid-Claude-session): `commit_authority_cli.py:195-198` inferred the channel from `JARVIS_COMMIT_CHANNEL` **hardcoded-defaulting to `"ide"`**. A Cursor Agent's headless `git commit` shares the SAME process tree / identical env as a human Cursor SCM commit → both resolved `ide` → the Agent borrowed the operator's interactive grant. Fix (composes existing arch, NO parallel denial logic, NO new crypto): `operator_commit_authority.resolve_commit_channel()` — an operator channel must be **earned** by a signed short-TTL **operator-presence marker** (`~/.jarvis/commit_authority/presence.json`, reuses `_sign`/`_verify`/`_read_secret`, TTL env `JARVIS_COMMIT_PRESENCE_TTL_S` default 900s), minted ONLY at operator-only entry points (`issue_grant`/`enable_authority`). No presence → `AUTONOMOUS` → existing `ledger_sovereignty` gate decides. CLI `or "ide"` replaced + AST-pinned gone. **SOUNDNESS COUPLING (not silent):** OCA-master-ON is a real gate ONLY when `JARVIS_LEDGER_SOVEREIGNTY_ENABLED=true` (else autonomous = legacy pass-through; `test_no_grant_blocks` re-encoded across both states). 240 tests green. **MANDATORY operator policy:** keep Cursor background Agents STOPPED on this repo root until Slice 3; enable `JARVIS_LEDGER_SOVEREIGNTY_ENABLED=true` for the gate to bite. Interim containment applied: all grants revoked, reissued branch-scoped (`--branch`, 90m) not whole-repo/8h. **GitIndexGuard arc** (separate, same incident): Slice 1 substrate + Slice 2 boot-wiring/SSE `git_index_anomaly` — PR #42212 (S1) merged, PR #42218 (S2) OPEN. Guard detects missing `.git/index` (the Agent-unlink "7856 deletions" illusion) → advisory `read-tree HEAD` rebuild, default-OFF. The `.git/index` wiper itself was diagnosed as the Cursor Agents (operator-side; stop them).

**PERSISTENTMASTER + SOVEREIGNTY ACTIVATED — PR #42240 MERGED to main (381746d973, 2026-05-19).** Root problem: `ledger_sovereignty.master_enabled()` was env-ONLY → a shell `export` can't reach Cursor GUI-git → the OCA channel-resolution fix (#42212) was toothless in the GUI path (the sovereignty gate it delegates to was never ON there). Fix: new `persistent_master.py` — generic signed out-of-repo master-enable factored from OCA Slice 3 #0; composes the canonical `roadmap_reader` HMAC + the ONE OCA per-machine secret (zero crypto dup; AST-pinned no-parallel-hmac / compose-canonical / NEVER-raise). `ledger_sovereignty.master_enabled()` = env OR `persistent_master.is_persistently_enabled("ledger_sovereignty")`. 19 spine + 215 regression green. **ACTIVATED**: signed record written at `~/.jarvis/persistent_master/ledger_sovereignty.json` → `master_enabled()=True with env UNSET`. End-to-end verified on real git repos: Agent (forged env=ide, no presence, unowned)→`denied_sovereignty`; operator (presence+grant)→`authorized`; this-branch hook no-token→exit 0. Disable: `persistent_master.disable_persistent_master("ledger_sovereignty")`.

**TWO MANDATORY OPERATOR ACTIONS:** (1) **Checkout realignment** — the persistent record only bites code that HAS the new `ledger_sovereignty`/`persistent_master`; the operator's Cursor checkout must merge main (381746d973) or it runs the OLD env-only sovereignty (record inert there → no protection yet, but also no lockout). (2) **New commit ritual under active sovereignty** — every Cursor SCM commit needs a fresh presence marker; presence is minted by `commit_authority_cli grant`/`enable`. Default presence TTL 900s (`JARVIS_COMMIT_PRESENCE_TTL_S`; the slice3 grant minted 28800s=8h). If presence expires → channel resolves AUTONOMOUS → unowned main → `DENIED_SOVEREIGNTY`. So: `commit_authority_cli grant --channel ide --branch <branch> --minutes N` before a commit session (re-mints presence). Keep Cursor background Agents STOPPED. Slice 3 branch `ouroboros/oca-slice-3` off main; 8h ide grant `691433421977…` branch-scoped.

**OCA SLICE 3 (#1–#6) COMPLETE + MULTI-ENTRY PRESENCE ROOT-FIX — PR #42396 MERGED to main (112c16f759, 2026-05-19).** #1 `commit_repl.py` (auto-discovered `/commit`, composes substrate only, branch-defaulted grant = full ritual). #2 `commit_authority_archive.py` (bounded `c-N` ring + durable `flock_append_line` JSONL, closed 6-kind taxonomy, master default-FALSE). #3 SSE `commit_authority_decision_recorded` + `GET /observability/commit-authority` (archive emits SSE single-source). #4 `auto_committer` unified `verify_pre_commit(channel="autonomous")` LITERAL (AST-pinned: never env/resolve) → verdict→skipped_reason. #5 `commit_authority_cli hook post-commit` + signed verified-marker → `bypass_suspected` (detect-only) / `consume_grant` (oneshot, default OFF). #6 flag seeds + `test_ledger_sovereignty` isolates persistent-master input. 252 tests green (37 new). **PRODUCTION LOCKOUT root-fixed**: presence was single-global last-write-wins → branch-bound `/commit grant` clobbered operator presence → other-branch commit → `denied_sovereignty`. Now **multi-entry store keyed by (repo_fingerprint, branch)** — mint adds/refreshes only its entry, prunes expired, drop-oldest bounded (`JARVIS_COMMIT_PRESENCE_MAX_ENTRIES` default 64); `valid_operator_presence` matches exact-branch OR whole-repo("") entry; concurrent actors/branches/repos coexist. **Signed transitional dual-format bridge**: every mint also writes a top-level signed whole-repo `record` so pre-realignment (old single-record reader) checkouts stay unblocked on ANY branch; new readers are entries-first. **MANDATORY operator action: realign Cursor checkout to main 112c16f759** — the dual-format bridge is transitional; durable multi-entry needs the new code running. Until then operator is unblocked via the signed whole-repo bridge record (already written to `~/.jarvis/commit_authority/presence.json`).

**OCA SLICE 4 COMPLETE — #2 PR #42956 + #1 PR #42975 MERGED to main (885dfeebbd, 2026-05-19).** #2 `harness_sovereignty_pin.py` — 2 auto-discovered AST invariants making the §1 franken-commit defenses structurally un-removable (autocommitter pre-stage gate source-order pinned + harness owned-workspace boot phase); green-on-real + red-on-7-synthetic (10 tests). #1 `commit_authority_daemon.py` — AF_UNIX socket (no port/network), auth=filesystem perms (0o600 socket in 0o700 dir; REFUSES to serve if it can't chmod-lock), bounded ≤8KiB line protocol, closed verb taxonomy {refresh,status} only, composes ONLY OCA public surface (issue_grant/verify_pre_commit/valid_operator_presence — zero parallel auth/crypto), stale-socket probe-then-unlink, live-socket-never-clobbered, master default-FALSE, 3 flag seeds + AST pin (21 tests). Daemon verified end-to-end on operator checkout (`status` → ok/master/presence/ide/authorized). **AUTONOMOUS-COMMIT PATH STRUCK AGAIN**: a Cursor Agent autonomously committed #2's files onto `predictive-resilience-s1` (8fcf55cfab, verbose-LLM-prose + `[integrity-verified]` trailer — same signature as earlier rogue d802b15a5a) before I branched. Diagnosed: 8fcf55cfab's files BYTE-IDENTICAL to main's canonical #42956 version, zero unique content → `reset --hard main` on s1 was verified-safe (proven, not blind force) — root-correct cleanup of the contamination the OCA arc exists to prevent. s1 realigned to main 885dfeebbd (0/0); operator checkout has ALL new code; durable whole-repo presence + grant re-established; Cursor-path hook exit 0. Lesson reinforced: keep Cursor background Agents STOPPED on this repo root — they autonomously commit WIP/duplicate work; the structural fix (sovereignty+presence+multi-entry+daemon) is the durable defense but operator policy still required until Agents are off.

Slice 3 LEGACY-NOTE (superseded — done): #1 `commit_authority_repl.py` (/commit verbs), #2 `commit_authority_archive.py` (c-N ring), #3 SSE `commit_authority_decision_recorded`+`GET /observability/commit-authority`, #4 `AutoCommitter` composes `verify_pre_commit(channel=autonomous)`, #5 post-commit grant-consume+bypass-detect, #6 memory+tests. CLI verb for `persistent_master` enable/disable (operator-reproducible sovereignty toggle) is a Slice-3 addition.

Original Slice 3 PENDING note (superseded by above for #0): `commit_authority_repl.py` (/commit verbs auto-discovered), `commit_authority_archive` + SSE `commit_authority_decision_recorded` + `GET /observability/commit-authority`, `register_flags()` FlagRegistry seeds (substrate already has 5; archive/repl add more), `AutoCommitter` composing `verify_pre_commit(channel=autonomous)` in `commit()`, post-commit grant-consume + `bypass_suspected` detect.

**Slice 1 detail — COMPLETE 2026-05-19** (substrate + spine):
- `backend/core/ouroboros/governance/operator_commit_authority.py` — pure substrate, master `JARVIS_OPERATOR_COMMIT_AUTHORITY_ENABLED` default-FALSE (§33.1, byte-identical hook chain until graduated).
- Closed taxonomies (AST-pinned): `CommitAuthorityVerdict` (8: authorized/denied_no_grant/denied_expired/denied_scope/denied_governance_drift/denied_sovereignty/disabled/channel_unknown), `CommitChannel` (5: repl/cli/ide/daemon/autonomous).
- Composes (zero duplication): `ledger_sovereignty` (autonomous-channel ownership, delegated), `governance_manifest.verify_governance_state` (drift hash-cap), `cross_process_jsonl.flock_append_line` (append-only grant ledger), `roadmap_reader.compute_signature/verify_signature` (canonical constant-time HMAC — NO parallel hmac; fail-closed if unimportable). `operation_mode` read-only for adaptive PLAN/ANALYZE TTL shortening.
- Grant model: time-bounded HMAC-signed grants at `.jarvis/commit_authority/grants.jsonl`; per-machine secret auto-bootstrapped at `~/.jarvis/commit_authority/secret` (0600, O_EXCL, **out-of-repo, no shell-env dependency** — this is what makes the IDE path work). Hand-edited/forged grant line → signature fails → treated as DENIED_NO_GRANT. Revoke = append-only tombstone (by id or `*`-all); optional one-shot `consume_grant`.
- Public API NEVER raises: `verify_pre_commit / issue_grant / revoke_grants / consume_grant / master_enabled`.
- 6 AST pins (verdict taxonomy / channel taxonomy / authority-asymmetry / master-default-false / composes-canonical / subprocess-discipline). 5 FlagRegistry seeds.
- 59 spine tests green (`tests/governance/test_operator_commit_authority.py`); 117 neighbor substrate tests green; `test_meta_phase_runner.py` 33 green. Pre-existing unrelated collection errors in `test_opp_miner_merkle.py`/`test_phase11_graduation_pins.py` (intake.sensors) — NOT in changeset.

**Pending — operator-gated, each a separate PR (do NOT auto-proceed):**
- **Slice 2**: versioned hook chain (`scripts/install_hooks.py` + thin tracked `scripts/hooks/pre-commit` → `python3 -m ...commit_authority_cli hook pre-commit`, chain to `pre-commit.project`), **replace the untracked bash `.git/hooks/pre-commit`** (security-sensitive, hard-to-reverse to operator's local hooks), post-commit bypass-detect, + AutoCommitter `commit()` integration (channel=autonomous, sovereignty required, new skipped_reason). Reference: current bash hook reads `JARVIS_COMMIT_TOKEN_HASH_FILE` (default `~/.jarvis/commit_token.sha256`), execs `$(git rev-parse --git-path hooks)/pre-commit.project`.
- **Slice 3**: REPL `/commit status|grant|revoke|recent` (`commit_authority_repl.py`, auto-discovered like `mode_repl.py`/`posture_repl.py`) + `commit_authority_archive.py` (bounded ring, `c-N` refs) + SSE `commit_authority_decision_recorded` in `ide_observability_stream.py` + `GET /observability/commit-authority` in `ide_observability.py`.
- **Slice 4** (optional): async local authority broker over Unix socket `~/.jarvis/commit_authority.sock` so GUI git asks Python without any env var.

Acceptance bar (done = operator commits from Cursor GUI after `/commit grant`, no shell export; no-grant fails with clear verdict not raw bash; AutoCommitter still refuses non-owned main; governance drift still blocked; hooks versioned/reinstallable; observability triad live). Manifesto §6/§8/§33.1, Reverse Russian Doll. See also [[project-iron-gate-commit-and-gh-workflow]] (the operator-held commit-token + gh sandbox workflow this supersedes for the human path).
