---
title: Project Section40 Open Items
modules: [backend/core/ouroboros/governance/governance_manifest.py, backend/core/ouroboros/governance/auto_committer.py, backend/core/ouroboros/governance/governance_boundary_gate.py, backend/core/ouroboros/governance/m10_arch_proposer_graduation_contract.py, backend/core/ouroboros/governance/unified_graduation_dashboard.py, backend/core/ouroboros/governance/cross_session_coherence_rig.py, backend/core/ouroboros/governance/mirror_self_spec_drift.py, backend/core/ouroboros/governance/mirror_self_test.py, backend/core/ouroboros/governance/conversation_bridge.py, backend/core/ouroboros/governance/meta_prior_learning.py, backend/core/ouroboros/governance/autonomous_graduation_engine.py, backend/core/ouroboros/governance/graduation_override_ledger.py]
status: historical
source: project_section40_open_items.md
---

**§40 ADVANCED FORWARD ROADMAP audit + closure (2026-06-05).** §40 (PRD line 38) is a 22-item ~10-week roadmap. A filename-grep wrongly flagged 4 items as "genuinely open" (#2 hash-cap, #4 M10 graduation, #6 coherence harness, #14 mirror-self). **Verify-first (4 parallel Explore agents + confirmation greps) OVERTURNED that — 3 of 4 were already built under different names; only #14 had a real code gap.** Lesson: filename-grep is NOT a graduation audit — the files exist under non-obvious names.

**State of the 4 (confirmed by reading code, not agent word):**
- **#2 hash-cap signature gate — CODE-COMPLETE.** `governance_manifest.py` (FileSignature/ManifestSnapshot/sha256, `verify_governance_state`/`is_refusal_verdict`/`refresh_signed_manifest`) + **already integrated into `auto_committer.py` lines ~373-387** (refuses commit on `governance_manifest_drift`). Master `JARVIS_GOVERNANCE_MANIFEST_ENABLED` §33.1 default-FALSE (opt-in: operator must baseline first). Composes governance_boundary_gate.py (#1, default-TRUE, also already built). Remaining = OPERATOR-operational only (run refresh-manifest to baseline, flip flag).
- **#4 M10 ArchitectureProposer graduation — CODE-COMPLETE.** Substrate Slices 1-5 all in `governance/m10/` (primitives, unhandled_pattern_miner, proposal_synthesizer, lifecycle 5-layer validation, proposal_store, cadence_runner, observability, repl). Graduation contract `m10_arch_proposer_graduation_contract.py` (758 LOC, `is_ready_for_graduation`, 5-gate verdict, AST pins) **and already wired into `unified_graduation_dashboard.py` lines 399-527**. Master `JARVIS_M10_ARCH_PROPOSER_ENABLED` default-FALSE. Remaining = optional thin M10-LOCAL surface (`/m10 graduation-status` REPL + HTTP route) — convenience only, dashboard already covers it.
- **#6 cross-session coherence harness — CODE-COMPLETE.** `cross_session_coherence_rig.py` (`walk_session_arc`→`ArcDriftReport`, 4 fingerprinters, composes canonical `cross_session_harness.compute_drift`), reads `.ouroboros/sessions/*/summary.json` via last_session_summary. Mathematical primitives all exist (Move 7=cross_op_semantic_budget, Move 4=invariant_drift_auditor, M9=curiosity_gradient). Master `JARVIS_CROSS_SESSION_COHERENCE_RIG_ENABLED` §33.1 default-FALSE. Remaining = EXTERNAL PhD plotting notebook + an empirical ≥50-session/≥7-day baseline run (not code).
- **#14 mirror-self test — CLOSED THIS SESSION (PR #69283, `mirror_self_spec_drift.py`).** Core ledger `mirror_self_test.py` (prediction/actual calibration + belief bridge + SSE + 5 AST pins) already shipped; the missing half was "reproduce canonical behavior from CLAUDE.md spec, drift→repair". NEW validator: parse CLAUDE.md `(JARVIS_FLAG, claimed_bool default)` × FlagRegistry actual default → SpecDriftRecord ONLY when (registered AND BOOL AND mismatch); doll-metric(#15)-gated severity (WARN→CRIT at completion_ratio≥0.75); feeds EXISTING invariant_drift pipeline via new `DriftKind.SPEC_DRIFT`+`to_invariant_drift_records()` (no parallel observer/bridge); DETECTION+ADVISORY only (never mutates). §33.1 `JARVIS_MIRROR_SELF_SPEC_DRIFT_ENABLED` default-FALSE. **LIVE = ALIGNED (13 claims, all match, 0 drift)** — healthy. 37 tests + 333 regression.

**Genuinely still-open §40 items (precise re-audit 2026-06-05):** 20/22 have files; ONLY **#12 Schelling-point prior** (no file) + **#20 cross-repo causal mirror** (trigger-gated on J-Prime, deferred by design) are unbuilt. #17 conversation_bridge.py + #22 meta_prior_learning.py DO exist (file-present). §40.6 (dated 2026-05-11) already declared "all 22 closed."

**Slice 96 — Autonomous Telemetry-Driven Graduation Engine MERGED (PR #69284, `autonomous_graduation_engine.py` + `graduation_override_ledger.py`).** Solves the "we don't do manual audits to flip flags" mandate. AUTONOMOUSLY evaluates §33.1 graduation readiness by composing the 8 graduation contracts + `unified_graduation_dashboard` + `GraduationLedger.eligible_flags()` + `shipped_code_invariants.validate_all()` against absolute thresholds (zero regression / zero AST drift / contract-ready; AND-composed, any fail→HOLD+delta). **TIERED autonomy (the §1 zero-order-doll invariant):** `FlagSpec.category is Category.SAFETY` → SAFETY tier → APPROVAL_ADVISORY (operator one-click); else STANDARD → AUTO_FLIP via durable `.jarvis/graduation_overrides.jsonl` + `apply_overrides_to_environ()` boot applier (sets os.environ only if not operator-set; **NO source edit → honors hash-cap/boundary gate; env is the sanctioned opt-in channel**). SAFETY structurally cannot auto-flip (3 barriers). **Adversarial review CAUGHT 2 fail-OPEN holes → flipped to fail-CLOSED:** unknown/None FlagSpec.category → was STANDARD/auto-flip → now SAFETY (positive-recognition required); validate_all() RAISING → was swallowed-to-() = "clean" → now None sentinel → Gate B HOLDs all. §33.1 master + apply-gate both default-FALSE. 26 tests + 177 regression. **First LIVE readout = 0 candidates** (graduation ledger dormant, no clean-session evidence). Composition seam: FlagRegistry is pinned "descriptive-only, zero authority — no registry.set()"; flips ONLY via source-edit (trips boundary gate) or env (sanctioned) — Slice 96 uses env.

**§40 #14 spec-drift residual CLOSED** this session too (PR #69283) — see [[project_section40_open_items]] above. PRD §40.6.1 addendum (PR #69285) records all 2026-06-05 completions honestly. **14 PRs merged this session total.** file-present ≠ master-active (most §33.1 default-FALSE); Slice 96 automates the EVALUATION, not safety-flag activation. See [[project_cc_parity_benchmark]], [[project_repo_git_pr_ops]].
