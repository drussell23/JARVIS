---
title: Project Repair Context Bridge
modules: [docs/architecture/REPAIR_CONTEXT_BRIDGE_ADD.md, tests/governance]
status: open
source: project_repair_context_bridge.md
---

After the Oracle Persistence + Lazy-Traversal epics closed (PRs #69543–#69553 merged), the operator asked to build an "Ouroboros Self-Healing Mutation Engine" (sensor event bus → blast-radius → generate+verify+commit). **Audit finding (Explore agent, grounded in OUROBOROS_VENOM_PRD.md + code): ~80-90% ALREADY EXISTS** — the whole repo IS the self-healing engine. On-track per PRD (vision = proactive autonomous organism; §9 Phases 1-5 delivered; §13 success criteria mostly ✅; the one ⚠️ = multi-file autonomous changes at scale).

What exists (do NOT rebuild): 20 sensors incl `TestFailureSensor` → `TrinityEventBus`/`UnifiedIntakeRouter` (test_failure priority 1); 11-phase orchestrator FSM; L2 `repair_engine.py` (`JARVIS_L2_ENABLED` default-true, 5-iter, iterate-with-feedback, **already has a `repair_context` slot** in `_generate_repair_candidate` + provider `generate(...,repair_context=)`); `SemanticGuardian` (10 patterns, post-VALIDATE/pre-GATE, soft→hard); Iron Gate; post-apply VERIFY; `AutoCommitter` (O+V-signed commits); `failure_classifier.patch_signature_hash`. Oracle blast-radius used by `oracle_prescorer.py` + `context_expander.py` (neighborhood injection) BUT NOT inside the L2 repair loop.

THE GENUINE GAP = L2 generates fixes BLIND to graph structure. **Repair Context Bridge ADD** (`docs/architecture/REPAIR_CONTEXT_BRIDGE_ADD.md`, PR #69554 OPEN, branch repair/context-bridge-add): 3 phases, all glue + ONE new gate, leveraging the just-shipped lazy GraphBackend:
1. Deep failure ingestion — enrich `TestFailure` signal (`intent/test_watcher.py`+`signals.py`) w/ frame-by-frame traceback AST-mapped to Oracle node keys (line→node via stored line_number+line_count). Additive, fail-soft.
2. Graph-informed context (STEER) — new `repair_context_bridge.py` populates the EXISTING repair_context slot w/ a dependency cone (compute_blast_radius/find_call_chain/get_dependencies via SqliteLazyGraphBackend, sub-10MB). HONEST: prompt clause steers, doesn't enforce.
3. Pre-flight Structural Validation Gate (ENFORCE — Zero-Regression Shield) — new `structural_validation_gate.py` parses candidate via EXISTING `ast_compile_helper.analyze_python_source_for_oracle`, simulates cone-scoped graph delta (no disk write), REJECTS new-cycle/new-dead-code/severed-call-chain → divergence signature appended to L2 failure context → EXISTING iterate-feedback loop self-corrects (bounded by existing max_iters/timebox, NO new loop). Mirrors SemanticGuardian seam + soft→hard (new_cycle hard first; dead-code/severed-chain soft until soak). Complementary to SemanticGuardian (pattern vs structural-delta).

Gated `JARVIS_REPAIR_CONTEXT_BRIDGE_ENABLED` + `JARVIS_REPAIR_STRUCTURAL_GATE_ENABLED` default-OFF (byte-identical L2 when off). 3-slice decomposition. Honest bounds: steer-vs-enforce, cone-scoped (regression outside cone out of scope), structure-not-behavior (VERIFY stays behavioral authority). ADD merged (#69554). See [[project_oracle_sqlite_persistence]].

**SLICE 1 BUILT (PR #69555 OPEN, branch repair/slice1-signal-ingestion):** Deep Semantic Failure Ingestion. New pure+fail-soft `governance/intent/repair_traceback.py` — `parse_pytest_tracebacks` (parse `--tb=short` FAILURES into per-test frames via `_BLOCK_RE` header + `_FRAME_RE` `file.py:line: in func`) → `_frames_for_test` (correlate `file::Class::test` id to block by func name) → `map_frames_to_nodes` (innermost-span line→node via injectable `NodeResolver.nodes_in_file`+`get_node`; `_to_rel` KEY FIX: pytest runs cwd=repo so RELATIVE frame paths ARE the Oracle file_path — only ABSOLUTE paths need relative_to(root), robust to mapping-process CWD≠repo) → `build_traceback_map` deepest-first de-duped `fault_node_keys`. Prod resolver = `get_oracle()._graph._backend` (lazy, fail-soft). Wired into `test_watcher.py`: additive `TestFailure.traceback_evidence` field; async `_enrich_failures` offloads parse+sqlite map via `asyncio.to_thread` (zero-block sensor loop); merged into `IntentSignal.evidence` in `process_failures`. Gate `JARVIS_REPAIR_CONTEXT_BRIDGE_ENABLED` default-OFF→evidence byte-identical. 32 new tests, 174 intent-suite green. **§8 Q2 RESOLVED in ADD §3.1 Dynamic Graph Reachability Matrix:** severed call-chain = regression ONLY when it flips a downstream component from reachable-from-live-entry-point to unreachable (pre-delta vs post-delta reachability of endpoint from root set R); dead/orphaned-only severance = authorized structural pruning (ACCEPT + non-blocking `structural_prune` telemetry). Entry-point roots (active test suites incl. failing test, `__main__`/loop, live external callers) DERIVED from graph never hardcoded; reuses shipped `descendants`/`find_call_chain` over cone what-if subgraph (no new traversal engine); soft→hard graduation.

**SLICE 1 MERGED** (PR #69555 squash, main d010ed9). **SLICE 2 BUILT (PR #69556 OPEN, branch repair/slice2-context-bridge):** Graph-Informed Cognitive Context (the steer). New `governance/repair_context_bridge.py` — `RepairCone` (structured proximity-ordered node/edge set, NOT embedding) + `RepairContextBridge`. ADAPTIVE fault-key resolution (no hardcode): Slice 1 `fault_node_keys` parsed from `ctx.intake_evidence_json` → `find_nodes_in_file(target_file)` → `find_nodes_by_name(test)` (sharper w/ Slice 1, works standalone). COMPOSES shipped Oracle CKG primitives (`get_oracle()._graph`): `compute_blast_radius`(downstream dependents, direct-then-transitive proximity order)/`get_dependencies`(upstream)/`find_call_chain`(test→fault causal path) — NO new traversal. `async build()` SELF-GATES on master flag + offloads SYNC lazy-graph query path via `asyncio.to_thread` (zero-block L2 loop). Caps env-tunable `JARVIS_REPAIR_CONE_MAX_SYMBOLS`(50)/`JARVIS_REPAIR_CONE_BLAST_DEPTH`(2), top-K-by-proximity truncation. Fail-soft everywhere→None cone→graph-blind repair. INJECTION: additive `RepairContext.dependency_cone` field (op_context.py, default None→prompt byte-identical); populated in `repair_engine._run_inner` via new `_build_dependency_cone` (lazy bridge instance, fail-soft) for NEXT iteration's GENERATE; rendered in `providers._build_codegen_prompt` REPAIR MODE block ONLY when present. Shared master `JARVIS_REPAIR_CONTEXT_BRIDGE_ENABLED` (Phase 1+2). KEY DATA PATH: Slice1 evidence→intake serializes→`ctx.intake_evidence_json`→bridge. `RepairContext` is frozen — additive field w/ default works w/ `dataclasses.replace`. NodeID str = `repo:file:name`. repair_engine logger is `_logger` not `logger`. 15 bridge tests + 112 repair-engine + 200 intent/bridge green; OFF byte-identical. PRE-EXISTING unrelated collection errors in tests/governance (merkle_consult_enabled missing from opportunity_miner_sensor; l2_exercise_corpus fixtures mis-collected) — NOT my scope. **SLICE 2 MERGED** (PR #69556 squash, main 2387aa3). **SLICE 3 BUILT (PR #69557 OPEN, branch repair/slice3-structural-gate) — ARC COMPLETE (the enforce/zero-regression shield):** New `governance/structural_validation_gate.py` — `StructuralValidationGate` + 3 zero-LLM proofs over an ISOLATED what-if delta. Runs POST-GENERATE/PRE-SANDBOX in `repair_engine._run_inner` (before test run → structural regression fed back without burning a validation run). **PROOFS:** (1) Acyclicity Guard HARD (DFS back-edge, cycle absent pre-delta→reject, coords=closed loop); (2) Path Reachability Matrix §3.1 (BFS pre/post from roots; node flips reachable-from-live-root→unreachable=reject; dead-only severance=`StructuralPrune` ACCEPT+telemetry, never false-reject); (3) Boundary Invariant Verification SOFT (changed intra-file symbol signature w/ un-updated in-cone callers — as-built 3rd proof; standalone dead-code subsumed by reachability prune path). **BLINDSPOT ARMOR (isolation+thread-safety):** candidate parsed OFF-PROCESS via `analyze_python_source_for_oracle` (own interpreter, never touches live backend); what-if = per-call local `_DeltaGraph` from READ-ONLY cone snapshot (`OracleConeReader`), NEVER written back to SqliteLazyGraphBackend → concurrent iters can't pollute/contend. **PHASE 3 FEEDBACK:** structured `DivergenceSignature`(kind+severity+exact AST coords: closed loop/severed edge path/symbol+old→new sig), hashed via `failure_classifier.patch_signature_hash`, rendered to targeted correction → routed into L2 as `failure_class="structural"` RepairContext → existing iterate-feedback loop self-corrects via `continue`→regenerate. BOUNDED: `JARVIS_REPAIR_STRUCTURAL_MAX_REJECTS`(3) consecutive rejects fall through to sandbox (friction not wall) + each reject consumes 1 max_iterations slot (NO new unbounded loop). Per-check severity env `JARVIS_REPAIR_STRUCT_{CYCLE,REACH,BOUNDARY}_SEVERITY`(cycle/reach hard, boundary soft); `JARVIS_REPAIR_STRUCTURAL_SOFT_BLOCKS` graduates soft→blocking post-soak. Gate `JARVIS_REPAIR_STRUCTURAL_GATE_ENABLED` default-OFF→L2 byte-identical; fail-soft→ACCEPT on any sim failure. WIRING: shared `_build_cone_object` (reused by Slice2 render + Slice3 gate; `bridge.build(force=True)` bypasses steer flag so gate gets cone under its own master); `last_failing_tests` tracks classification.failing_test_ids for reachability roots; reset `structural_reject_streak` on sandbox proceed. KEY SHAPES: `analyze_python_source_for_oracle(caller,source,*,filename,repo_name,relative_path)→OracleAnalysisResult(.outcome[.value=='ok'],.nodes[NodeData.node_id/.signature],.edges[(NodeID,NodeID,EdgeData.edge_type.value)])` async process-pool; CKG `get_edges_from(k)→[(tgt,{edge_type})]`/`get_edges_to`/`get_node(k)→{signature}`/`find_nodes_by_name`. 20 gate + 132 repair-engine + 209 bridge/intent green; OFF byte-identical. **3-SLICE REPAIR CONTEXT BRIDGE ARC COMPLETE:** Slice1 ingest(fault coords)→Slice2 steer(cone in prompt)→Slice3 enforce(structural gate). **SLICE 3 MERGED** (PR #69557 squash, main b77a6bc).

**L2 ENGINE COMPLETION (PR #69558 OPEN, branch repair/l2-completion) — "Coordinated Transactional & Stochastic Mutation Engine":** completes the L2 engine on top of the bridge. 2 new pure modules + thin wiring; all gated default-OFF→L2 byte-identical. **Phase 1 multi-file** (`repair_multifile.py`): `extract_candidate_files`+`topo_sort_files` (dependency-before-dependent via Oracle `find_nodes_in_file`+`get_dependencies`, cycle-safe Kahn); `repair_engine._resolve_multifile_batch` + multi-file sandbox apply loop (sandbox = atomic boundary, all-or-nothing); primary file = topo-root (full_content set to it for gate/cone); tests scoped to all changed files; `JARVIS_L2_MULTIFILE_ENABLED`. (L2 previously HARD-STOPPED on multi-file: no top-level full_content→`candidate_unusable`.) **Phase 2 stochastic escape** (`repair_progress.py`): the 2 flat stops (oscillation_detected, no_progress_streak) → bounded escalation; deterministic ladder (localized→full-method→module rewrite) via new `RepairContext.escalation_directive` (rendered FIRST in REPAIR MODE) + cone_depth_bump (threaded `_build_dependency_cone`→`_build_cone_object`→`bridge.build(depth_override=)`→`_build_sync` base+override); budget `JARVIS_L2_MAX_ESCALATIONS`(2)→guaranteed termination; `JARVIS_L2_DIVERGE_ESCAPE_ENABLED`. **Phase 3 progress v1.1** (`repair_progress.py` `RepairProgressTracker`): sig-set strict-narrowing=progress at constant count + Operational Velocity Score; non-positive velocity+persistent errors→`_throttle_graph_cache()` calls Oracle backend `apply_pressure("high")`; `JARVIS_L2_PROGRESS_V11_ENABLED`. 22 unit tests + 147 repair-engine + 317 combined green; OFF byte-identical. BRANCH HYGIENE LESSON: committed L2 onto already-merged slice3 branch by mistake; net diff was still correct (slice3 files identical to main don't reappear) but recreated clean single-commit branch off origin/main via `git checkout <sha> -- <files>`.

**DOC SURVEY (4 parallel Explore agents over PRD/North-Star/ADDs/specs/code) — verified unbuilt O+V gaps for future arcs:** (1) **"Unwired brain"**: 6 synthetic-soul learning modules EXIST w/ 0 live-path callers — `predictive_postmortem`/`postmortem_fusion`/`sleep_consolidation_pass`/`meta_prior_learning`/`adversarial_autobiography`/`counterfactual_rehearsal_mode` (North Star §51 Tier1/Slice101 = live-loop's OWN roadmap → HIGH COLLISION RISK if wired manually). (2) `adversarial_reviewer{,_service,_hook}.py` exist, no orchestrator post-PLAN call. (3) **L2 gaps (NOW BUILT in #69558)**. (4) Tier-D blockers (3-6mo): OS-level runtime containment (seccomp/gVisor, ~8.6% runtime-construct escape residual), operator-independent recursion-depth counter, M10 ArchitectureProposer wiring (Order-2 driver). (5) JARVIS_LEVEL_OUROBOROS.md tiers: OperationAdvisor (pre-CLASSIFY judgment), PredictiveRegressionEngine, Emergency Protocols/AlertAccumulator, Personality Engine, Autonomous Daily Review. (6) Other: vision_vlm_adapter.py:147/231 stub→real DoubleWord, dependency_file_gate multi-language, RISE trajectory fine-tuning collection. AGENTS WRONGLY listed already-merged work (RepairContextBridge/SQLite-lazy/telemetry-unification/distillation) as "not started" — they read DRAFT docs; FILTER survey output against actual main.

**SOAK PENDING:** operator chose soak AFTER this build. Graduation flips `JARVIS_REPAIR_CONTEXT_BRIDGE_ENABLED`+`JARVIS_REPAIR_STRUCTURAL_GATE_ENABLED`+(L2) `JARVIS_L2_MULTIFILE_ENABLED`+`JARVIS_L2_DIVERGE_ESCAPE_ENABLED`+`JARVIS_L2_PROGRESS_V11_ENABLED` default-ON + soft→hard boundary. NEEDS #69558 merged first + live battle-test (macOS-constrained per [[project_sovereign_execution_boundary]]; run bounded headless from MAIN checkout w/ .env).
