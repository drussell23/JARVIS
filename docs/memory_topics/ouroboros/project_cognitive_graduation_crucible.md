---
title: Project Cognitive Graduation Crucible
modules: [backend/core/ouroboros/governance/, scripts/install_live_fire_soak_cron.sh, scripts/crucible_cadence.sh, tests/governance/test_ttft_zeroshot_decay.py, docs/superpowers/specs/2026-06-20-sovereign-transport-profiler-matrix.md, scripts/ignite_sovereign_cloud_node.py]
status: open
source: project_cognitive_graduation_crucible.md
---

PRD Priority #1 = Live-Fire Graduation Cadence (flip 12+ default-false cognitive flags after clean soaks). The operator authorized building a "Sovereign Cognitive Graduation Crucible" to do it autonomously. **Key audit finding: ~95% already shipped + gated off** — do NOT rebuild. Existing substrate (all `backend/core/ouroboros/governance/`): `flag_registry` (flag+default discovery), `adaptation/graduation_ledger.py` (`CADENCE_POLICY`, `record_session`, `eligible_flags`, `is_eligible`), `phase9_orchestrator.next_recommended_flag`, `graduation/live_fire_soak.py` (forks battle-test, `DEFAULT_SEED_INTENTS_PER_SOAK=3`), `graduation/graduation_contract.py` (`arbitrate` priority matrix anomaly>recovery>predicate>blocklist, `get_contract`, `predicate_requires_live_kernel_validation` capstone pattern), `graduation/telemetry_parse.py` (`parse_metrics`→Metrics), `autonomous_graduation_engine.py` (`evaluate_graduations`/`execute_graduations`, AUTO_FLIP→override_ledger; Gate A eligibility+B AST+C contract), `graduation_orchestrator.persist_flag_to_env` (.env, credential-safe), `graduation_override_ledger` (shadow-mode fail-closed-true). **`sleep_daemon.py:195-209` ALREADY calls evaluate+execute_graduations when engine flag on** = the self-driving idle loop pre-exists.

**Built the genuine 5% gap (5 slices, all gated default-off + fail-soft + byte-identical off; PRs #69606+#69608 code, #69609 PRD §3.11 — ALL MERGED to main):**
- S1 TTFT/AST math-veto: `graduation/crucible_verdict.py` (pure stdlib: ttft_degraded abs-ceiling `JARVIS_CRUCIBLE_TTFT_CEILING_MS`=30000 + ratio `..._DEGRADE_RATIO`=1.5 vs EWMA baseline; ast_corrupted; crucible_evidence bundle) + telemetry_parse Metrics fields (ttft_samples_ms, ast_corruption_signals) + `graduation_contract.predicate_cognitive_graduation` (default for any non-builtin flag; **VETO-ONLY+fail-open** so it never undoes arbiter recovery-override P2 — caught+fixed that regression).
- S2 Telemetry Manifest: `graduation/telemetry_manifest.py` (deterministic [SOVEREIGN GRADUATION] PR body: per-soak TTFT/AST/FSM table + aggregate + why-merge + 🛟 Rollback Strategy (env hot-revert + git revert) + sha256[:16] digest; `manifest_recommends_merge` pure gate).
- S3 source-of-truth PR: `graduation/graduation_pr_proposer.py` `flip_default_to_true` (PURE AST-validated rewriter, flips os.environ.get falsy default→"true", refuses 0/>1-match/already-true/parse-fail) + `propose_graduation_pr` (gated `JARVIS_CRUCIBLE_GRADUATION_PR_ENABLED`) + orange_pr_reviewer body_override/title_override (additive). **CAGE-RESPECTING: proposes PR, never self-merges** (Order-2 amendment_requires_operator locked-true).
- S4 wiring: live_fire_soak folds crucible_evidence into EvidenceRow.telemetry["crucible"] + `recent_clean_crucible_evidence(flag,limit=3)`; `execute_graduations` AUTO_FLIP→`_maybe_propose_source_pr` (fail-soft).
- S5.5 literal-site locator: `locate_default_literal_file` — **FlagSpec.source_file points to DECLARATION site, NOT default-literal site (only 6/96 align!)**. Bounded codebase search (backend/+scripts/, skips .git/.venv/.claude/tests) for single flippable falsy default; hint wins; abstains on ambiguous. Clean-resolvable 6→10; other 87 correctly abstain (indirect accessors/multi-site) — conservative, no wrong rewrites.

**First Cognitive Ignition PROVEN deterministically on REAL flag** `JARVIS_EXPLORATION_LEDGER_ENABLED`: located exploration_engine.py (not the registry's exploration_ledger.py), flipped ""→"true" AST-valid, rendered full manifest (3/3 PASS, TTFT 842ms<30000, 0 AST, 0% FSM-exhaust, digest e8ad507b71c5fd88), title `[SOVEREIGN GRADUATION] Activated JARVIS_EXPLORATION_LEDGER_ENABLED`. 38 crucible tests + 200+ graduation/contract/soak/orange green.

**Master flags to IGNITE (all default-off):** `JARVIS_GRADUATION_LEDGER_ENABLED`, `JARVIS_AUTONOMOUS_GRADUATION_ENGINE_ENABLED`, `JARVIS_LIVE_FIRE_GRADUATION_SOAK_ENABLED`, `JARVIS_LIVE_FIRE_USE_GRADUATION_CONTRACT`, `JARVIS_LIVE_FIRE_TELEMETRY_ARBITER_ENABLED`, `JARVIS_PHASE9_ORCHESTRATOR_ENABLED`, `JARVIS_CRUCIBLE_GRADUATION_PR_ENABLED`. KEEP shadow mode ON (PR-proposes, not .env auto-flip). Cadence driver: `scripts/install_live_fire_soak_cron.sh` or sleep_daemon idle.

**Unattended cloud ignition = genuine multi-HOUR real-clock process**: per flag needs 3 fresh CLEAN soaks (~15-40min each), node needs `gh auth` for the PR (prior nodes logged "gh auth login" errors), Spot reclaim risk over the window. Deterministic ignition proves the machinery; the full autonomous-soak→PR is the remaining real-clock work. See [[project_aegis_shields_up_corrected_diagnosis]] (the enabling cloud substrate).

## Immortal Cadence (Slice 6, PR #69610 MERGED) + LIVE IGNITION (2026-06-20)
Made the cadence preemption-immortal + auth-autonomous, then provisioned a live crucible node:
- **JIT GH auth** (`deploy/gcp_ouroboros_startup.sh`): reads token from Secret Manager `github-token` (preferred) → metadata `jarvis-gh-token` (fallback) → `.env` GH_TOKEN/GITHUB_TOKEN (env_file→container; gh+git use it). I CREATED the empty `github-token` secret + granted compute SA `888774109345-compute@developer.gserviceaccount.com` secretAccessor. **OPERATOR MUST ADD THE TOKEN VALUE** (I can't fabricate one): `printf '%s' "<ghp_TOKEN>" | gcloud secrets versions add github-token --data-file=- --project jarvis-473803`. Without it the cadence SOAKS + accumulates immortal evidence but PR-push fails (logged); picks up the token on next Spot restart.
- **GCS amnesia-proofing**: `state_persistence_daemon` +`gcs` backend (gsutil -m rsync -r -d; reuses build_backup_commands). Startup script RESTORES `.jarvis` from `gs://jarvis-473803-deployments/crucible-state/.jarvis` on every boot BEFORE container-up (preempted Soak2→resume Soak3). `crucible_cadence.sh` syncs after EVERY soak.
- **Cadence overlay** `docker-compose.crucible.yml` + `scripts/crucible_cadence.sh`: ONE soak at a time (no battle-test contention) → evaluate/execute_graduations (propose PR) → GCS sync → sleep 30s. Sets the 7 master flags; KEEPS shadow_mode ON (PR-proposes; Order-2 human/OCA merge gate intact). Armed via `jarvis-crucible-mode=true` metadata (`ignite_sovereign_cloud_node.py --crucible`); legacy soak node unchanged.
- **NODE #1**: `jarvis-ouroboros-soak-20260620-031649` — provisioned 03:16, ran ~6h, Spot-reclaimed/auto-deleted. Surfaced 4 live bugs (below). GCS vault was EMPTY at death (gsutil-in-container bug) → ledger lost.

## Live-soak bugs found + fixed (2026-06-20, each its own PR, all merged + hot-deployed)
The running cadence surfaced real bugs the unit tests didn't. Triaged signal-vs-noise each time (3 correctness, 3 cosmetic, rest healthy telemetry left intact):
- **#69611 repo_root absolute**: `_build_codegen_prompt` `relative_to` crashed — `FlagSpec.source_file` is relative (`.`, JARVIS_REPO_PATH unset) vs ABSOLUTE pytest target paths → L2 generate_error poisoned evidence. Fix: `_resolve_effective_repo_root`/`_abs_repo_root` always return absolute (resolve+fallback cwd); registry normalizes JARVIS_REPO_PATH.
- **#69612 autarky backoff-wait**: transient DW TIMEOUT (39s backoff) routed STANDARD op to disabled Claude → `fallback_skipped` despite 410s budget. Fix `autarky_should_wait_and_retry` (candidate_generator): no-fallback + eta<budget → wait+retry primary ONCE (bounded), else degrade. Env `JARVIS_AUTARKY_BACKOFF_WAIT_ENABLED`.
- **#69615 timeout-salvage (THE graduation blocker)**: soak wrote `session_outcome=complete` summary, then HUNG ~5min in cleanup → 2700s subprocess kill → harness reported `session=unknown/infra` and DISCARDED the completed summary → EVERY soak graded infra, clean count stuck at 0 forever. Fix (`_run_battle_test_subprocess`): on TimeoutExpired, read the on-disk summary; if session_outcome==complete, grade on it. Env `JARVIS_LIVE_FIRE_TIMEOUT_SALVAGE_ENABLED`.
- Cosmetic log-severity (#69613 BudgetSynth, #69614 DW-AUTARKY-ENGAGED/HEDGE-GOVERNOR/CAPABILITY-ALIGNMENT WARNING→INFO via `provider_availability.is_expected_unavailability`): expected-autarky steady state shouldn't be WARNING. NOT hot-deployed (don't reset the soak clock; deploy on restart). Healthy DEBUG/INFO telemetry (AstCompileHelper/WallClockWatchdog/DreamEngine/CommProtocol) LEFT INTACT — silencing = anti-§7.

## Native GCS Vault + GracefulTeardownMatrix (#69616 MERGED) — true immortality
- **Native GCS (no gsutil)**: prior amnesia-proofing called `gsutil` INSIDE the lean container (no gcloud CLI) → "state sync failed", nothing persisted, preemption wiped ledger. Fixed: `state_persistence_daemon` gcs backend now native `google-cloud-storage` SDK (ADC from GCE metadata server), `gcs_push`/`gcs_pull` via asyncio.to_thread, `--once`/`--restore` CLI. `build_backup_commands("gcs")` now returns [] (native owns it). crucible_cadence.sh: native `--restore` before first soak + native `--once` push after each. `google-cloud-storage` added to requirements.txt + docker/requirements-soak.txt. Compute SA `888774109345-compute@` granted `storage.objectAdmin` on `gs://jarvis-473803-deployments`.
- **Teardown (kill 5-min hang)**: root cause = DW background loops (`dw_discovery_refresh_loop`/`dw_heavy_probe_loop`) leaked + wedged on aiohttp connector-timeout awaits. `dw_discovery_runner.shutdown_background_loops(timeout_s=5)` (bounded asyncio.wait_for cancel, never raises); `GLS.stop()` calls it FIRST + `force_session_reset` (wait_for-bounded) before drain. 14 vault + 5 teardown tests.
## Convergence diagnosis (2026-06-20) — why soaks never graded CLEAN (3-layer, proven over 2 nodes/~5h)
Used existing telemetry (no new tracer). Ops reach GENERATE (advisor NOT blocking), Venom loop FAST (7.6s — not the bottleneck). The black hole = **DW model calls fail**: pinned gpt-oss-120b→DoublewordInfraError, sentinel models→infra err, 397B-dottxt→TimeoutError 180s. THREE compounding layers:
1. **Aegis lease 403** (aegis-ON): pre-forward `lease_<kind>` 403 (forwarding.py:406), NO credential-injection log → fails at the lease/session handshake (NOT header-stripping; org doesn't inject DW key — aegis does; daemon-signs lease model, no shared lease secret to desync). Root = forked battle-test inherits parent's CONSUMED single-use bootstrap PSK / stale JARVIS_AEGIS_URL.
2. **DW model health** (aegis on AND off): some models `entitlement_blocked:status_403` ("blocked by a routing rule"), others slow/timeout. **Key fact: DW key IS entitled to 26 models incl gpt-oss-120b; DIRECT curl to gpt-oss-120b SUCCEEDS** — so it's selection/path, not the key.
3. **TTFT/AST veto** (my Slice 1, working AS DESIGNED): slow ops → TTFT degraded → `predicate_cognitive_graduation` vetoes → `contract_metrics_predicate_downgraded` → non-clean. Correct anti-degraded-graduation behavior.
**Aegis-OFF baseline (node #2) PROVED pipeline flawless: op-019ee6d6 reached state=applied.** But baseline STILL clean=0 over 154min (layers 2+3 persist without aegis). So aegis was NOT the sole blocker.

## Layer 1+2 fixes (PR #69617 MERGED, built locally, NOT yet live-validated)
Reuse-first (composes HeavyProbe/topology_sentinel/TtftObserver/aegis_preflight; NO new prober, NO hardcoded model whitelist):
- **Layer 1 (aegis lifecycle)**: `live_fire_soak._build_env_for_flag` strips `JARVIS_AEGIS_BOOTSTRAP_PSK`+`JARVIS_AEGIS_URL` per fork → each soak's own preflight mints fresh PSK/daemon (no consumed-PSK 403).
- **Layer 2 (fleet health)**: armed `JARVIS_TOPOLOGY_SENTINEL_ENABLED`+`JARVIS_TOPOLOGY_HEAVY_PROBE_ENABLED` in crucible.yml (entitlement_blocked→TERMINAL_OPEN quarantine was INERT — sentinel defaulted false!) + added cold-storage LATENCY quarantine seam in candidate_generator selector (`_latency_quarantine_enabled` default-true, skips TtftObserver.is_cold_storage models when another candidate remains). Fleet adapts: 403→TERMINAL_OPEN, slow→cold-skip → routes to fast entitled models. 22 tests.
- **RESIDUAL RISK**: built locally + unit-tested, NOT live-validated. Clean soaks need fast+entitled+diff-capable models to EXIST after quarantine (gpt-oss-120b proven fast direct). Next: re-ignite shields-up node → validate aegis lease handshake works per-fork + fleet routes around slow/blocked models + state=applied through armed aegis.

- **NODE #2 (TORN DOWN 2026-06-20 to stop Spot spend on blocked pipeline; GCS state preserved)**: `jarvis-ouroboros-soak-20260620-125320` provisioned 2026-06-20 12:53 CRUCIBLE MODE. Monitor: `gcloud compute ssh <node> --zone us-central1-a --project jarvis-473803 --command 'sudo docker logs -f jarvis-sovereign-prod'`. Hot-deploy a fix to a running node: ssh → `cd /opt/jarvis/jarvis-ai-agent && sudo git fetch origin main && sudo git reset --hard origin/main && sudo docker compose -f docker-compose.prod.yml -f docker-compose.gcp.yml -f docker-compose.crucible.yml up -d --build` (.jarvis bind-mount + .env GH token persist across recreate). Each refresh RESETS the soak clock — only hot-deploy correctness fixes, not cosmetic.

## Sovereign Zero-Shot & Decay Matrix (PR #69619 MERGED, main 9ea1604193) — 1-strike timeout quarantine
**Problem the σ-test couldn't solve**: TtftObserver `is_cold_storage` needs n>=3 samples (`_MIN_N_FOR_NONDEGENERATE_VARIANCE`) before its σ-test can flag a slow model → a 180s-TimeoutError model taints 2 MORE soaks (clean count stuck) before its variance window fills. (Confirmed live on node 180012: quarantine grew 1→7 but slow models still got picked before σ-flag; 1/3 clean.)
**Fix (additive to EXISTING observer persistence — NO duplicate "Immortal Topology Memory" module; REJECTED that as redundant after verifying TtftObserver already persists 16 models to `.jarvis/dw_ttft_observer.json` and rehydrates per fork)**:
- `dw_ttft_observer.py`: `_zero_shot_bans: Dict[str,float]` (model→ban-timestamp). `record_timeout(model_id, op_id="")` = 1-strike ban + autosave (gated, never raises). `is_cold_storage` PREPENDS `_zero_shot_active()` BEFORE the σ-test (bypass n>=3). `_zero_shot_active` decays expired bans ON READ — TTL `_zeroshot_ttl_s()` env `JARVIS_TTFT_ZEROSHOT_TTL_S` default 28800s/8h clamped [300s,24h]. Master `zeroshot_timeout_quarantine_enabled()` env `JARVIS_TTFT_ZEROSHOT_ENABLED` default true. Persisted additively in `zero_shot_bans` field (NO schema bump, SCHEMA_VERSION stays ttft_observer.1 → old state loads) → ban survives fork (immortal like entitlement bans) yet decays by TTL (transient outage self-forgives). `clear()`/`load()`/`save()` updated.
- `candidate_generator.py`: dispatch failure handler (~L3924, after dw_fault_taxonomy import) → `if isinstance(exc, asyncio.TimeoutError) or _s241_gen_timeout(exc):` lazy `get_ttft_observer().record_timeout(model_id, op_id=op_id_short)` (fail-soft). Composes the EARLIER `_latency_quarantine_enabled` cold-storage skip seam (#69617) → a timed-out model is skipped on the VERY NEXT op, not 3 ops later.
- 10 tests `tests/governance/test_ttft_zeroshot_decay.py` + 8 fleet_exhaustion = 18 green. Decay test backdates timestamp (`time.time()-9*3600`) NOT sleep (TTL floor 300s).
**HOT-DEPLOYED to node `jarvis-ouroboros-soak-20260620-180012`** then TORN DOWN (see Transport Profiler section). Container = `jarvis-sovereign-prod`, repo on node = `/opt/jarvis/jarvis-ai-agent`.

## Sovereign Transport Profiler & Batch Exemption Matrix (PR #69621 MERGED, main d5340baf14) — THE convergence root-cause fix
**Diagnosis (live on node 180012, zero-shot seam VALIDATED first via #69620)**: zero_shot_bans populated correctly (3 models) — fix worked — BUT this exposed the REAL blocker. Direct DW RT probe to gpt-oss-120b = **http=200, 1.23s (DW HEALTHY)**, yet the soak's heavy codegen timed out 90–180s across ALL models. Root cause: diff-capable codegen resolves to `-dottxt` model variants that ONLY serve via the DW BATCH API (RT yields `done_before_content`, empty). TWO layers disagree on transport: (a) budget layer `_compute_primary_budget` (pre-call) sizes the op for the **autarky RT budget 180s** because the transport-hedge (default-on) makes `_slice36_should_force_batch` return False; (b) dispatch layer then races RT∥batch but on `-dottxt` only the batch arm can win → strangled by the 180s budget → TimeoutError → fleet exhaustion → no state=applied → 0 clean. NOT model health (zero-shot would wrongly blacklist the fleet's best models for batch latency).
**Reuse-first audit (3 parallel Explore agents) = ~90% ALREADY BUILT**: `op_park_store.ParkedOpStore`+`park_signal`+`generate_park_wrapper.maybe_park_or_resume` (WIRED into generate_runner.py:839, 3-path PARK-EMIT/RESUME/LEGACY, out-of-pool continuation w/ WIDENED 390s timeout), `BatchFutureRegistry`(register/resolve/reject/wait)+`event_channel.py:/webhook/doubleword`+3-tier race `_await_batch_result`(webhook∥adaptive-poll FIRST_COMPLETED), `background_agent_pool.submit_for_resume`/`is_resumed_dispatch`, `dw_transport_disambiguator`(classifies done_before_content), `state_persistence_daemon`(GCS). DID NOT REBUILD.
**The 10% bridge (operator Option 1 = learn-then-detach + immortal profile)**:
- **S1 `dw_transport_profile.py` (NEW)**: immortal per-model batch-only profile → `.jarvis/dw_transport_profile.json` (GCS-backed, rehydrates per fork). `record_batch_only`/`is_batch_only`/`clear`, default TTL=0 (immortal; positive TTL re-opens RT re-probe), master `JARVIS_DW_TRANSPORT_PROFILE_ENABLED` default-true failure-path-only. Mirrors TtftObserver persistence, reuses its `_atomic_write`. No hardcoded model list.
- **S2 LEARN (doubleword_provider `_s190_hedge_outcome`)**: `winner=="batch" and rupture_swallowed` = canonical RT-yields-done_before_content signal w/ resolved model in scope → `record_batch_only`. Gated, fail-soft.
- **S3+S4 (candidate_generator, before budget layer ~L5454)**: `is_batch_only(model_id)` → force `_force_batch=True` (REUSES existing batch-budget branch L7201 → batch cap, NO new budget code) + stamp `ctx.async_batch_payload=True`. Zero-shot seam (L5639) grants async_batch_payload ops **ban immunity**.
- **S5 (op_park_store.should_park_for_route + generate_park_wrapper)**: async_batch_payload op parks regardless of queue_pressure on `_BATCH_CAPABLE_ROUTES`={standard,complex,background} (NOT the throughput-tuned _DEFAULT_PARK_ROUTES which omits standard — the wedge proved standard diff-codegen needs it) → frees worker → out-of-pool continuation(390s) → BatchFutureRegistry resolves → submit_for_resume re-hydrates → FSM→state=applied. No re-park loop (continuation calls generate() direct; resumed never re-parks).
- `OperationContext.async_batch_payload: bool=False` (byte-identical default). docker-compose.crucible.yml flips `JARVIS_BG_PARK_ENABLED`(default-off per §33.1, the ONLY non-default knob)+`JARVIS_DW_TRANSPORT_PROFILE_ENABLED`. 30 new tests (12 profile+7 park gate+11 zeroshot)+88 regression green. Spec: `docs/superpowers/specs/2026-06-20-sovereign-transport-profiler-matrix.md`.
**KEY LESSON**: when convergence stalls, probe DW DIRECTLY (curl) to separate provider-health from workload/transport issues — DW was healthy; the bug was our budget/transport-layer disagreement. Goal: fresh node JARVIS_AEGIS_ENABLED=true → engine LEARNS batch-only models → detaches → 3/3 clean → first `[SOVEREIGN GRADUATION]` PR.

### RE-IGNITION (2026-06-20 20:35) — node `jarvis-ouroboros-soak-20260620-203510`
Fresh Spot e2-custom-8-16384, CRUCIBLE MODE, shields-up (JARVIS_AEGIS_ENABLED=true via docker-compose.gcp.yml overlay), JIT GitHub auth via Secret Manager `github-token` (GH_TOKEN absent from .env — Secret Manager path used). Ignite from WORKTREE (`.claude/worktrees/discoroute`) — main checkout's ignite script is STALE (no --crucible flag); worktree has current + is at merged code. Cmd: `DOUBLEWORD_API_KEY=$(grep ^DOUBLEWORD_API_KEY= .env|cut -d= -f2-) python3 scripts/ignite_sovereign_cloud_node.py --crucible`. Node clones main d5340baf (Transport Profiler Matrix) + restores GCS state on boot. Monitor: `gcloud compute ssh <node> --zone us-central1-a --project jarvis-473803 --command 'sudo docker logs -f jarvis-sovereign-prod'`. Convergence path: boot→build→first soak LEARNS -dottxt batch-only→tags ASYNC_BATCH_PAYLOAD→detaches(park, frees worker)→out-of-pool continuation(390s)→BatchFutureRegistry resolves→resume→state=applied→3/3 clean→[SOVEREIGN GRADUATION] PR.

### LEARN-HOOK MIS-WIRE found+fixed LIVE (PR #69622 MERGED, main 8fbe1de281) — matrix was INERT
First soak on 203510 proved the matrix INERT: transport_profile EMPTY despite 16 batch submits → nothing tagged → -dottxt still timed out 90-180s (26 errors, 1 state=applied). ROOT CAUSE: learn hook sat in `_s190_hedge_outcome` w/ condition `winner=="batch" AND rupture_swallowed`, but (a) most batch dispatches are FORCE-BATCH (cortex-elected) which SKIPS the hedge entirely (`if not _slice36_force_batch:` gates it) → hedge ran only 1-of-16 ops; (b) that 1 hedge win had rupture_swallowed=False (RT lost on speed / done_before_content w/o rupture flag) → condition never matched. FIX: moved learn signal to `submit_batch` "Batch submitted async" point (line ~1771) — the SINGLE seam common to ALL batch paths (force-batch/hedge/sequential). `_effective_model` there resolves via the SAME per-attempt ContextVar override (`set_dw_model_override`) the candidate_generator budget seam reads → recorded model_id MATCHES is_batch_only(model_id) check exactly (both -dottxt variant). Kept narrow hedge hook (idempotent subset). Hot-deployed to 203510 (HEAD 8fbe1de). **LESSON: a detection hook on a conditional/optimized path (hedge) misses the common case (force-batch) — wire learn signals at the BROADEST common seam.** Watch for: `.jarvis/dw_transport_profile.json batch_only` populating, `[TransportProfile] model=X tagged ASYNC_BATCH_PAYLOAD`, PARK-EMIT/RESUME, state=applied. Verify worker not wedged.

### PROFILE LEARNS + BUDGET EXEMPTION PROVEN, but PARK was broken by FROZEN CTX (PR #69623 MERGED, main 94f3de740c)
After #69622, profile LEARNED 4 batch-only models (35B-dottxt, 397B-dottxt, gpt-oss-120b, gpt-oss-20b). Budget exemption PROVEN WORKING: `primary_budget=300.0s` ×7 (was 180s), batches COMPLETE ×10, a 397B-dottxt op reached `outcome=ok` via batch in 86.8s, DW RT healthy (200/0.64s). BUT state=applied stuck at 1 because: **PARK-EMIT=0 — workers WEDGED on up-to-300s synchronous batch waits** (throughput collapse, ~1 op/soak reaches APPLY). TWO park bugs: (1) **OperationContext is a FROZEN dataclass** → `context.async_batch_payload=True` silently failed in its try/except (the field was never settable; _resolve_effective_model docstring records the same FrozenInstanceError that killed the old setattr override pattern); (2) the budget-seam tag is set INSIDE generate(), AFTER maybe_park_or_resume's park decision → too late anyway. FIX (#69623): stop tagging the frozen ctx; EVERY consumer resolves "batch-bound?" DIRECTLY from the immortal profile w/ the resolved model — park gate via NEW `generate_park_wrapper._resolve_async_batch_payload(ctx, route)` (resolves route's ranked dw_models via provider_topology, True iff any is_batch_only, computed BEFORE dispatch); budget seam keeps `is_batch_only→force_batch`; ban seam checks `is_batch_only(_dp_model)`. Frees worker → existing out-of-pool continuation(390s)→BatchFutureRegistry→resume→state=applied, pool free for concurrent ops. Hot-deployed 94f3de7. **LESSON: OperationContext is FROZEN — never mutate it mid-pipeline; use ContextVar OR resolve from a process-global/persistent source (profile) at each consumer.** Also seen: gpt-oss-120b intermittent DoublewordInfraError (both transports) → RaceTriage blacklists+rotates (correct, DW-side). Remaining unknown: whether parking lifts state=applied to 3 clean, OR a downstream GENERATE-ok→VALIDATE→APPLY layer still gates (only 1 COMPLETE/1 APPROVE seen; most ops stuck GENERATE/GENERATE_RETRY).

### PARKING WORKS, then continuation-timeout vs batch-budget collision (PR #69624 MERGED, main 67ac226f6c) — 5th fix in the cascade
After #69623: PARK-EMIT=8/RESUME=4/ParkRequested=9 — parking FIRES, workers freed. DW batches COMPLETE (7, 0 batch-level timeouts). BUT every op_summary still error (300006ms timeouts + 9.8s/74s/257s errors), state=applied still 1. ANOMALY: 7 batches complete at DW level yet ops error — incl one batch completing at 257s. ROOT (3rd instance of the SAME budget-layer disagreement): the out-of-pool park continuation's OUTER wait_for stays at legacy gen wall (~185s for non-thinking seed-defect ops) while the op was parked BECAUSE it's batch-bound (inner gets 300s batch budget) → continuation SEVERS the batch at 185s before it completes (completion counted at DW level, op already errored). FIX (#69624): widen continuation timeout to the SAME batch floor `force_batch_gen_timeout_floor_s()`=330s (JARVIS_DW_BATCH_TIMEOUT_S 300+overhead 30) for batch-bound ops (resolved via _resolve_async_batch_payload, same predicate as park gate). max() only widens. Captures 185-330s completion window. **EMERGING WALL (honest): DW batch latency/reliability under load** — some batches genuinely >300s (300006ms timeouts), gpt-oss-120b DoublewordInfraError (both transports), RT probe degraded 0.64s→6.2s. All DW models fsm_exhausted repeatedly. The matrix MACHINERY is fully proven (learn→budget-exempt→park→resume→continuation-aligned all fire), but state=applied scaling now depends on DW completing heavy seed-defect batches within 330s. **5 hot-deploys deep (#69619-69624); each fix revealed the next budget collision (180→300→continuation 185→330). If 330s still insufficient, the wall is DW-side latency not harness code — report honestly, don't blindly extend timeouts (operator-rejected).** Pattern of root cause throughout: budget computed at one layer disagrees with transport reality at another. Node 203510 HEAD 67ac226.

### CRITICAL SELF-CORRECTION: aegis-403 was MISDIAGNOSED → real cause = batch latency; Infinite-Horizon Matrix (PR #69625 MERGED, main 558111bbb1)
**I was WRONG about the aegis batch-retrieval 403.** Confirmed via: ZERO `Retrieve failed: <status>` lines across all sessions (that log fires on retrieval HTTP>=300, i.e. a real 403) + actual failure is `Primary failed (mode=TIMEOUT, DoublewordInfraError: Batch retrieval failed)`. The `status_403` I cited was the HeavyProbe entitlement block on `lightonai` model — CONFLATED with batch retrieval. "Batch retrieval failed" is raised (doubleword_provider:2607) when `poll_and_retrieve` returns None — and with no retrieval-403, that means the batch AWAIT timed out (batch hadn't finished within budget). NOT auth. Told operator I was wrong BEFORE building the aegis fix (which would've ripped open a healthy proxy — the exact "fix the misdiagnosed thing" trap). Operator praised the self-correction. **LESSON: confirm the EXACT failure status from the authoritative log line, don't infer from a nearby grep match.**
**Real cause: DW batch-completion latency is stochastic** — batches DO complete (7-10/window, one outcome=ok@86s) but heavy seed-defect codegen is slow under load; the 300s/330s OUTER budget severed actively-processing batches. The poll layer (`_adaptive_poll_batch`) is ALREADY lifecycle-aware (returns only on completed OR terminal failed/expired/cancelled, else polls to DOUBLEWORD_MAX_WAIT_S=3600s). Only the outer budget cut it short.
**Infinite-Horizon Matrix (operator-authorized, rejected lighter-target/probabilistic/quit):** a PARKED batch op has a FREED worker (zero CPU) → no reason to time it out while DW works. `candidate_generator`: `batch_sla_horizon_s()` (env `JARVIS_DW_BATCH_SLA_HORIZON_S` default=DOUBLEWORD_MAX_WAIT_S 3600s, clamp [300s,24h]) + task-local ContextVar `_PARKED_BATCH_HORIZON` (set_/reset_/_active) → force_batch budget branch lifts cap to horizon ONLY when ContextVar set (in-pool dispatch NEVER inherits → can't wedge live worker). `generate_park_wrapper._spawn_park_continuation`: batch ops get continuation outer wait_for=horizon + FRESH deadline (now+horizon) so inner remaining-budget=horizon; ContextVar set around generate(), reset in finally. compose JARVIS_DW_BATCH_SLA_HORIZON_S=3600. Bounded by wall-cap 2400s in soaks (covers operator's "let DW take 20min"=1200s). +2 tests, 72 regression green. Hot-deployed 558111b (6th deploy: #69619-69625). Convergence now gated only by whether DW completes the batch within the wall-cap — the harness no longer severs it. Node 203510 HEAD 558111b.

### 🎯 CONVERGENCE ACHIEVED — first flawless state=applied through the full detached-batch pipeline (2026-06-21 06:11, node 203510)
The ENTIRE chain proven live on op-019ee8ca-6923: park continuation **completed=1** (was 0), `outcome=ok total_ms=429211` (a **429s batch COMPLETED** — every prior version severed it at 300/330s), then `RESUME path → dispatching VALIDATE → dispatching GATE → LEDGER_TERMINAL state=applied → dispatching COMPLETE`. **learn→budget-exempt→park(free worker)→wait-full-horizon→batch completes@429s→resume→VALIDATE→GATE→state=applied→COMPLETE.** The Infinite-Horizon Matrix captured the slow-but-completing batch exactly as designed. This is the first mathematically-pure state=applied through the async detached-batch pipeline shields-up (AEGIS on).
**Graduation: 2/3 clean soaks** for `JARVIS_COMMAND_BUS_BRIDGE_ENABLED` (sessions bt-2026-06-21-010345 @01:48 + bt-2026-06-21-051020 @05:55, both outcome=clean in `.jarvis/graduation_ledger.jsonl`). Cadence needs 3 clean (CADENCE_POLICY) to auto-fire the `[SOVEREIGN GRADUATION]` PR via graduation_pr_proposer. One clean soak away. The candidate flag the cadence is graduating = JARVIS_COMMAND_BUS_BRIDGE_ENABLED (not the transport-profiler flags — those are infra, default-on). Monitor for 3rd clean → `gh pr list --search "SOVEREIGN GRADUATION in:title"`.
### CLEAN-GRADE BLOCKER: parked batch makes session look IDLE → idle-reap → in_flight → infra (PR #69626 MERGED, main 313f6dab83)
state=applied LANDED (op-019ee8ca) but soak 060611 graded INFRA: session_outcome=in_flight, stop_reason=idle_timeout. Timeline: op parked 06:16:46 (worker freed, awaiting DW out-of-pool) → GLS-op activity check saw "0 ops progressing" ~300s → idle watchdog reaped 06:21:53 → in_flight → graded `session=unknown outcome=infra ops=0 dur=2700.0s` (NOT clean). The Infinite-Horizon parking made the session LOOK idle while healthily waiting on DW. FIX: registered a session-liveness probe in harness.py (~L1238, the SAME `register_session_liveness_probe` mechanism that protects fire-and-forget autoscore evals) returning hot while the BG pool has any live `_park_continuation_tasks` → ActivityMonitor keeps poking the idle watchdog while a parked ASYNC_BATCH_PAYLOAD op awaits DW → no mid-batch idle-reap. Wall-clock cap (2400s) stays OPAQUE to this probe (Watchdog Isolation Invariant) = hard ceiling intact. Inert when nothing parked (byte-identical). Hot-deployed 313f6da (7th deploy). **LESSON: freeing a worker (park) can make a session look idle to a liveness watchdog — register a liveness probe for the out-of-pool work.** This closes the gap between "state=applied lands" and "soak grades clean." Node 203510 HEAD 313f6da. Still 2/3 clean; this fix should let the convergence soak grade clean → 3rd clean → [SOVEREIGN GRADUATION] PR.

### WALL-CAP SHUTDOWN cut before complete summary (PR #69627 MERGED, main 30726b9ed2)
Liveness probe (#69626) WORKED: idle_fired=0, state=applied landing, soak now runs to wall-cap (2402s) as designed. But STILL graded infra: at wall_clock_cap the graceful shutdown drains parked continuations + DW loops + `_generate_report` (stamps session_outcome=complete), and that drain exceeded the **30s default `JARVIS_BATTLE_SHUTDOWN_DEADLINE_S`** → BoundedShutdownWatchdog `os._exit(75)` at 30s → only the partial atexit summary (in_flight) survived → graded `session=unknown infra` despite a clean wall-cap end with state=applied. Timeline proof: 07:44:25 wall fired→atexit partial(in_flight)→ShutdownWatchdog armed 30s; 07:44:46 Oracle done + park continuation cancelling (drain PROGRESSING, not hung); 07:44:55 ShutdownWatchdog os._exit. FIX: env-only, raise `JARVIS_BATTLE_SHUTDOWN_DEADLINE_S=180` in crucible compose (well under the 2700s live_fire_soak subprocess wait) so the graceful report completes → COMPLETE summary → clean grade (wall-cap = clean-bar-equiv to idle_timeout). shutdown_background_loops (#69616) already wired into GLS.stop so DW loops drain bounded. Hot-deployed 30726b9 (8th deploy). **LESSON: the Infinite-Horizon parked-continuation lifecycle makes wall-cap shutdown slower → the 30s bounded-shutdown deadline (sized for fast idle shutdowns) is too tight; the complete summary needs the drain to finish.** Node 203510 HEAD 30726b9. Still 2/3 clean — this should let the wall-cap soak finally grade clean → 3rd → graduation PR.

### Sovereign Telemetry Flush Failsafe (PR #69628 MERGED to main 91b841b9f3, NOT hot-deployed per operator)
Operator-authorized failsafe so a hung/overrunning teardown can NEVER cost the summary (the exact infra-grade failure: wall-cap fired→drain overran 30s→os._exit→partial in_flight summary despite state=applied). TWO layers: (1) **Separation of Concerns** (harness finally block): `_generate_report()` now runs BEFORE `_shutdown_components()` — COMPLETE summary persisted before the slow drain (reads recorder/ledger/score-history, no dep on drained components; guarded so report failure doesn't block drain). (2) **Unkillable Watchdog Hook** (shutdown_watchdog): os._exit() skips atexit, so BoundedShutdownWatchdog now calls a registered sync `_pre_exit_flush` callback in the final millisecond before os._exit; harness registers `_watchdog_pre_exit_flush` (sync, idempotent no-op-if-written, fail-soft, stamps complete for clean terminals else incomplete_kill; individually try-wrapped — os._exit ALWAYS fires). +3 watchdog tests (flush-before-exit ordering / raising-flush-still-exits / no-flush-still-exits); 41 watchdog+partial-shutdown green. **Initially NOT hot-deployed; THEN HOT-DEPLOYED (9th deploy, 91b841b) after 180s PROVEN to fail.** Live confirmation the 180s was insufficient: session 075858 — 08:39:36 WallClockWatchdog fired→ShutdownWatchdog ARMED(180s); 08:39:57 Oracle shutdown complete(+21s); then **159s of silence**; 08:42:36 ShutdownWatchdog FIRED(elapsed=180s)→os._exit→summary in_flight→infra. So the drain GENUINELY HANGS past 180s (not just slow) — no deadline value fixes a true hang; the failsafe (write complete summary BEFORE the drain) is the structural fix. Per operator's conditional ("hot-deploy if the current node fails to produce the PR"), deployed the failsafe to node 203510 (HEAD 91b841b). Now the soak writes session_outcome=complete BEFORE the hanging drain → graded clean despite the hang → 3rd clean → graduation PR. **CRITICAL OPS HAZARD: the discoroute worktree is being RESET ('reset: moving to HEAD' in reflog) between Edit calls — uncommitted edits get wiped repeatedly. WORKAROUND: patch via python script + `git checkout -b` (preserves working tree, NOT `-B origin/main` which resets) + immediate `git add` + `git commit --no-verify` in ONE atomic bash call, before the reset window. Verify with `git show HEAD:<file>`.**

### WAL clobber — the LAST graduation blocker (PR #69629 MERGED, main 5f5a4b2af1)
With the failsafe deployed, bt-084639 PROVED report-before-drain works (`Summary written` 09:27:10 BEFORE drain, state=applied 08:51:54) — yet summary stayed in_flight because summary.json mtime=**09:30:01** (rewritten ~9s before os._exit). Culprit: the **SessionWAL periodic checkpoint** (`_slice12g3_periodic_checkpoint_loop`, every 15s) writes summary.json with in_flight state, AND its CancelledError handler emits `force_checkpoint(shutdown_cancel)` — NEITHER checked `_summary_written`, so during the hanging drain the WAL kept clobbering the complete summary. FIX (#69629): both WAL write paths skip once `_summary_written` True (periodic: `if _session_wal is None or _summary_written: return`; cancel: `if _session_wal is not None and not _summary_written:`). 41 tests green. **The complete-summary-survival stack is now: report-before-drain (writes complete first) + WAL-guard (won't clobber) + watchdog pre-exit flush (backstop on os._exit).**

### NODE 203510 auto-DELETED at 6h Spot lifetime (~09:35 UTC); GCS state preserved (44 files incl 2 clean soaks + learned transport profile). RE-IGNITED fresh node `jarvis-ouroboros-soak-20260621-023742` (pulls main 5f5a4b2 = COMPLETE fix set: Infinite-Horizon + liveness-probe + 180s-deadline + failsafe + WAL-guard; restores GCS = 2 clean already). Needs just 1 more clean → [SOVEREIGN GRADUATION] PR. Monitor: `gcloud compute ssh jarvis-ouroboros-soak-20260621-023742 --zone us-central1-a --project jarvis-473803 --command 'sudo docker logs -f jarvis-sovereign-prod'`.

### 🎯 3RD CLEAN ACHIEVED + Temporal Lineage Waiver — the final graduation gate (PR #69630 MERGED, main 86461bef52)
Fresh node 023742 soak bt-2026-06-21-095234 graded **outcome=clean** (dur=2430.2s NOT 2700, session parseable NOT unknown, ops=6) — the report-before-drain + WAL-guard + watchdog-flush stack WORKED, complete summary survived the hanging drain. **clean count = 3/3.** But auto-graduation still blocked: `is_eligible` requires `progress["clean"]>=required AND progress["runner"]==0`. JARVIS_COMMAND_BUS_BRIDGE_ENABLED had clean=3 BUT **runner=5** → ineligible. The 5 runner rows (06-20 evening, PRE-fix) had notes `complete_no_runner_failures|contract_metrics_predicate_downgraded` + kind=`default_conservative`: COMPLETE sessions, ZERO real runner faults, downgraded ONLY by the contract TTFT/metrics predicate (DW batch latency, since fixed), then conservatively bucketed runner. Existing waivers missed them (suffix mismatch: they match `contract_metrics_predicate_downgraded` not the Slice-5 `contract_predicate_downgraded_clean`; not incomplete-summary). Append-only ledger → runner==0 unreachable forever without a waiver. **FIX = Sovereign Temporal Lineage Waiver (operator: no blanket waiver — preserve safety gate):** `lineage_waiver.is_legacy_infra_latency_downgrade(outcome,notes,recorded_at_epoch,cutoff)` forgives ONLY if outcome==runner + notes has `complete_no_runner_failures` + ends with `contract_metrics_predicate_downgraded` + `recorded_at_epoch < cutoff` (env `JARVIS_GRADUATION_LATENCY_WAIVER_CUTOFF_EPOCH` default **1782021888** = Infinite-Horizon merge 558111b @ 2026-06-21T06:04:48Z). Post-cutoff downgrades stay HARD runner failures (ruthless — future regressions still block). progress() routes forgiven rows to NEW audit-visible bucket `waived_legacy_infra_latency` (distinct from clean + hard faults); append-only ledger NEVER mutated (in-memory re-aggregation only). VERIFIED: 5 pre-fix+3 clean → clean=3 runner=0 waived=5 ELIGIBLE=True; +1 post-fix downgrade → runner=1 ELIGIBLE=False. 10 tests. Hot-deployed 86461be; `JARVIS_CRUCIBLE_GRADUATION_PR_ENABLED=true` armed → next graduation pass should evaluate eligible → graduation_pr_proposer opens the [SOVEREIGN GRADUATION] PR.

### 🎉 [SOVEREIGN GRADUATION] PR OPENED — https://github.com/drussell23/JARVIS/pull/69632 (2026-06-21)
THE GOAL ACHIEVED. After the temporal waiver made the flag eligible, the autonomous_graduation_engine evaluated `JARVIS_COMMAND_BUS_BRIDGE_ENABLED` → `READY → AUTO_FLIP` (Gate-A telemetry + Gate-B AST + Gate-C contract all pass), auto-flipped the env-override, AND the graduation_pr_proposer opened **PR #69632** `[SOVEREIGN GRADUATION] Activated JARVIS_COMMAND_BUS_BRIDGE_ENABLED` — cage-respecting source rewrite (`"JARVIS_COMMAND_BUS_BRIDGE_ENABLED", ""` → `..., "true"` in autonomy_command_bus_bridge.py, default False→True), machine-generated PR body with mathematical proof: 3/3 clean soaks, per-soak TTFT 12673/12740/18801ms all < 30000ms ceiling ✅ (batch latency HEALTHY post-Infinite-Horizon), AST 0 corruption ✅, evidence digest 0e257e2481b5ab30. Branch `ouroboros/review/sovereign-graduation-jarvis_command_bus_bridge_enabled`. PROPOSES never self-merges (Order-2 cage intact).
**TWO BLOCKERS resolved at the PR-proposer stage:** (1) `literal_site_unresolved` — fixed PR #69631 (locator regex `(?P=q)\s*\)` → `\s*,?\s*\)` to match black-formatted multi-line trailing-comma `os.environ.get(\n "FLAG", "",\n)`). (2) `pr_create_returned_none` — the soak container's `/app` is the BAKED image source with NO `.git`; the proposer needs a real git repo + git identity. WORKAROUND (used to open #69632): in-container `git clone --depth 30 https://x-access-token:$GH_TOKEN@github.com/drussell23/JARVIS-AI-Agent.git /tmp/grad_ws` + `git config --global user.email/name` + run propose_graduation_pr(repo_root=/tmp/grad_ws). **DURABLE FOLLOW-UP NEEDED for fully-hands-off autonomy: mount a git workspace into the soak container + set JARVIS_AUTO_COMMIT_WORKSPACE + bake git identity, so the cadence's _propose() opens PRs with zero manual assist.** NOTE: a fresh clone is required per attempt — a failed commit leaves the literal already flipped→"true" → next run hits literal_site_unresolved (not-falsy).

**TOTAL ARC: 14 PRs #69619-69632, ~125 tests, 11 hot-deploys + 2 fresh ignitions → 🎯 [SOVEREIGN GRADUATION] PR #69632 OPEN — Zero-Shot quarantine + seam-fix + Transport Profiler Matrix (learn/budget/park/resume) + frozen-ctx fix + continuation-budget + Infinite-Horizon. Each deploy fixed a real budget-layer disagreement (180→300→continuation→330→horizon) revealed only by live soak. Plus one major self-correction (misattributed aegis-403 → real cause batch latency).**

### SEAM-LOCATION BUG found+fixed LIVE (PR #69620 MERGED, main 3a56865e58) — the quarantine was wired at the wrong layer
**Live evidence on node 180012**: repeated 180s `TimeoutError` on `Qwen/Qwen3.5-397B-A17B-FP8-dottxt` / `Qwen3.5-35B-A3B-FP8-dottxt` (op_summary `error_class=TimeoutError stage_STAGE_PROVIDER_GENERATE_ms=179997`), BUT `.jarvis/dw_ttft_observer.json` `zero_shot_bans` stayed EMPTY → record_timeout NEVER fired. ROOT CAUSE: the original hook (#69619) sat in the OUTER dispatch failure handler (candidate_generator ~L3931), but the sentinel-dispatch FSM re-wraps a primary TimeoutError into `RuntimeError('all_providers_exhausted:fallback_skipped:no_fallback_configured')` BEFORE it reaches that handler → `isinstance(exc, asyncio.TimeoutError)` never matched (PROVEN: `is_generation_timeout(RuntimeError('all_providers_exhausted'))==False`). FIX: added the hook at the `_call_primary` STAGE_PROVIDER_GENERATE seam (~L5597) — the `except Exception as _dp_exc` around `_primary.generate()` wrapped in `race_or_wait_for(timeout=primary_budget)` — the ONLY point where the raw unwrapped budget-timeout TimeoutError is visible WITH the resolved `_dp_model` in scope (defined L5515-5517), before FSM wrapping. Both runtimes py3.11 (asyncio.TimeoutError IS builtins.TimeoutError) + race_or_wait_for raises asyncio.TimeoutError → isinstance matches. KEPT the outer-handler hook too (covers RT-direct timeouts + tool-loop deadlines `_s241_gen_timeout` that propagate unwrapped) → 2 complementary seams (`record_timeout` count=2). +1 regression test (guard accepts raw TimeoutError, rejects wrapped RuntimeError) = 19 green. Redeployed to node (HEAD 3a56865). **LESSON: a wrapped-exception layer can silently swallow an isinstance guard — verify the fix FIRES on the live failure path, not just that it unit-tests.**
