---
title: Project Slice104 Tier2 Containment
modules: [tests/architecture/test_slice104_containment.py, tests/architecture/test_slice106_live_containment.py, backend/core/ouroboros/governance/recursion_depth_gate.py, backend/core/ouroboros/governance/runtime_sandbox.py, backend/core/ouroboros/governance/container_sandbox.py, backend/core/ouroboros/governance/image_provisioner.py, backend/core/ouroboros/governance/governed_loop_service.py, backend/core/ouroboros/governance/candidate_generator.py]
status: historical
source: project_slice104_tier2_containment.md
---

**Slice 104 — Tier 2 Containment Protocol. MERGED 2026-06-06 (PR #69301, squash e5dbabd59d, main synced 0/0).** Attacks the 2 Tier-D hurdles PRD §51.11.4 names. **HONEST: host is macOS/arm64 (M1, 16GB) — seccomp + Linux namespaces DO NOT EXIST here; did NOT claim a kernel syscall sandbox (operator agreed the boundary up front).**

**1. Operator-Independent Recursion-Depth Gate (RRD §23.5) — BUILT, un-bypassable, FULLY tested.** `recursion_depth_gate.py`: `self_modification_depth(flags)` (trailing consecutive-True run = chain "Order depth", pure); `RecursionDepthTracker` process-singleton (note_apply(touched_governance): gov→increment, body→reset 0; thread-safe, in-memory by design — reboot resets chain); `evaluate_recursion_gate(target_files, chain_depth=None)` → HALT when effective_depth (chain_depth+1) > `max_recursion_depth()` (env JARVIS_MAX_RECURSION_DEPTH default 3, clamp[1,16]); `_touches_governance` composes governance_boundary_gate.evaluate_target_files→BOUNDARY_CROSSED; `recursion_depth_floor()`→"blocked" on HALT. Master JARVIS_RECURSION_DEPTH_GATE_ENABLED **DEFAULT-TRUE** (safety gate, mirrors governance_boundary_gate). WIRED: `risk_tier_floor._recursion_depth_floor` composed into recommended_floor (strictest-wins; "blocked"=ordinal 3 un-bypassable) + tracker advanced at orchestrator `_record_ledger` APPLY-terminal seam (state=="applied" → note_apply(ctx.target_files)). Hurdle #2 = DONE (prereq for safe Order-2 RSI).

**2. OS-Level Runtime Containment (§43 Arc 5) — primitive built, honest platform split, NOT live-wired.** `runtime_sandbox.py`: `run_contained_code(code, worktree, policy)` runs code in SEPARATE process via subprocess `[sys.executable,'-I','-c',code]` with cwd=worktree + env=stripped(default {}) + preexec_fn rlimits (RLIMIT_CPU/AS/FSIZE + os.setsid) + timeout→SIGKILL. ContainmentBreach enum (NONE/DISABLED/SPAWN_FAILED/TIMEOUT/NONZERO_EXIT/SIGNAL_KILLED/POLICY_VIOLATION). `detect_platform()`+`guarantees_for_platform()` returns TRUTHFUL per-platform list (macOS: out_of_process/stripped_environment/cwd_confined/resource_limits/hard_timeout_sigkill + "no_kernel_syscall_sandbox"/"network_not_blocked"/"absolute_fs_not_blocked"; Linux WITH registered backend: +seccomp/namespace/network-denied/abs-fs). `set_linux_isolation_backend(fn)` = injectable Linux seccomp/nsjail/bubblewrap/Docker interface (NOT shipped — honest-inert on darwin; SWE-bench `swe_bench_pro/container_engine.py` has Docker precedent). Master JARVIS_RUNTIME_SANDBOX_ENABLED **default-FALSE** (execution-path change). **NOT wired into live APPLY/VERIFY — deferred + needs Linux host for the real seccomp backend to close the ~8.8% residual (§41.11.2).** Hurdle #1 = scaffolded + proven for testable macOS guarantees.

**TDD tests/architecture/test_slice104_containment.py (12):** sandbox contains rogue subprocess.Popen env-exfiltration + infinite-recursion (secret never leaked, parent alive), timeout-kills infinite loop, confines relative writes to worktree, honest-guarantee-list, disabled-default; recursion gate exact math, within-bound ALLOWED, beyond-bound HALT, non-gov never halts, tracker climbs/resets, **live-tracker floor blocks runaway chain (un-bypassable "blocked")**, disabled-inert. 67 regression green (risk_tier_floor 43 + phase5 proof gate 12 + slice104 12). PRD §51.11.4 added.

**Slice 105 — Linux/Docker Containment Bridge. MERGED 2026-06-06 (PR #69302, squash dcdfd0c698, main 0/0).** Closes the macOS Arc-5 gap: Docker Desktop on macOS/arm64 runs a REAL Linux kernel in a VM → hardened Docker container = genuine kernel containment, IDENTICAL profile to GCP/Linux prod, locally verifiable (answers "Docker instead of GCP": YES). `container_sandbox.py` = general-purpose ContainerizedSandboxBackend: **COMPOSES (verify-first, NOT duplicate) Slice 92's already-proven `container_engine.build_hardened_security_argv()`** (--cap-drop ALL/--network none/no-new-privileges/--read-only/--pids-limit/non-exec-tmpfs — written precisely to neutralize the 5 residual run_body_* sinks = the 8.8%) + Slice 104 ContainmentResult/Breach types. `build_container_argv` (pure hardened argv: worktree sole rw mount at /work, rootfs ro, python3 -I -c code), async `run_in_container` (injectable `_real_docker_run` IPC, rc 0→ok/124→TIMEOUT/nonzero→breach), `run_payload_contained` (gated live-wire: None→legacy, ContainmentResult→caller records breach+falls back). `sandbox_profiles/strict_runtime.seccomp.json` opt-in defense-in-depth (default-allow + 29 host-escape syscall ERRNO deny-list; safe, python never issues those). Master JARVIS_RUNTIME_SANDBOX_ENABLED default-FALSE + JARVIS_RUNTIME_SANDBOX_BACKEND(local|container) + JARVIS_RUNTIME_SANDBOX_IMAGE(python:3.11-slim) + JARVIS_RUNTIME_SANDBOX_SECCOMP_PROFILE. **HONEST: Docker NOT running this session → tests mock-based (injectable DockerRun, deterministic, 11 pass); real-Docker integration test (network-egress-denial = kernel proof) gated skipif daemon-down, runs on any Docker host. Did NOT ship unverified default-deny seccomp (broken-interpreter risk). REMAINING: orchestrator VERIFY one-line adoption of run_payload_contained + real-Docker soak (deployment step).** PRD §51.11.5. ~8.8% now CLOSEABLE on any Linux/Docker host.

**Slice 106 — Live Empirical Containment Receipt + VERIFY FSM Wiring. MERGED 2026-06-06 (PR #69303, squash fef05c7e73, main 0/0).** Operator booted Docker (29.5.2 on M1/arm64) → containment PROVEN against REAL Linux kernel. **LIVE RECEIPT (codified as permanent gated test `tests/architecture/test_slice106_live_containment.py`, 2 live Docker tests PASS not-skipped):** malicious payload EXECUTED in hardened container but net=EGRESS_BLOCKED (--network none) + etc/root WRITE_BLOCKED (--read-only rootfs) + work=WORK_WRITE_OK (worktree = sole writable host mount) + infinite-loop TIMEOUT-killed @4s + parent never crashed. The 8.8%-class sink is EMPIRICALLY contained. FSM WIRING: `run_payload_contained` wired at orchestrator post-apply VERIFY seam (~line 8549, gated/additive/fail-safe, fully try/except — breach NEVER crashes FSM); when JARVIS_RUNTIME_SANDBOX_ENABLED on + JARVIS_RUNTIME_SANDBOX_VERIFY_PROBE configured → candidate validation runs contained → ContainmentBreach(timeout/signal/nonzero) sets `_verify_test_passed=False` (REUSES existing failure→L2/GENERATE_RETRY routing) + records FALSIFYING quarantine belief via `container_sandbox.record_containment_breach_belief` (composes belief_revision → Phase-3 avoidance digest steers GENERATE away → Phase-6 consolidation). Default-FALSE → byte-identical. **HONEST: full-project-suite containerized VERIFY needs a project image (slim lacks deps) + prod soak = deployment step; mechanism+breach-quarantine+gated-wire are built/live-proven/tested.** 5 new tests + 37 regression green. PRD §51.11.6. **Hurdle #1 OS-Level Runtime Containment EMPIRICALLY DEMONSTRATED + FSM-integrated; remaining = operational (project image + soak), not architectural.** Docker pull needs dangerouslyDisableSandbox (registry network); docker commands need it (socket).

**Slice 107 — Ephemeral Project Container Pipeline. MERGED 2026-06-06 (PR #69304, squash 2927cab546, main 0/0).** Tier-D runtime cage = end-to-end DevOps pipeline, ALL live-proven vs Docker daemon. (1) Multi-stage multi-arch `sandbox_profiles/Dockerfile.verify-sandbox` + `requirements-sandbox.txt` — image carries ONLY pytest stack (pytest/asyncio/timeout/json-report/anyio), project code MOUNTED ro at run time, heavy ML deps (torch/whisper, 308-line requirements.txt) NOT baked → cold-boots fast (built 56MB/5.6s), arm64(M1)+amd64(GCP). (2) `image_provisioner.py` async daemon: image_state_hash(sha256 of requirements-sandbox+Dockerfile)→stamped as `org.jarvis.state-hash` label; image_present_and_current (docker image inspect label==hash); provision_image (rebuild w/ --label only when stale, idempotent); run_provisioner_daemon (boot bg task). Master JARVIS_IMAGE_PROVISIONER_ENABLED default-FALSE. LIVE-PROVEN: stale→rebuilt→current→no-op. (3) `container_sandbox.run_pytest_in_container` + `build_pytest_container_argv` (hardened, worktree ro mount /work, NO forced --platform=native arch — fixed amd64-bias rc=125 bug) + `_parse_pytest_summary` (general regex pytest summary parser, NOT SWE-bench expected-id parser). LIVE-PROVEN: 2 passed/1 failed/3 total parsed correctly inside --network none/--cap-drop ALL/--read-only container. (4) `docs/operations/shadow_soak_runbook.md` = exact full-shadow-mode launch commands + ledgers-to-watch + operator-only un-shadow. **HONEST: verify-first found backend.core.ouroboros.__init__ EAGER-imports aiohttp+engine chain → FULL governance suite in-container needs a PROJECT image (heavy, from requirements.txt, documented in runbook); light image + pipeline (build/provision/pytest/parse) live-verified; did NOT build multi-GB project image (impractical) nor fake it.** 12 tests (mock + live Docker, all pass) + 29 regression. PRD §51.11.7. **Docker IS the GCP bridge (Docker Desktop = real Linux kernel on M1). Build→provision→contain→verify end-to-end. Both Tier-D hurdles (recursion bound + runtime containment) now MECHANISM-COMPLETE + live-proven; remaining = project image + 12-18mo shadow wall-clock evidence (operational, not architectural).** docker cmds need dangerouslyDisableSandbox (socket+registry net).

**Slice 108 — Production Layer-Cached Container. MERGED 2026-06-06 (PR #69305, squash c542c07b18, main 0/0).** Closes the full-suite gap. **KEY EMPIRICAL FINDING (sys.modules probe, NOT guess): `import backend.core.ouroboros.governance.*` pulls a MEDIUM closure (aiohttp+cryptography+networkx+redis+opentelemetry+psutil+yaml+aiosqlite+aiofiles+uuid6+xxhash+typing_extensions ~22 pkgs) — NOT torch/whisper/transformers/numpy/scipy (vision/voice only). Governance VERIFY image = moderate not multi-GB.** `sandbox_profiles/Dockerfile.production-sandbox` + `requirements-governance.txt`: layer-cached multi-stage multi-arch, BuildKit pip cache-mount, ARG REQUIREMENTS parameterizable (governance default OR full requirements.txt for ML variant). **IMMUTABILITY INVARIANT: deps layer keyed ONLY on requirements file (immutable cache); source NEVER baked → worktree volume-mounted ro → single-file mutation = ZERO rebuild.** Built LIVE 72MB/11s. `image_provisioner` now image-agnostic (JARVIS_SANDBOX_REQUIREMENTS_FILE/_DOCKERFILE; hashes configured pair; live-proven production pre-warm). `scripts/launch_shadow_soak.sh` = final executable (boots Docker, build-if-stale, ignites substrates, launches fail-closed shadow). **MARQUEE LIVE: real O+V governance suite test_slice101_phase5_proof_gate (12 tests, full governance import chain) PASSED inside hardened --network none/--read-only/--cap-drop ALL production container vs ro worktree mount — no rebuild.** 5 tests + 17 regression. PRD §51.11.8. **BOTH Tier-D hurdles now mechanism-complete + FSM-integrated + true suite runs contained in layer-cached prod image. Remaining = 12-18mo shadow wall-clock evidence (a CLOCK not code). Launch via scripts/launch_shadow_soak.sh.**

**Session arc total: 19 PRs (Slice 101 P1-8, 102, 103, 104, 105, 106, 107, 108). Next per PRD = wire runtime_sandbox into live APPLY/VERIFY + implement+verify the Linux seccomp backend on a Linux/GCP host (the only thing that closes the 8.8% on production).** Repo shared-tree hazard recurred: loop left stray unstaged deletion of governed_loop_service.py in primary checkout (restored from committed main; same pattern as candidate_generator.py — always check `git status` after ff-merge). See [[project_slice102_graduation_soak]], [[project_slice101_architecture_audit]], [[project_repo_git_pr_ops]].
