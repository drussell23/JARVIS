---
title: Project Slice 46 Monotonic Deadman
modules: [tests/.../test_todo_scanner_trigger_tag.py]
status: historical
source: project_slice_46_monotonic_deadman.md
---

Slice 46 — Monotonic Heartbeat Alignment & Suspension-Resilient Armoring. **MERGED main 2026-05-30** PR #65631 (squash tip 4eda00e3ea). Opus-reviewed READY TO MERGE (safety daemon). Builds on [[project_slice_45_terminal_worker_tools]].

**Root cause (verify-first FALSIFIED the authorized runbook).** v41's "574s GIL wedge" was a PHANTOM: the Mac slept ~519s (`WallClockWatchdog: monotonic=86s wall=605s skew=519s`). `LoopDeadman` tracked heartbeat age with `time.time()` (WALL-clock), which jumps on host sleep/NTP → the sleep gap read as a wedge → fired `os._exit(75)`. The originally-authorized Slice 46 (multiprocessing isolation of the 2.53M-node Oracle index) would have hardened against a GIL wedge that NEVER HAPPENED — Oracle init is already executor-isolated (0.53s, Slice 32) and `run_pytest_subprocess` is already async, so the pytest TIMEOUT at 574s was a SYMPTOM of the sleep, not a cause. **Decisive diagnostic insight: the ControlPlaneWatchdog runs AS an asyncio callback on the loop it monitors, so its MainThread snapshot can only ever catch ITSELF — useless for GIL-holders; the SidecarProfiler logged 0 stuck-frames because the whole PROCESS was suspended (not GIL-starved); and the WallClockWatchdog's skew line was the actual evidence.** Operator authorized the rescope.

**Fix (LoopDeadman ONLY).** ControlPlaneWatchdog + SidecarProfiler audited and found ALREADY monotonic (`elapsed = monotonic()-t0`; snapshot `now = ts_monotonic`) — left unchanged (AST-pinned, zero churn; neither has os._exit authority). LoopDeadman: (1) wedge age now `time.monotonic()` (macOS/Linux monotonic PAUSES during suspend → slept host yields near-zero age → no false wedge; a real CPU wedge still advances monotonic + fires); (2) `_last_heartbeat_wall` kept alongside ONLY to feed an explicit host-suspension guard — when `age>timeout`, compute `skew=age_wall-age_mono`, if `skew>JARVIS_SUSPENSION_SKEW_THRESHOLD` (default 5.0, floor 1/ceil 300) → `_handle_host_wake_recovery` (log + count + re-baseline both clocks, NO os._exit) + `continue`. Seeded flag in FlagRegistry (TIMING). **Opus verified the re-arm CANNOT permanently disarm** (re-baselines wall in lockstep → genuine wedge re-presents with skew≈0 next poll → fires) and the one TOCTOU split-clock read FAILS SAFE (toward firing). 10 suspension-mock tests + 182 cross-arc green.

**v42 capability soak (bt-2026-05-30-211718, $0.0225, wall_clock_cap @954s, caffeinate -dimsu wrapped to prevent host sleep) — THE FIRST APPLY OF THE v25→v42 ARC + both fixes PROVEN end-to-end:**
- **Slice 46 PERFECT:** 0 WEDGE DETECTED, 0 os._exit fires (the 2 grep hits were the "armed" log message), 0 clock skew (caffeinate held), max real lag ~1.8s. Phantom-wedge kill GONE.
- **Slice 45 PROVEN in prod:** background `todo_scanner` ops (read_only=False) used the LEAN prompt (`preloaded=1` → Iron Gate credit) and the model made REAL `read_file` tool calls — the FIRST DW tool executions in the entire arc (`tool_round_complete round=0 tools=1 names=read_file`). Exploration gate fully unblocked.
- **FIRST APPLY (op-019e7ac7):** full governance chain executed — `GATE can_write allowed=True` → `SemanticGuard findings=0` → `REVIEW-SHADOW aggregate=APPROVE` → `Checkpoint created` → `APPLY mode=single candidate_files=1` on `tests/.../test_todo_scanner_trigger_tag.py` → POSTMORTEM → `apply_evidence_stamped` → TestRunner resolved targets. The applied diff was a COHERENT, VALID change: added `-> None` type annotations + a class docstring + Iron-Gate ASCII-healed 8 em-dashes→hyphens. Real autonomous code change written to disk.
- **NO commit/RESOLVED yet** — the wall_clock_cap (+ my reap) cut the session before VERIFY/commit completed for that op (`AutoCommitter: No changes detected` was a different op/timing). Other patch ops correctly returned `2b.1-noop` after reading the file (TODO already addressed — appropriate conservatism). Working-tree debris (the applied test-file change) REVERTED post-audit; proof lives in the debug.log + this memory, not the diff.

**Where the arc stands now (blocker chain): v34 /v1/files-400 (S42) → v36 fleet-empty (S41) → v37 30s-sever (S43) → v38/v39 worktree-scan wedge (S44) → v40b exploration-gate deadlock (S45) → v41 phantom-wedge os._exit (S46) → v42 = FULL CHAIN FUNCTIONAL, FIRST APPLY.** Transport, tool-protocol, loop-health, and clock-safety are ALL closed. The remaining gap to a RESOLVED (apply+verify+commit) is purely: (1) a longer/uninterrupted soak window so VERIFY+AutoCommit complete after APPLY (the 600s cap + caffeinate-shutdown drain truncated it at 954s), and (2) sensor-surfaced ops where a real net-change patch is warranted (many TODOs the model reads turn out to be no-ops — correct behavior). NEXT (operator-gated): a longer-wall soak (e.g. 1800s) to let an APPLY run through VERIFY→AutoCommit→RESOLVED. NO new code blocker remains. See [[project_slice_45_terminal_worker_tools]] [[feedback_no_preresult_euphoria]].
