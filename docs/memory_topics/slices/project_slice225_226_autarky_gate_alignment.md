---
title: Project Slice225 226 Autarky Gate Alignment
modules: []
status: historical
source: project_slice225_226_autarky_gate_alignment.md
---

**GOAL-001::file-00 has TWO distinct, variable failure modes** (which one fires depends on the Claude-breaker state at the moment file-00 generates). Both had to be fixed. Branch stack: A1 (`c9c554bffd`) → Slice 225 Phase 2 (`3ae77863ff`) → Slice 226 (`7cb8dd7ab4`), on `governance/slice-226-gate-alignment` (off the battle-test branch; NOT yet on main — main is at Slice 218 `9dc9e79884`, local main stale at Slice 167).

**Mode 1 — provider exhaustion (`circuit_breaker_tripped:terminal_quota`).** DW primary severed at the 30s `_PRIMARY_MAX_TIMEOUT_S` cap → handed to Claude fallback → Claude `400 "credit balance too low"` (out of credits) → classified `TERMINAL_QUOTA` (economic refusal, `provider_retry_classifier.py:219`) → breaker trips → `all_providers_exhausted`. **Slice 225 Phase 2 (Sovereign DW Autarky) fixes this:** `candidate_generator._compute_primary_budget` gains `fallback_dead`; when `_claude_breaker_open()` is True the DW primary gets the FULL remaining budget (≤ `JARVIS_DW_AUTARKY_MAX_BUDGET_S`, default 180s) instead of severing into a dead lane. Mirrors the existing `force_batch` precedent. Gated `JARVIS_DW_AUTARKY_ENABLED` default-TRUE. Emits `⚡ DW AUTARKY ENGAGED`. LIVE-VALIDATED: 2 real engagements granting DW 143.9s. 6 TDD + 49 cascade-regression green.

**Mode 2 — Iron Gate block (`generation_failed`, NOT exhaustion).** DW healthy, generated a candidate in 10.5s for \$0.00085 — but the Iron Gate rejected it `exploration_insufficient: 0/1` across BOTH GENERATE attempts → retry budget exhausted → `generation_failed`. ROOT CAUSE = a catch-22: `doubleword_provider._generate_realtime` skips the Venom tool loop for `complexity in {trivial,simple}` (cost), but the Iron Gate (`exploration_engine`/orchestrator) requires ≥1 exploration call ("read the target file") for every non-trivial op. A `simple` op on a venom-eligible route is denied tools, then rejected for not using them. The intended escape (preloaded-prompt credit, orchestrator.py:4871) silently fails when the target is too large to inline (`semantic_index.py` = 3246 lines). **Slice 226 (Gate Alignment) fixes this:** two shared predicates in `exploration_engine.py` — `exploration_gate_demands_tools(complexity)` + `compute_tool_loop_suppressed(...)` — that BOTH the capability plane (provider tool-skip) and the security plane (gate) consult, so they can't contradict. DW now keeps the tool loop ON when the gate will demand exploration for a simple op; trivial stays exempt; BACKGROUND/SPEC keep their route-based preload-credit skip; L2 repair still skips. `⚡ CAPABILITY ALIGNMENT` warning on override. Gated `JARVIS_GATE_ALIGNMENT_ENABLED` default-TRUE. **VERIFY-FIRST: the Prime/Claude provider skips tools route-based only (no complexity skip) → no catch-22 there → fix is correctly DW-only, no duplicate change.** 13 TDD + 164 regression green.

**Mode 3 — RT/BATCH hedge pre-empts exploration (`generation_failed`, layer 3).** Even with S226 opening the tool loop, file-00 still got `0/1`: the proactive transport hedge (`dw_transport_hedge.py::hedged_race`, Slices 188-192) races RT (runs the Venom tool loop → explores) vs BATCH (`_generate_via_batch`, single completion, NO tool loop). `FIRST_COMPLETED` → when the batch arm finishes first, its un-explored candidate reaches the gate → reject. The RT arm's `list_dir`/`glob_files` calls (which the active gate set orchestrator.py:4810 DOES credit — so no "deep exploration heuristic" needed; that would've regressed) were discarded. **Slice 227 (Context-Aware Hedge Governor, `5f9a7b08d0`) fixes this:** `hedged_race` gains `prefer_fast` — a winning BATCH result is held in a speculative buffer while the race waits for RT; batch is used ONLY if RT ruptures/fails (rupture protection fully preserved). prefer_fast derived from the SAME `exploration_gate_demands_tools` predicate → one source of truth across capability+security+concurrency planes. `⚡ HEDGE GOVERNOR` warning. Master `JARVIS_HEDGE_GATE_AWARE_ENABLED` default-TRUE. Phase-2 trace confirmed hedged_race is pure asyncio (no locks/deadlock). 9 TDD + 59 hedge regression green. **BANKED: A1+225+226 in PR #69473 (cherry-picked clean onto origin/main `9f8625a775`, skipping the soak-chore/INFO commit); 227 on branch `perf/slice-227-hedge-governor`, not yet PR'd.** file-00 stack = 3 stacked failure modes (provider-exhaustion / gate-tool-skip / hedge-preemption), all fixed; live validation of 227 pending.

**Mode 4 — weak model can't do agentic tool-use (the last mile).** With S225/226/227 all aligned + firing live (`hedge WON by rt`, tools open), file-00 STILL got `exploration_insufficient: 0/1` — the model (`Qwen3.5-35B-A3B`) emitted a patch with ZERO tool calls. ROOT CAUSE = the catalog ranker (`dw_catalog_client.py:130`) parses TOTAL params 'first match wins' (`Qwen3.5-35B-A3B → 35`), blind to the `A3B` ACTIVE count — so it rated a **3B-active** MoE as a mid-tier 35B model and routed agentic ops to it; 3B-active models can't do reliable agentic tool-use (BFCL/SWE research: the strong ones are Kimi-K2.6 / DeepSeek-V4-Pro / GLM-5.1 / Devstral-2 / Qwen-397B — all in DW's catalog). **Slice 228 (`3c6dfb8970`):** parse the standard `A<N>B` token into `ModelCard.active_parameter_count_b` (dense→falls back to total; curated active map for tokenless MoEs Kimi/DeepSeek/GLM, env-extensible); `_score` ranks capability on ACTIVE params (gate `JARVIS_DW_ACTIVE_PARAM_SCORING_ENABLED` default-TRUE) → demotes 3B-active Qwen-35B, elevates high-active agentic heavyweights; family preference via EXISTING env knob `JARVIS_DW_FAMILY_PREFERENCE=moonshotai:1.0,deepseek-ai:0.9,mistralai:0.8,zai-org:0.7,Qwen:0.5` (config, NO model names in code; Zero-Trust PromotionLedger untouched). **REJECTED the plan's hardcoded-5-model-list + quarantine-bypass (violates no-hardcode + Zero-Trust); built metadata-derived instead — generalizes to any future large-active model.** Linear (not exponential) active weighting. 9 TDD + 176 catalog regression green. On branch `perf/slice-228-active-parser`, not yet banked; live validation pending. file-00 = FOUR stacked failure modes now addressed (provider-exhaustion / gate-tool-skip / hedge-preemption / weak-model-selection).

**Mode 5 — routing starvation of the elite pool (corrects TWO of my own wrong hypotheses).** S228 active-param ranking worked (Qwen-397B became the STANDARD default) — but file-00 still failed on the weak models. I first claimed "capable models rupture on DW RT" (WRONG — never confirmed), then "they're quarantined" (WRONG — PromotionLedger shows Kimi-K2.6/DeepSeek-V4-Pro/GLM-5.1 ALL `promoted=True, trusted_seed`). Verify-first truth: **the elites were dispatched ZERO times because they live on the COMPLEX route's pool while file-00 is classified `simple`→STANDARD** (pool = Qwen-397B + the two weaklings); when Qwen-397B drifts on tool-call JSON (`json_parse_error_after_heal`), the walk falls to 3B-active models that can't drive a tool loop. **DW DOES have capable agentic models — file-00 just couldn't reach them.** **Slice 229 (`bdf022d91a`, authorized as '228 Route Elevation'):** pure `elevate_pool_for_exploration` in provider_topology.py — when the S226 floor predicate demands tools, PREPEND the COMPLEX pool (already agentic-ranked) to the route's pool, deduped; BG/SPEC excluded; `⚡ ROUTE ELEVATION` flash; master `JARVIS_ROUTE_ELEVATION_ENABLED` default-TRUE. One predicate now spans 4 planes: capability(S226)/security(gate)/concurrency(S227)/routing(S229). 6 TDD + 73 stack regression. Live validation pending.

**Mode 6 — no feedback from gate rejection to model rotation (Slice 230, `6c1c67971e`).** Live probe proved S228/229 WORK: the dynamic catalog ranks the STANDARD pool elite-first (`V4-Pro, Kimi-K2.6, GLM-5.1, Qwen-397B, V4-Flash, 35B`), elevation correctly no-ops. Yet dispatch attempted ONLY V4-Flash, twice: the sentinel walk stops at the first TRANSPORT success, and a weak model instantly emitting a no-tool patch IS a transport success (outcome=ok, 15.7s); the Iron Gate rejection happens later and nothing fed it back → GENERATE_RETRY re-picked the same model forever. FIX: `DriftType.EXPLORATION_INSUFFICIENT` (documented deliberate-slice extension; AST pin updated 3→5 — pin was ALREADY broken since S194's DUAL_ARM_FAILURE, verified pre-existing via stash) + `_slice230_record_exploration_drift(op_id, generation.model_id)` at the gate rejection site → existing `has_drifted` makes the retry walk rotate to the next ranked (elite) model. `⚡ GATE→ROTATION` flash. 3 TDD + 61 regression. NOTE: zsh does NOT word-split unquoted vars — `pytest $FILES` passes ONE arg → 'collected 0 items'; use explicit filenames. Also: operator committed `3574bb3c65` (roadmap.draft.yaml update) directly onto the working branch mid-session.

**Verify-first saves this arc (the user authorized elaborate plans repeatedly overturned):** "reflective critic mesh" (no model output to critique — provider 400s); "predictive congestion wave-guide" (already built — 6 probe modules + Brier-calibrated `dw_failure_predictor` rupture forecaster + S201 Thompson bandit + Slice-172 predictive preemption); "SPRT interceptor" (genuinely-additive — no existing sequential test — but gated behind validation + unrelated to file-00's actual gate block). The honest fix each time was smaller and targeted the real blocker.

See [[project_a1_completion_writeback]], [[feedback_observability_over_silent_reroute]].
