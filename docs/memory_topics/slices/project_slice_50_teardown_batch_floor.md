---
title: Project Slice 50 Teardown Batch Floor
modules: []
status: historical
source: project_slice_50_teardown_batch_floor.md
---

Slice 50 MERGED 2026-06-01 (PR #65636, squash 44d7ff73ff). Closes the two internal-platform anomalies from the v45 probe ([[project_v45_preflight_soak]]). **Both runbook premises were FALSE and corrected by verify-first grounding before any code** (same discipline that paid off in Slices 42/45/47).

**Phase 1 — Bounded watchdog observer teardown** (`file_watch_guard.py`). Runbook said target `_clear_emitters()` in file_watch_guard — but that's in the THIRD-PARTY watchdog lib (`observers/api.py`), uneditable, and the file ALREADY had `join(timeout=5.0)` on line 887 (unreachable because `stop()` on line 886 blocks first). Real chain: `BaseObserver.stop()` → `on_thread_stop()` → `unschedule_all()` → `_clear_emitters()` → `emitter.join()` **unbounded**. watchdog observer+emitter threads are `daemon=True` (verified live), so `_stop_watchdog` now runs blocking `stop()/join()` on a daemon helper thread + bound-joins it via `loop.run_in_executor(None, helper.join, deadline)` against `JARVIS_EMITTER_TEARDOWN_DEADLINE_S` (default 10s). On timeout: ABANDON the daemon handle (interpreter never waits on daemon threads → exit path cleared), increment class counter `_emitter_teardown_abandoned`, advance. Observer detached up-front (idempotent/re-entrant).

**Phase 2 — Force-batch GENERATE deadline floor** (`candidate_generator.py` helpers + `phase_runners/generate_runner.py` LIVE + `orchestrator.py` parity). Runbook said "eliminate hardcoded 220s / bypass doomed RT stream" — **debug.log falsified BOTH**: (a) no hardcoded 220 — it's `remaining = _remaining_seconds(deadline)`; (b) NO RT stream ran — Slice 36 already skips RT for force-batch; the 98.5s StreamRender was the telemetry wrapper timing the batch poll (tokens=0). REAL root cause: op-…e944 was `route=standard, complexity=trivial` → GENERATE deadline = route base `JARVIS_GEN_TIMEOUT_STANDARD_S=220` (R1 thinking-cap floor →360 only fires for likely-thinking ops, NOT trivial). force_batch → `_compute_primary_budget(remaining=220, force_batch=True)=min(220,300)=220` (Slice 43's batch_cap=300 alignment was already correct) → async batch poll severed at 220s with 300s lease unused (`TimeoutError elapsed=220s`, 0 APPLY). Fix: new shared resolvers `force_batch_gen_timeout_floor_s()` (=`JARVIS_DW_BATCH_TIMEOUT_S` + `JARVIS_FORCE_BATCH_GEN_OVERHEAD_S` default 30) + `apply_force_batch_deadline_floor(gen_timeout, force_batch=)`. Wired into BOTH GENERATE paths (mirror of R1 floor), gated by `_slice36_should_force_batch`. SAFE by construction: force-batch only when Claude disabled (pure-DW) → no cascade calibration regressed; floor is `max()` so wider COMPLEX windows preserved.

**CRITICAL wiring catch (Slice 45 lesson applied):** the R1-floor comment "Parity with generate_runner (the LIVE phase-dispatcher path)" + the v45 trace `PhaseDispatcher → GENERATERunner` proved the orchestrator inline block is NOT the live path. Added the floor to `generate_runner.py` (LIVE) too, NOT just orchestrator — otherwise it'd be dead code in production. 2 AST wiring pins enforce presence in BOTH paths.

12 new tests (4 teardown + 6 floor-logic + 2 wiring pins), 122 transport/budget adjacent green. 4 pre-existing failures (3 `test_primary_budget_tier3_cap` expecting 30 but getting 90/44 — stale vs evolved heavy-TTFT scalar; 1 `test_slice36_adaptive_transport::test_ast_pin_slice36_env_default_on`) VERIFIED present on clean main via stash — unrelated drift, candidate for a separate cleanup slice.

Also trimmed CLAUDE.md 40.9k→35.9k chars (5 CLOSED UX bullets condensed; detail stays in memory files) — cleared the >40k perf warning.

**Follow-ups (NOT in scope):** (1) separate hardcoded `220.0` in IMMEDIATE→STANDARD demotion retry path `generate_runner.py:1745`; (2) the post-recovery validation soak stays GATED on DW streaming-transport recovery (still `upstream_degraded`) — Slice 50 makes force-batch ops ABLE to use their full lease but can't manufacture upstream capacity; (3) the 4 pre-existing tier3-cap/slice36 test failures. See [[project_v45_preflight_soak]] [[project_slice_43_batch_timeout]] [[project_slice_49_external_sentinel]]
