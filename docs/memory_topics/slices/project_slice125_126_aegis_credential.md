---
title: Project Slice125 126 Aegis Credential
modules: []
status: merged
source: project_slice125_126_aegis_credential.md
---

**The DW `402 "balance too low"` that defeated FIVE soaks was NEVER out-of-credits, NEVER a capture-ordering bug, NEVER an injection-code bug — it was a STALE KEY in `backend/.env` shadowing the funded repo-root `./.env`.** Resolved across Slices 125 + 126.5. THREE wrong hypotheses before the telemetry exposed it — verify-first discipline (illuminate, don't guess) was the whole game.

**Slice 125** (MERGED PR #69330 `8cf740f66c`, PRD §51.11.25, v3.16): `aegis/credential_env_loader.py` (stdlib, allowlist-only, NO `source .env`, explicit-env-wins, redacted sha256[:8] fingerprints) loads funded keys before the Aegis snapshot (wired preflight step 0 + bash twin in launch_shadow_soak.sh). `aegis/credential_probe.py` two-arm boot probe (direct vs Aegis-routed) + `JARVIS_AEGIS_CREDENTIAL_PROBE_ENABLED` fatal-halt. 13 tests. **HONEST: this did NOT fix the 402** (the key was always reaching the daemon) — defensive only.

**Slice 126.5** (MERGED PR #69331 `037617dc1b`, PRD §51.11.26, v3.17): permanent redacted fingerprint telemetry at daemon env-LOAD (`daemon.py build_app`) + injection microsecond (`forwarding.py:480`). ONE live run exposed the ghost: daemon loaded `DOUBLEWORD_API_KEY=sha256:f4354214`, injected the same (NO load→inject mutation — injection always faithful), but the funded key is `sha256:6c4bf566`. **Root cause: stale unfunded DW key in `backend/.env` (f4354214) auto-loaded ahead of funded root `./.env` (6c4bf566), preserved by explicit-env-wins.** (ANTHROPIC matched in both = no conflict; only DW.) **Fix:** neutralized the stale `backend/.env` DOUBLEWORD_API_KEY line (commented; gitignored/local; value never read/printed). **Permanent guard:** loader now emits loud redacted CONFLICT warning on any env-vs-.env credential mismatch (`format_report`). **KILL-SHOT VERIFIED LIVE: post-fix env-load+injection=`6c4bf566`, 402-count=0, DW completion SUCCEEDED.** 15 tests.

**KEY LEARNINGS:** (1) The injection code (`forwarding.py` forward_request + `passthrough.py` forward_passthrough) is STATICALLY CORRECT — reads `os.environ[credential_env_var]` + injects `Bearer`; DW endpoint descriptors correct (auth_header=Authorization, HEADER_BEARER, LLM_COMPLETION). Don't touch it. (2) `_spawn_daemon` passes `dict(os.environ)+credentials` to the daemon subprocess. (3) Aegis IS a forwarding proxy: JARVIS→{aegis_url}/v1→daemon→api.doubleword.ai; `dw_aegis_base_url()`/`dw_session_auth_header()` in aegis_provider_bridge. (4) `backend/.env` is a SEPARATE secrets file from root `.env` — it can shadow root via find_dotenv auto-load; **always fingerprint-compare both when a credential misbehaves.** (5) Funded DW key works on ALL models direct (Qwen3.5-397B/35B, DeepSeek-V4-Flash) — proven by direct curl probes. (6) `ImmutableBudgetStateMachine` EXISTS in aegis (forward_request arg) — NOT the hallucinated governance one from Slice 118.

**STATE:** DW pipeline ONLINE (402=0). Layer-4 armed, isolation+quarantine+idle-fix all proven. The whole Tier-D ignition stack (Slices 120-126.5) is built+merged. The LAYER4 runtime line is now ACHIEVABLE (no longer blocked) — needs a fresh op to complete a DW generation through the post-GENERATE risk gate. Operator's funded key + signed roadmap in place. See [[project_slice122_sovereign_keys]], [[project_slice120_layer4_authority]]
