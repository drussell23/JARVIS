---
title: Project Slice127 Blueprint Handoff
modules: [docs/superpowers/plans/2026-06-06-slice-127-economic-sovereign-matrix.md, tests/governance/test_provider_retry_classifier.py]
status: historical
source: project_slice127_blueprint_handoff.md
---

**UPDATE 2026-06-06 (Slice 127 BUILT — verify-first correction).** The locked §51.11.D blueprint MISDIAGNOSED the root cause. Ground truth from the canonical `debug.log` of `bt-2026-06-07-040933` (line 1592): the 16 sticky breaker trips were **Claude HTTP 400 "credit balance too low"** (`fallback_err_class=BadRequestError`) → `BadRequestError` is in `_TERMINAL_CONFIG_CLASSES` (`provider_retry_classifier.py:144`) → classified `TERMINAL_CONFIG` → the **per-op** Slice-7 breaker sticky `OPEN_TERMINAL` on the 1st hit (`escalated_to_global=False`, 16 SEPARATE ops). It was **NOT** the `/v1/models` 401 (a benign `SemanticTriage` warning at line 46 that NEVER reaches the generation breaker) and **NOT** a GLOBAL breaker. Blueprint directive #2 (401 quarantine) addressed a non-problem → DESCOPED. Real root cause: a "credit balance too low" 400 is an ECONOMIC refusal (recoverable once funded — the Anthropic twin of the DW 402 that Slice 124 handles), misclassified as a permanent CONFIG fault. Operator authorized "Corrected Superset" (Option 1) after the misdiagnosis was surfaced.

**BUILT this session (PR #69333, branch `worktree-slice-127-economic-sovereign`, 3 commits off `dfb07b62a3`; NOT yet merged — operator frames merge as the ignition step after P3).**
- **P1 — economic reclassification (the actual root fix).** `provider_retry_classifier.classify()` gained `failure_message` + `economic_reclassify` params; an economic block (composing `economic_router.is_hard_economic_block`) → recoverable `TERMINAL_QUOTA`, never sticky `TERMINAL_CONFIG`. NO new RetryDecision member (closed 4-value AST pin intact). The classifier is **AST-pinned PURE-DATA (no `os.environ`)** — the env gate `JARVIS_ECONOMIC_RECLASSIFY_ENABLED` (default-FALSE §33.1) lives in `economic_router.economic_reclassify_enabled()`, NOT the classifier. **Strengthened `is_hard_economic_block`** to match Anthropic's actual "credit balance IS too low" phrasing — the original "balance too low" marker MISSED it, and that miss WAS the bug. Wired at `candidate_generator.py:~5692` breaker call site (`str(inner_exc)` + gate).
- **P2 — per-provider economic self-healing (COMPOSE, don't rebuild).** The EXISTING `claude_circuit_breaker.py::ClaudeCircuitBreaker` is already a per-Anthropic-lane self-healing breaker (CLOSED/OPEN/HALF_OPEN + recovery window) — it only tripped on TRANSPORT. Added `record_economic_exhaustion()` (distinct counter; trips OPEN at low threshold; self-heals via existing recovery-window→HALF_OPEN→`record_success`). Master `JARVIS_CLAUDE_ECONOMIC_BREAKER_ENABLED` (default-FALSE). Wired at the fallback site so future ops route around the broke lane (existing `should_allow_request` gate at `candidate_generator.py:4318-4339`, which only covers Claude-as-PRIMARY). **Per-provider isolation realized by composing this existing per-lane breaker, NOT a parallel `CircuitScope.PER_PROVIDER` registry** (operator's anti-duplication mandate — there are already 3 breaker systems: Slice-7 `circuit_breaker.py` per-op, `claude_circuit_breaker.py` per-Claude-lane, `dual_lane_breaker.py` total-outage). 150 tests green.

**SLICE 127 COMPLETE + VERIFIED LIVE (2026-06-07). P1+P2+P3+P2.1 all merged; soak proved it.** main = `e29636079a` (PR #69335, P2.1) on `923ee8eb3a` (PR #69334, P3) on `79977dd0ba` (PR #69333, P1+P2). **P2.1 — fallback-skip gate (PR #69335):** the live soak exposed that `_generate_immediate` does "Claude direct, skip DW" and the `should_allow_request` gate only covered Claude-as-PRIMARY, so IMMEDIATE ops ground against broke Claude instead of failing to DW. Fix: pure `immediate_reroute_to_dw` helper + `_generate_immediate` consults the Claude lane breaker → reroutes to `_call_primary` (DW) when OPEN (HALF_OPEN probe keeps Claude-direct to self-heal). Master `JARVIS_FALLBACK_SKIP_GATE_ENABLED` default-FALSE.

**SOAK VERIFICATION (3 runs, bt-2026-06-07, bounded $2/40min/10min-idle, funded DW key 6c4bf566 + Claude genuinely out-of-credit = real economic failure):** Slice 127 PROVEN END-TO-END — `terminal_config` (old 16-trip brick) = **0**; `terminal_quota` (P1) ×10; `ECONOMIC TRIP` (P2) fired; `IMMEDIATE reroute → DW` (P2.1) ×3 + DW `/v1/chat/completions` injected with funded key ×6. **Op-completion + LAYER4 NOT reached — root cause is ENVIRONMENT, not Slice 127:** running the heavy 6-layer soak NESTED under the agent process tree starves the asyncio event loop — isolation-ON → Oracle subprocess Pool fails "daemonic processes are not allowed to have children" ×4789 → starvation; isolation-OFF (`JARVIS_ORACLE_PROCESS_ISOLATION_ENABLED=0`) → in-process Oracle GIL-bound 1.1GB graph load → 12-13s loop lags (the exact coupling Slice 111-114 fixed). Both disrupt DW generation before tokens return. **For a clean op-completion + real `LAYER4:` line: run the soak in a DEDICATED terminal (not nested under the agent) + provide the operator's Ed25519-signed roadmap (Slice 120/122, fail-closed without it).** Soak masters to enable: `JARVIS_ECONOMIC_RECLASSIFY_ENABLED JARVIS_CLAUDE_ECONOMIC_BREAKER_ENABLED JARVIS_DW_DYNAMIC_RECOVERY_ENABLED JARVIS_FALLBACK_SKIP_GATE_ENABLED` =1.

**P1+P2+P3 ALL MERGED (2026-06-06).** main = `923ee8eb3a` (PR #69334, P3) on top of `79977dd0ba` (PR #69333, P1+P2). **P3 = DW transport dynamic exponential self-healing** (PRD §51.11.27, banner 3.20): VERIFY-FIRST narrowed it to ONE change — the DW lane ALREADY self-heals (`dw_transport_degraded_preflight` auto-probes once the `TRANSPORT_DEGRADED` verdict goes stale) and EMPTY_RESPONSE is ALREADY `RETRY_TRANSIENT` (0-token → `DoublewordInfraError(status 0)` → TIMEOUT/CONTENT_FAILURE → RETRY_TRANSIENT); the only gap was the hardcoded-default 120s STATIC freshness window. New `dw_transport_recovery.py` (thread-safe singleton COMPOSING `circuit_breaker.full_jitter_delay`) → dynamic exponential window keyed to rupture episodes: `note_degraded` (debounced by base → burst=1 episode), `note_recovered` (DW success → reset to 0 instantly), `dynamic_recovery_window_s` = `max(base, full_jitter_delay(episode-1, base, cap))`. Master `JARVIS_DW_DYNAMIC_RECOVERY_ENABLED` default-FALSE (base 30s, cap 600s); 3 gated best-effort sites in candidate_generator (`_note_dw_candidate_success`→reset, `_note_dw_live_transport_degraded`→episode, `dw_transport_degraded_preflight`→dynamic window). Did NOT add an EMPTY_RESPONSE FailureMode (cosmetic). 137 tests green. **P4 ignition soak:** launch with `JARVIS_ECONOMIC_RECLASSIFY_ENABLED=1 JARVIS_CLAUDE_ECONOMIC_BREAKER_ENABLED=1 JARVIS_DW_DYNAMIC_RECOVERY_ENABLED=1` (+ a funded lane) → verify the loop survives single-lane economic failure + reaches a real `LAYER4:` line. The whole Slice 127 ENGINEERING is complete; only the soak (funded keys + ~40min) remains.

**REMAINING (NOT built):** P3 = DW `live_transport` rupture self-healing (compose the Slice 83 streak/sever in `candidate_generator.py:~3383-3420` + `_note_dw_live_transport_degraded@:945` with a recovery window so a severed DW lane auto-probes back; + `EMPTY_RESPONSE`→`RETRY_TRANSIENT` for the 0-token `first_token_ms=-1` producer at `stream_renderer.py:303`). **NO byte-stream resume is achievable** — LLM completions can't byte-resume mid-chunk; "resume" = request-level stateful retry + lane recovery only (honest scoping; claiming otherwise = false attestation). P4 = ignition soak (needs operator-funded lane + ~40min to reach `LAYER4:`). Plan: `docs/superpowers/plans/2026-06-06-slice-127-economic-sovereign-matrix.md`.

**KEY MECHANICS for next instance.** Worktree commits hit the Iron Gate `denied_sovereignty` — needed `ledger_sovereignty.mark_owned(worktree_root, session_id=..., branch_name=...)` (the WorktreeManager auto-stamps trees IT creates; `EnterWorktree`-made trees lack the marker) PLUS `commit_authority_cli grant --channel autonomous`. Agent commits are the AUTONOMOUS channel (no TTY) which delegates to `ledger_sovereignty.is_owned()` and ignores grants — so the OWNERSHIP MARKER is the load-bearing one. CRITICAL FILE-PATH LESSON: when in an `EnterWorktree` worktree, Edit/Write MUST use the worktree absolute path (`.claude/worktrees/<name>/...`); using the main-tree path silently edits the loop's checkout (had to `git restore` + relocate). The classifier purity AST pins (`tests/governance/test_provider_retry_classifier.py`: `test_no_module_level_os_environ_read`, `test_module_has_exactly_one_public_classify_function`, `test_all_exports_present`) are strict — keep env reads OUT of the classifier. Verify-first ALWAYS (the blueprint itself was wrong — same pattern as the 5-consecutive-slice runbook overturns). See [[project_slice125_126_aegis_credential]], [[project_repo_git_pr_ops]]
