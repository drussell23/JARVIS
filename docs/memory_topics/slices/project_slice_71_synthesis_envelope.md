---
title: Project Slice 71 Synthesis Envelope
modules: [tests/unit/misc/test_guiprocess.py, backend/core/process_manager.py]
status: historical
source: project_slice_71_synthesis_envelope.md
---

**Slice 71 — fallback synthesis inherits the op wall envelope — MERGED to main 2026-06-02** (`5bf21d0969`, local squash-merge, not pushed/PR'd).

**Verify-first correction of the runbook (3rd time this arc):** runbook said "fast-cascade FSM + budget inheritance in `orchestrator.py`." REJECTED — orchestrator generation blocks are DEAD parity code (Slice 45/50 lesson); the live path is `candidate_generator.py` (`_call_fallback`) → `providers.py` (`ClaudeProvider.generate`) → `tool_executor.py` (`ToolLoopCoordinator`). Budget inheritance (90s/360s floor + `max_cap` post-acquire refresh, cand_gen:4949-4989) and the DW total-outage breaker (Slice 53 `dual_lane_breaker`/`_note_dw_total_outage`) ALREADY exist. The real defect was NOT the FSM.

**Root cause (fully traced, v69 soak bt-2026-06-02-232715):** the `budget=1.0s` Claude starvation = a Venom **continuation** round (`is_tool_round = round_index>0`, tool_executor.py:5502) deriving its per-round/stream budget from the *consumed* GENERATE-phase `deadline` (`providers.py` `_generate_raw`:7905 + `deadline_mono`:8732 + legacy loop:8785, all via `_remaining_utc_budget_s(deadline, floor_s=1.0)`). Earlier rounds consumed the phase window → `_remaining_utc_budget_s` floored to 1.0s → Claude got 1.0s → `first_token=NEVER`. Meanwhile `OperationContext.pipeline_deadline` (op_context.py:790, the WALL envelope stamped at submit) still had 313s. (The 313s-pre_sem vs 1.0s-budget "contradiction" resolved once I realized `tool_round=yes` ⇒ continuation, not round 0.)

**Fix:** flag-gated pure helper `providers._synthesis_envelope_deadline(context, phase_deadline)` returns the MORE generous (later) of phase deadline and `context.pipeline_deadline`; wired at the 3 ClaudeProvider tool-loop budget sites. `max()` never shrinks; outer `_call_fallback` `_race_or_wait_for` + wall watchdog stay the ceilings (inner-only fix, no FSM/outer change). Master `JARVIS_FALLBACK_SYNTHESIS_ENVELOPE_ENABLED` default-TRUE. +1 diagnostic log line. 8 tests; provider/fallback/cascade/tool-loop suite 734 passed; 9 adjacent fails pre-existing (AST-pin/dispatch drift, byte-identical on clean main).

**Scored soak bt-2026-06-03-035359 (Slice 71 on): stop=wall_clock_cap+atexit_fallback, $0.00, NO score — but BIG forward progress + NEW blocker.** DW was UP this run (`/v1/models` 200; Slice 71 inheritance correctly stayed DORMANT since fallback wasn't starved — max() no-op proves safety). Claude SYNTHESIZED a candidate (`🧬 synthesizing via Claude`, "Multi-object response 22 blocks") — **GENERATE fully unblocked past the v69 1.0s starvation; no EXHAUSTION/first_token=NEVER.** Slice 64/69 write-root WORKED (APPLY targeted the qutebrowser worktree). **NEW binding blocker:** the single injected qutebrowser op (`op-019e8ba2…-cau`, fail_to_pass=`tests/unit/misc/test_guiprocess.py::test_error`, a GUIProcess bug) — the Venom tool loop EXPLORED THE JARVIS CODEBASE not the qutebrowser worktree, so the model patched `backend/core/process_manager.py` (a JARVIS file, semantically "process"-related) and emitted that JARVIS-relative path. APPLY rebased onto `swebp_wt/.../backend/core/process_manager.py` → **ENOENT** → change-engine error → postmortem → no patch → no container score. Benchmark isolation (Slice 63) worked (only 1 op). `Slice 3G repo_root_override` (resolve_envelope_repo_root) confines APPLY write-root but NOT the model's EXPLORATION/tool-loop search root.

**NEXT (needs operator go — new work):** "Slice 72" candidate = confine the benchmark Venom tool loop's exploration (read_file/search_code/glob) to `prepared.worktree_path` so the model edits qutebrowser source, not JARVIS files. The model already understood the problem domain (process handling) — it just searched the wrong tree. Likely in `tool_executor.py` repo-root resolution + the swe_bench envelope's evidence.repo_root threading into Venom's search root (not just the write root). Slice 69 + 71 themselves are DONE and proven. See [[project_slice_69_manifest_isolation]] [[project_swe_bench_closed_loop_gap]].
