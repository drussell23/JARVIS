---
title: Slice 83 + the DW-transport root-cause correction (2026-06-03/04)
modules: [tests/governance/test_slice83_priority_dispatch.py, tests/aegis/test_slice85_concurrent_forwarding.py, scripts/swe_bench_pro_provider_spend.py, backend/core/ouroboros/governance/provider_topology.py, backend/core/ouroboros/governance/candidate_generator.py, backend/core/ouroboros/governance/doubleword_provider.py, backend/core/ouroboros/governance/tool_executor.py, backend/core/ouroboros/governance/context_compaction.py]
status: historical
source: project_slice_83_and_dw_transport_rootcause.md
---

# Slice 83 + the DW-transport root-cause correction (2026-06-03/04)

## Slice 83 — MERGED to main (PR #69254, squash `7f4721fe42`), PROVEN in production
Two coupled fixes, both surfaced by the abandoned cost-cascade sweep #6:
- **Phase 1** (`provider_topology.py::_trusted_seed_dw_models_for_route`): capability-priority sort via the catalog classifier `_score` (params-weighted COMPLEX/STANDARD, cost-aware BG/SPEC), fail-soft to insertion order. COMPLEX now ranks `DeepSeek-V4-Pro → Kimi-K2.6 → GLM-5.1 → Qwen397B → DeepSeek-V4-Flash → Qwen35B` (was insertion order, Qwen35B led).
- **Phase 2** (`candidate_generator.py::_dispatch_via_sentinel`): granular per-model circuit breaking. `JARVIS_DW_LIVE_TRANSPORT_SEVER_THRESHOLD` (default 3; `=1` = exact legacy Slice 73). Rotates to next coder on a single `live_transport`, only severs after N consecutive; non-transport failure (429/5xx/parse) resets the streak.
- **Production-validated** (session bt-2026-06-04-020554): logs show `attempting model=deepseek-ai/DeepSeek-V4-Pro` (top coder leads — Phase 1) then `granular isolation streak=1<3 — rotating` through DeepSeek→Kimi→GLM then `streak=3>=3 — severing` (Phase 2). Old Slice 73 would have severed on DeepSeek's first failure and never tried Kimi/GLM.
- 13 tests `tests/governance/test_slice83_priority_dispatch.py`, all green. The 6+3 `test_topology_sentinel_dispatch`/`test_provider_topology_v2` failures are pre-existing local-only `.jarvis/dw_promotion_ledger.json` contamination (verified identical on clean main via stash; pass on clean CI).

## ROOT CAUSE of the "DW down" blocker — it is CLIENT-SIDE (overturns [[project_slice_51_disambiguation]] "SERVER" verdict)
The v44→v64 arc concluded "DW serving is broken / returns empty." **That is now FALSIFIED.** Out-of-band aiohttp probes (key from .env, direct to api.doubleword.ai, bypassing JARVIS) on 2026-06-03:
- All 4 coders (DeepSeek-V4-Pro, Kimi-K2.6, GLM-5.1, Qwen397B): **non-streaming HTTP 200 + real content, <2.5s.**
- DeepSeek-V4-Pro **streaming, `reasoning_effort=none`, ~200-line prompt: HTTP200, ttfb 1.4s, 60 chunks, 716 chars content. CLEAN.**
- DeepSeek-V4-Pro **streaming, `reasoning_effort=high`: `ClientPayloadError: Response payload is not completed: TransferEncodingError` — DW SERVER RUPTURES the chunked stream at effort=high.**

Two compounding CLIENT-SIDE failures, both mislabeled `live_transport:RuntimeError` for months:
1. **`reasoning_effort=high` → DW server stream rupture.** JARVIS sends `high` for `complexity=complex` SWE-bench ops (Slice 55 P2 `_reasoning_effort_for`, `doubleword_provider.py:120`). Override exists: `JARVIS_DW_REASONING_EFFORT=none` (env wins, line 126).
2. **The Aegis passthrough proxy ruptures the SSE stream.** With `JARVIS_AEGIS_ENABLED` true, ALL DW upstream routes through `{JARVIS_AEGIS_URL}/v1` (`doubleword_provider.py:616` → `aegis_provider_bridge.dw_aegis_base_url`). Even with `JARVIS_DW_REASONING_EFFORT=none` forced, JARVIS GENERATE still fails at exactly 30s (`_PRIMARY_MAX_TIMEOUT_S`) with `live_transport` — while the IDENTICAL direct effort=none probe streams clean. Boot log shows the smoking gun: `aiohttp ClientConnectionResetError: Cannot write to closing transport` inside `AegisPassthrough` web_response. The only material difference between working-direct and failing-JARVIS is the Aegis hop. (Also reaped 10+ orphaned `aegis.daemon` zombies from killed soaks — possible contention contributor.)

Also: Slice 36 transport selector (`_slice36_should_force_batch`, env `JARVIS_DW_FORCE_BATCH_STANDARD_COMPLEX=0` to disable) forces STANDARD/COMPLEX+Claude-disabled ops onto the BATCH path on stale "v31 evidence RT TTFT 66s" — but RT now has 1.4s ttfb. Batch path hung 300s/0-tokens; RT path fails at 30s via Aegis. Neither is the real fix; **Aegis SSE forwarding is.**

## Slice 84 + 84b MERGED 2026-06-04 — root cause was NOT Aegis, it was JARVIS's own 30s TTFT cap
Verify-first OVERTURNED the Aegis hypothesis above: `aegis/forwarding.py` already streams chunk-by-chunk via `iter_any()`; raising the Aegis `sock_read` 12× (`JARVIS_AEGIS_UPSTREAM_SOCK_READ_S=360`) had ZERO effect — the 30s was JARVIS's own `_PRIMARY_MAX_TIMEOUT_S`. The REAL three defects (all client-side, all fixed):
- **Slice 84 Phase 1** (`candidate_generator._is_heavy_model`, PR #69255 squash `2421c95db0`): the adaptive TTFT widening (2.5×→75-150s) only matched markers `("397B","Kimi")` → DeepSeek-V4-Pro (1000B) + GLM-5.1 (754B) got the bare **30s** cap → killed at `elapsed=30.01s` before first token. Now param-aware: any model ≥`JARVIS_HEAVY_MODEL_MIN_PARAMS_B` (100B) via Slice 82 catalog is heavy. No hardcoding.
- **Slice 84 Phase 2** (`_reasoning_effort_for`): clamp effort to serveable ceiling (default "medium", env `JARVIS_DW_MAX_REASONING_EFFORT`). Probe: DeepSeek streams none/low/medium clean, **ruptures at high** (`TransferEncodingError`); heavy_code/architectural→high→rupture.
- **Slice 84 Phase 3** (`_generate_realtime`): `result.with_tool_records(tool_records)` — the RT path dropped its Venom records → Iron Gate exploration gate saw 0/2 → rejected every candidate. Now mirrors ClaudeProvider.
- **Slice 84b** (`scripts/swe_bench_pro_soak.sh`, PR #69256): the 2.5× default (75s) still severs SWE-bench's ~80-200s tool-loop generations (cap bounds WHOLE attempt, not just TTFT). Soak profile sets `JARVIS_PRIMARY_HEAVY_TTFT_SCALAR=8` (→240s ceiling) + pins `JARVIS_DW_FORCE_BATCH_STANDARD_COMPLEX=0` (RT/tool-loop path, not the batch lane that hangs 300s/0-token).

**PROVEN cost-cascade**: DeepSeek-V4-Pro produced a real candidate for **$0.0031** (7 tool calls, full Venom loop) vs Claude's $0.24 on the same qutebrowser problem — ~77× cheaper. 12 new Slice-84 tests; 330 adjacent green (the 1 section39 route-set fail is pre-existing on main).

## Slice 85 MERGED 2026-06-04 (PR #69257 `735489194764`) — convergence gate + Aegis cleared
First 6-instance sweep (bt-2026-06-04-041943, scalar=8/240s) = **0 candidates, $0.0210 DW / $0 Claude** — NOT a success (DW spent that EXPLORING; every op ruptured or wandered). Honest receipt stays the single-op **$0.0031 → 1 candidate**, NOT the concurrent sweep. Two stress behaviors, BOTH verify-first-diagnosed (NOT concurrency):
- **Concurrency hypothesis FALSIFIED**: direct probe = 6 concurrent DW streams → 0 ruptures; `tests/aegis/test_slice85_concurrent_forwarding.py` = 6 concurrent paced streams through real Aegis forwarding → all complete clean. DW endpoint AND Aegis proxy both concurrency-clean. The "9 ruptures" were actually `TimeoutError first_token_ms=-1` (175-242s no token) = upstream prompt-compilation delay on the HARD problems (NodeBB/element-web), not transport/concurrency.
- **The 114k wander** (25 tool calls/486s/0 patch): root cause = the exploration-budget nudge only counts rounds where EVERY tool ∈ narrow `{read_file,search_code,get_callers}`; rounds mixing glob_files/list_dir/git_log slipped it → nudge fired 0×. Fix (Slice 85 Phase 3, `tool_executor.py`): cumulative tool-agnostic counter over read-only nav superset `_READONLY_EXPLORATION_TOOLS` → `JARVIS_TOOL_LOOP_CONVERGENCE_EXPLORE_CALLS` (default 14, 0=opt-out) → fires the EXISTING Slice 3E nudge via new `_trigger_context` axis. Phase 2 (compaction) NOT built — context_compaction.py (Gap #8) already exists; wander was convergence not size. 12 tests; 8 adjacent tool_executor source-pin fails pre-existing on main (stash-verified).
- Slice 85 branch was `infrastructure/slice-85-adaptive-concurrency-limits` (NO admission controller built — the concurrency premise was falsified; built the convergence gate + diagnostic instead).

## Slice 86 MERGED 2026-06-04 (PR #69259 `5c307ee0ec`) — loop-hygiene; 6-instance failures = CAPABILITY ceiling
Root cause of the multi-instance stalls = JARVIS event-loop starvation: `GovernedLoopService._oracle_index_loop` calls `incremental_update([])` (empty list → falsy → else-branch FULL repo scan = 48-72s CPU freeze on the loop → `ControlPlaneStarvation lag` to 10,000ms → stream-reading coroutines can't consume DW's bytes → `first_token_ms=-1`). NOT upstream delay (DW streams content ≤35s even on 21k-token prompt), NOT transport timeout, NOT concurrency (6 concurrent direct streams + 6 through Aegis both clean). Fix: `_oracle_full_scan_suppressed_by_benchmark()` gates the periodic full scan under `JARVIS_BENCHMARK_ISOLATION_MODE` (kill-switch `JARVIS_BENCHMARK_SUPPRESS_ORACLE_FULL_SCAN` default-on; targeted post-APPLY reindex unaffected; search_code uses ripgrep not the graph). 7 tests. **DECLINED runbook's geometry-paced TIMEOUT scaling — limiter was the 240s primary-budget cap not transport; more time ≠ fix for a frozen loop.**
**Validation sweep (bt-2026-06-04-061913): Slice 86 WORKED — Oracle full-scans 0 (was 3+), loop lag 10s→3.4s. BUT 6-instance sweep STILL 0 scored rows: ops stall at `elapsed=240.01s` (full primary budget, 0 content) on element-web + NodeBB. CRITICAL: those are the SAME problems that were CAPABILITY MISSES EVEN WITH CLAUDE (see [[project_eval2_macro_sweep]]). DW reasoning 240s/no-content on them = capability ceiling, NOT infra. DW $0.0181/Claude $0.**

## STRATEGIC VERDICT (2026-06-04): infra stack 83-86 DONE; STOP the 6-instance chase
5 slices merged (83/84/84b/85/86); whole v44-v64 "DW down" saga = stack of CLIENT-SIDE bugs, all fixed; verify-first overturned a runbook premise on 5 consecutive slices. **The 6-instance set was the wrong target — 2-3 of its problems (element-web TS, NodeBB JS) don't resolve even for Claude.** No transport/loop fix makes a model solve beyond its capability. **DEFENSIBLE RECEIPT = single-op DW $0.0031 → 1 real patch vs Claude $0.24 (~77×).** The 6-instance $0.018-0.021/0-scored runs are capability-limited, NOT receipts — never cite as success. **For a SCORED DW row: run qutebrowser (proven-resolvable) DW-ONLY SOLO** — now that 84(tool-records→exploration counts)+85(convergence)+86(loop-hygiene) are merged, it has a genuine shot at the first DW-only scored row. The hard problems are a model-capability question, orthogonal to the cost-cascade thesis. Recommend stopping reflexive multi-instance slices.

## (historical) full 6-instance cost-cascade ledger sweep notes
Sweep launched Claude-OFF with the merged Slice-84/84b defaults + 6 verified ids ([[project_eval2_macro_sweep]]). qutebrowser is the clean-resolve pole; element-web/NodeBB-76c6 are known capability misses even with Claude. Auto-emit the cost chart post-run via existing `scripts/swe_bench_pro_provider_spend.py` (reads summary.json.cost_breakdown). Tuning still open: per-problem TTFT may need >240s on the largest repos (ansible 2028-line, NodeBB 151-line); watch for residual `elapsed=240` severs.

Verified ids (cached + sweap images present): qutebrowser `…b3c171`, element-web `…67b2e5-vnan`, NodeBB-76c6 `…917516`, NodeBB-04998908 `…c5b5-vnan`, ansible-be2c376 `…512f5`, ansible-c616e54a `…83f86`. Spend this session ~$0 (all soaks exhausted pre-candidate). Builds on [[project_first_container_scored_row]].
