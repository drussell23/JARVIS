---
title: Project Slice 45 Terminal Worker Tools
modules: [scripts/trace_qwen_tool_syntax.py]
status: historical
source: project_slice_45_terminal_worker_tools.md
---

Slice 45 — Autonomous Tool-Modality Interception & Adaptive Schema Bridge. **MERGED main 2026-05-29** PR #65629 (squash tip c7c87d17e9). Two Opus reviews (first = DO NOT MERGE, caught dead-code; v2 = READY TO MERGE). Builds on [[project_slice_44_worktree_reaper_oracle]].

**The v40b blocker (B): exploration gate unsatisfiable on pure-DW.** v40b produced the arc's FIRST DW candidate but Iron Gate rejected it (`exploration_insufficient: 0/1`) — model made 0 tool calls. ROOT CAUSE = **predicate mismatch** (NOT a parse bug, NOT model incapacity):
- PROMPT layer (`providers._build_tool_section`): `should_skip_venom_for_route("background")` → returns `""` → model never shown the tool list or the `2b.2-tool` schema.
- EXEC layer (`doubleword_provider` `_skip_tools = complexity=="trivial"`): non-trivial BACKGROUND still RUNS the Venom loop → `parse_fn` called on output the model was never told to shape → None every round → 0 tool calls → deadlock.
The Slice 12AF suppression assumed background never runs the loop ("Claude does the real work later") — FALSE when Claude disabled and DW is the terminal worker.

**Phase 1 verify-first trace (`scripts/trace_qwen_tool_syntax.py`, 2 live DW batch completions):** decisive bisection — tools-advertised → Qwen-397B emits FLAWLESS `2b.2-tool` (parallel tool_calls, read_file+search_code) in 5.8s, parser caught; tools-suppressed → narrates "I cannot see the file… I would need to read it" (wants to explore, no means). Verdict **PREDICATE_FIX_SUFFICIENT** → NO parse adapter (runbook Branch A unneeded), NO exploration-enforcement prompt (Branch B unneeded). The runbook's framing was wrong; the real fix is advertising tools.

**Fix (env-gated, BACKGROUND-only, byte-identical legacy when Claude enabled / flag off):** new leaf `dw_terminal_worker_policy.background_is_terminal_worker()` = route=="background" AND `JARVIS_DW_BACKGROUND_VENOM_ENABLED` (default true) AND `claude_is_disabled()`. Consumed at: (1) `_should_use_lean_prompt` → terminal-worker bg uses the LEAN prompt (advertises tools AND preloads target files for Iron Gate credit); (2) `_will_skip_tools` (DW) → for terminal-worker mirrors exec gate EXACTLY (skip only trivial — closes the residual simple-bg half); (3) `_build_tool_section` → restructured so route-skip suppression is the SAFE DEFAULT, terminal-worker exception relaxes only when policy True, import-failure→SUPPRESS. Seeded flag in FlagRegistry (ROUTING). 20 tests + 227 cross-arc green (1 pre-existing 12AF Site5 generate_runner-drift red, unrelated).

**CRITICAL review catch (Opus #1):** my first patch was DEAD CODE in prod — `_should_use_lean_prompt` returned False for background → full prompt → `if tools_enabled:` gate (DW call site defaults tools_enabled=False) → `_build_tool_section` NEVER called. My Phase 1 trace validated the MODEL (called `_build_tool_section` directly) but not the WIRING. v2 routes through LEAN (which calls `_build_tool_section` unconditionally + preloads files). Opus #2 verified all 5 findings fixed + cross_repo doubly-mitigated (cross_repo guard ordered after skip-route block + urgency_router routes cross_repo to IMMEDIATE never background) + no orphan-2b.2-tool. Lesson: an end-to-end ASSEMBLED-PROMPT test (not just component tests) is mandatory — added 5 such pins.

**v41 capability soak (bt-2026-05-29-213224, $0.00, NO APPLY, NO GENERATE):** Slice 45 was **NOT exercised** — the session wedged before any op reached GENERATE. Boot succeeded (Aegis ✓, reaper reaped 1 orphan ✓, Oracle indexed **2,530,316 nodes** ✓), then the asyncio loop **wedged 574s** (single `lag_ms=55437`) → LoopDeadman `os._exit(75)`, but GIL-held so it couldn't even exit cleanly (duration_s=2775 ≫ 600s wall cap; had to OS-SIGKILL the wedged proc + orphan aegis daemon manually). The wedge coincided with `TestWatcher.run_pytest pid=53053` (PytestHelper TIMEOUT fired at 574s not 30s — the asyncio timeout couldn't fire because the loop wasn't spinning ⇒ the late timeout is a SYMPTOM of the wedge, not the cause). cost $0 = no provider call completed. **Binding blocker is AGAIN loop-health (v40b boundary A), new form: a GIL-level wedge ~coincident with TestWatcher's pytest subprocess + the 2.5M-node Oracle index.** `run_pytest_subprocess` itself IS async (`await asyncio.wait_for`), so the true GIL-holder is elsewhere (Oracle/sensor) — needs a dedicated SidecarProfiler-driven loop-health probe, NOT transport/gate work. Slice 44 fixed ONE loop-health cause (worktree scan); v38→v41 shows loop-health is now THE wall.

**Net:** the exploration-gate fix is correct + merged + proven (trace + 20 tests + 2 reviews), but capability bar stays 0 APPLY because loop-health kills the session before the chain completes. Next legitimate investigation (operator-gated): bisect the 574s GIL-wedge — prime suspects the 2.5M-node Oracle index build/query + TestWatcher sensor on the 16GB M1. See [[project_slice_44_worktree_reaper_oracle]] [[feedback_no_preresult_euphoria]].
