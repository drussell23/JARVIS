---
title: Project Slice98 Risk Convergence
modules: [backend/core/ouroboros/governance/schelling_consensus_prior.py, backend/core/ouroboros/governance/dynamic_risk_convergence.py, backend/core/ouroboros/governance/ambiguity_sensor_mesh.py, backend/core/ouroboros/cross_repo_mesh/ripple_emitter.py, orchestrator.py]
status: historical
source: project_slice98_risk_convergence.md
---

**Slice 98 — Dynamic Risk-State Convergence (recoverable safe-default under ambiguity). MERGED 2026-06-06.** NOT §40 #12 — verify-first found **#12 Schelling-point consensus prior was ALREADY built** (`schelling_consensus_prior.py`, 1068 LOC / 64 tests / commit 9804b2fd0d; the "no file" in my own §40.6.1 addendum was a grep error — it greps as `schelling_consensus_prior` not bare `schelling`). **§40 board is 100% CLOSED (all 22 items have files).** The user's broader "Schelling failsafe" runbook had 2 DANGEROUS phases I rejected + redesigned: (a) cage fail-closed-on-"unmappable"-code = catastrophic FP that neuters autonomous dev (the cage blocks KNOWN-bad, not unknown — inverting it = block-everything-novel); (b) autonomous sever-write dead-man's-switch = deadlock + violates watchdog-isolation. Built the SAFE version instead (user agreed).

**THE design principle (load-bearing): recoverable = PURE FUNCTION of the CURRENT ambiguity window. NO latch, NO state-machine, NO manual reset. When signals age out, the floor relaxes automatically.**

- **Phase 1 — PRD §40.6.1 correction** (#12 built; board closed).
- **Phase 2 (JARVIS, PR #69288)** `dynamic_risk_convergence.py`: `record_ambiguity(AmbiguitySignal {CROSS_REPO_HANDSHAKE_FAILURE, CONTRADICTORY_OUTPUT, MALFORMED_INTENT}, now, weight)` → bounded time-windowed deque; `convergence_score(now)` = sum of in-window weights (PURE); `recommended_convergence_floor(now)`: <3→None, <6→notify_apply, >=6→approval_required (ConvergenceBand NORMAL/ELEVATED/PARANOIA). **Composes the EXISTING `risk_tier_floor.recommended_floor()` as one more strictest-wins candidate** (lazy-import + try/except so a convergence crash can NEVER break/weaken the safety floor; master-OFF → recommended_floor BYTE-IDENTICAL). SSE `schelling_convergence_changed` on band transition (observability-only — verified it never feeds the floor = no latch). Master `JARVIS_DYNAMIC_RISK_CONVERGENCE_ENABLED` §33.1 default-FALSE. 20 tests + 43 risk_tier_floor regression unchanged. Review: latch-hunt (forced SSE band=PARANOIA w/ 0 signals → floor still None), inert-byte-identical, floor-robustness-under-crash, clock-skew/full-deque safe.
- **Phase 3 (siblings — jarvis-prime#3, reactor-core#2)** `cross_repo_mesh/partition_degradation.py`: `partition_posture(now, last_verified)` PURE — disabled→NORMAL; never-received(None)→NORMAL (cold-start no-false-paranoia); stale(now-last_verified>window)→OBSERVATION_ONLY; fresh→NORMAL. `record_verified()` hooked into listener VERIFIED path (best-effort) moves freshness FORWARD → auto-recovery. Master `<REPO>_PARTITION_DEGRADATION_ENABLED` default-FALSE. 9 tests each. Soft reversible posture (read-only stance), NOT an irreversible sever.
- **Phase 4** chaos/recovery folded into Phase-2+3 tests (throttle→recover, transient-blip, zero false-opens).

**Slice 99 — Ambiguity Sensor Mesh + live producer hooks WIRED (PR #69289), closing the Slice-98 producer gap.** `ambiguity_sensor_mesh.py`: 3 best-effort PULL-model producer hooks (`note_cross_repo_emit_failure`/`note_llm_contradiction`/`note_malformed_intent` — tiny O(1) appends to bounded deques, NEVER raise, inert master-off, AST-pinned to NOT call record_ambiguity directly) + async sampler (`run_sensor_mesh_once`/`run_sensor_mesh_loop` asyncio.sleep cadence, iterations-bounded) that reads sliding-window counts → fires `record_ambiguity(signal, weight, decay_s)` on threshold breach. **Per-signal DECAY added to convergence engine** (record_ambiguity gains decay_s; signal record now (signal,ts,weight,decay_s); per-signal effective_window — PRESERVES pure-function recovery, no latch; backward-compat decay_s=None). Master `JARVIS_AMBIGUITY_SENSOR_MESH_ENABLED` §33.1 default-FALSE. **WIRED: emit-side cross-repo failure hook** in `ripple_emitter.py` (lazy+try/except at sign_error+write_failed — fire-and-forget honored, emit-side only NO ACK; sensor raise CANNOT alter EmitResult). **DEFERRED (honest, 0-line orchestrator diff): LLM-contradiction hook** exposed+tested but NOT wired into the 6000-line GENERATE_RETRY FSM (1-line follow-up `note_llm_contradiction(op_id)` at orchestrator.py ~6087 guarded by retry-count; safety>completeness). Chaos test: dual burst (LLM-loop + partition) → both fire → REAL risk_tier_floor → approval_required → auto-recover after decay. 23 tests + 95 regression. Review: own hot-path probe (emit unbreakable by raising sensor), latch-hunt (none), pull-model AST-enforced, inert-default.

**Slice 100 — FSM Sentinel Guard MERGED (PR #69290): the deferred LLM-contradiction hook is now WIRED into the orchestrator GENERATE_RETRY loop, safely + permanently.** `ambiguity_sensor_mesh.observe_generate_retry(op_id, attempt_num, *, detail, now_unix) -> bool`: fires `note_llm_contradiction` ONLY when attempt_num >= `JARVIS_SENTINEL_CONTRADICTION_ATTEMPT_THRESHOLD` (default 2); entirely try/except-wrapped (returns False on error), PULL-model (accumulator only, never record_ambiguity — AST-pinned), inert sensor-master-off. **orchestrator.py +18/-0 ADDITIVE edit at ~line 5626** (the GENERATE_RETRY advance, inside `except Exception as exc:` opened ~5273, after `generate_retries_remaining -= 1` @5469 where the `<0` exhaustion path already returned @5570): lazy-import AND sentinel call inside ONE `try:/except Exception: pass` (even ImportError swallowed); reads only ctx.op_id/generate_retries_remaining/max_generate_retries/exc; touches NO FSM control-flow/counter/loop/return — FSM proceeds byte-identically under total sensor failure. **FULL REFLEX ARC NOW LIVE: GENERATE_RETRY → sentinel → sensor mesh (pull) → async sampler → record_ambiguity → convergence engine → risk_tier_floor → auto-recovery.** 8 tests (chaos: attempts 1..5 → fires → REAL risk_tier_floor → approval_required, nothing raised; genuine static-AST try/except-enclosure check verified by mutation) + 43 regression. Review: own FSM-unbreakable + pull-model + never-raises probes; pre-existing-on-base failure = `test_call_fallback_outer_retry` in candidate_generator (untouched). The cross-repo emit-failure signal AND the LLM-contradiction signal are BOTH wired live now.

All masters §33.1 default-FALSE. **Session: 21 JARVIS PRs + 4 sibling PRs (Slices 97/98/99/100).** See [[project_slice97_cross_repo_mesh]], [[project_section40_open_items]], [[project_repo_git_pr_ops]].
