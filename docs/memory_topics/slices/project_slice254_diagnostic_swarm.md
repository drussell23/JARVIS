---
title: Project Slice254 Diagnostic Swarm
modules: [backend/core/diagnostic_swarm.py, backend/core/cybernetic_reanimation.py, tests/governance/test_slice254_diagnostic_swarm.py, tests/governance/test_reanimation_kernel_wiring.py]
status: merged
source: project_slice254_diagnostic_swarm.md
---

**Slice 254 — Ephemeral Swarm Intelligence & Dynamic Diagnostic Delegation. MERGED PR #69507, main `df79b514`.** Gives the reanimated muscle a brain: a trapped COMPLEX anomaly spawns an ephemeral read-only diagnostic sub-agent that enriches the Slice-253 endorsement decision with root-cause analysis. Builds on [[project_slice253_shadow_endorsement]] + [[project_slice252_shadow_telemetry]].

**Built:**
- `backend/core/diagnostic_swarm.py` (NEW, decoupled/duck-typed, in-sandbox-testable): `DiagnosticSubAgent` (ephemeral, STRUCTURALLY READ-ONLY — no execute/endorse/kill/shed attr; investigate() wrapped in asyncio.wait_for(ttl default 60s) so hung probe can't leak; fail-soft timeout/error→finding-with-status, never raises; injectable probe, `DefaultDiagnosticProbe`=best-effort psutil). `SubAgentOrchestrator.handle_trap` NON-BLOCKING (loop.create_task + strong task-refs no-GC; skips non-complex/disabled/no-running-loop fail-soft). `DiagnosticFindingsStore` (capacity-capped OrderedDict keyed by action_id + per-key asyncio.Event so REPL can AWAIT a fresh diagnosis). `DiagnosticFinding` frozen (action_id/op_id/organ/summary/facts/status/elapsed_s). `is_complex_trap` (triggering_signal startswith anomaly_detected/resource_pressure). `enriched_endorsement_prompt`(diagnosis ABOVE the 253 [Y/N] line; None→"diagnosis pending") + `endorsement_prompt_with_diagnosis(orch,payload,wait_s)` (awaits store, bounded, decision never gated on swarm). Flags: `JARVIS_DIAGNOSTIC_SWARM_ENABLED` default-TRUE, `JARVIS_DIAGNOSTIC_AGENT_TTL_S`=60, `JARVIS_DIAGNOSTIC_FINDINGS_MAX`=64.
- `cybernetic_reanimation.py`: TRAP OBSERVERS — `register_trap_observer`/`unregister_trap_observer`/`reset_trap_observers`/`_notify_trap_observers` (fan-out NEVER raises) fired from shadow_guard+shadow_guard_async AFTER stashing (captures sig_repr once, passes {organ_name,intended_action,triggering_signal,action_id,op_id}). This is the IN-PROCESS kernel-free equivalent of subscribing to SHADOW_ACTION_TRAPPED on SupervisorEventBus — the trap chokepoint is single source of truth, observers see what broker sees. Observers MUST be non-blocking.

**Tests:** `test_slice254_diagnostic_swarm.py` 23 decoupled (in-sandbox): env flags+complexity filter (parametrized); sub-agent finding/TTL-timeout(delay>ttl)/probe-error/structurally-read-only(no forbidden attrs); store put-get-wait_for(present+concurrent+timeout)-bounded; orchestrator non-blocking spawn (store.get None until task awaited)+skip disabled/non-complex/no-loop(sync ctx); enrichment ordering (summary index < [Y/N] index); full chain via real EventActivationDispatcher→shadow_guard_async→observer→spawn→wait→enriched. +1 kernel-chain (sandbox-off, 9 total in test_reanimation_kernel_wiring.py): real ANOMALY→real SelfHealingOrchestrator via build_resilience_dispatcher→trap→orchestrator.attach_to_trap_stream spawns ephemeral agent w/o blocking dispatch→probe root-cause "python(pid=999)" piped into enriched prompt+action still endorsable. 80 green incl 249/252/253/foundation, zero regression.

**Reanimation arc now:** telemetry (252) → endorse (253) → swarm-enriched endorse (254). Trap chokepoint fans to: stash (253 pending registry) + telemetry (252 broker) + observers (254 swarm). Context: main also carries autonomous loop's #69504 Sovereign Fusion. Patterns: OCA worktree mark_owned; kernel tests dangerouslyDisableSandbox; injectable probe for sandbox-safe tests; non-blocking create_task w/ strong task-set + add_done_callback(discard).
