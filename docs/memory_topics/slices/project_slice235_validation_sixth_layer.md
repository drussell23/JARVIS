---
title: Project Slice235 Validation Sixth Layer
modules: []
status: merged
source: project_slice235_validation_sixth_layer.md
---

Slice 235 ("adaptive 2b.1-diff + fail-safe degradation", commits 9577d608d0 keystone + 7f95fb69d6 wiring, branch perf/slice-235-adaptive-diff) bounded validation soak ran 2026-06-13 in the `jarvis-dw-cortex-soak` container with four credit/time guards (`--cost-cap 5`, `--max-wall-seconds 2400`, AEGIS `$2.00/hr+session`, and `JARVIS_CLAUDE_BREAKER_PERSIST_ENABLED=true` to restore the economic-OPEN breaker so Claude is routed-around and GOAL-001 generation goes to the DW elite pool). Result = **OUTCOME #3 (a sixth layer)**, diagnosed not fixed. Total spend $0.20. Claude calls: 0 (credits fully protected).

**Neither target signal fired:** `emitting 2b.1-diff` = 0×, `GOAL-001 state=applied` = 0×. Instead: the DW model (Qwen/Qwen3.5-397B-A17B-FP8) emitted the `"schema_version": "2b.1"` framing 3× but with **`full_content` not diff** → a 63KB inlined-file JSON string that corrupted mid-string → `JSONDecodeError` 6× (the exact "Layer-5 disease" the keystone commit documents) → `deadline_exhausted_pre_fallback` 45× → hibernation cycle (recovered each time, never permanently stuck).

**Root cause (the sixth layer) — DW codegen model SELECTION, not entitlement, not a Slice 235 bug.** The Slice 235 gate works exactly as designed: `should_force_full_content()` (providers.py `resolve_force_full_content`) engages diff only when `schema_capability == full_content_and_diff` AND target file > `JARVIS_DIFF_SCHEMA_THRESHOLD_LINES` (default 800). Size gate PASSED (semantic_index.py = 3246 lines). Capability gate FAILED: `resolve_diff_capability_for_model` (providers.py:297) maps model→family→capability; `_diff_capable_families()` default = `{moonshotai, deepseek-ai, zai-org}` (env `JARVIS_DW_DIFF_CAPABLE_FAMILIES`). Qwen family → `full_content_only` (correctly — 397B can't emit reliable verbatim diffs). The selector picked `Qwen/Qwen3.5-397B-A17B-FP8` **98×** for codegen and an elite diff-capable model **0×**, even though all three diff-capable elites are ENTITLED + reachable + FAST (live-probed 2026-06-13: moonshotai/Kimi-K2.6 HTTP 200 2.5s, deepseek-ai/DeepSeek-V4-Flash 200 1.2s, zai-org/GLM-5.1-FP8 200 1.3s) and rank ABOVE Qwen in `JARVIS_DW_FAMILY_PREFERENCE` (moonshotai:1.0 deepseek-ai:0.9 … Qwen:0.5). The IMMEDIATE→DW reroute path (GOAL-001 = `critical_urgency:roadmap` → IMMEDIATE; Claude breaker OPEN → reroute to DW) appears pinned to the **sealed Qwen-397B baseline brain** (brain_selection_policy.yaml notes STANDARD "SEALED … Qwen 397B isolation benchmark") rather than the family-preference-ranked diff-capable elites.

**Disposition:** Slice 235 itself is sound — committed + tested, gate logic correct. The fix is a deliberate change to which brain serves IMMEDIATE-rerouted-to-DW codegen. Do NOT widen `_diff_capable_families` to include qwen (it genuinely can't emit reliable diffs). Validation overlay: `docker-compose.slice235-validation.yml`.

**RESOLVED — Slice 236 (commit cfa9e89fa1, branch perf/slice-235-adaptive-diff), TDD'd + LIVE-VALIDATED 2026-06-13.** Root cause confirmed via code archaeology: the Qwen pin is NOT a deliberate "use Qwen for diffs" decision — it is an UNCONDITIONAL baseline fallback. IMMEDIATE route is sealed (`brain_selection_policy.yaml: immediate {dw_allowed:false, dw_models:[]}`, Manifesto §5 Claude-only); when Claude breaker is OPEN the `immediate_reroute_to_dw` gate (candidate_generator.py:4045) escapes the seal via `_call_primary(ctx,deadline)` with NO model_id; `_resolve_effective_model` (doubleword_provider.py:1235) then: sentinel ContextVar empty → `model_for_route_unified("immediate")`=None (sealed/empty) → falls to `self._model`=Qwen-397B (the `DOUBLEWORD_MODEL` default). `JARVIS_DW_FAMILY_PREFERENCE` was only read in the catalog *classifier*, never in generation-time resolution. **Fix lives in the selector, not the gate/allowlist.** providers.py pure seams (reuse the gate's own `_max_target_line_count`+`_diff_schema_threshold_lines` → no size-signal drift): `op_warrants_diff_capable_model`, `select_diff_capable_model` (ranks injected catalog by diff-capable family + JARVIS_DW_FAMILY_PREFERENCE; no model NAMES in code), `capability_aware_default_model` (pure orchestration), `capability_aware_selection_enabled` (`JARVIS_DW_CAPABILITY_AWARE_SELECTION_ENABLED` default-TRUE; OFF/small/no-elite/already-capable byte-identical). doubleword_provider.py `_capability_aware_default` thin wrapper wired into the `self._model` baseline fallbacks ONLY (sentinel/topology overrides untouched — deliberate). 25 TDD tests + contextvar stub update; +25 passing / 0 new failures (the 6 dispatch + tool_use_interface failures are PRE-EXISTING, proven via stash).

**LIVE SOAK RESULT (same bounded overlay, $5/40min/breaker-OPEN, image cfa9e89fa1, 16 min, $0.34, 0 Claude calls):** SIXTH LAYER FIXED — `Slice235 adaptive-diff: emitting 2b.1-diff` fired **14×** (on moonshotai/Kimi-K2.6 + deepseek-ai/DeepSeek-V4-Flash); my `Slice236 capability-aware selection` log fired **45×** routing IMMEDIATE→DW ops from baseline Qwen-397B → Kimi-K2.6; model selection inverted (elites Kimi 66 + deepseek-V4 45 = 111 vs Qwen 44; was Qwen 98/98); 63K-blob JSONDecodeError 1 (was 6). **BUT a SEVENTH LAYER is now the binding constraint on full GOAL closure: `tool_loop_deadline_exceeded`** — the Venom exploration tool loop (read_file/search_code/get_callers) blows the generation deadline on the heaviest multi-file roadmap GOAL ops (e.g. op targeting semantic_index.py 3246L + goal_inference.py), gets classified `live_transport:RuntimeError`, the STANDARD sentinel walks all DW models (each re-runs the same over-budget loop), then `fallback_tolerance=cascade_to_claude` cascades to the credit-dead Claude → `terminal_quota` → `deadline_exhausted_pre_fallback` (31) → cooldown→retry cycle. ORTHOGONAL to model selection (present on Qwen in the original run too; all models run the same tool loop) → diagnose-don't-fix-tired per operator. NET: ops whose tool loop FITS the budget complete the full pipeline end-to-end (`mark_gates_passed`→`candidate_passed_break`→`outcome:applied`); only heavy GOAL ops blow the deadline.

**BANKED (2026-06-13): Slices 225–236 MERGED to main via PR #69475 (merge sha 000cd084d3); origin/main carries the validated diff-emission path. Branch perf/slice-235-adaptive-diff pushed.**

**Slice 237 (op-weight-scaled convergence, commit d94ed4c92c on branch perf/slice-237-convergence-deadline) — the seventh-layer fix: ENGAGES but is NECESSARY-BUT-INSUFFICIENT for the heavy-GOAL capstone (honest result, NOT claimed as a win).** Root cause investigated (not guessed): the Slice-85 `_should_force_convergence` cumulative threshold (14) is STATIC + op-weight-blind; heavy ops (25-45s/round) blow the deadline before 14 calls accrue. Fix REUSES the trigger: `scale_convergence_threshold` (tool_executor.py) scales the threshold DOWN by op weight (`target_line_count / heavy_lines`, floored at min_calls=4), threaded via new `op_weight_lines` param on `ToolLoopCoordinator.run` wired at ALL 3 `_tool_loop.run(` sites (DoublewordProvider:3801 + PrimeProvider + ClaudeProvider), each reusing `providers._max_target_line_count` (same signal the gate reads → no drift). 19 TDD tests, +0 regressions. **LIVE-VERIFIED ENGAGING:** in-container `_max_target_line_count(semantic_index.py+goal_inference.py, /app)=3246 → scaled threshold=4`; `final-write nudge triggered (trigger=context_horizon, remaining=430.5s)` fires EARLY. **BUT the soak did NOT achieve the capstone:** over 19 min the counts climbed BACK to the s236 baseline (tool_loop_deadline_exceeded 6, deadline_exhausted 31), NO heavy multi-file GOAL reached state=applied (only the light requirements.txt op did, via a real unified diff), $0.18, 0 Claude calls. **TWO COMPOUNDING ADDITIONAL LAYERS block the capstone (diagnosed, NOT fixed — more than "one layer" remains):** (8) **cascade-to-dead-Claude** — `circuit_breaker_tripped:terminal_quota` ×28 dominant: on a DW `live_transport:RuntimeError` hiccup the STANDARD `fallback_tolerance=cascade_to_claude` routes to the credit-dead Claude → terminal_quota → `ForegroundCooldown` retry cycle (×26) → counts accumulate (this is the deferred sentinel/cascade sub-problem, now confirmed dominant). (9) **TestCoverageEnforcer scope amplification** — the heavy GOAL targets semantic_index.py+goal_inference.py which LACK test coverage, so the orchestrator INJECTS a "generate tests for 8 uncovered files" instruction → the op balloons into an 8-file test-gen task no exploration bound can fit in the deadline. Also: convergence fires early (430s) for some ops but LATE (23-58s) for others → late ones still blow the final write. **Slice 237b (commit bb6dfabc04, branch pushed) — the COMPLEMENT: scale the final-write RESERVE up by op weight.** Code-level investigation of why 237's threshold-scaling was insufficient found the missing half: `final_write_reserve_s` was op-weight-blind (`min(10s, 25% budget)` ≈ 10s). Firing convergence early is useless if only ~10s is held for the forced write — a heavy multi-file diff can't generate in 10s → the write blows the deadline OR the Slice-233 enforcement FINALIZEs patch-less (`remaining_s <= final_write_reserve_s`) → cascade. `scale_final_write_reserve` (tool_executor.py) scales reserve UP with weight (capped at `max_fraction`=0.5 of budget, env `JARVIS_TOOL_LOOP_FINAL_WRITE_RESERVE_MAX_FRACTION`); wired through `_build_budget_plan(op_weight_lines)`. For the 3246-line GOAL: reserve 10s→~41s. 27 TDD tests total, +0 regressions. **SOAK (4th, bb6dfabc04, $0.27, 0 Claude): both scalings DEMONSTRABLY reduce the seventh-layer per-attempt failure — at 11min deadline_exhausted=5 (was 31), tool_loop_deadline=2 (was 6). BUT over the run the totals CLIMB BACK (15min: deadline_exhausted=21, tool_loop_deadline=6, terminal_quota=35) and NO heavy GOAL applied.**

**ROOT CAUSE of the climb-back IDENTIFIED — the SENTINEL AMPLIFICATION is the multiplier (no longer deferrable; it's why counts don't reach zero):** a heavy op that blows the tool-loop deadline → RuntimeError classified `source=live_transport` → the sentinel WALKS EVERY DW model RE-RUNNING the same doomed over-budget loop (`source=live_transport` ×11 sentinel-failures vs only 3 genuine `live_transport:RuntimeError`) → cascade_to_claude → terminal_quota → ForegroundCooldown retry → repeat. So ONE heavy op generates ~3-4× (sentinel walk) × N (cooldown retries) failure events. The convergence+reserve fix lowers the PER-ATTEMPT cost but the sentinel re-multiplies the residual.

**MERGED (2026-06-13): Slices 237+237b → main via PR #69476 (f12650beaf); Slices 238+238b → main via PR #69477 (27f64ed07a). Each layer banked on its own merit.**

**Slice 238+238b (cascade-to-dead-Claude, layer 8) — CLEANLY CLOSED + VALIDATED.** Root cause: the cascade/fallback path read NO breaker state — it blindly called the configured Claude fallback (credit-dead → BadRequestError 400 → terminal_quota → cooldown cycle); the dead-Claude call reached Claude from EVERY `_call_fallback` caller, not just the sentinel `cascade_to_claude`. Fix (reuses the SAME read-only `_claude_breaker_open` the primary lane respects; no second quota check): (238) sentinel guard `should_cascade_to_claude` + suppress→Slice-180 immortal DW-retry; (238b) CENTRAL `_call_fallback` guard — `_fallback_is_claude()` (via provider_name, so non-Claude fallbacks unaffected) + breaker-OPEN → raise existing `fallback_skipped:claude_breaker_open` (Slice-19b, non-hibernation) instead of calling the dead lane. Gated `JARVIS_CASCADE_BREAKER_CONSULT_ENABLED` default-TRUE (failure-path-only; breaker-CLOSED byte-identical). 13 TDD, +0 regressions. **SOAK-VALIDATED under the WORST case (total DW transport outage + economically-dead Claude): terminal_quota=0 (was 60), BadRequestError=0 (was 23), deadline_exhausted=0, 32 suppressions + 50 clean fallback_skipped, $0.00 spend, 0 Claude calls — clean degrade, no poison cycle.** (applied=0 that run only because DW itself was transport-down — environmental; DW-healthy runs apply, per s237b.) LAYER 8 DONE.

**Slice 239 (adaptive test-sharding, layer 9) — MERGED to main via PR #69478 (20341841c9).** TestCoverageEnforcer no longer inlines "generate tests for N uncovered files" into the heavy primary patch; when budget is tight AND >2 files uncovered it compiles an ISOLATED test-coverage payload and emits it to the EXISTING UnifiedIntakeRouter WAL queue (source=test_coverage, background tier) so the primary patch graduates the Iron Gate clean + a later independent op fulfils coverage. `intelligence_hooks.py`: `detect_uncovered`/`should_decouple_test_gen` (route-scaled budget floor × >2 gate × heavy/multifile boosters, env-tunable, no hardcoded cap)/`build_test_coverage_envelope` (dedup-stable)/`test_sharding_enabled` default-TRUE; orchestrator pre-GENERATE seam decouple→ingest else fail-soft inline. Decoupled op flows FULL Iron Gate (no bypass). 22 TDD, +0 real regressions (cross-file batch failures = pre-existing flaky goal-inference singleton pollution, proven on clean tree). **NOT YET soak-validated — Phase-5 capstone soak (clean heavy-GOAL state=applied end-to-end, exhaustion=0) is the remaining proof.**

**CAPSTONE SOAK (9th, image 1209bd5e7b, all layers, --force-recreate, breaker-OPEN, 0 Claude calls, $0.00): CLEANEST EXHAUSTION PROFILE YET — tool_loop_deadline_exceeded=0, deadline_exhausted=0, terminal_quota=0 (all zero). L6 emitting 2b.1-diff x8, L8 cascade SUPPRESSED x32 (clean degrade). BUT the heavy-GOAL APPLY was NOT observed, for layered verify-first reasons: (1) WIRED-WRONG-SEAM — Slice 239 wired orchestrator._run_pipeline but the LIVE test-coverage enforcement is in phase_runners/plan_runner.py:753 (phase-runner refactor); the soak proved it (plan_runner's OLD backtick-count log fired x2, ZERO decouple decisions). FIXED in Slice 239b (commit 54c1e4c17c, MERGED PR #69479 2b5a2a6acb) — plan_runner.run() now carries the decouple wiring + a regression pin (test_live_plan_runner_seam_is_wired). (2) DW TRANSPORT-ROUGH WINDOW AGAIN (live_transport x12) → ops degrade cleanly, no apply (recurring environmental/infra factor across s238/capstone soaks — DW streaming reliability, NOT addressed by slices 235-239). (3) the roadmap's heavy GOAL targets only 2 uncovered files (semantic_index.py+goal_inference.py) → below the >2 decouple gate → inlines (correct per spec); observing L9 FIRE needs a >2-uncovered GOAL the roadmap doesn't contain. NET: failure modes ELIMINATED (exhaustion=0) + all seams now LIVE-wired, but the integrated 'heavy GOAL state=applied in one run' is STILL UNOBSERVED — gated by recurring DW transport instability (infra) + no >2-uncovered roadmap GOAL to exercise L9. BUILD-HYGIENE checked: .dockerignore DOES exclude __pycache__; the stale orchestrator.cpython-312.pyc was wrong-python-version noise (container=3.11.15), not a shadow.**

**Slice 240 Phase 2 (dynamic cost-vs-bandwidth shard trigger) — MERGED PR #69480 (f9645e6631).** Stripped the Slice-239 hardcoded ">2 uncovered" integer gate; should_decouple_test_gen now = pure inequality est_test_tokens > velocity_tok_s × remaining_s (the operator's Files×TokensPerFile > Velocity×Budget formula). estimate_test_gen_tokens derives cost from each uncovered file's ACTUAL line count (data-driven); ratios+velocity env-tunable (JARVIS_TEST_SHARD_{TOKENS_PER_LINE,TEST_MULTIPLIER,VELOCITY_TOK_S,DEFAULT_FILE_LINES}). Both seams (orchestrator + plan_runner) updated. 31 TDD, +0 regressions. **Slice 240 Phase 1 (byte-offset stream healer) NOT BUILT — verify-first verdict: architecturally impossible (DW = stateless OpenAI-compat /v1/chat/completions SSE; no session/offset; drop = re-request) AND the soaks' `live_transport` is predominantly RuntimeError('tool_loop_deadline_exceeded') MISCLASSIFIED into LIVE_TRANSPORT default bucket (candidate_generator.py:3694) — a budget exhaustion, not a socket drop. REAL transport levers (scoped follow-up, not faked): (T1) fix the misclassification so budget-timeout stops falsely degrading DW surface health + the live_transport sever counter (add a non-transport FailureSource e.g. GENERATION_TIMEOUT in topology_sentinel.py; surface-health-degrade + sever are keyed on ==LIVE_TRANSPORT so a new source auto-avoids them); (T2) graduate the EXISTING RT-vs-batch hedge dw_transport_hedge.py (JARVIS_DW_TRANSPORT_HEDGE_ENABLED default-FALSE) — the genuine storm-proof structural mechanism, already built+tested but dormant.**

**Slice 241 T1 (GENERATION_TIMEOUT classification) — MERGED PR #69481 (c934011161).** is_generation_timeout in dw_fault_taxonomy (markers tool_loop_deadline/max_rounds/round_budget/starved/generation_timeout — VERIFIED COMPLETE: tool_executor raises exactly tool_loop_deadline_exceeded + tool_loop_max_rounds_exceeded, both covered); FailureSource.GENERATION_TIMEOUT weight 0.0 + propagated-var (env-propagation contract caught the omission); classifier labels it FIRST so the ==LIVE_TRANSPORT degrade/sever consumers auto-skip. 11 TDD, +0 new regressions (the env-propagation-contract failure I introduced was caught + fixed; 9 pre-existing dispatch/preflight failures proven via stash). **T1 SOAK (10th, c934011161, $0.00, 0 Claude): poison metrics ZERO — terminal_quota=0, deadline_exhausted=0, BadRequestError(dead-Claude)=0 (Slice 238 solid). generation_timeout label fired (1x). BUT NO heavy GOAL applied + DW produced ZERO candidates this run ($0.00 = never billed; 4 ops generation_exhausted_unrepairable). The 12 live_transport + 10 severs were NOT tool_loop_deadline (that=0) — the underlying RuntimeError is `all_providers_exhausted:fallback_skipped:claude_breaker_open` (the CORRECT 'gave up cleanly' exhaustion wrapper) RE-CLASSIFIED as live_transport when it bubbles through PLAN-EXPLOIT DAG unit_error. ROOT this run: DW itself failed to generate for EVERY op (a DW-transport-rough window — the SAME recurring environmental factor across the s238/capstone/T1 soaks; when DW was healthy in the s237b run, elites served + ops progressed).**

**EYES-OPEN LAYER COUNT (honest, between here and a clean heavy-GOAL capstone): (L-A, the binding one, INFRA not code) DW's own generation reliability in the soak window — DW keeps having rough windows where every model fails before billing ($0.00); this is DW-side / Mac-Docker→DW network, NOT our governance logic. T2 (RT-vs-batch hedge) is the ONLY remaining CODE lever that might help (race batch when RT ruptures), but can't help if DW is wholesale down. (L-B, cosmetic T1-class, no poison) the exhaustion-wrapper RuntimeError (all_providers_exhausted:fallback_skipped) is re-misclassified as live_transport+sever when it bubbles through PLAN-EXPLOIT DAG units — inflates false-sever telemetry but causes no harm (238 absorbs it); fixable by extending the taxonomy to the DAG-unit seam, low value. (L-C, unobserved) the final heavy-GOAL apply itself — never observed because DW hasn't had a healthy window. NET: the governance pipeline (235-241T1) is correct + merged + has driven poison metrics to ZERO; the binding constraint is now DW infra reliability, not our code. Decision point: continue (T2 hedge — the one code lever for DW flakiness) OR plant the flag on the proven pipeline.**

**Slice 241 T2 (RT-vs-batch transport hedge graduated default-ON) — MERGED PR #69482 (cf2513b4075). FLAG PLANTED — arc SEALED 2026-06-14.** dw_transport_hedge.transport_hedge_enabled default FALSE→TRUE; the pre-built+pre-wired (_s189_transport_hedge_active→hedged_race) dual-stream arbitrage now active: race RT(fast) vs batch(stable), first-success wins + loser cancelled, RT-rupture swallowed→batch returned seamlessly (rupture-pivot), prefer_fast holds batch speculative so RT exploration clears the Iron Gate. 63 tests green (T2 + slice188/189/190/194/227); +0 regressions (updated slice189 flag-off test to set kill switch). Honest limit: cannot save a WHOLESALE DW outage (both arms fail). **FINAL SOAK (11th, cf2513b407, $0.00, 0 Claude): hedge ACTIVE + racing (14 races), but this window was a TOTAL DW OUTAGE — 46 DoublewordInfraError across BOTH RT+batch, all 14 races cleanly ABANDONED (RaceTriage hard_blockage=True→rotate). terminal_quota=0, deadline_exhausted=0, 0 Claude, $0.00 — clean degradation under total external failure. No capstone (DW wholesale down — exactly the hedge's named limit), per Phase-4 plan plant the flag regardless.**

**═══ O+V SOVEREIGN PIPELINE SEALED (slices 235→241, 18 PRs #69475-69482 merged) ═══ The governance layer is structurally complete + proven: every poison metric (tool_loop_deadline_exceeded, deadline_exhausted, terminal_quota, dead-Claude BadRequestError) driven to ZERO; DW-sovereign (0 Claude calls, $0 under outage); diff-emission + convergence + cascade-breaker + test-sharding + generation-timeout-label + dual-stream hedge all merged. The remaining gap to a heavy-GOAL state=applied is NOT governance code — it is DW's own external generation reliability (recurring wholesale-outage windows in the Mac-Docker soak; when DW was healthy in the s237b window, elites served + diffs emitted + ops progressed). Capstone observation is gated on a healthy DW window, not on more slices. ZERO further governance slices needed.**

**ARC STATUS: layers 6 (235/236 diff-emission), 7 (237/237b convergence), 8 (238/238b cascade), 9 (239+239b sharding, live-seam), 9-math (240P2 cost-model trigger), 10 (241T1 generation-timeout label), 11 (241T2 RT-vs-batch hedge) all FIXED + MERGED to main. Soak-VALIDATED: 6 (emitting 2b.1-diff 14×), 8 (terminal_quota→0). Soak-ENGAGED-but-insufficient-alone: 7. NOT-yet-soaked: 9. The integrated end-to-end heavy-GOAL capstone (a heavy multi-file GOAL applying its primary patch with tool_loop_deadline_exceeded + deadline_exhausted at zero) has NOT yet been observed in a single run — it is the remaining unproven item, pending the Slice 239 capstone soak.**

**(historical) Slices 237+237b (convergence threshold + reserve scaling) measurably reduced the seventh-layer deadline failures (5 vs 31 early) The capstone (clean heavy-GOAL state=applied, exhaustion→0) is now blocked by THREE separable layers, in priority order: (8) SENTINEL AMPLIFICATION — classify `tool_loop_deadline_exceeded` as an OP-LEVEL exploration-exhaustion (NOT a model-transport failure) so the sentinel does NOT re-walk every DW model re-running the doomed loop; reuse per-op state, don't add a parallel counter (this is the dominant multiplier — fix FIRST). (9) cascade-to-dead-Claude terminal_quota when breaker economic-OPEN (reuse Slice 146 machinery so STANDARD cascade_to_claude respects the OPEN breaker). (10) TestCoverageEnforcer amplifying a heavy multi-file GOAL into an 8-file test-gen task under one deadline. The convergence bound alone (237/237b) is necessary but NOT sufficient.** See [[project_slice131_cost_sovereign]].
