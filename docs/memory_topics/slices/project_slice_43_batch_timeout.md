---
title: Project Slice 43 Batch Timeout
modules: [backend/core/ouroboros/governance/candidate_generator.py, backend/core/ouroboros/governance/doubleword_provider.py]
status: historical
source: project_slice_43_batch_timeout.md
---

Slice 43 — Async Batch Timeout Alignment. **MERGED main 2026-05-28** PR #63983. Opus-reviewed READY TO MERGE. Builds on [[project_slice_42_aegis_multipart]] + [[project_slice_41_batch_aware_fleet]].

**v37 audit (bt-2026-05-28-235234) — what it proved + the new blocker:** Slice 42 fully worked — the **18KB AND 33KB** real-op uploads cleared Aegis (zero HTTP 400), `_create_batch` succeeded, **batch `7b7a7b52` submitted async + registered** in batch_future_registry. THREE firsts beyond v36 (fleet open + dispatch + batch routing already in v36; v37 added: upload clears + batch created). BUT capability bar still 0 APPLY — new blocker: the op **severed at the 30s `_PRIMARY_MAX_TIMEOUT_S` RT reflex cap** while batch_future_registry waits up to `_DW_MAX_WAIT_S=3600`. Architecture: provider.generate() decides RT-vs-batch INTERNALLY via `_slice36_should_force_batch`; candidate_generator wraps it in `wait_for(timeout=primary_budget)` where primary_budget was the 30s reflex cap → killed the async batch poll mid-flight (batch abandoned, candidate wasted).

**Phase 1 (the fix, `candidate_generator.py`):** `_compute_primary_budget` gains `force_batch` param. force_batch=True → `max(min(remaining, JARVIS_DW_BATCH_TIMEOUT_S=300.0), 0.0)` — bypasses BOTH the 30s reflex cap AND the fallback reserve (force-batch requires Claude disabled per Slice 36 → no fallback to reserve for, per Slice 20A). Wired at the GENERATE call site (`_call_primary`) via the SAME `_slice36_should_force_batch(context)` predicate the provider uses (lazy import, try/except→False fail-safe → legacy budget). **RT path BYTE-IDENTICAL** (Slice 28's 12 adaptive-budget tests green = proof). Env-tunable, no hardcoding.

**Phase 2 (ALREADY satisfied — no new code):** `_adaptive_poll_batch` (doubleword_provider.py:3358-3363) already breaks on `failed`/`expired`/`cancelled` → `return None` (clean failover, no hang). Pinned with an AST regression test instead of duplicating (leverage existing).

**Opus review:** ALIGNMENT sound (cg + provider call the same pure predicate on the same context → budget matches the transport actually taken; divergence needs sub-ms env/ledger flip). Fallback-bypass provably safe (Claude-disabled ⟹ no fallback). **Benign residual gap (follow-up, NOT a blocker): the PLAN primary path (candidate_generator ~3795) also severs batch at 30s** — but PLAN failures degrade gracefully (advisory, not candidate-producing); v37's wedge was GENERATE. Candidate for a future micro-slice if PLAN-via-batch matters.

**8 Slice 43 tests + 77 cross-arc regression green.**

**v38 capability run (LAUNCHED at merge, $1/600s, pure-DW + surface-health on, ledger primed):** tests whether the 33KB batch is now awaited to completion (up to 300s) → output_file retrieved → candidate parsed → **first APPLY?**. The blocker chain across the arc: v34 /v1/files 400 (Slice 42) → v36 fleet-empty (Slice 41) → v37 30s-sever (Slice 43). Per [[feedback_no_preresult_euphoria]]: record the artifact + the NEXT blocker if one surfaces (candidate parse/VALIDATE/GATE/APPLY each add steps; the user's Slice 43-Phase-3 extraction-engine idea becomes relevant IF the retrieved completion is markdown-wrapped vs clean JSON). [TO UPDATE with v38 outcome.]

**v38 OUTCOME (bt-2026-05-29-005010, 2026-05-28 ~18:01, $0):** DEEPEST the arc has reached — all 3 transport fixes engaged + held: active_batch_only=2 (fleet open, Slice 41), 33KB upload cleared Aegis no-400 (Slice 42), **Batch 0c4281c8 submitted async** (upload+_create_batch ✓), **primary_budget=220.0s** (Slice 43 batch budget engaged, 30s sever GONE). BUT still 0 APPLY — new blocker is a DIFFERENT domain: **control-plane loop starvation** (`[ControlPlaneStarvation] lag_ms=3791.6 — main asyncio loop is starved` → LoopDeadman → os._exit/SIGKILL; summary in_flight). The session was KILLED before batch 0c4281c8 could complete/retrieve. ("empty" in logs = red herring, a thresholds.yaml config read, NOT an empty batch.) The transport path is now fully unblocked (v34 /v1/files-400 → v36 fleet-empty → v37 30s-sever ALL cleared); the remaining blocker is **loop-health (§48 domain): Oracle indexing + sensor fan-out starving the event loop on the 16GB M1**, likely aggravated by the new 220s synchronous batch wait holding a worker slot → ties back to [[project_stage_1_6_park_spike]] (release slot during batch wait). NEXT investigation is loop-health, NOT transport. NO new slice started — operator-gated.

**Launch hygiene note:** v38 was launched via `nohup ... &` inside a run_in_background call → the launcher task completes immediately and the soak runs DETACHED (no clean completion notification). Monitor via the session dir (.ouroboros/sessions/bt-2026-05-28-*) — poll for summary.json. Future soaks: run the harness DIRECTLY under run_in_background (no nohup&) so the task tracks the full soak lifetime.
