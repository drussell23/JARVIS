---
title: Project Slice 49 External Sentinel
modules: [backend/core/resilience/file_watch_guard.py, backend/core/ouroboros/oracle.py, backend/core/ouroboros/governance/intake/sensors/opportunity_miner_sensor.py, backend/core/ouroboros/governance/external_watchdog.py, backend/core/ouroboros/battle_test/harness.py, backend/core/ouroboros/governance/phase_runners/classify_runner.py]
status: historical
source: project_slice_49_external_sentinel.md
---

**Slice 49 MERGED to main 2026-05-30 (PR #65634, ed277c072d).** Closes the v44 wedge + generalizes Slice 48. TDD, 17 new tests, 64-green consolidated, regression-clean. **All 3 runbook targets CORRECTED via verify-first** (FileWatchGuard already excluded the trees; Oracle._save_cache was already bounded; advise() already gates — same pattern that saved Slices 48):

**Phase 1 — scanner traversal exclusions (the REAL v44 FS-scan culprit):** FileWatchGuard ALREADY excluded .jarvis/.worktrees/swe_bench_pro (file_watch_guard.py:103-127) — runbook's target was a no-op. Storm came from PYTHON scanners walking+ast-parsing .jarvis/swe_bench_pro (437MB/26839 files): (a) `oracle.py EXCLUDE_PATTERNS += ".jarvis"` — substring match (`pattern in path_str`) meant Slice44's `.worktrees` NEVER matched `.jarvis/swe_bench_pro/worktrees`; (b) `opportunity_miner_sensor.py` replaced unprunable `rglob("*.py")` with `_iter_python_files_pruned` (os.walk + in-place dirs[:] prune over `_WALK_PRUNE_SEGMENTS`) — rglob stats the whole subtree before any filter, so post-walk `_is_production_code` was too late.

**Phase 2 — External Subprocess Watchdog (GIL-immune; the v44 fix):** new stdlib-only `external_watchdog.py`. v44 proved the in-process resource-zero thread CAN be GIL-starved (73min>40min cap, never fired). Detached child process SIGKILLs parent from OUTSIDE the interpreter — GIL-immune by construction (MORE isolated than resource-zero thread; honors Slice 47 invariant). Parent `beat()`s WALL ts (monotonic NOT cross-process comparable); child decides via pure `evaluate_kill`: **BUDGET kill wall-authoritative** (fires at cap regardless of GIL — the actual v44 fix), **STALENESS kill suspend-aware** (wall>>monotonic via `_suspend_detected` ⇒ host sleep ⇒ no false wedge — Slice 46 lesson applied cross-process). Wired in harness.py: `_arm_external_watchdog` after in-process hard-deadline thread (budget=cap+JARVIS_EXTERNAL_WATCHDOG_MARGIN_S 90s so in-process layers go first), `_beat_external_watchdog` each `_monitor_wall_clock` tick, `_disarm_external_watchdog` at clean shutdown start (BoundedShutdownWatchdog covers shutdown hangs). Master `JARVIS_EXTERNAL_WATCHDOG_ENABLED` default-on. **END-TO-END test spawns real victim `sleep` proc + confirms SIGKILL.** Wiring AST-pin (§8).

**Phase 3 — universal ingest stratification (soft, fleet-wide):** advise() ALREADY hard-gates EVERY op (classify_runner.py:396) — no TaskAdmissionController (doesn't exist). Applied Slice 48 soft penalty to `UnifiedIntakeRouter.ingest → _compute_priority` (`+stratification_penalty` to priority int; lower=higher priority so adding deprioritizes). `target_stratification.ingest_priority_penalty` (worst target file dominates, projected onto 0..scale, capped — never swamps base 1..99). Covers BOTH -miner AND -cau tracks. Test-gen escape `_is_test_generation_intent`. Master `JARVIS_INGEST_STRATIFICATION_ENABLED` default-on. Authority invariant: priority ordering ONLY.

**Pre-existing failures (NOT mine, verified byte-identical on clean main):** intake-router test_submit_called/test_dead_letter (GLS.submit mock), oracle_readiness libmalloc comment pin. Sandbox-only: slice32 process-pool PermissionError (pass 11/11 unsandboxed). Plus the standing merkle/_check_auto_ack_lane/resolve_envelope_repo_root drift from Slices 48.

**v45 soak NOT YET LAUNCHED — gated on DW transport recovery.** v44 showed DW generation transport DOWN (both Qwen `live_transport RuntimeError` → EXHAUSTION → L3 READ_ONLY_PLANNING). With Claude disabled, no GENERATE possible → any soak = guaranteed 0-APPLY until DW recovers. Slice 49 ensures the soak now runs CLEAN (no FS-scan wedge; external watchdog will fire the cap properly even if DW down) but cannot manufacture model capacity. Launch v45 only once DW `live_transport` recovers. See [[project_slice_48_v44_soak]] [[project_slice_47_v43_soak]]
