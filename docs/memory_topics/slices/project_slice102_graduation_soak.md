---
title: Project Slice102 Graduation Soak
modules: [tests/architecture/test_slice102_graduation_soak.py, tests/architecture/test_slice103_shadow_mode.py]
status: historical
source: project_slice102_graduation_soak.md
---

**Slice 102 — Autonomous Graduation Soak (Core Ignition proof). MERGED 2026-06-06 (PR #69299, squash f0daa18205, main synced 0/0).** TEST-ONLY (zero production code) — `tests/architecture/test_slice102_graduation_soak.py` (5 tests) proves the Slice 96 autonomous_graduation_engine EARNS + executes a master flip from telemetry, never violating Tiered Authority. Hermetic: all graduation+cognitive ledgers → tmp; ignited state asserted vs FAKE env dict; NO real state mutated; no live LLM. Phases: (1/2) synthetic epochs → clean telemetry (entropy engine governed-only urgency=low emission; autobiography CORPUS_CLEAN via injected clean git runners; belief telemetry recorded); (3a) EARNED — below clean-threshold nothing flips (no premature ignition); (3b) at threshold engine AUTO_FLIPs STANDARD cognitive substrates (entropy/sleep/rehearsal) + routes SAFETY cage flags (SemanticGuard, governance_boundary_gate) to APPROVAL_ADVISORY (structurally refused at override-ledger tier gate, never in auto-flip ledger); (4) apply_overrides_to_environ(fake_env) activates ONLY cognitive substrates, SAFETY stays dormant, operator env-precedence respected (zero-order doll). 31 passed (existing engine suite + soak agree). HONEST: proves MECHANISM only; real prod graduation still needs §41 wall-clock evidence + operator turning on engine master + registering flags in real cadence policy. Did NOT make that prod change (autonomy-expansion = separate operator decision).

**GRADUATION ENGINE MECHANICS (reference, verified 2026-06-06):**
- `autonomous_graduation_engine.evaluate_graduations(*, now_unix, ledger, registry, validate_all_fn) -> GraduationEngineReport` (.decisions/.auto_flipped/.advisories/.held). Candidates from `ledger.eligible_flags()` (is_eligible = clean>=required AND runner==0). 3 gates: A=eligibility, B=AST stability (`_drifted_source_files(validate_all_fn)`; validator RAISES→fail-closed HOLD-all; source_file in drifted set→HOLD AST_DRIFT), C=contract (`unified_graduation_dashboard.aggregate_dashboard`; None=vacuous/pass, False=HOLD CONTRACT_NOT_READY). READY→tier.
- **TIER**: `is_safety = category=="safety" OR category-unregistered/unknown OR (boundary_crossed AND category=="experimental")`. SAFETY→APPROVAL_ADVISORY, STANDARD→AUTO_FLIP. **Slice 101 substrate files live IN governance/ (boundary-crossed) → must register with NON-experimental known category (OBSERVABILITY) to get STANDARD.** Unregistered flag → `registry.get_spec`=None → HOLD UNKNOWN_SPEC (never reaches tier). Category enum: safety/timing/capacity/routing/observability/integration/experimental/tuning.
- `execute_graduations(report) -> ExecutionResult` (.recorded_overrides/.advisories_emitted). AUTO_FLIP→`graduation_override_ledger.record_graduation(decision)` (STRUCTURAL tier gate `_PERMITTED_TIER="standard"`, refuses non-STANDARD→returns False); APPROVAL_ADVISORY→advisory JSONL. Does NOT mutate os.environ live.
- `graduation_override_ledger`: `apply_overrides_to_environ(environ=None)` (boot applier; gated `apply_enabled()`=engine-master OR JARVIS_GRADUATION_OVERRIDE_APPLY_ENABLED; sets environ[flag]="true" for STANDARD authorized rows, RESPECTS env-precedence; read-side also drops non-STANDARD). `all_overrides()`, `ledger_path()`.
- **Env vars (redirect to tmp for hermetic tests):** JARVIS_AUTONOMOUS_GRADUATION_ENGINE_ENABLED, JARVIS_GRADUATION_OVERRIDE_APPLY_ENABLED, JARVIS_GRADUATION_OVERRIDE_LEDGER_PATH, JARVIS_GRADUATION_ADVISORY_LEDGER_PATH, JARVIS_GRADUATION_LEDGER_PATH, JARVIS_UNIFIED_GRADUATION_DASHBOARD_* . Canonical test pattern: `_StubLedger(eligible, progress_map)` + `FlagRegistry()`+`reg.register(FlagSpec(name,type=FlagType.BOOL,default,description,category,source_file))` + `validate_all_fn=lambda:()`.
- FlagSpec fields: name,type,default,description,category,source_file,example,since,posture_relevance,aliases.

**Slice 103 — Production Shadow Mode + PRD §51.11 sync. MERGED 2026-06-06 (PR #69300, squash 7012620a2b, main synced 0/0).** Deploys live evidence accrual without ceding the actuator. THE actuator boundary: `graduation_override_ledger.shadow_mode_enabled()` FAIL-CLOSED DEFAULT-TRUE (custom logic NOT _env_truthy: `raw is None → True`; `raw.strip().lower() not in ("0","false","no","off")` → unset OR GARBAGE stays shadowed; only explicit falsey un-shadows — my own test caught the _env_truthy garbage-hole). In shadow, `record_graduation` writes AUTO_FLIP receipt to SHADOW ledger (`JARVIS_GRADUATION_SHADOW_LEDGER_PATH` default .jarvis/graduation_shadow.jsonl) + returns False → override ledger (which apply_overrides reads) stays empty → NO OS-flip even w/ engine+applier on. STANDARD-only tier gate still dominates (SAFETY never actuates). `_append_record(record, *, path=None, kind="override")` refactored to take path/kind. sleep_daemon.run_sleep_cycle_once step 4: off-hot-path graduation shadow-eval gated on engine master (evaluate_graduations()+execute_graduations() compose REAL cadence/registry/validator). Existing actuation tests (engine suite + Slice 102 soak fixtures) explicitly set JARVIS_GRADUATION_SHADOW_MODE=false (they test the flip path). test_slice103_shadow_mode.py (6): default-shadow, garbage-shadow, receipts-accrue-no-override-no-flip, un-shadowed-flips, SAFETY-refused-even-unshadowed. 53 passed. **DEFERRED operational follow-up: register the 3 substrates (entropy/sleep/rehearsal) in CADENCE_POLICY (adaptation/graduation_ledger.py — has content-asserting tests in 7 files, risk) + FlagRegistry seed + wire a clean-session recorder (record_session per clean session) so evidence accrues — onboarding not engineering; record_session rejects unknown-flag so cadence registration is prerequisite.** PRD §51.11 added (v3.8): crosses out §51.3 "10 unwired"→Tier1 ~CLOSED (8 PRs); confirms cognitive_bus/CC/mesh wired; redefines frontier = 2 Tier-D hurdles: OS-Level Runtime Containment (§43 Arc5, ~8.8% bypass: seccomp/namespaces/Docker round APPLY/VERIFY) + Operator-Independent Recursion-Depth Counter (RRD §23.5, Order-2 prereq). **THE NEXT HIGHEST-PRIORITY TARGET per PRD = OS-Level Runtime Containment (§43 Arc 5) — the only unconditional containment guarantee + precondition for operator-out-of-loop.**

See [[project_slice101_architecture_audit]].
