---
title: Project Slice131 Cost Sovereign
modules: [backend/intelligence/context_awareness_intelligence.py, backend/intelligence/model_selector.py, backend/core/ouroboros/governance/route_decision_service.py]
status: merged
source: project_slice131_cost_sovereign.md
---

**Slice 131 — The Cost Sovereign Matrix.** Born from operator's "I'm burning Claude API money + DW is flaky + we have dead cost code not being used." A verify-first cost audit + 2026 LLM-cost research (FrugalGPT/RouteLLM cascade, semantic caching/GPTCache, prompt caching, batch 50%, context compaction → stacked 70-85% cut). Authorized as full 5-phase build, default-FALSE gated, TDD, PR-per-phase ("squash-merge each step"). Branch: `architecture/slice-131-cost-sovereign`.

**THE AUDIT (verified vs live tree):**
- **LIVE & working:** Claude prompt caching (`JARVIS_CLAUDE_PROMPT_CACHE_ENABLED` default-TRUE, wraps SYSTEM block only at `providers.py:6102-6112` `_apply_prompt_caching`); SemanticTriage (cheap 35B NO_OP filter, default-TRUE); context_compaction; cost_governor (per-route cost tracking).
- **DORMANT (built, OFF) — the burned money:** `provider_response_cache.py` (exact-match repo-state-keyed, fail-closed on git diff) + `economic_router.py` (cheap-tier failover on DW 402/429). Both were default-FALSE.
- **DEAD:** `autonomy/iteration_budget.py` (0 imports). `adaptation/per_order_mutation_budget.py` (dormant, JARVIS_ADAPTIVE_PER_ORDER_BUDGET_ENABLED default-FALSE, awaiting orchestrator wiring).
- **MISSING (verified absent):** Claude Batch API (`grep` = 0 hits — flat 50% for BG/SPEC); tools-block prompt caching (only system cached — BUT note render order tools→system means the system breakpoint ALREADY caches tools as prefix; 2a's real value = tools cache *independently* when the volatile StrategicDirection/momentum-digest system prompt changes); semantic (embedding near-match) caching — O+V already has `SemanticIndex`/fastembed bge-small infra to compose.
- **Correction:** audit suggested `claude-3-5-haiku` = RETIRED (Feb 2026). Current cheapest = `claude-haiku-4-5` ($1/$5).

**PHASE 1 — MERGED (PR #69340, main `640ad5bac5`, 2026-06-07).** The immediate money-saver, live default-on:
- GRADUATED `JARVIS_PROVIDER_RESPONSE_CACHE_ENABLED` → default-TRUE (`provider_response_cache.py:81` `response_cache_enabled`). Correctness-fail-closed (git diff re-keys), availability-fail-open.
- GRADUATED `JARVIS_ECONOMIC_ROUTER_ENABLED` → default-TRUE (`economic_router.py:46`). Only acts on provider-FAILURE path → cannot raise happy-path spend.
- NO-HARDCODE refactor: `economic_failover_model(policy_path=None)` resolves cheap Claude tier from `brain_selection_policy.yaml` → new top-level `cost_optimization.claude_low_cost_model: claude-haiku-4-5`. Env `JARVIS_ECONOMIC_FAILOVER_MODEL` override wins. `.py` embeds NO claude-* string (test-pinned). Verified live: router on, cache on, failover→claude-haiku-4-5. 36 tests green (8 new `test_slice131_cost_sovereign_p1.py` + updated `test_slice124_economic_router.py` for graduated defaults). Both hot-revertible `=false`.

**PHASE 2-4 — VERIFY-FIRST MAP (deep dive done 2026-06-07; NOT yet built — each is invasive hot-path or a new subsystem, deferred to fresh-budget sessions; rushing = the shortcut the operator forbade):**

- **P2a tool-block caching — PREMISE OVERTURNED, but a REAL (invasive) optimization remains.** O+V's Venom tools are NOT a native Anthropic `tools=[]` array — they're TEXT built by `_build_tool_section()` (`providers.py:1713`, called 2332/3131) and appended to the **volatile per-op lean prompt** (`parts.append(...)` after preloaded files + task), so the (stable) tool catalog is **currently NOT cached** (re-billed every op). `_CODEGEN_SYSTEM_PROMPT` (`providers.py:166`) is a stable constant cached via `_build_cached_system_blocks` (6086) — but it's the whole-file codegen instruction prompt (schema rules), NOT the tool catalog. **Root-cause fix (invasive, needs cache-telemetry verify):** restructure the lean-prompt assembly so stable content (tool catalog + schema rules + manifesto) sits in a cached PREFIX (its own cache_control breakpoint, ≤4 cap, prefix-order: system→messages) and volatile content (task, source snapshot, preloaded files) follows AFTER. NOT a one-line add. `ClaudeProvider.__init__` requires `api_key` (test with `ClaudeProvider("test-key")`).
- **P2b Claude Batch API:** async path for BACKGROUND/SPECULATIVE → `/v1/messages/batches` (flat 50% off; verified ZERO batch usage today). Master `JARVIS_BATCH_ROUTING_ENABLED` default-FALSE. Compose `should_skip_venom_for_route` (BG/SPEC already skip venom). Substantial async submit+poll+result build.
- **P3 semantic response cache:** new module composing existing `SemanticIndex`/bge-small — embed query intent, cosine-scan recent high-confidence cache, intercept near-match above threshold → $0. Master `JARVIS_SEMANTIC_CACHE_ENABLED` default-FALSE. MOST SELF-CONTAINED of the remaining (new module, not a hot-path refactor) — best next build.
- **P4 CAI "auto" router (operator's HEADLINE ask — like Cursor's auto-LLM).** COMPOSE existing (verified to exist, do NOT rebuild): `backend/intelligence/context_awareness_intelligence.py` `ContextAwarenessIntelligence.predict_intent(text)`/`analyze_context(data)`/`understand_with_reasoning(...)`; `backend/intelligence/model_selector.py` `IntelligentModelSelector.select_best_model(QueryContext)`/`select_with_fallback(...)` (singleton `get_model_selector()`); `backend/core/ouroboros/governance/route_decision_service.py` `RouteDecisionService.decide(...)`/`select(...)`/`record_cost(...)`. Wire CAI difficulty/intent signal → IntelligentModelSelector → cheapest-capable model first → economic_router failover. `UrgencyRouter` routes by urgency today, NOT difficulty — this adds the difficulty/contextual dimension. Applies to DW + all providers.

See [[project_slice127_blueprint_handoff]], [[project_slice125_126_aegis_credential]].
