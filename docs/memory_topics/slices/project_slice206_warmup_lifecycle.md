---
title: Project Slice206 Warmup Lifecycle
modules: []
status: historical
source: project_slice206_warmup_lifecycle.md
---

**Slice 206 — Boot-Warmup Lifecycle (MERGED #69447, main `60810ffd17`, 2026-06-10).** Addresses the organism's own #1 self-proposed priority (PR#69445): control-plane starvation.

**S207/208 FOLLOW-UPS:** S207 loop-guard FAILED LIVE (#69448 294d3549c9): redirect=0, freeze still 23s. 206+207 BOTH MISDIAGNOSED — build runs in a daemon THREAD, freeze is GIL contention (CPU-bound Python holds GIL → starves loop regardless of thread; to_thread CANNOT fix CPU loop-block, only PROCESS isolation). Real fix = dormant `JARVIS_COMPUTE_ISOLATION_ENABLED` (Slice 150 subprocess, tested; env-only, recreate no-rebuild): 23s→13.5s (~40%) but NOT eliminated — direct cold-start `idx.build()` caller (not via build_async isolation branch) still ~13.5s sync. STARVATION STILL OPEN. **S208 Epistemic Integrity (#69449 bb0aab05ef):** 3 SemanticGuardian deception patterns (self_signing_attempt/metric_counter_suppressed/chronos_continuity_laundering) HARD→APPROVAL_REQUIRED — makes my session-long refusal of metric-gaming (S202/205/206) STRUCTURAL. HONEST: pattern detectors, NOT un-gameable. DECLINED Phase-3 variance-invariant (incoherent: no cheap patch-simulation). semguard count 12→15; 11 tests.

**DIAGNOSIS-FIRST (the key lesson):** LoopSink telemetry (already existed — Slice 12K + `telemetry/loop_sink`) proved the 59 starvation events were MISLEADING — one-time boot-warmup blocking (semantic_index.build kind=sync ~10s cold-start-on-main; posture commit_ratios kind=async 28s SLOW-AWAITED-SUBPROCESS-not-loop-block; oracle init; intake ingest) conflated with steady-state. Steady-state = microseconds. Offload utils ALREADY EXIST: `semantic_index.build_async`(S33), `goal_inference.build_offloaded`(S149), `posture.build_bundle_async`(S33/52). Plan's assumed targets (registry/mmap/bandit writes) were WRONG — microsecond ops, refused to wrap them (to_thread overhead > op).

**REFRAMED plan's "starvation absolute zero" into HONEST reclassification (not gaming):** `init_lifecycle.py` BOOT_WARMUP→STEADY_STATE; during warmup, watchdog records `warmup_lag` (NEW registry counter, VISIBLE, INFO log `[ControlPlaneWarmup]`, skips heavy 12K snapshot) NOT `control_plane_starvation_events`; steady-state path unchanged (WARNING+snapshot). ANTI-GAMING: hard deadline `JARVIS_INIT_WARMUP_MAX_S` default 180s force-transitions to steady regardless of signal → warmup can't mask real starvation forever. Gated `JARVIS_INIT_LIFECYCLE_ENABLED` default-FALSE (OFF=steady always=byte-identical). GLS.start: enter BOOT_WARMUP + PROACTIVE off-loop pre-warm via `get_default_engine().build_offloaded(force=True)` + `get_default_index().build_async()` in gather, then `mark_warmup_complete` + `[InitializationGuard] WARMUP_COMPLETE`. KEY watchdog impl: warmup branch uses `continue` to skip steady WARNING+snapshot (loop is `while True`). 10 tests; 62 combined. Test-isolation gotcha: registry tests MUST set hermetic `JARVIS_OBSERVABILITY_REGISTRY_PATH` tmp (default .jarvis/.bin causes cross-test pollution). NOTE: gh merge hit transient HTTP 401 GraphQL blip — retry worked (auth valid, repo scope). See [[project-slice204-chronos-continuity]].
