---
title: Project Slice 44 Worktree Reaper Oracle
modules: []
status: historical
source: project_slice_44_worktree_reaper_oracle.md
---

Slice 44 — Watchdog/Index Exclusion & Dynamic Reaper Coverage. **MERGED main 2026-05-29** PR #65628 (squash tip 401b56544f). Opus-reviewed (caught a Critical, fixed + re-verified). Fixes the v38/v39 control-plane loop-starvation kill. Builds on [[project_slice_43_batch_timeout]].

**Diagnostic chain (5 wrong suspects eliminated before the real cause):** v38 LoopDeadman/SIGKILL → I hypothesized batch-slot starvation (park/resume); FALSIFIED (freeze at boot, before batch). v38 LoopSink top = commit_ratios 118s; FALSIFIED (already async via create_subprocess_exec — Slice 33; its blocked_ms is async-ELAPSED inflated by the loop being jammed elsewhere; git log -50 = 0.38s). Branch cleanup (12,913→362 local branches, 61 worktrees pruned, gc); did NOT fix it (not the ref count). v39 SidecarProfiler STUCK_FRAME = **`threading.py _wait_for_tstate_lock stuck_for_s=51.2` in `_shutdown_components → fs_event_bridge.stop → file_watch_guard._stop_watchdog → thread.join`, plus `oracle.py:2173 scan_dir`**. fs_event_bridge FALSIFIED (FileWatchGuard default exclude_top_level_dirs already excludes .worktrees/.ouroboros at scheduling). **ROOT CAUSE: 62 orphaned `.worktrees/ouroboros__auto__bt-*` (492,414 files / 13GB full repo checkouts) that Oracle._find_python_files indexed (EXCLUDE_PATTERNS lacked .worktrees) → recursive scan_dir held the GIL → loop starvation.** The 62 accumulated because reap_orphans only matched `unit-`.

**Fix 1 (reaper, worktree_manager.py):** `reap_orphans` matches a multi-prefix array via `_resolve_reap_prefixes(primary)` = primary + env `JARVIS_WORKTREE_REAP_PREFIXES` or default extras. **CRITICAL detail (Opus caught, e2e-pinned):** the debris's git BRANCH is `ouroboros/auto/bt-*` (SLASHES) while the DIR is `ouroboros__auto__bt-*` (`__`), AND the production boot reaper's worktree_base (`~/.jarvis/ouroboros/worktrees`) ≠ `<repo>/.worktrees` where debris lives → `lives_under_base`=False + (`__`-only prefix) `branch_matches`=False → my first attempt reaped 0 of 62 in prod. Fix: default extras = `("ouroboros/auto/bt-", "ouroboros__auto__bt-", "soak-")` — the SLASH branch form is load-bearing because `git worktree list --porcelain` is repo-global → `branch_matches` fires base-independently. Signature byte-compatible (boot callers auto-reap the extras). Real worktrees (feat/*) never match; soak- ≠ real `soak/p0-*` (which becomes dir `soak__p0-*`).

**Fix 2 (oracle.py):** added `.worktrees` + `.ouroboros` to `OracleConfig.EXCLUDE_PATTERNS` (substring match) → `_find_python_files.scan_dir` stops walking the 62× duplicate checkouts + the session tree. This is the EFFECTIVE GIL-scan fix (Oracle was the scanner).

**NOT touched: fs_event_bridge** — its FileWatchGuard default already excludes .worktrees/.ouroboros at the scheduling layer (verified). The runbook's fs_event_bridge target was the wrong module.

**10 Slice 44 tests** (resolver default/env/dedup; reaper prefix-matching incl. branch slash form; Oracle exclusion; **e2e: slash-branch worktree reaped base-independently + real feat/* worktree preserved**) + 10 worktree-isolation regression (legacy unit- intact) + oracle/dw_heavy_probe green. Env-tunable, no hardcoding; composes existing reaper rmtree machinery + Oracle exclude list.

**Manual git hygiene done this session (one-off, not committed — local repo surgery):** deleted 12,270 `fix/ci/*` + 274 `ouroboros/battle-test/*` + 64 `ouroboros/auto/*` local branches (12,913→362), pruned 61 worktrees, gc. (NOTE the on-disk 13GB `.worktrees` dirs were NOT removed by that prune — they're registered; the Slice 44 reaper removes them at boot.) packed-refs still ~64k from remote-tracking origin/fix/ci/* refs (off the boot hot path).

**v40 boot CRASHED** (`failed_bootstrap_timeout`) — 24 zombie aegis daemons from prior soaks saturated Aegis bootstrap. Killed them, relaunched as **v40b**.

**v40b OUTCOME (bt-2026-05-29-200702, 2026-05-29 ~13:07 PDT, $0.013, wall_clock_cap, in_flight):** THE DEEPEST the arc has EVER reached — two firsts:
1. **Slice 44 reaper PROVEN in prod: `reap_orphans: reaped 61 orphan` → worktrees 62→1, 13GB stripped at boot.** The fix works. Oracle no longer indexes .worktrees.
2. **`BACKGROUND: DW produced 1 candidates in 90.5s` (Qwen3.5-397B RT, $0.0045, 0 tool calls)** — the FIRST candidate ever generated end-to-end through DW in the v25→v40 arc. Transport path fully open.

**Still 0 APPLY. TWO new boundaries surfaced (record per [[feedback_no_preresult_euphoria]]):**
- **(A) Loop starvation REDUCED, not eliminated:** worst lag 54s (v39) → **36s** (v40b); reaper killed the dominant 492k-file Oracle scan but residual ~36s spikes remain. Worst-lag snapshot MainThread is in the bare event loop (`base_events._run_once`) with no single sync GIL holder — it's now **distributed GIL contention** from concurrent ThreadPool workers (asyncio_N, advisor-blast_0/1 in `cooperative_fs_io._read_text_worker`, etc.) starving main-loop scheduling. No single scan to exclude this time. Still 3 `os._exit`/SIGKILL.
- **(B) NEW HARD BLOCKER — exploration gate unsatisfiable on pure-DW:** the candidate was REJECTED at the Iron Gate (`exploration_insufficient: 0/1`). Tally: **1 candidate produced, 2 Iron Gate rejects, 3 ops got a tool loop (budget 355.9s, hard_rounds=10), but 0 read_file/search_code/get_callers calls EVER executed.** The DW Qwen models emit a patch directly with 0 tool calls even when the Venom loop is enabled → Iron Gate (correctly, by design — exploration-first) rejects every exploration-less patch → retry still 0 tool calls → loop. Root question for the next slice: is Qwen's tool-call wire-format not being parsed by Venom (tool calls made-but-unrecognized → counted as 0), OR does Qwen genuinely not choose to call tools? Either way the exploration-first contract is structurally unmeetable on the DW path right now.

**Blocker chain across the arc:** v34 /v1/files-400 (S42) → v36 fleet-empty (S41) → v37 30s-sever (S43) → v38/v39 loop-starvation (S44 — reaper proven, residual GIL contention remains) → **v40b: (A) residual GIL contention + (B) DW exploration-gate 0-tool-calls**. The blocker moved from transport → loop-health → into the GENERATE/tool-protocol layer for the first time. NO new slice started — operator-gated. Next investigation: bisect whether DW/Qwen tool calls are emitted-but-unparsed vs not-emitted (tool_executor parse path + a single live DW RT op with tool-loop tracing).
