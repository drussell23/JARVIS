---
title: Project Slice 69 Manifest Isolation
modules: [tests/governance/test_slice69_diff_isolation.py]
status: historical
source: project_slice_69_manifest_isolation.md
---

**Slice 69 — Sovereign Worktree Alignment & Manifest Diff Isolation — MERGED to main 2026-06-02** (`daec214575`, local squash-merge, not yet pushed/PR'd).

**What shipped:**
- **Phase 1 (regression pin, NO code change):** Runbook asked to edit `change_engine._redirect_target` to sniff the `swe_bench_pro` source channel and bind to `prepared.worktree_path`. REJECTED verify-first — Slice 64 (`c250939b40`, merged) ALREADY does this: `_swe_bench_write_root(ctx)` → `ChangeRequest.write_root` → `_effective_write_root` precedence #1. The runbook edit would duplicate working logic + push benchmark knowledge into the deliberately-generic write layer. Pinned existing behavior with a test instead; `_redirect_target` untouched.
- **Phase 2 (the real fix):** Folded the held Slice 68 working-tree capture (`git add -A && git diff --cached base_commit`) into `capture_produced_patch` (per_problem_harness.py) + added pure helper `_strip_test_patch_sections(diff, prepared.target_paths)`. Strips the harness's pre-applied `test_patch` footprint from the captured model patch. Without it, the container scorer's `git apply <test_patch> && git apply <model_patch>` (container_engine.py:280, step 2 has NO `|| true` guard) double-applies the test hunks → hard-fail → `scoring_error`. Exact repo-relative path match (never basename); never raises (fail-safe to unfiltered). Standalone Slice 68 branch ABANDONED (its capture over-captures without the filter — why it was held).
- **Tests:** `tests/governance/test_slice69_diff_isolation.py` (12 green). Module suite green except 4 pre-existing `test_swe_bench_pro_autoscore_wiring.py` taxonomy/`phase_a_disabled` fails (verified byte-identical on clean main).

**Scored soak bt-2026-06-02-232715 — NO score, $0.00, binding blocker = DW-down (UNCHANGED from pre-conversation state).** Preflight all green (started Docker daemon server 29.5.2; pulled correct image `jefzda/sweap-images:qutebrowser.qutebrowser-qutebrowser__qutebrowser-…b3c` 690MB amd64 — runbook's tag `qutebrowser__qutebrowser-…v2ef375ac` was WRONG; corrected truncated `SWEBP_INSTANCE_IDS` to full `…v2ef375ac784985212b1805e1d0431dc8f1b3c171`). Soak booted (oracle cache load 107s), DW reachable via Aegis (201 on /v1/files), entered path B (harness-inject GENERATE→APPLY→VERIFY, `SCORE_REJECT_TEST_MODS=true`). **Then: op-019e8acf…-cau route=standard complexity=complex — BOTH DW Qwen3.5-35B+397B `live_transport:RuntimeError`; Claude fallback starved to `budget=1.0s` (first_token=NEVER, bytes_received=0) after DW retries ate the GENERATE window → EXHAUSTION ×3 → ForegroundCooldown 60s park → Slice 49 ExternalWatchdog SIGKILL pid 6029 (heartbeat_stale, GIL-immune) → summary `in_flight`.** GENERATE never produced a candidate ⇒ loop never reached APPLY ⇒ **Slice 69's filter never exercised.** NOT a Slice 69 bug — platform correct; same blocker as [[project_swe_bench_closed_loop_gap]].

**NEXT (needs operator go — new work, not autonomous):** (1) wait DW transport recovery, OR (2) build the FailbackFSM fast-cascade-on-structural-DW-failure slice so Claude gets the FULL GENERATE budget (not 1.0s) the instant both DW models return `live_transport:RuntimeError` — this is the lever that would let the qutebrowser run actually score via Claude. The extreme `budget=1.0s` fallback starvation is the concrete symptom to fix. Slice 69 itself is done; nothing to re-run until DW recovers or the fallback-budget slice lands. Leftover empty `ouroboros/auto/bt-2026-06-02-232715` branch is harmless harness debris.
