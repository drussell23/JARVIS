---
title: Project Operation Timeline Rewind
modules: [docs/architecture/OUROBOROS_VENOM_PRD.md, backend/core/ouroboros/governance/operation_timeline.py, tests/governance/test_operation_timeline.py]
status: historical
source: project_operation_timeline_rewind.md
---

PRD **§42** (`docs/architecture/OUROBOROS_VENOM_PRD.md:9549`, written 2026-05-16) blueprints the causal **Operation Timeline read-model + RewindCoordinator** — closing **Gap #1 only** (checkpoint/rewind/time-travel) from the 2026-05-16 operator-commissioned reactive-vs-proactive critique. Gaps #2–#5 explicitly out of scope; §42 architects the read-model so Gap #5 (forensic morning-after) can later compose it, builds no Gap #5 code.

Root cause confirmed by exploration: rewind substrate exists but fragmented across 8 modules with no causal join layer; the one genuinely missing primitive is a durable cross-session `op_id ↔ signal ↔ diff ↔ commit_hash ↔ checkpoint ↔ outcome` index. `OpsDigestObserver` already emits every piece via `on_commit_succeeded`/`on_apply_succeeded`/`on_verify_completed` but scatters them into session-local summary.json.

3 layers: A = authority-free read-model subscribing the existing OpsDigestObserver protocol → `.jarvis/operation_timeline.jsonl` via canonical `flock_append_line` (schema `timeline.1`, ref prefix `r-N`); B = RewindCoordinator (operator-initiated ONLY) — preview delegates ReviewBranchManager, committed revert = `git revert --no-edit` (no history rewrite), in-flight revert composes existing pre-apply snapshot restore; C = `/timeline`/`/rewind`/`/revert` verbs + `r-N` in unified `/expand` + extend IDEObservabilityRouter + existing SSE broker. 7 AST pins (1–4 read-model authority-free, 5–7 irreversibility ban), 7 FlagRegistry seeds (masters default-FALSE per §33.1, safety preconditions default-TRUE).

**Status: §42.11 signed + Slice 1 LANDED 2026-05-16.** `backend/core/ouroboros/governance/operation_timeline.py` (new, additive — ZERO shared-module edits, zero behavior change) + `tests/governance/test_operation_timeline.py` (26 green: AST pins 1–4 + zero-behavior-change proof + causal-merge + bounded idempotent replay + fail-closed + 3 FlagRegistry seeds). `OperationTimeline` conforms to the OpsDigestObserver protocol shape (3 `on_*` methods) but is deliberately NOT wired into the single global `_OBSERVER` pointer — the composite fan-out seam (so SessionRecorder + timeline coexist) is the Slice-2 wiring concern (touching ops_digest_observer.py now would exceed read-model+spine). Schema `timeline.1` defined with all Slice-2 causal fields as `Optional=None` (forward-compatible — Slice 2 populates, never bumps schema). Master `JARVIS_OPERATION_TIMELINE_ENABLED` default-FALSE; auto-discovered by §33.3 walker (top-level governance pkg) — zero flag_registry_seed.py edits. AST pins 3+4 are AST-node based (not substring on source — docstring legitimately describes the ban). **Slice 2 LANDED (code complete + tested) 2026-05-16 — commit staged, pending operator Iron Gate authorization.** The repo's own pre-commit Iron Gate blocks commits without `JARVIS_AUTHORIZE_COMMIT_TOKEN` (operator-only crypto auth); operator chose to commit themselves. 11 files staged; drafted commit message at `.jarvis/_slice2_commit_msg.txt` (`git commit -F` it, then `git push origin main`). Slice 2 delivered: (1) additive `on_op_classified` on the OpsDigestObserver Protocol + `_CompositeOpsDigestObserver` fan-out (root fix for SessionRecorder coexistence — register/get/reset byte-identical) + add/remove fns; (2) orchestrator emits on_op_classified at the canonical INTENT seam (orchestrator.py:~2560, mirrors on_apply pattern — NO parallel op_id→envelope registry); (3) timeline read-only DiffArchive join fills diff_ref/file_paths/risk_tier (on_op_classified risk_tier wins); terminal_state+checkpoint_ref HONESTLY deferred (no canonical singleton source — refused to fabricate parallel state); (4) harness wires timeline into fan-out + replay_from_disk on boot; (5) GET /observability/timeline + EVENT_TYPE_OPERATION_TIMELINE_ROW SSE; (6) `r-N` /expand prefix + auto-discovered `/timeline` verb; (7) SessionRecorder explicit no-op on_op_classified. test_operation_timeline.py 39 green; regression clean (ide_observability+stream 90, last_session ×3+harness 66+, flag_registry_seed_truth 6). Sanctioned pin update in test_last_session_summary_v1_1a (deliberate protocol extension per that test's own contract). **NO rewind code (Slice 3). NO capability/graduation claim — masters default-FALSE pending §42.9 empirical ladder.** Slice 3 = RewindCoordinator (preview via ReviewBranchManager / `git revert` committed / snapshot-restore in-flight / blast-radius from file_paths join) + `/rewind` `/revert` + AST pins 5-7. See [[project-stage-1-6-park-spike]], [[feedback-no-preresult-euphoria]].
