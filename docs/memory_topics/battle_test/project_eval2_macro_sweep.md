---
title: Project Eval2 Macro Sweep
modules: [scripts/swe_bench_pro_report.py, scripts/utils/async_pr_janitor.py, scripts/swe_bench_pro_provider_spend.py]
status: merged
source: project_eval2_macro_sweep.md
---

**EVAL-2 (session bt-2026-06-03-094511) — O+V's first 6-instance SWE-Bench-Pro macro sweep.** Driven by `SWEBP_INSTANCE_IDS` (6 ids) through the Slice 75 multi-instance parser → 6 ProblemSpecs → `parallel_evaluate` → per-instance container scoring → durable `resolved`-stamped rows in `.jarvis/swe_bench_pro/results.jsonl`.

**Honest dual-metric result (always report BOTH, never one alone — résumé/UPenn-grade accuracy per [[feedback_no_preresult_euphoria]]):**
- **Strict: 1/6 = 16.7%** (every non-resolved counted)
- **Operational (fairly-attempted): 1/3 = 33.3%** (excludes 3 INCONCLUSIVE instances that never got a fair attempt)

Per-instance: qutebrowser ✅ RESOLVED (held-out pass); element-web (TS) + NodeBB-76c6 (JS) = genuine capability misses (patch produced, held-out tests failed); NodeBB-04998908 = `terminal_timeout` (DW-outage starvation, INCONCLUSIVE); ansible×2 = `prepare_failed` (`git clone rc=128`, INCONCLUSIVE). **NEVER cite the bare 1/6 — it conflates infra/provider exclusions with capability fails.**

**Root cause of the 2 ansible `rc=128` (verify-first corrected the runbook's "rate-limiting/socket-drop" hypothesis — 7th premise corrected this program):** the two ansible instances are the SAME repo at different commits; cold cache + 3 BG workers → concurrent same-repo clones raced into the same `swebp_cache` path → second hit `destination already exists and is not an empty directory`. NodeBB pair didn't race because its cache pre-existed (cache-hit, no fresh clone).

**Documented:** PRD §50.11 + Appendix D v3.9 (PR #69242, merged to main `0e530dde46`). soak.sh `MAX_WALL`/`IDLE_TIMEOUT` made env-overridable (same PR).

**Slice 76 (Resilient Ingress) — ALL 4 PHASES DONE + MERGED to main 2026-06-03 (PR #69243, commit `61ef1a80a9`).** Verify-first + TDD throughout; 355 blast-radius tests green (12 pre-existing `phase_a_disabled` taxonomy fails verified identical on clean main):
- **P1 resilient clone** (`per_problem_harness.py`): per-repo `asyncio.Lock` (`_repo_clone_locks`/`_clone_lock_for`) serializes same-repo cold clones + bounded purge-retry (`JARVIS_SWE_BENCH_PRO_CLONE_MAX_ATTEMPTS`=3, `..._CLONE_RETRY_BACKOFF_MS`=500). 7 tests.
- **P2 pre-flight DW gate** (`candidate_generator.py`): `dw_transport_degraded_preflight()` reads the EXISTING `dw_surface_health` ledger (NO new probe — verify-first improvement over runbook's 5s socket probe) for fresh `DIRECT_STREAMING TRANSPORT_DEGRADED` → cascades to Claude pre-semaphore. `JARVIS_DW_PREFLIGHT_GATE_ENABLED`+`..._FRESHNESS_S`=120 default-on, conservative+fail-open. Wired in `_dispatch_via_sentinel` before sentinel dispatch, gated on `cascade_to_claude`. 9 tests.
- **P3 dual-metric analytics** (`result_store.py`+`scripts/swe_bench_pro_report.py`): `EvaluationCategory` enum + `dual_metric_rates()`; report renders Strict vs Operational natively. Never flatters model (eval=unresolved→capability_miss). 10 tests.
- **P5 async PR janitor** (`scripts/utils/async_pr_janitor.py`): drains **67,744** (NOT ~400 — verify-first found true scale) `app/github-actions` bot PRs at ~1/s w/ adaptive backoff + checkpoint resume. Triple-gate (bot-author+bot-title+immutable 13-PR backstop) NEVER closes a human PR. LAUNCHED bg (PID 40979); ~488 closed, protected #36818/#253/#106 verified still OPEN; ~65h full drain, resumable via `.jarvis/pr_janitor_checkpoint.jsonl` (rerun w/ `GH_TOKEN=$(gh auth token) nohup python3 scripts/utils/async_pr_janitor.py &`). 14 tests.

**Slice 77 (Dynamic Transport Telemetry) — MERGED to main 2026-06-03 (PR #69244, `c9a25d2b57`).** Closes the P2 gap found in the 1st re-run: P2's `dw_surface_health` ledger was written ONLY by one-shot BOOT probes → stayed `direct_streaming=healthy` while live GENERATE failed `live_transport:RuntimeError` → P2 never fired. Fix: new `_note_dw_live_transport_degraded()` in candidate_generator records `DIRECT_STREAMING→TRANSPORT_DEGRADED` at the existing `failure_source is FailureSource.LIVE_TRANSPORT` choke point (after sentinel.report_failure, before Slice 73 sever) → next op's P2 gate fires. Self-healing via P2 freshness window. 6 TDD tests, 386 blast-radius green. **The exact 3 real ids (runbook's were FABRICATED again — verify-first caught it): `instance_ansible__ansible-be2c376ab87e3e872ca21697508f12c6909cf85a-vba6da65a0f3baefda7a058ebbd0a8dcafb8512f5`, `instance_ansible__ansible-c616e54a6e23fa5616a1d56d243f69576164ef9b-v1055803c3a812189a1133297f7f5468579283f86`, `instance_NodeBB__NodeBB-04998908ba6721d64eba79ae3b65a351dcfbc5b5-vnan`.** All 3 Docker images local; ProblemSpecs cached at `.jarvis/swe_bench_pro/cache/`.

**RE-RUN CONCLUSION (both attempts, 2026-06-03): clean N=6 BLOCKED on DW upstream recovery — NOT a code problem.** All Slice 76/77 infra VALIDATED in production: P1 fixed clone race (ansible now prepares+reaches GENERATE); P2+Slice77 reactive telemetry CONFIRMED working (ledger flipped `direct_streaming: healthy→transport_degraded` on 1st live failure; FailbackFSM went `FALLBACK_ACTIVE`). **Definitive root cause via FSM evidence: `FALLBACK_ACTIVE fsm_failure_mode=TIMEOUT` (6×) — DW hard-down (v44→v64 blocker) so Claude is the only lane, and Claude TIMES OUT generating the large multi-file patches these complex frontier instances (route=standard complexity=complex) need (streamed 159s before cap in run 1). 0 verdicts both runs, ~$0 spent (exhausts, doesn't bill).** Both soaks killed (futile under DW-down). **DECISION: cite existing §50.11 (1/3=33.3% fairly-attempted) as the honest resume-grade number — it's UNCHANGED + true; the 3 stay inconclusive for a documented upstream reason. HOLD clean re-run until DW confirmed back.** Lone remaining client lever = "Slice 78" longer GENERATE budget for complex swe_bench ops (would convert Claude-timeouts→fair capability-misses→clean N=6) BUT per logged "slice treadmill" lesson, NOT recommended vs waiting for DW. Real 3 ids + images cached (see above) for when DW recovers.

**DW transport CONFIRMED UP 2026-06-03 (direct out-of-band probes):** `Qwen3.5-35B-A3B-FP8` + `Qwen3.5-397B-A17B-FP8` (BARE names) return content via both non-streaming AND streaming SSE (~1-2s TTFT, finish=stop) with `reasoning_effort=none`. The `-dottxt` real-time variants are HTTP 403 entitlement-blocked. **So earlier "DW upstream-down" was WRONG** — DW serves fine; the soak's `live_transport:RuntimeError` is CLIENT-SIDE/misclassified. Agent diagnosis: it's a bare `RuntimeError("all_providers_exhausted")` defaulted to LIVE_TRANSPORT by the classifier — the REAL binding blocker is **BUDGET STARVATION** (Venom tool-loop needs 45s min_ttft_floor but ops drop to ~16s remaining; Claude works fine — first_token=1.5s, 43KB — but capped at ~174s budget on complex multi-file instances → CancelledError). NOT a transport break.

**Slice 78 MERGED to main 2026-06-03 (PR #69245, `7ef57de360`) + janitor self-heal (PR #69246, `63af707cd5`).** Dual-track, verify-first DISPROVED multiple runbook premises:
- **Track 1 Iron Gate test-only guard** (`patch_domain_guard.py` + orchestrator Gate 4.5): rejects candidates modifying ONLY test files → GENERATE_RETRY w/ corrective feedback (early catch vs SCORE_REJECT_TEST_MODS' post-hoc scoring reject). `JARVIS_PATCH_DOMAIN_GUARD_ENABLED` default-on, swe_bench-gated, naming-heuristic-only. 15 tests. Runbook's `autonomy/iron_gate.py` doesn't exist (gates in orchestrator); candidates are full_content not diffs (inspect paths).
- **Track 2 Aegis** (`forwarding.py`): runbook's "strip -dottxt" + "fix flush buffer-drop" BOTH DISPROVEN (no -dottxt transform in code; forward loop byte-faithful). Shipped only the 2 proven defects: `sock_read` env-tunable `JARVIS_AEGIS_UPSTREAM_SOCK_READ_S` (default UNCHANGED 30s — direct TTFT ~1-2s so not the confirmed cause) + caught the uncaught `iter_any` upstream timeout (was escaping forward_request's "never raises" → 0-byte EOF → misclassified live_transport) → clean UPSTREAM_UNREACHABLE. 6 tests. **HONEST: hardening + clean classification, NOT a confirmed unblock.**
- **Janitor self-heal**: crashed after 1976 closes on uncaught `ClientOSError Errno 54`; now catches transient `(ClientError,OSError,TimeoutError)` → backoff+retry. RUNNING again (PID 51918, ~2031+ closed, resumable via checkpoint).

**REAL NEXT LEVER for clean N=6 (not yet built): budget allocation** — these complex swe_bench ops are `route=standard` (~120-174s) but need more for Claude to finish a multi-file fix; the tool-loop 45s min_ttft_floor also starves them. A budget/route bump for complex swe_bench ops would convert the Claude-timeouts→fair capability-misses→clean N=6. DW being UP means it could ALSO just carry these if the misclassification→exhaustion cycle is fixed. **Existing §50.11 (1/3=33.3%) remains the honest citable number.**

**Slice 79 (adaptive temporal budget) MERGED to main 2026-06-03 (PR #69247, `b880e5654c`) — the budget lever.** Verify-first: the "complexity-adaptive envelope" engine ALREADY EXISTED dormant = `adaptive_gen_budget.py` (built 2026-05-16 for THIS exact ansible-killed-by-wait_for failure). Computes PayloadWeight from ctx geometry (text tokens + file_count), scales route-base `_gen_timeout` by `1+score` (bounded, saturated max_mult=6), wired at generate_runner.py:695 (highest seam → deadline + outer wait_for + tool-loop BudgetPlan all inherit coherently). Was just never graduated (default-FALSE). **Slice 79 = GRADUATE it (flip default→TRUE)** — NOT a new build (runbook's per_problem_harness analyzer would've duplicated). Floor=route-base (trivial ops byte-identical, zero regression), ceiling=wall-cap*0.5. Only heavy multi-file payloads (ansible/NodeBB big problem statements) gain runway → fixes tool-loop `remaining<45s` starvation + ~174s Claude cap. 17+125 tests green. Revert: `JARVIS_ADAPTIVE_GEN_BUDGET_ENABLED=false`.

**MACRO SWEEP #3 LAUNCHED 2026-06-03 (PID 58092, `.jarvis/eval2_macro_s79.log`):** main@b880e5654c, 6 REAL ids (runbook fabricated again), COST_CAP=25, MAX_WALL=5400, adaptive_budget=ON, `JARVIS_AEGIS_UPSTREAM_SOCK_READ_S=120` (Slice 78 — DW CONFIRMED UP via direct probe so give reasoning TTFT room thru Aegis). DW is healthy now + adaptive gives complex instances runway → best shot at clean N=6. Watch: do ansible/NodeBB now fairly-attempt (CAPABILITY_MISS/RESOLVED) vs still terminal_timeout? On completion: `swe_bench_pro_report.py` → append to PRD §50.11.

**Slice 80 (geometry stamp → adaptive budget) MERGED to main 2026-06-03 (PR #69248, `79ae0ed66c`).** Closes the Slice 79 gap (sweep #3: adaptive budget read ctx.target_files which is DELIBERATELY EMPTY for swe_bench → complex instances got ~1.25x not 3-6x → ~150s → Claude maxed 16k tokens/budget cut mid-patch). Fix: P1 `envelope_builder._build_evidence` stamps `swe_bench_geometry:{file_count,changed_lines}` (via compute_patch_geometry on gold_patch) into evidence at inject; P2 `adaptive_gen_budget.compute_payload_weight` falls back to stamp when target_files empty (`_ctx_static_geometry`) + new changed_lines term (`JARVIS_ADAPTIVE_GEN_LINES_REF`=200) for few-files/many-lines ansible profile. **Benchmark-SAFE (verify-first load-bearing): intake_evidence_json is consumed ONLY for repo-root+budget, NEVER in the model prompt (providers.py:4906/8838=resolve_envelope_repo_root) → budget-only metadata, no solution leak; target_files stays empty.** Effect verified: ansible 4f/2028L→220s→1320s(6x), NodeBB 11f/151L→~590s, qutebrowser 1f/23L→~300s efficient. 10+17+135 tests green.

**MACRO SWEEP #4 LAUNCHED (PID 61429, `.jarvis/eval2_macro_s80.log`):** main@79ae0ed66c, 6 REAL ids, COST_CAP=25, MAX_WALL=6000, Slice80 geometry-budget ON + aegis sock_read=120. Sweep #3 killed (0 verdicts, old weak signal). **THE shot: complex instances now get 3-6x runway + DW up. Watch: do ansible/NodeBB finally fairly-attempt (CAPABILITY_MISS/RESOLVED) vs terminal_timeout?** On completion → swe_bench_pro_report.py → PRD §50.11. NOTE persistent unknown: DW-via-Aegis still threw live_transport in sweep #3 despite sock_read=120 (NOT a 30s cut; Claude carries via fallback) — if Claude now has runway (Slice 80) it may not matter.

**Slice 81 (stale-detection calibration + Claude output budget) MERGED to main 2026-06-03 (PR #69251, `75d31d5964`).** Closes the sweep-#4 false-positive: Slice 80 gave a complex op budget=1287s but harness `_OP_STALE_THRESHOLD_S=600s` declared it stale → `stop_reason=stale_ops_detected` killed the session mid-flight + muddied ledger (overwrote qutebrowser resolved/pass→unresolved). **Verify-first corrected 2 of 3 runbook phases:** P1 pulse heartbeat ALREADY wired+called (`_emit_stream_activity`→`last_activity_at_utc`, Move-2-v4 max(transition,activity)) — bug was CALIBRATION not missing pulse (600s < adaptive budget; long NON-streaming phases like VERIFY pytest have no heartbeat). P3 teardown ALREADY bounded (Slice 48 join+abandon; tombstone = backstop working). Fix: stale-threshold default 600→1200 + soak.sh computes `OUROBOROS_OP_STALE_THRESHOLD_S` above per-op ceiling (caps `JARVIS_ADAPTIVE_GEN_WALL_FRACTION=0.40`→2400s, threshold 2700s for MAX_WALL=6000). P2 Claude base max_tokens 16384→32768 (large single-file rewrites truncated; dynamic scaling keys off empty target_files) + soak `JARVIS_CLAUDE_MAX_OUTPUT_TOKENS=32768`. 6+33 tests green.

**MACRO SWEEP #5 LAUNCHED (PID 65198, `.jarvis/eval2_macro_s81.log`):** main@75d31d5964, ALL fixes 76-81, 6 REAL ids, COST_CAP=25, MAX_WALL=6000, stale-threshold 2700s>2400s per-op ceiling. **Sweep #4 PROVED Slice 80 works (ansible budget=1287s, 0 exhaustion, 3 APPLY, ansible-be2c376 prepare_failed→resolved/fail=fair capability miss) but died early on stale_ops false-positive — now fixed.** THE shot: budget adequate + sessions survive long ops + bigger output. Watch: do all 6 reach scored verdicts now? On completion → swe_bench_pro_report.py → PRD §50.11. NOTE: report takes latest-per-id so this sweep's rows win (don't clear ledger).

**SWEEP #5 = FIRST COMPLETE N=6 (all fixes 76-81, Claude baseline) 2026-06-03.** Slice 81 calibration WORKED — session survived all 6 long generations (vs sweep #4 died at 0). **But 0/6 RESOLVED** (strict 0%, operational 0/4): ansible-be2c376 resolved/fail, NodeBB-76c6 resolved/fail (2 fair capability misses), element-web + qutebrowser unresolved/skipped (2 capability misses), ansible-c616e54a + NodeBB-04998908 terminal_timeout (2 infra). **qutebrowser REGRESSED from original resolved/pass→unresolved.** Cost: **Claude $4.12 / DW $0.0044 (99.9% Claude)** — quantified baseline via `scripts/swe_bench_pro_provider_spend.py` (reads summary.json cost_breakdown). Hypothesis: big Slice-80 budgets (1287s) + 32k max_tokens caused "cognitive wandering" / non-convergence (OR stochastic). Infra solid; capability regressed.

**DW COST RESEARCH (web + live probe 2026-06-03):** DW serves Claude-class agentic coders LIVE on account: GLM-5.1-FP8 (SWE-bench Pro 58.4%, BEATS Opus 4.6), Kimi-K2.6 (Pro 58.6%, Verified 80.2%), DeepSeek-V4-Pro (Verified 80.6%), DeepSeek-V4-Flash — all HTTP 200, 5-25× cheaper than Claude. Devstral-2-123B catalogued but 404 not-served (DW-side deploy needed; not blocking — others equal/better).

**Slice 82 (DW coder fleet eligibility) MERGED 2026-06-03 (PR #69253, `4a2c09b347`):** coders' ids lack `\d+B` token→parse_parameter_count None→COMPLEX min_params_b=30 gate REJECTED them→Claude always carried. Fix (metadata only, no hardcoded routing): `_KNOWN_MODEL_PARAMS_B` map in dw_catalog_client (GLM=754,Kimi=1000,DeepSeek-V4-Pro=1000,Flash=100) + pricing_oracle GLM/Kimi patterns + soak.sh `JARVIS_DW_TRUSTED_MODELS` attests them. Dynamic catalog→gate→rank→ledger unchanged. 70 tests green (3 provider_topology_v2 fail LOCAL-only = populated `.jarvis/dw_promotion_ledger.json` gitignored runtime contamination, fails on main too, clean CI passes).

**SWEEP #6 LAUNCHED (PID 69801, `.jarvis/eval2_sweep6_dwcoders.log`):** main@4a2c09b347, **Claude OFF (`JARVIS_PROVIDER_CLAUDE_DISABLED=true`)→DW coders carry GENERATE**, `JARVIS_ADAPTIVE_GEN_MAX_MULTIPLIER=3` (tighter "dense-coder" budget — verify-first: adaptive budget is pre-dispatch/provider-agnostic so NO clean provider-aware code hook; global-tighter IS the DW-coder profile when Claude off), 6 REAL ids, COST_CAP=15. **THE experiment: GLM-5.1/Kimi (beat Claude on SWE-bench Pro) cheaper AND higher-resolve vs sweep #5's 0/6 Claude baseline? Watch: do DW coders handle the Venom tool loop solo (no Claude fallback) + resolve rate + cost.** On completion → report + PRD §50.11 cross-model comparison.

The 6 exact instance ids are in PRD §50.11 / the harness_inject log. See [[project_first_container_scored_row]] (the N=1 qutebrowser RESOLVED that this scaled from).
