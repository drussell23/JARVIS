---
title: Project Cc Parity Benchmark
modules: [scripts/security/run_cc_parity_calibration.py]
status: merged
source: project_cc_parity_benchmark.md
---

The **Constitutional Classifiers analog** (§40.7.4 mapping #2 / PRD §41.11.2) — Derek's UPenn-admissions portfolio priority. Measures O+V's OWN threat model (adversarial code patches escaping the cage = AST phase-runner validator + SemanticGuardian), NOT a reproduction of Anthropic's 86%→4.4% chatbot number. Honest analog only; 4.4% borrowed as a comparison gate. Engine: `self_immunization.py`, master `JARVIS_ANTIVENOM_SELF_IMMUNIZATION_ENABLED` §33.1 default-FALSE. Harness: `scripts/security/run_cc_parity_calibration.py`. **All slices below MERGED to main 2026-06-05.**

**The $0/0 arc was CLIENT-SIDE, fully illuminated (never guessed):**
- **95a-2** (#69274): decouple LLM quota from deterministic fill — LLM was invoked with `n=max(0, per_pattern-len(candidates))`=0 because the 8 deterministic operators filled the small per_pattern → never called. Added `llm_per_seed`; honest `ConfigStarvationError` (call_attempts==0) vs `AdversarialTelemetryPanic` (auth) split.
- **95a-3** (#69275): THE Aegis unblock. (1) Aegis fail-closes EVERY budget cap (per-route + session + hourly) to 0.0 when unset → daemon denied every lease `cost_ceiling_exceeded`. `--bootstrap-aegis` set none. `_authorize_aegis_caps()` sets all three from `--budget-usd` BEFORE the daemon snapshots them; single-seam `_MUTATION_LEASE_ROUTE="IMMEDIATE"`. (2) `AegisLeaseError` was swallowed at 3 caller layers (per-seed except / `task.result()` driver / `summarize_campaign` drain) → re-raise before the broad except. Worktrees have NO `.env` (gitignored, lives in main repo) → daemon 503 `upstream_credential_unavailable`; source the key from main `.env` for runs.
- **95d** (#69277): async per-seed multi-call **batching engine** (`JARVIS_ANTIVENOM_BATCHING_ENABLED`, `_MAX_CALLS_PER_SEED`=12, both default-FALSE) breaks the `max_tokens`≈14/call ceiling; O(1) `_ast_structural_hash` dedup (ast.dump annotate_fields/include_attributes off → sha256); `EscapeCaptureSink` (`JARVIS_ANTIVENOM_ESCAPE_CAPTURE_ENABLED`) persists FULL escaped source (cache stores metadata only). Legacy single-call path byte-identical when off.

**Honest FAIL baselines (real Aegis spend, canary-attested — NOT a PASS):**
- v1 (95c #69276, `production_parity_ledger_v1.json`): 658 mutations, **6.42%** escape, $0.92. Single-call (418 LLM, capped). FAIL.
- v2 (95e #69278, `production_parity_ledger_v2.json`): 1566 mutations (budget-bound at $5.27; literal ≥3000≈$10), **8.91%** escape, FAIL. **Escape rate ROSE with scale — v1 underestimated; v2 is the trustworthy baseline.** Deterministic operators 0% escape; ALL escapes are novel LLM `identity` variants (~10.6% LLM-only). 135 full sources captured at `.jarvis/antivenom_escapes.jsonl` (gitignored).

**RIGOROUS classification (by reading the 135 captured sources): bypasses are a small set of evasion TECHNIQUES across 19 families — per-family hardcoding would be wrong:**
1. **indirection/aliasing** (defeats name-matching): `[os.system][0]()`, lambda/helper wrap, `setattr(self,'_x',eval)`, aliased-import `.loads`. Static-fixable (light taint).
2. **synonym constructs** (defeats node-type/name enumeration): comprehension vs for/if statement (`module_level_for_with_call`=32!), `inspect.getmembers` vs `dir`, `setattr(ctx,…)` vs `ctx.x=`, `__getattribute__` vs `getattr`. Static-fixable (walk comprehensions + expand sink sets).
3. **constant-in-variable**: banned name in a local. Static-fixable (constant propagation, Slice 86 folder precedent).
4. **runtime-primitive residual** (eval/deserialize of runtime-constructed values) = §43 Arc 5 OS-containment boundary. Small.

**v3 LOCKED baseline (95e2 #69279, `production_parity_ledger_v3.json`):** authorized $12 for literal ≥3000 → produced only 1472 mut spending $5.09 ($6.91/58% UNSPENT). **≥3000 is DIVERSITY/dedup-bound, NOT budget-bound** (model exhausts ~40-50 distinct variants/seed; 30-seed saturation ≈1470-1570 mut). Literal 3000 needs corpus expansion (~60-100 seeds), not $. Baseline LOCKED by CONVERGENCE: v2 8.91%/1566 + v3 8.73%/1472 → ~8.8%, FAIL.

**Phase-4 hardening (LIVE cage `meta/ast_phase_runner_validator.py`, zero-FP hard invariant):**
- **95f MERGED (#69280)** — synonym-construct hardening: Rule 7 +`__getattribute__`/`__getattr__`; new Rule 12 `inspect.{getmembers,getattr_static,getmro,get_annotations}` (bare-`inspect` only) → `INSPECT_INTROSPECTION` + `JARVIS_AST_VALIDATOR_BLOCK_INSPECT_INTROSPECTION` default-TRUE; Rule 4 +`setattr(ctx,...)`/`delattr(ctx,...)` (literal `ctx` first-arg only) + `is_ctx_mutation_block_enabled()`. NO broad comprehension/control-flow rule (FP trap). 44 tests incl 22-pattern clean-control matrix; reviewer's own FP probes all clean. Interception unit-PROVEN (inspect/getattribute/setattr-ctx now BLOCKED). Known UNDER-blocking (not FP): from-import + aliased-`inspect` → 95g. counts updated banned-attrs 9→11, reasons 13→14.
- **Phase-4 empirical descent measurement BLOCKED on Anthropic CREDIT EXHAUSTION** (2026-06-05): ~$11-12 session spend exhausted credits → model calls return `400 BadRequestError 'credit balance too low'` → `generated=0/$0` → honest `AdversarialTelemetryPanic` (no fake PASS). NOT a code bug. Re-run the batched+capture calibration to measure aggregate escape-rate descent ONCE CREDITS ADDED.
- **95g MERGED (#69281)** — indirection/aliasing taint: EXTENDED Rule 10 (`_resolves_to_banned_name` container-unwrap `[os.system][0]`/single-elem list/tuple + literal 0/-1; `_find_alias_defeat` from-import bindings + import-as module aliasing). DEFERRED lambda-wrap/setattr-keyed (interprocedural, FP-risk); helper-fn-wrap = documented runtime residual. **Adversarial review CAUGHT a default-on FP**: module-BLIND from-import bare-name fallback false-positived `from re import compile`/`from gzip import open`/`from io import open`/`from codecs import open` → FIX = module-specific dotted-match only (bare-name fallback removed), 4 idioms pinned in clean-control. 47 tests + 229 regression.
- **95h MERGED (#69282)** — constant-propagation: EXTENDED Rule 11 (`_collect_banned_const_names` = name bound to plain str-Constant whose value ∈ `_BANNED_INTROSPECTION_ATTRS` + `b=a` alias; `_check_taint_flow` check (c) flags `getattr/setattr/delattr(obj, <banned-const>)` at **args[1] key position only**). Honest: Rule 7 already catches inline/single-binding getattr; 95h's NEW coverage = setattr/delattr-with-banned-const + alias-propagated getattr. 47 tests + 276 regression; 19 reviewer FP probes clean (incl value-position trap `setattr(obj,"_safe","__class__")` correctly NOT flagged). reuses TAINT_EXPLOIT, no new reason/flag.

**HARDENING TRILOGY COMPLETE (95f+95g+95h): all 3 static-fixable evasion classes closed, ZERO clean-control FP.** Remaining residual = eval/deserialize of runtime-constructed values = §43 Arc 5 OS-containment (architectural, not static). **STILL PENDING: empirical post-hardening descent re-measurement (BLOCKED on Anthropic credit exhaustion — add credits then re-run batched+capture calibration to show ~8.8%→lower).** Optional corpus expansion (~60-100 seeds) for literal ≥3000.

**§40 ADVANCED FORWARD ROADMAP audit (2026-06-05):** §40 = 22-item ~10-week roadmap; THIS arc = ONLY item #3 (Antivenom self-immunization). ~14/22 have impl files from prior sessions (#1 governance_boundary_gate.py, #5 mcp_output_scanner, #7 counterfactual_rehearsal_mode, #8 adversarial_autobiography, #9 belief_revision_ledger, #10 sleep_consolidation_pass, #11 postmortem_fusion, #13 anti_fragility_budget, #15 second_order_doll_metric, #16 compositional_curiosity, #18 predictive_postmortem, #19 proof_carrier_transport, #21 cognitive_load_shedding). Genuinely OPEN: **#2 hash-cap signature gate, #14 mirror-self test, #4 M10 ArchitectureProposer graduation, #6 cross-session coherence harness** (+ #12/#20/#22 deferred/trigger-gated). "file exists" ≠ "graduated" — per-item graduation audit not yet run.

**OCA grant lapses on every branch switch** — re-grant per slice. **11 PRs merged this session** (95a-2/95a-3/95c/95d/95e/95e2/95f/95g/95h). See [[project_repo_git_pr_ops]], [[project_slice_83_and_dw_transport_rootcause]].
