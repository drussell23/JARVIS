---
title: Sovereign Epistemological Context Matrix — Phase 1 (2026-06-21, branch `fleet/sovereign-epistemic-context-matrix`, NOT merged — built via subagent-driven-development)
modules: [docs/superpowers/specs/2026-06-21-sovereign-epistemic-context-matrix-design.md, docs/superpowers/plans/2026-06-21-sovereign-epistemic-context-matrix-phase1.md, backend/core/ouroboros/governance/]
status: historical
source: project_epistemic_context_matrix.md
---

# Sovereign Epistemological Context Matrix — Phase 1 (2026-06-21, branch `fleet/sovereign-epistemic-context-matrix`, NOT merged — built via subagent-driven-development)

**Goal:** Cure the "seventh layer" — heavy multi-file GOAL `tool_loop_deadline_exceeded` (Venom re-explores the codebase from scratch every op, blows the generation deadline, cascades to dead Claude → `deadline_exhausted`). Fix = collapse exploration from O(N) blind search to directed semantic retrieval backed by long-term memory, without weakening the cage. The memory arc and the heavy-GOAL-execution arc are the SAME arc. Spec: `docs/superpowers/specs/2026-06-21-sovereign-epistemic-context-matrix-design.md`; plan: `docs/superpowers/plans/2026-06-21-sovereign-epistemic-context-matrix-phase1.md`.

**Reuse-first (the principal win):** ~70% already existed. DAG prefetch = `oracle.get_fused_neighborhood` (oracle.py:4163, already booted as a GLS daemon). Truth Guard = extend `state_drift.py` sha256 (this branch's own Slice 247/248 domain). ActionOutcomeMemory primitive existed dormant (Phase 2, deferred). Venom budgets + `per_round_observer` (tool_executor.py:6554) + final-write nudge existed. Net-new = 3 leaf modules + wiring.

**3 new modules (`backend/core/ouroboros/governance/`):**
- `epistemic_quarantine.py` — `atomic_read_and_hash` (single-read, no tear window), `QuarantineLedger(path, session_id)` session-scoped + fail-open + `.reconcile(root)`. The cross-process Truth-Guard barrier (`.jarvis/epistemic_quarantine.jsonl`).
- `epistemic_prefetch.py` — `build_prefetch_manifest` (heavy-gated, oracle fused neighborhood → bounded ranked hash-validated `PrefetchEntry` tuple, seed-byte budget); `_normalize_neighborhood` (flattens the REAL `FileNeighborhood` dataclass — category lists of `repo:rel` strings, NO per-file score — into ranked entries, leverage-ordered callers→CALL_GRAPH highest, `.name` matches Iron Gate categories); `is_heavy_goal`; `revalidate_manifest` (Truth-Guard at consume: re-hash vs live disk, drop+quarantine stale).
- `context_governor.py` — `InformationGainGovernor`: lightweight TF-cosine Δ (sub-ms, NO model call, deep embeds are coordinator's async concern), elastic budget (`_WARM/_COLD_BUDGET_SCALE` env-tunable), `build_governor_for(context)` factory (heavy-gated, fresh per attempt), `_IronGateFloorAdapter` (wraps `exploration_engine.evaluate_exploration`). **LR1** round-0 baseline = prefetch excerpts (no Δ inflation). **LR3** one-shot deadlock breaker → `deadlock_break` directive (missing Iron-Gate categories → canonical tools) then `deadlock_failed` (self-defends via `_deadlock_pending` even if coordinator skips `mark_deadlock_round_consumed`).

**Wiring:** op_context.py (frozen `prefetch_manifest` field, TYPE_CHECKING import); orchestrator.py (prefetch trigger after dep-summary block ~3493, truth-guard revalidate after GENERATE advance ~4024, `except GovernanceDeadlockError`→`terminal_reason_code=deadlock_override_failed` non-retry ~5726, `_resolve_session_id` via `get_active_session_id`); providers.py (both Prime+Claude `run()` sites: `prefetched_candidates=ctx.prefetch_manifest` + `governor=build_governor_for(context)`); tool_executor.py (`run(prefetched_candidates=, governor=)`, `_build_prefetch_seed_prefix` labelled HINT-not-Iron-Gate, governor verdict block in per_round_observer seat with LIVE `ExplorationLedger.from_records(records)`, `GovernanceDeadlockError`, action constants shared from context_governor); governed_loop_service.py stop() (LR2 reconcile via same `get_active_session_id`).

**Operator-ratified resolutions (binding):** LR1 prefetch=Δ round-0 baseline; LR2 quarantine session_id-bound (no infinite TTL) + terminal reconcile; LR3 deadlock breaker EXACTLY one round → fatal `deadlock_override_failed` (no loop, no GENERATE_RETRY).

**Gating (all fail-soft, default byte-identical when off):** `JARVIS_EPISTEMIC_PREFETCH_ENABLED` (true), `JARVIS_CONTEXT_GOVERNOR_ENABLED` (true), `JARVIS_GOVERNOR_DEADLOCK_BREAKER_ENABLED`, `JARVIS_ACTION_OUTCOME_MEMORY_ENABLED` (false, Phase 2). Governor + prefetch both **heavy-gated** (`is_heavy_goal` = multi-file OR blast_radius>threshold) → light ops byte-identical regardless of flags.

**Two integration bugs caught by the cross-cutting review (per-task tests passed in isolation but the chain was broken — the value of end-to-end review):** (1) `GovernanceDeadlockError` was swallowed by candidate_generator's 8 broad `except (Exception, CancelledError)` cascade sites → added `except GovernanceDeadlockError: raise` ahead of each → LR3 terminal now reachable. (2) GLS reconcile was dead code (`self._session_dir` never set on the service) → switched reader to `get_active_session_id()` matching the writer → LR2 reconcile now fires.

**Verification:** 65 arc tests + 141 regression-spine (exploration/state_drift/tool_loop/op_context) green. **Zero real regressions** (the 2 failing nodes — epistemic_budget AST-pin substring collision, dw_discovery quarantine test — proven pre-existing by removing the test file/stashing and re-running on BASE). Final review: SHIP WITH NOTES (all addressed: telemetry §9 added, classifier wired live; remaining caveat = prefetch skipped when CONTEXT_EXPANSION disabled — governor still builds cold).

**NOT yet validated:** the actual deadline cure (heavy multi-file GOAL `state=applied` + `tool_loop_deadline_exceeded`=0) is an OPERATOR-RUN live soak on GCP Spot 8-CPU — unit tests prove the machinery, not the outcome. Phase 2 (ActionOutcomeMemory → GENERATE prompt) deferred. M9/M10 cognitive arcs come AFTER heavy-GOAL execution proven.

**Ops:** Worktree `.claude/worktrees/discoroute` had a harness file-cache quirk mid-session (Edit reported success but writes didn't always flush) — subagents worked around via on-disk python/sed + git-verify; trust `git show`/`grep` over Read for these files. Also restored leaked uncommitted conflict-marker WIP in `battle_test/shutdown_watchdog.py` (origin/main HEAD is clean; the markers were stash leakage, not committed). See [[project_slice235_validation_sixth_layer]] (the seventh-layer deadline this cures), [[project_sovereign_exec_engine]] (host-bound execution / GCP venue).
