---
title: A1 Live-Fire Chaos Harness (2026-06-24, MERGED PR #69694) — the O+V base-autonomy proof rig
modules: [scripts/chaos_injector_ast.py, scripts/a1_graduation_auditor.py, scripts/a1_live_fire_chaos_harness.py]
status: merged
source: project_a1_chaos_harness.md
---

# A1 Live-Fire Chaos Harness (2026-06-24, MERGED PR #69694) — the O+V base-autonomy proof rig

**The RIGHT pivot** after honest pushback: the Command Node + Swarm were operator-directed OFF-roadmap pivots; PRD Priority #1 is the **Live-Fire Graduation Cadence / A1 gate** (composite autonomy stuck ~35%; the 12+ cognitive flags "never fired E2E with all on"). **Recon verdict: ~95% already built** — `a1_trace.py` (5 hops emit→ingest→dequeue→submit→accept, wired), OrangePR+AutoCommitter (autonomous PR), graduation_ledger CADENCE_POLICY (33 pinned flags + 3/5-clean cadence) + autonomous_graduation_engine, live_fire_graduation_soak.py + cron, sovereign_iac_hypervisor (Linux node) + deploy/ouroboros_linux_prod.env, the SSE stream, AND the self-detection path is REAL (`TestWatcher` autonomously polls pytest → `TestFailureSensor` → IntentEnvelope → UnifiedIntakeRouter; `fs_event_bridge` publishes fs.changed.*). **The real blocker is RUNNING the soak on Linux (never landed an autonomous PR; Mac can't converge), + a ~5% harness.** Built the 5%:
- **`scripts/chaos_injector_ast.py`** (`8bc6f9df`, 43 tests): EXTERNAL saboteur. Dynamic AST target acquisition — scans backend/ safe dirs (denylist-absolute: never the governance safety cage/tests/__init__/migrations), `_PurityVisitor` default-deny finds a PURE LEAF fn with a corresponding GREEN test, mutates ONE op/literal (binop/cmpop/boolop flip or return-literal), **VERIFIES the bug turns the test RED in a hermetic subprocess** (inert→revert+retry). Manifest-before-mutate (`.jarvis/chaos_manifest.json`) + byte-identical `--revert`. NO hardcoded target. Found 10 real in-repo candidates; proved inject→RED→revert live.
- **`scripts/a1_graduation_auditor.py`** (`c25b475403`, 31 tests): real-time SSE subscriber + log-tail for the 5 [A1Trace] hops. Audits the flag set DERIVED from CADENCE_POLICY (env-overridable `JARVIS_A1_AUDIT_FLAGS`, NOT hardcoded) — each must be observed-evaluated w/o false-positive rejection; **UNVERIFIABLE is an honest verdict (no fake-pass; strict→fail)**. **Absolute Intervention-Lock:** `GraduationFailedException` on ANY human gate (plan_pending/ask_human/CLARIFICATION/APPROVAL_REQUIRED) BEFORE the terminal Sovereign CRITICAL_ELEVATION merge (the only allowed human gate). `A1_DISPATCH_PROVEN` ⟺ all 5 criteria (5 hops in order + FSM CLASSIFY→applied + flag-audit pass + lock clean + autonomous PR); names FAILURE LOCUS on failure → `a1_verdict.json`.
- **`scripts/a1_live_fire_chaos_harness.py`** (`52b49e5d`, 17 tests): orchestrator — preflight→inject→soak(ouroboros_battle_test --production-soak --headless)→concurrent live-audit→verdict, **chaos-revert-ALWAYS (finally + SIGINT/SIGTERM handlers — repo never left broken)** + autopsy-on-failure (reuses sovereign_sentinel) + cost-bounded money-gated `--remote` (reuses IaC hypervisor + dead-man). Composed env sets the derived flags ON + ROADMAP_ORCHESTRATOR=1 + IDE_STREAM=1 + A1_TRACE=1 + the Linux overlay. `--dry-run-local --stub-soak` → A1_DISPATCH_PROVEN + tree reverted-clean (wiring PROVEN). `--remote` refuses w/o `--i-understand-this-spends-money` + prints ~$0.08/hr Spot estimate.

**STATUS: harness BUILT + wiring PROVEN locally; the actual PAID Linux soak run (the real O+V base-autonomy proof) is PENDING — needs operator go (real money, multi-hour, expected first-contact friction → localize the blocker, not necessarily a clean PR attempt-1).** Dev-Mac realities (not bugs): chaos injector can't confirm green-test pre-inject (pytest rc=5 collection), CADENCE_POLICY heavy import needs aiohttp (AST fallback) — both fine on the Linux node. Monitoring plan = close-watch run #1 ([A1Trace] + auditor live) + sovereign_sentinel autopsy for the black-box.

See [[project_a1_intake_dispatch]] (the A1 diagnosis: strategic-GOAL source default-OFF was the original gap), [[project_sovereign_swarm]] (the off-roadmap pivot before this), [[project_sovereign_sentinel_autopsy]] (the autopsy reuse).
