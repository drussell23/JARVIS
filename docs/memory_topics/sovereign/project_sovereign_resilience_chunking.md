---
title: Sovereign Resilience & Chunking Matrix (2026-06-21, branch `worktree-sovereign-resilience-chunking`, NOT yet merged)
modules: [scripts/ignite_sovereign_cloud_node.py]
status: historical
source: project_sovereign_resilience_chunking.md
---

# Sovereign Resilience & Chunking Matrix (2026-06-21, branch `worktree-sovereign-resilience-chunking`, NOT yet merged)

**What it is:** the two downstream FSM blockers that stopped a *dispatched* strategic GOAL (A1 proven in the wild 2026-06-22) from converging to a merged autonomous PR. Built after the live GCP soak diagnosis. Branch off `origin/main` a00d35243b. ~5700 lines, ~25 commits, 173 arc tests green, TWO Opus cross-cutting reviews CLEAN.

## The two matrices (all gated default-OFF, fail-soft, OFF byte-identical)
- **Matrix A — Sovereign Transport Circuit Breaker** (`transport_circuit_breaker.py`): per-lane CLOSED/OPEN/HALF_OPEN. Live soak root cause = batch-lane retrieval TIMEOUT while realtime healthy (1.7s); `batch_lane_healthy` computed-but-never-acted-on. Batch trips OPEN (adaptive failure-RATIO, no fixed N) → `select_lane` ROTATES to realtime; jittered recovery → HALF-OPEN async probe → CLOSED. **REUSE-FIRST (critical):** delegates recovery timing to existing `dw_transport_recovery.DWTransportRecovery` (which composes `circuit_breaker.full_jitter_delay` + Slice-242 prior) — NO reimplemented jitter/backoff; composes `dual_lane_breaker` (total-outage→terminal-pause) by disjointness (both lanes OPEN → don't rotate). The FIRST final-review caught my initial dup (SHA-256 jitter) → reworked. Wired into `candidate_generator` dispatch + a `governed_loop_service` probe daemon tick. Master `JARVIS_TRANSPORT_BREAKER_ENABLED`.
- **Matrix B — Adaptive Recursive Chunking:** on Advisor BLOCK (`orchestrator._decompose_block_or_legacy`), decompose into AST-symbol-scoped (`ast_symbol_scoper.py`, pure-AST + `ast.parse` integrity gate B1a) + test-first (`goal_decomposition_planner.decompose_for_block` — prepends a "generate PyTest suite" sub-goal the mutation `depends_on`) sub-goals, bounded by `adaptive_recursion_governor.py` (dynamic depth/fan-out from queue+loop-latency+MemoryPressureGate, NO literal cap), de-duped by `recursion_dedup.py` (hashlib sha256 + bounded FIFO ledger), re-injected via `multi_step_orchestrator.advance_orchestration`. Parent terminates `decomposed` not `failed`. Masters `JARVIS_RECURSIVE_CHUNKING_ENABLED` + the existing multi-step/decomposition chain. **Never weakens the OperationAdvisor — it SATISFIES the gate (builds its own test safety net).**
- **C1 root-cause (Call-Graph Risk Matrix, `call_graph_blast.py` + spec `2026-06-21-sovereign-call-graph-risk-matrix.md`):** the OperationAdvisor measured blast radius over the IMPORT graph (file importers) so a symbol-scoped sub-goal inherited the whole file's blast (50). Now: `symbol_blast_radius` queries `oracle.get_callers` (the EXISTING call graph) for the transitive callers of the scoped symbols. `SubGoal.scoped_symbols` propagates via envelope evidence → `ctx.intake_evidence_json` → `advise_async` auto-extracts. Gated `JARVIS_ADVISOR_CALLGRAPH_BLAST_ENABLED` default-false. Keystone wire: orchestrator passes `intake_evidence_json` to `advise_async`.

## Process lessons (load-bearing)
- **Final cross-cutting review caught 2 Critical + 2 Important dormant-wiring defects** (3rd time on this program): C1 AST scope computed-then-DISCARDED; C2 probe never called (OPEN never self-heals); I1 op silently lost when decompose emits 0 sub-goals; I2 breaker recorded ALL failure sources (spurious trips). ALL fixed. The review is non-negotiable — per-task green is NOT enough.
- **Parallel worktree-isolated subagents (Agent `isolation:"worktree"`) branch off origin/main (baseRef `fresh`), NOT the current feature branch.** So two tracks (Infra Surgeon C2/I1/I2/M2/M3 + Call-Graph Architect C1/M1) reproduced the arc leaves off main → integration reconciliation needed. Verified faithful reproduction via diff-drift checks (breaker had 0 dup-marker diffs) + the arc test suite as the arbiter (96 arc tests survived). Clean-take new files; combine-files reconciled; ran full suite.
- **Fan-out cap must NOT sever a dependency chain:** the seam's `subs[:max_fanout]` dropped the mutation (keeping only the test) under load → fixed to `_prereq + _dependent[:max_fanout]` (keep all prerequisites).
- **Pre-existing test reds** (verified vs origin/main): opp_miner_merkle/phase11/l2-fixture COLLECTION errors; worktree-aware AST pins (`resolve_envelope_repo_root` absent on main); `_BLAST_RADIUS_CACHE_SHARED` cross-test pollution (passes isolated). None from this arc.
- **Reuse-first is the #1 review axis here:** existing infra found + reused — `dw_transport_recovery`, `full_jitter_delay`, `dual_lane_breaker`, `oracle.get_callers`, `MemoryPressureGate`, `multi_step_orchestrator`, `goal_decomposition_planner`. The first review caught a parallel-health-tracker dup before it shipped.

## C2 SOAK #1 (2026-06-22, node deleted) — found a 3rd-level dormant wire + DW-noise lesson; chunking fix MERGED (#69646)
First armed C2 soak (node jarvis-ouroboros-soak-20260621-232249) ran ~40min: A1 dispatch + Advisor BLOCK fired correctly (17 BLOCKs) but **0 decomposed** — chunking dormant. **Root cause: `JARVIS_PHASE_RUNNER_CLASSIFY_EXTRACTED` defaults TRUE**, so the LIVE BLOCK is `phase_runners/classify_runner.py:490` (a verbatim transcription of the OLD inline block), which terminated `advisor_blocked` and NEVER called the chunking seam the fix wave wired into orchestrator.py's now-dead inline block. **BOTH code reviews missed it** (tests monkeypatch `orch._decompose_block_or_legacy` directly; neither exercised the default-on extracted runner). **FIX MERGED #69646:** classify_runner BLOCK now routes through `orch._decompose_block_or_legacy` + the C1 `intake_evidence_json` wire. LESSON: when wiring a seam at a phase boundary, wire it in the EXTRACTED phase_runner (the default-on live path), not just the legacy orchestrator inline block — grep ALL emit sites of the log string.
**DW citizenship (Meryem @ DW co-founder relayed via operator):** (1) reasoning_effort=none errors on gpt-oss-120b were YESTERDAY's pre-fix soaks; the floor fix (`_DEFAULT_DW_MODEL_MIN_EFFORT="deepseek-v4-pro:low,gpt-oss:low"`, doubleword_provider.py:169) is LIVE-CONFIRMED working — zero reasoning errors + zero upstream 4xx on the C2 node. Support: support@doubleword.ai. (2) The soak generated DW NOISE: heavy GOAL generation (3247-line context) times out on BOTH transports (`fast=RuntimeError, stable=DoublewordInfraError`) → 19-model exhaustion storms. The transport breaker is NOT the hero (both lanes fail on the HEAVY workload, not just batch) — **chunking is the real DW-load fix** (small symbol-scoped requests). DELETED the stuck/noisy node promptly (good-citizen). Open: the breaker doesn't trip on these because they surface as `fsm_exhausted` at candidate_generator (which I2 deliberately filters) — a real interaction; but moot if chunking shrinks requests so they don't time out. **Do NOT leave a stuck/non-converging soak running — it's pure DW noise.**

## Status: MERGED (#69644 matrices, #69645 C2-arm, #69646 classify-runner-chunking) + C2 SOAK #1 done (node deleted).
**All merged:** #69642/#69643 (A1) + #69644 (matrices) + #69645 (C2 arm — adds the 4 convergence masters to docker-compose.gcp.yml: `JARVIS_TRANSPORT_BREAKER_ENABLED`/`JARVIS_RECURSIVE_CHUNKING_ENABLED`/`JARVIS_ADVISOR_ORACLE_BLAST_ENABLED`/`JARVIS_ADVISOR_CALLGRAPH_BLAST_ENABLED`). **C2 node `jarvis-ouroboros-soak-20260621-232249` (e2-custom-8-16384 SPOT, us-central1-a, 6h) IGNITED 2026-06-22 ~06:23Z** via `scripts/ignite_sovereign_cloud_node.py` from main (funded DW key + HMAC + GH-token-via-Secret-Manager). Monitor: `gcloud compute ssh jarvis-ouroboros-soak-20260621-232249 --zone us-central1-a --project jarvis-473803 --command 'docker logs -f jarvis-sovereign-prod'`. **Convergence proof chain to watch:** `[A1Trace] accept` (dispatch) → `Advisor BLOCKED` (GOAL-001) → `BLOCK decomposed` (chunking, test-first) → `[TransportBreaker] OPEN/rotate` + `call-graph blast` → generation on realtime lane → `ouroboros/review` orange PR. Call-graph degrades to file-level if the oracle isn't built on the node (test-first still clears the coverage veto). Cleanup if needed: `gcloud compute instances delete jarvis-ouroboros-soak-20260621-232249 --zone us-central1-a --project jarvis-473803`.

## (history) code-complete, double-review-CLEAN. Proof = a future operator soak where GOAL-001 BLOCKs → decomposes → test-first + symbol-scoped sub-goal clears the (call-graph) Advisor → generation on the breaker-rotated realtime lane → orange PR. See [[project_a1_intake_dispatch]] (A1 dispatch proven in the wild, the upstream milestone).
