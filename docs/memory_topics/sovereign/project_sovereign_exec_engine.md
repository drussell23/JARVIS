---
title: Sovereign Execution Engine (2026-06-19, PR #69580, branch `fleet/sovereign-exec-engine`, NOT merged)
modules: [scripts/dw_ov_repair_demo.py, backend/core/ouroboros/governance/session_budget_authority.py, backend/core/ouroboros/governance/background_agent_pool.py, backend/core/ouroboros/governance/candidate_generator.py, circuit_breaker.py, backend/core/ouroboros/governance/intent/test_watcher.py, scripts/launch_linux_prod.sh]
status: merged
source: project_sovereign_exec_engine.md
---

# Sovereign Execution Engine (2026-06-19, PR #69580, branch `fleet/sovereign-exec-engine`, NOT merged)

Three live-evidence-driven fixes to the active Ouroboros control plane. Built TDD, gated, reversible, in worktree `.claude/worktrees/sovereign-exec-engine` off merged main (`8bf494052e`). 3 commits.

**1. Reservation TTL sweep + queue-drain release** (`session_budget_authority.py` + `background_agent_pool.py`). Live bug: every preflight REFUSED, `other_reserved=$0.50==whole cap`, cost $0. Root cause: foreground op reserves full cap; queued ops cancelled at `background_agent_pool.stop()` (~line 498) never hit the orchestrator terminal `release_reservation` hook → leak forever. Fix: lazy `sweep_stale_reservations(now=)` inside `acquire_reservation`+`check_preflight` (env `JARVIS_SBA_RESERVATION_TTL_S` default 600s — GENEROUS so live 130s+ tool-loops aren't swept mid-flight; NO background task = zero added event-loop load) + precise `release_reservation` at the drain-cancel site. **LIVE-VALIDATED:** $5 re-run → 0 refusals (was 14), op reached GENERATE. 4 tests (incl. the exact lock: A reserves full cap→B blocked→A stales→lazy sweep reclaims→B succeeds).

**2. Per-provider terminal_quota isolation** (`candidate_generator.py` ~line 6481). Live bug: Claude credits dead → `circuit_breaker_tripped:terminal_quota` → `all_providers_exhausted` even after `DW AUTARKY ENGAGED`. **CONFIRMED by controlled experiment: terminal_quota 5→0 when Claude disabled.** Root cause: Claude's economic 402 records on Claude's GLOBAL lane breaker (correct) AND was tripping the provider-NEUTRAL per-op breaker (`circuit_breaker.py` keyed by op_id, `_apply_trip_table` TERMINAL_QUOTA→OPEN_TERMINAL sticky) → poisons op for DW. Fix: confirmed Claude economic block downgrades ONLY the decision the per-op breaker sees → `type(decision).RETRY_TRANSIENT` (telemetry already published true decision; Claude global breaker still owns death). Genuine structural/config/timeout STILL trip (tested). Pure predicate `quota_isolation_skips_op_breaker(is_provider_economic_block, isolation_enabled)`. Gated `JARVIS_PROVIDER_QUOTA_ISOLATION_ENABLED` default-true. 4 tests. (NOT a classifier patch, NOT crypto-UUID over-eng — right-sized per-provider attribution.)

**3. Sovereign Remote Execution Bridge** (`deploy/ouroboros_linux_prod.env` + `scripts/launch_linux_prod.sh` + `test_watcher.py` env hook). pytest 30→180s (NEW env `JARVIS_INTENT_PYTEST_TIMEOUT_S` — TestWatcher `pytest_timeout_s` was a constructor default 30.0, now env-overridable), AST pool 1→`$(nproc)` (`JARVIS_AST_HELPER_POOL_MAX_WORKERS` default is **1**!), BG pool 3→cores/2, $10 cap + 60min wall. Linux-only launcher refuses macOS.

**LOAD-BEARING FINDING (proven 3× — do NOT relitigate):** `state=applied` is **host-bound, not code-bound** on the 16GB M1. Across three battle-test runs (initial $0.50, $5 budget-fixed, $5 Claude-disabled-autarky): budget fix eliminated refusals, contamination bypassed/fixed — yet STILL 0 applied because (a) nested event-loop **starvation** (35–48 on-loop blocks of 900–1245ms on intake.ingest/posture), (b) TestWatcher pytest **SIGKILLed at 30s** → isolated seed defect (`add_two` a-b bug) NEVER detected, (c) DW autarky **generation_exhausted** (`dw_heavy_probe_loop`, even as sole provider). First blood requires the Linux host the Bridge provisions. **Don't chase state=applied from a nested Mac session again.** Battle-test from worktree: real log goes to the `--headless` stdout redirect, NOT `<session>/debug.log` (which is empty under the worktree path-split) — grep the redirect file.

**DW-PRIMARY O+V PROVEN TO state=applied (2026-06-20, PR #69582 `scripts/dw_ov_repair_demo.py`):** DeepSeek-V4-Pro drove the FULL O+V cycle (GENERATE→VALIDATE/ast→APPLY→VERIFY/pytest→state=applied) on a real defect (`return a-b`→`a+b`), 2.2s, $0.00009, pure DW autarky (no Claude/J-Prime), ON the M1. KEY REFRAME: the host wall is ONLY the full autonomous loop (16 sensors+posture+oracle+dream starving one event loop) — NOT DW's generative capability. DW IS a working O+V primary today; only the sensor-swarm that FEEDS it autonomously needs the Linux orchestrator (PR #69581 merged). To prove DW-O+V locally, bypass the 6-layer battle-test stack and exercise the core cycle directly (single generation doesn't starve). PRs merged this session to main (03e72da7f7+): #69579 fleet evaluator, #69580 exec engine, #69581 cloud orchestrator. #69582 (demo) open.

Also: Claude API credits are DEPLETED (terminal_quota / "credit balance too low" / breaker OPEN) — Tier-1 fallback dead until topped up; DW is funded (sha256:6c4bf566). Builds on [[project_sovereign_fleet_evaluator]] (merged PR #69579, same session).
