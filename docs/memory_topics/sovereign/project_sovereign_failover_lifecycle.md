---
title: Sovereign Provider Failover Lifecycle + Recovery Forecaster (2026-06-23)
modules: [scripts/bake_jprime_golden_image.py, scripts/chaos_injector.py, scripts/adversarial_cognitive_soak.py, backend/run_server.py, backend/core/ouroboros/governance/outage_ledger.py, backend/core/ouroboros/governance/recovery_forecaster.py, backend/core/ouroboros/governance/recovery_throttle.py, backend/core/ouroboros/governance/failover_deadman.py, backend/core/ouroboros/governance/sovereign_self_termination.py, backend/core/ouroboros/governance/failover_lifecycle.py, backend/core/ouroboros/governance/dpo_synthesizer.py, rsi_training_hypervisor.py]
status: merged
source: project_sovereign_failover_lifecycle.md
---

# Sovereign Provider Failover Lifecycle + Recovery Forecaster (2026-06-23)

**The arc:** when DW collapses (and Claude is depleted), **GCP J-Prime — the self-hosted Mind — awakens from a golden-image snapshot, carries code generation through the outage, then tears itself down the instant DW is sustainably healthy.** Cost bounded to the outage window. A recovery forecaster (EWMA + live velocity gradient — NOT heavy ML; sparse data) paces the awaken/probe/handback. Spec: `docs/superpowers/specs/2026-06-23-sovereign-provider-failover-lifecycle.md`. Branch `worktree-sovereign-failover-lifecycle`.

**Trinity reality (verified by reading the repos, NOT inferring):** Body=JARVIS (this repo). **Mind = J-Prime** (`~/Documents/repos/jarvis-prime`): 11 self-hosted GGUF specialists, **code→Qwen2.5-Coder-7B (70.4% HumanEval)**, CPU-only (`e2-highmem-4`, llama.cpp / `run_server.py` OpenAI-compat :8000), golden-image boot. **Nerves = Reactor-Core** (`~/Documents/repos/reactor-core`): **model training/fine-tuning/experience-collection/deployment** (the RSI flywheel — NOT a "sandbox/Soul"; the PRD §24 framing is stale). I had MISdescribed reactor-core as Soul/sandbox from the stale PRD — corrected by reading its README.

**Cost model (the operator's #1 concern — solved):** the old "Invincible Node" stays 24/7 (~$100/mo) — that's the bill the operator disabled. The intelligent cost system ALREADY EXISTS + default-on (`gcp_vm_manager`: `JPRIME_IDLE_STOP_ENABLED`, `IntelligentGCPOptimizer` $5/day `CostBudget`, idle-stop halts billing + `ensure_static_vm_ready` ~87s restart). Locked **cost-optimal last-resort tier**: code-only Qwen2.5-Coder-7B on **e2-highmem-2 (16GB)**, **delete-to-snapshot dormancy (~$0.50/mo, image-only)**, **Spot-first→on-demand-fallback** for the active burst. Cryo-trigger blip-skip: `AWAKEN iff is_global_outage ∧ R_recovery > C_coldstart·margin` → never wake J-Prime for a flicker. Net ~$1-3/mo vs ~$100. GCP project was 100% clean at start ($0 accruing; golden image had to be baked from scratch).

**Three advanced layers (in spec):** (1) **Dynamic Polling Backoff** — `probe_interval(t,p50,p90)` decelerates far below p50, dense at p50, exponential backoff past p90 (reuse `full_jitter_delay`). (2) **Trinity Telemetry Bridge** — async fire-and-forget `asyncio.create_task` publish of outage metadata → `telemetry_ingestor`; live forecast reads ONLY the local ledger (Reactor offline = invisible). (3) **Cryo-Trigger** — forecast dictates the awaken threshold, synthesized with delete-to-snapshot. **Load-bearing invariant: observed-gated authority** — forecast only PACES; awaken-ready + handback are observed-probe-gated; a wrong forecast = bounded cost wobble, never a correctness/op-loss break. Reactive floor works with zero forecasting.

## Phase 1 — MERGED PR #69673 (2026-06-23)
- `scripts/bake_jprime_golden_image.py` — autonomous code-only bake: provision e2-highmem-2 on-demand → startup-script installs Ollama + `ollama pull qwen2.5-coder:7b` → **validation lock** (real code-gen test, never snapshot a broken image) → `images create` golden image → delete-to-snapshot (no orphan billing). Hardened with **abort-autopsy + early-fail + live-progress** (Sentinel autopsy principle). `--dry-run` default; `--execute` real. 20 tests.
- `outage_ledger.py` — durable `.jarvis/outage_ledger.jsonl` ring (forecaster reads it) + async fail-soft TrinityEventBus export (`publish(persist=False)`, 5s-bounded). Wired into `candidate_generator` AFTER `quarantine_op` seals the op + `try/except`-wrapped (cannot touch op-never-lost) + at `record_sweep(success=True)` recovery. OFF byte-identical. 10 tests.
- **GOLDEN IMAGE SECURED:** `jarvis-prime-coder-golden-20260623` (family `jarvis-prime-coder`, READY). Validation PASS — qwen2.5-coder:7b generated correct `is_even(n)`. Dormant: image-only, no VM/disk.
- **AWAKEN one-liner** (Phase 3 uses): `gcloud compute instances create jarvis-prime-failover --image-family=jarvis-prime-coder --provisioning-model=SPOT --instance-termination-action=DELETE ...`

**THE bake root-cause (load-bearing lesson):** run 1 aborted BLIND (no diagnosis) → added the abort-autopsy → run 2's autopsy caught it: **`panic: $HOME is not defined`** — GCP metadata startup-scripts run as root with HOME unset; Ollama (Go) panics in `envconfig.Models()` at CLI AND server startup (NOT disk/network — 24G free). Fix: `export HOME=/root` before any ollama call + robust `/run/systemd/system` detection, regression-guarded. Run 3 succeeded. **LESSON: a bake/remote-op tool MUST capture an autopsy before teardown — never abort blind; the autopsy paid off on its first real failure.**

## Phases 2+3 — MERGED PR #69674 (2026-06-23, Opus review CLEAN no-Critical/no-High)
- **Phase 2** (`recovery_forecaster.py` + `recovery_throttle.py`): EWMA-MTTR + percentile + within-outage velocity gradient. **Data Poverty Override** — N<5 real CLOSED outages → `LOW_CONFIDENCE`; the throttle then IGNORES the EWMA + returns the `JARVIS_SAFE_POLLING_INTERVAL_S` (60s) safe interval (no hallucinated intervals on N=1; the override is the FIRST gate, before any p50/p90 math).
- **Phase 3a** (`failover_deadman.py`): Dead-Man's Switch startup-script — node self-deletes via metadata-SA-token Compute REST DELETE (mirrors `sovereign_self_termination.py`, NO gcloud on node) if `:11434` idle > `JARVIS_DEADMAN_IDLE_TIMEOUT_S` (1800s). Boot-grace (no premature kill), dual-signal idle (journalctl /api/ + heartbeat file), DECOUPLED (fires even if the Body dies). Default-ON.
- **Phase 3b** (`failover_lifecycle.py`): FSM DORMANT→AWAKENING→SERVING→HANDBACK. Cryo-trigger `AWAKEN iff is_global_outage ∧ R > C*margin` (C=`JARVIS_JPRIME_COLDSTART_S` 180, margin 1.5; blip-skip R<C; LOW_CONFIDENCE→reactive-floor `JARVIS_OUTAGE_CONFIRM_S` window). Observed-gated handback (`window_full` + `success_rate>=0.6` held `JARVIS_RECOVERY_HYSTERESIS_CYCLES`=2 consecutive + min-uptime). **AWAKENING-timeout** self-heal (proactive teardown if node never ready, don't lean on the deadman). Awaken sets `--scopes=cloud-platform` + `--instance-termination-action=DELETE` + injects the deadman script. Gated `JARVIS_FAILOVER_LIFECYCLE_ENABLED` default **FALSE**.
- **Phase 3c** (DAG re-entry): when `is_jprime_serving()`, dispatch routes to LocalPrimeClient(base_url=jprime_endpoint) at the top of `_dispatch_via_sentinel` (bypass DW); `drain_cryo_dlq` replays the sealed ops on the SERVING transition. `window_full` public predicate added to provider_quarantine (swapped T3's private `_get_window` reach).
- **Review-proven:** (A) OFF byte-identical (0 deletions; `lifecycle_enabled()` short-circuits before the controller is consulted); (B) no op-loss (local-route error→DW fall-through, DLQ entries never dropped, hook error→SERVING holds); (C) no bill-forever (3 independent teardowns: FSM handback + deadman + Spot DELETE). 162 tests. **Load-bearing invariant: observed-gated authority** (forecast paces, reality decides).
- Minor non-blocking (documented, not fixed): `note_outage`/`note_dw_success` are dead seams (FSM self-drives from the gradient — harmless gated API); deadman couples to the `ollama` systemd unit name (cost-safe bias if it changes).

## GRADUATED — `JARVIS_FAILOVER_LIFECYCLE_ENABLED` default TRUE (2026-06-23, MERGED PR #69676)
Flipped default-ON after the **Adversarial Cognitive Soak** proved the full cognitive net LIVE against local Ollama `qwen2.5-coder:7b` ($0 GCP — all local M1). Hot-revert `JARVIS_FAILOVER_LIFECYCLE_ENABLED=false` → inert. Built this round (chaos PR #69675 + graduation PR #69676):
- **`scripts/chaos_injector.py`** (#69675) — deterministic FSM gauntlet (503-storm→awaken, **phantom-recovery race** = DW heals mid-awaken→reaches DORMANT no-thrash, assassination) driving the REAL controller+gradient with a fake clock; gated, triple-gated live scaffold. + **LIVE dead-man drill PROVEN**: orphaned node from the golden image self-DELETEd via SA-token REST at **T+221s** (no orchestrator) — masterstroke validated on real GCE.
- **VRAM pre-warm** (`failover_lifecycle` AWAKENING awaits `LocalPrimeClient.warmup` 1-token gen before SERVING) — kills the cold-start spurious timeout on the first failover op (a real finding: a cold 7B can't gen inside the 30s seed). No hardcoded sleep; gated; fail-soft (warmup fail→still SERVING, AWAKENING-timeout is outer bound).
- **Thrash-pivot** (`epistemic_feedback.should_pivot`) — composes budget-exhaustion with the legacy stuck-signature `pivot_verdict` (untouched/additive), so a THRASHING model (different failure each attempt → never accumulates repeated_signature_count) still decomposes instead of silently exhausting. **Real production gap the gauntlet exposed.** Wired in the soak AND `repair_engine`. Gated `JARVIS_EPISTEMIC_THRASH_PIVOT_ENABLED` default-true, OFF byte-identical.
- **`scripts/adversarial_cognitive_soak.py`** — drives REAL `LocalPrimeClient`→Ollama-7b + real `epistemic_feedback`/`decompose_for_block`/`isolate_symbols` + real pytest through adversarial payloads (merge_intervals / concurrency_lru / **paxos_election**). AST-sanitizes generated code to be importable; robust collection-error failure signature (tempdir-normalized).
- **THE FLIP-GATE (live):** Paxos anomaly forced the COMPLETE net — thrash (6 fails) → 8 Hybrid Epistemic Diffs + temp decay 0.7→0.175 → `[SOVEREIGN YIELD: UNRESOLVABLE PATH] reason=budget_exhausted` → **semantic symbol-scoped decompose** (13 sub-goals; `heartbeat_loop` separated from `advance_term`/`resolve_partition`; target_files→real written impl so scoping is real, NOT arbitrary) → retry against failure-implicated chunk → bounded, no lockup, no dropped state. Gate = graceful handling (Paxos convergence correctly SECONDARY/not-required; a 7B won't solve consensus). Honest finding: 7B convergence is STOCHASTIC (thrashed 7×, converged in 2× on the same TTL-LRU payload).
- ~250 tests across the arc. **OPS LESSON: "we test the net, not the acrobat's luck"** — a zero-shot convergence (run 1) FAILED to validate the repair loop; only the engineered-to-fail Paxos payload exercised pivot+decompose live.

## Phase 4 — Sovereign RSI Flywheel (fine-tune the J-Prime failover coder on O+V's own experience)
**Spec (costed, MERGED #69677):** `docs/superpowers/specs/2026-06-23-sovereign-rsi-flywheel-phase4.md`. Cost = PRIMARY constraint (operator: "bill EXTREMELY low"). 3 phases, 3 repos: 4a=JARVIS (Body), 4b=JARVIS provisioner wrapping reactor-core training, 4c=jarvis-prime Ollama hot-swap. **Cost model: ~$0-2/mo expected, $5/mo HARD cap.** Section 10 operator-authorized cheapest defaults: $5/mo ceiling, L4-first Spot (A100 only if dataset demands), `JARVIS_RSI_TRAINING_ENABLED=false` (GPU tier OFF until operator flips post data-review).
**Reactor-Core deep-dive verdict (read the repo): ~85% READY** — real production-grade SFT (HF SFTTrainer) + DPO pair generator + LoRA/QLoRA + GGUF quant (Q4_K_M) + DeploymentGate (40+ tests) + deploy-to-J-Prime (copy+symlink+`model_ready` event). 3 gaps Phase 4 closes: (4a) Body emits no code-gen-outcome experience; (4b) reactor-core has NO GPU-VM lifecycle (the cost red-flag → we wrap it w/ the failover node's Spot+dead-man+budget discipline); (4c) J-Prime hot-swap listener unverified. KEY INSIGHT: the epistemic-repair trajectory (failed draft→repaired pass) is a NATIVE DPO pair → flywheel spins on NORMAL DW operation, not just failovers.
**Phase 4a MERGED #69677 ($0/no-GPU):** `dpo_synthesizer.py` — epistemic-repair trajectory → token-dense `DPOPair` (schema-matched to reactor-core's `dpo_pair_generator`, no new Nerves schema). Wired fail-soft at `orchestrator._l2_hook` (L2_CONVERGED + candidate present). **EPISTEMIC PURITY GATE:** (1) cognitive-vs-infra isolation — `classify_rejection` DROPs infra-caused rejections (Aegis/network timeout, fsm_exhausted, 503/5xx, GENERATION_TIMEOUT via dw_fault_taxonomy+FailureSource), KEEPs only cognitive (pytest assertion/syntax/logical); **fails SAFE: unknown→infra→DROP** (never pollute w/ DoubleWord's infra problems); (2) golden-ratio — needs BOTH failed AND test-verified-repaired state; a YIELDED trajectory (no chosen)→drop whole pair (no half-pairs). AST-isolates only the changed symbol (reuses ast_symbol_scoper + dw_egress_interceptor.estimate_body_chars density cap, JARVIS_DPO_PAIR_MAX_CHARS 2048). Async fire-and-forget export (mirrors outage_ledger bridge); bounded `.jarvis/dpo_dataset.jsonl` ring + sha256 dedup; GCS export gated default-OFF (nothing leaves machine yet). 40 tests, fail-soft, OFF byte-identical.
**Phase 4b/4c PENDING (build after 4a data accrues + operator reviews):** 4b `rsi_training_hypervisor.py` (demand+volume trigger, min-viable Spot-L4 bid, **dead-man self-DELETE on GGUF-complete** reusing failover_deadman+SA-token-REST, IntelligentGCPOptimizer $5/mo cap, default-OFF). 4c J-Prime Ollama-native hot-swap (load+warmup+drain-old, rollback on gate-fail, no dropped DAG req). First fine-tune burst = operator-gated, measured vs the gauntlet (does the tuned 7B thrash less on TTL-LRU/Paxos?).

## Pending (next sessions)
- **First REAL GCP failover is unproven end-to-end** (proven in pieces: golden-image generates correct code [bake validation], awaken+dead-man [live drill T+221s], cognitive loop [local soaks], routing [unit tests], all safety invariants [Opus]). The flag is now ARMED — the next real DW global outage triggers the first live end-to-end awaken→serve→handback; it's protected by the proven dead-man + $5/day budget cap + blip-skip. Worth monitoring the first real firing.
- **Phase 4 (later, budgeted):** Reactor-Core training flywheel ingests the exported outage + generation-outcome dataset (the Trinity bridge from Phase 1) → fine-tunes the coder model → redeploys to J-Prime. Heavier GPU cost → budgeted bursts. (Would lift the 7B's stochastic convergence ceiling exposed by the gauntlet.)

See [[project_epistemic_feedback_lane_escalation]] (the quarantine that detects the outage this fails over from), [[project_jprime_local_tier]], [[project_sovereign_sentinel_autopsy]] (the autopsy principle reused in the bake).
