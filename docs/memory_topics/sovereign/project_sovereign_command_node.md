---
title: Sovereign Command Node — Biometric Mission Control (2026-06-23)
modules: [backend/voice_unlock/voice_biometric_intelligence.py, extensions/vscode-jarvis/src/api/, backend/voice_unlock/jarvis_proximity_integration.py, backend/core/ouroboros/governance/ide_observability_stream.py, backend/core/ouroboros/governance/ide_observability.py, backend/voice_unlock/voice_biometric_cache.py, backend/core/ouroboros/governance/critical_elevation.py, backend/core/ouroboros/governance/cross_repo_trust_ledger.py, Next.js, config.py, backend/core/ouroboros/governance/command_node/biometric_audit_ledger.py, backend/core/ouroboros/governance/command_node/command_node_router.py]
status: merged
source: project_sovereign_command_node.md
---

# Sovereign Command Node — Biometric Mission Control (2026-06-23)

**The pivot after the backend Sovereign Architecture arc completed:** the operator can't manage an AGI through terminal stdout + raw JSON. The Command Node is a web UI/UX dashboard. Spec: `docs/superpowers/specs/2026-06-23-sovereign-command-node.md` (MERGED #69685). **THE LAW: the UI is an operator surface, NEVER a new authority — the Immutable Orange Protocol + fail-CLOSED + the trust ledger all compose underneath; no UI action (even a valid biometric) can make a Mind/Nerves PR auto-merge.**

**Reuse map (verified, all real):** read-feed = `ide_observability_stream.py` (SSE `/observability/stream`, StreamEventBroker.publish ~1930, get_default_broker ~2230, typed events + replay + heartbeat) + `ide_observability.py` (read-only GET, loopback, rate-limited, authority-free). Blast-radius data = `oracle.compute_blast_radius → BlastRadiusReport.to_dict()` (directly+transitively_affected NodeID{repo,file,name,type}). Voice biometric (REAL, production-grade) = `backend/voice_unlock/voice_biometric_intelligence.py` `EcapaFacade` (ECAPA-TDNN) + `VBIConfig` (Bayesian auth threshold 0.85, anti-spoof physics/VTL deviation, drift) + `voice_biometric_cache.py`. Cross-repo gov = `critical_elevation.py` (CRITICAL_ELEVATION + Immutable Orange floor) + `cross_repo_trust_ledger.py` + OrangePRReviewer. Existing vscode-jarvis SSE parser (`extensions/vscode-jarvis/src/api/` + stream.parser/reconnect tests) to mirror.

## Phase 1 — Read-only dashboard MERGED PR #69686 (2026-06-23, $0, no new authority)
- **Backend** (`d35c7f250b`, 111 tests): additive SSE event types `fsm_phase_changed{op_id,phase,route,risk_tier}` / `cross_repo_elevation_pending{pr_id,target_repo,blast_radius_summary}` / `sovereign_yield{op_id,reason}` / `dag_node_updated{op_id,node_id,state}` on the EXISTING broker + thin fail-soft publish helpers. Read-only `GET /observability/blast-radius/{op_id}` → BlastRadiusReport.to_dict() (loopback+rate-limited+**authority-free**, lazy-imports oracle only). Caught+fixed: `sovereign_yield` was silently dropped (not in `_VALID_EVENT_TYPES`). `publish_sovereign_yield` wired at `emit_sovereign_yield`; fsm_phase/elevation/dag publish hooks DOCUMENTED as wire-sites (production wiring = careful follow-up, not forced).
- **Frontend** (`ef19ae30`, **18 vitest + tsc strict clean, 243 pkgs installed real**): `extensions/command-node/` Next.js (App Router) + React + TS + React Flow. `useSovereignStream` (native-fetch SSE MIRRORED from vscode parser — exp-backoff+jitter, Last-Event-ID replay, bounded buffer, poll fallback; NOT bare EventSource). 11-phase FSM ribbon + live DAG canvas + **interactive blast-radius graph (Body=blue/Mind=amber/Nerves=violet, center=mutated symbol→dependents, click→detail, cross-boundary badge)** + view-only ElevationQueue (disabled Phase-2-biometric authorize placeholder) + ConnectionStatus + sovereign_yield YieldToasts. Env-driven (`NEXT_PUBLIC_OBSERVABILITY_BASE`), no hardcoded localhost.

## Phase 2 — Biometric Edge-Gate MERGED PR #69687 (2026-06-23, security-reviewed, no Critical, default-OFF)
The Zero-Trust voice-biometric write-path + the Sovereign design-token system. **Voice pipeline is REAL** (recon win): `backend/voice_unlock/jarvis_proximity_integration.py:144 authenticate(audio_bytes, sample_rate)` runs the full ECAPA + anti-spoof + liveness + replay-detection; `config.py` already has require_liveness 0.80 / replay_detection / anti_spoofing_level high / challenge system; master voiceprint = `_reference_embedding`/`_hot_voiceprint_cache['owner']`. So the middleware WRAPS it (lazy-import, injectable verify fn) — no ECAPA reimpl.
- **Backend** (`af98bd95` + security fixes `3af2dac3`, 101 tests w/ regression): `command_node/biometric_auth_middleware.py` + `biometric_audit_ledger.py` + `command_node_router.py`. `GET /command-node/elevation/{pr_id}/challenge` mints single-use 256-bit nonce (`secrets.token_hex(32)`) + randomized challenge phrase, bound to pr_id+ast_mutation_id+blast_radius_hash, TTL 90s. `POST /command-node/authorize-elevation {pr_id,nonce,ast_mutation_id,audio_b64,sample_rate}`: (a) **nonce consumed ATOMICALLY under asyncio.Lock BEFORE verify** (replay/concurrent-reuse impossible) + TTL + pr_id/ast_id binding; (b) biometric reuse (score≥0.85 AND antispoof AND liveness, fail-closed defaults); (c) **MANDATORY Immutable Orange re-check** — prime/reactor + PERFECT biometric → ALWAYS REJECT, approve_fn never called (3 layers: inline env-free `frozenset({prime,reactor})` + affirmative jarvis-ONLY allowlist + critical_elevation floor); (d) **audit-then-approve** — durable hash-chained record written BEFORE approve_fn (audit I/O fail → REJECT `fail_closed:audit_unavailable`; no merge outruns its record). Hash-chain `record_hash=sha256(prev+canonical)` tamper-evident `verify_chain`. Audio NEVER persisted (sha256 only). Static-token/password/cookie → 400 reject-by-construction. Default-OFF → 404. Loopback/TLS + rate-limited. **Fail-CLOSED absolute**.
- **Frontend** (`0138373c`, 40 vitest + tsc strict + next build 4/4): **Sovereign design-token system** (`tailwind.config.js`+`app/globals.css` `:root`, military-grade console aesthetic, **ZERO hardcoded hex outside :root** grep-verified; tokens: --surface-0..3, --accent(-bright/-deep/-ring), --repo-body/mind/nerves, --state-ok/danger/warn/pending, --glow-primary/danger/ok, --font-mono/sans; `DESIGN_TOKENS.md` canonical list + `lib/tokens.ts` mirror — map future Figma extraction to THESE). **AuthorizeElevationModal = strict XState v5 FSM** (IDLE→AWAITING_MIC_PERMISSION→CAPTURING_AUDIO→PROCESSING_EMBEDDING→AUTHORIZED|REJECTED+ERROR; illegal transitions impossible — SUBMIT before CAPTURE_DONE dropped, no re-capture during PROCESSING) + `lib/audioCapture.ts` Web Audio (16kHz mono WAV→b64, mic ALWAYS released) + single-use-nonce retry (REJECTED→RESET re-fetches FRESH nonce). immutable_orange reject → distinct "Sovereign Law, not a biometric failure" copy. **Frontend NEVER decides auth — captures+transmits; backend sole authority.**
- **Security review (opus, no Critical):** Immutable Orange airtight under EVERY path; fail-CLOSED total; anti-replay sound (atomic consume-before-verify). 3 findings FIXED: M2 audit-then-approve, M1 real floor enforcement (killed dead `return True`), H1 `JARVIS_COMMAND_NODE_REQUIRE_PHRASE_MATCH` teeth + loud [SECURITY] warning (ASR phrase-match = Phase 3; flag default-OFF until then — H1's "fresh-nonce+old-audio relies on signal-level anti-spoof/liveness" is acceptable while OFF/loopback).

## Phase 3 — Biometric-Semantic Binding MERGED PR #69688 (2026-06-24, CLOSES H1)
The voice gate now verifies WHO (ECAPA) AND WHAT (ASR phrase-match) — concurrently, locally, fail-CLOSED both-must-pass. **REUSE: existing local Whisper** (`ml_engine_registry` WhisperWrapper, cached `whisper.load_model`, lazy-import, NO cloud → air-gap preserved). `command_node/semantic_phrase_match.py`: pure-stdlib token-level Levenshtein `word_error_rate`; `asr_phrase_match` transcribes → WER ≤ 0.10 (≥90%, env `JARVIS_COMMAND_NODE_WER_THRESHOLD`) → PASS, fail-CLOSED on transcribe-error/empty. **Concurrent gate:** ECAPA `voice_verify_fn` + ASR `phrase_match_fn` via `asyncio.gather(return_exceptions=True)` on the same buffer; `biometric_pass = ecapa_pass AND phrase_match_ok`. Matrix: (✓,✓)→AUTHORIZED, (✓,✗)→REJECT phrase_mismatch, (✗,✓)→REJECT biometric_mismatch, either-raises→REJECT fail_closed. **Monotonically MORE restrictive (added AND-term — can only strengthen).** `REQUIRE_PHRASE_MATCH` default→true; deferral warning removed (→ "semantic binding ACTIVE" log; inverse warning only if explicitly disabled). Audit payload += wer + transcript_hash(sha256, transcript NEVER persisted) + phrase_match_ok. Anti-replay + Immutable Orange + audit-then-approve preserved EXACTLY (127 backend tests green). **H1 RESOLVED — pre-recorded audio splicing defeated (needs the right voice AND the fresh randomized phrase, concurrently).**

## Figma MCP (connected 2026-06-23, Derek's account)
The Figma MCP is connectable (`mcp__plugin_figma_figma__*`). Extraction tools (`get_variable_defs`, `get_design_context`) REQUIRE a concrete node-specific URL (`figma.com/design/FILEKEY/Name?node-id=1-2`) — they do NOT read "current selection" remotely; whoami works. Derek had no ready Sovereign design file → DECISION: defined the Sovereign tokens IN CODE (the goal was no-hardcoded-CSS, not the Figma file; Figma was the means). Can push tokens INTO Figma via the MCP (generate_figma_design/create_new_file) or extract from a real design later → swap `:root` tokens. **LESSON: "use Figma" usually means "use a design-token system" — code-first tokens achieve it when no design exists; don't block on a Figma file.**

## Phase 2 ORIGINAL SPEC (superseded by the BUILT above — kept for reference)
The Cryptographic Biometric Edge-Gate: `POST /authorize-elevation` rejects passwords/tokens. Flow: server mints single-use TTL-bounded challenge (256-bit nonce + randomized time-based phrase bound to the PR's blast_radius_hash) → UI captures operator speaking the phrase → verify = freshness/anti-replay (nonce consumed atomically) + ECAPA-TDNN embedding ≥0.85 vs master voiceprint (reuse EcapaFacade) + existing anti-spoof (physics/VTL) + optional ASR phrase-match → only then call the EXISTING CRITICAL_ELEVATION approval path. **Immutable Orange STILL composes (Mind/Nerves never merge via UI).** + Immutable Audit Ledger: hash-chained (`record_hash=sha256(prev+payload)`) record per attempt binding voiceprint_id+ecapa_score+ast_mutation_id+audio_sha256 (reuse GraduationLedger JSONL pattern). Default-OFF `JARVIS_COMMAND_NODE_AUTH_ENABLED`. §9 open decisions: ASR phrase-match (recommend include), read-feed remoteness, audit-ledger location, stack (Next.js confirmed). **Phase 2 = mandatory security review (it's a governance write-path).**

See [[project_sovereign_cross_repo_mutator]] (the CRITICAL_ELEVATION + Immutable Orange this UI surfaces/authorizes).
