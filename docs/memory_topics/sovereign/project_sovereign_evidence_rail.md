---
title: Project Sovereign Evidence Rail
modules: [tests/governance/autonomy/]
status: historical
source: project_sovereign_evidence_rail.md
---

Built 2026-06-14 on branch `sovereign/distillation-phase-ab` (NOT merged to main yet). Two specs/plans under `docs/superpowers/{specs,plans}/2026-06-14-*`. Closes the "REVIEW/PLAN run in shadow forever" gap from [[project_phase_b_subagent_roadmap]] with an evidence rail that auto-graduates them after a 50-op aligned soak. 45 tests green; broader `tests/governance/autonomy/` 457 green.

**Four units (all default-ON but OFF-inert):**
- **Unit D — L3 Memory Governor** (`autonomy/l3_memory_governor.py` pure math + `subagent_scheduler.py` `_consult_memory_governor` composed into `_run_graph` clamp after the Slice 5 Arc B fan-out gate). Caps concurrent worktrees = `floor(avail_mb/JARVIS_L3_WORKTREE_RAM_BUDGET_MB[1500])`, strictest-wins with the gate's free-% caps. REUSES `MemoryPressureGate.probe()` (no new probe). Flags `JARVIS_L3_MEMORY_GOVERNOR_ENABLED`/`_WORKTREE_RAM_BUDGET_MB`. NOTE: default-on governor made pre-existing scheduler concurrency tests RAM-flaky on low-RAM hosts → added autouse `monkeypatch.setenv(...ENABLED,"false")` fixture to `test_subagent_scheduler.py`; proved host-independent via `JARVIS_L3_WORKTREE_RAM_BUDGET_MB=99999999 pytest` (forces clamp-to-1 on any host).
- **Unit B — Evaluator** (`shadow_evaluator.py`, pure, never raises, malformed→aligned=False). `evaluate_review` = binary block-vs-allow agreement (reservations→ALLOW, only "reject"→BLOCK; legacy BLOCK iff hard-finding OR tier∈{APPROVAL_REQUIRED,BLOCKED}). `evaluate_plan` = REFINEMENT check (DAG covers 100% of legacy tasks ∧ acyclic via module-level `_has_cycle` Kahn ∧ disjoint owned_paths); extra structure allowed. Public params typed `object` (honest — fed JSON).
- **Unit A — Telemetry Store** (`shadow_telemetry_store.py`) async SQLite `.jarvis/shadow_telemetry.db`, bounded `asyncio.Queue` + single `to_thread` writer (never blocks loop, never raises). Two-phase upsert keyed (op_id,agent) joins shadow verdict (early) + legacy outcome (late); injected evaluator computes alignment when both halves present. Rolling FIFO cap `JARVIS_SHADOW_TELEMETRY_MAX_ROWS_PER_AGENT[1000]`. `recent_aligned_streak` skips None(incomplete)/breaks on 0.
- **Unit C — Gate + Breaker** (`shadow_graduation_gate.py`). `ShadowGraduationGate.maybe_promote(agent)`: idempotent (short-circuits on `_is_authoritative` before store read), 50-soak (`JARVIS_SHADOW_GRADUATION_THRESHOLD`), flips `JARVIS_{PLAN,REVIEW}_SUBAGENT_AUTHORITATIVE`→true + `_SHADOW`→false via EXISTING `graduation_orchestrator.persist_flag_to_env` (credential-safe, see [[project_slice136...]]). `PlanBreaker.should_use_legacy(dag)`: CRITICAL mem pressure FIRST (pre-emptive)→ then empty/unparsable → then cyclical; trip→legacy. `build_rail_evaluator()` adapter unwraps PLAN legacy `{"flat":[...]}`. New SSE `EVENT_TYPE_AGENT_DEGRADATION` in `ide_observability_stream.py`.

**Live FSM wiring (orchestrator.py — verified by ast.parse only, sandbox can't import; split-brain guard):** `set_shadow_rail(store,gate)` + `_fire_shadow_gate` + `__init__` attrs (None default). Producers in `_run_plan_shadow` (legacy=`ctx.target_files`, shadow DAG `_execution_graph` tuple-of-tuples remapped unit_id/dependency_ids→id/deps/owned_paths) and `_run_review_shadow` (shadow=`_aggregate`, legacy=`ctx.risk_tier.name`+`semantic_guard_hard=False`[guardian signal not in scope at hook — honest limitation]). Authoritative: `_run_review_shadow` now returns `Optional[RiskTier]`, caller at ~7980 applies strictest-wins to GATE-local `risk_tier`; PLAN runs breaker→AGENT_DEGRADATION via `get_default_broker()`. GLS constructs/owns store in async `_build_components` (~4882, guarded `store_enabled()`, WARN-only), `aclose()` in `stop()`. **DAG execution is OBSERVER-ONLY (shadow DAG on ctx, not best_candidate, so `_materialize_execution_graph_candidate` never sees it) — endorsed-DAG-drives-execution is a deferred follow-up slice.**

**OFF-inert (bulletproof):** `JARVIS_SHADOW_TELEMETRY_STORE_ENABLED=false`→store never built→orchestrator `_shadow_store` stays None→all producer/gate/authoritative blocks skipped→FSM byte-identical. Authoritative flags default false. Short-circuit `and` at the REVIEW call site means OFF path never even reads `risk_tier`.

**NOT merged.** Branch is behind main (merge-base ~Slice 167); main now has loop's Slice 252/253. Spec §14 documents the orthogonality (see [[project_loop_shadow_mode_vs_evidence_rail]]). Merge needs rebase-onto-main with KEEP-BOTH on mechanical `EVENT_TYPE_*` conflicts in `ide_observability_stream.py` + `serpent_flow.py` (orchestrator.py/governed_loop_service.py do NOT conflict — loop never touched them).
