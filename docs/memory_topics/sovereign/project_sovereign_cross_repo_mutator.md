---
title: Sovereign Cross-Repo Mutator — Multi-Repo Blast Radius Quarantine (2026-06-23, MERGED PR #69678)
modules: [scripts/trinity_sandbox_egress_mock.py, scripts/cross_repo_first_surgery.py, scripts/sovereign_iac_hypervisor.py, backend/core/ouroboros/governance/critical_elevation.py, backend/core/ouroboros/governance/cross_repo_trust_ledger.py, backend/core/ouroboros/governance/cross_repo_master_flag.py, orchestrator.py, backend/core/ouroboros/governance/providers.py, backend/core/ouroboros/governance/saga/trinity_handshake_suite.py, backend/core/ouroboros/governance/saga/trinity_docker_runner.py, metrics_caller.py]
status: merged
source: project_sovereign_cross_repo_mutator.md
---

# Sovereign Cross-Repo Mutator — Multi-Repo Blast Radius Quarantine (2026-06-23, MERGED PR #69678)

**The frontier:** let O+V autonomously generate→apply→PR mutations into its own **Mind (jarvis-prime)** + **Nerves (reactor-core)**, not just **Body (JARVIS)** — the highest-risk capability in the architecture (it can rewrite its own brain + training pipeline). Spec: `docs/superpowers/specs/2026-06-23-sovereign-cross-repo-mutator.md`. Branch `worktree-sovereign-rsi-flywheel`.

**KEY FRAMING — this is GATING SUBSTRATE, the lock before the door.** The master cross-repo WRITE flag is NOT built and stays OFF (Body-only, byte-identical today). This PR builds only the three guardrails that will gate cross-repo writes WHEN that flag is eventually built. Deep dive found the cross-repo plumbing **~90% already built + tested** (CrossRepoScopePromoter gated-OFF, Oracle cross-repo graph `compute_blast_radius`/`get_dependents` keyed {jarvis,prime,reactor}, SagaApplyStrategy multi-repo+compensating-rollback, CrossRepoVerifier 3-tier, risk_tier_floor, OrangePRReviewer, GraduationLedger, trinity_integrator, docker-compose.{soak,prod,c2}.yml). So the guardrails are thin composition layers, NOT a rebuild.

**Trinity vs Provider axes (operator clarified):** the 3 REPOS (Body/Mind/Nerves) are the organism; **DoubleWord is the cheap Tier-0 PROVIDER** (not a repo) that does ~all day-to-day generation AND is the stochastic data-generator for the RSI flywheel. J-Prime (Mind) = self-hosted failover provider (Tier-2, ephemeral, rare). So DW does the work; the Trinity owns sovereignty (survives DW collapse). Activation status: Body always-on, Mind on-demand-failover (golden image baked, lifecycle graduated), Nerves wired-but-OFF (training tier default-OFF). O+V can READ/reason across all 3 repos today (RepoRegistry+Oracle span them via .env paths); autonomous cross-repo WRITE is this gated frontier.

## The three guardrails (G3 built FIRST — the lock before the door)
- **G3 — Adaptive Trust Ledger + CRITICAL_ELEVATION + Immutable Orange Protocol** (`critical_elevation.py` + `cross_repo_trust_ledger.py` + risk_tier_floor floor):
  - **IMMUTABLE ORANGE (Sovereign Law — most important):** prime/reactor targets are PERMANENTLY `>= approval_required` — a hardcoded `frozenset({"prime","reactor"})` floor evaluated FIRST (before any flag/ledger that could throw+skip it), read by NO env, un-disableable by any flag/graduation/trust/error. **"It can write its own brain; it can never merge its own brain."** Opus-PROVEN empirically: hostile env sweep (flags off/garbage, MIN_RISK_TIER=safe_auto, mixed-case, ledger-throws) → prime/reactor resolve approval_required EVERY time. The ONE intentional hardcode (a safety constant).
  - **Adaptive Trust Ledger (rejects magic N):** graduates the Body(jarvis) hard-halt only on a consecutive ZERO-rollback streak whose AST-complexity-weighted trust (Oracle blast-radius dependents + ast.walk node count + boundary depth + estimate_body_chars) clears a DYNAMIC threshold (`JARVIS_TRUST_BASE × max(complexity,1.0)`); ANY rollback/FRACTURE resets streak+trust to ZERO; requires demonstrated non-trivial complexity (`JARVIS_TRUST_MIN_COMPLEXITY` floor — trivial-PR VOLUME can't graduate). Only jarvis can graduate below Orange; prime/reactor never (Immutable Orange).
  - CRITICAL_ELEVATION = new tier ABOVE approval_required (ordered 3 in `_ORDER`, between approval_required=2 and blocked=4); a cross-repo PR is created but merge HARD-HALTS for explicit operator approval regardless of CI status, until trust-graduated.
- **G1 — AST blast-radius context** (`multi_repo/cross_repo_blast_context.py`): `Oracle.compute_blast_radius` → forces every cross-repo dependent (e.g. mutating a reactor symbol → all jarvis dependents) into the generation prompt under a `## CROSS-REPO BLAST RADIUS` header; token-budgeted, never-silent truncation, nearest-first. Authority-free read+render.
- **G2 — Air-gapped Trinity sandbox** (`saga/trinity_integration_gate.py` + `scripts/trinity_sandbox_egress_mock.py`): ephemeral `internal`-network Docker (all 3 repos from mutated tree) + EGRESS SINKHOLE (provider base-URLs→synthetic mock; assert_air_gapped probes that real DW/Claude/GCP hosts are UNREACHABLE → else FRACTURE) → handshake assert → `[SOVEREIGN YIELD: CROSS-REPO FRACTURE]` on fail → saga rollback. Teardown-always (`down -v` in finally). Fail-CLOSED: Docker-absent/not-air-gapped/up-fail/timeout → FRACTURE (never silent pass). ZERO live-call surface (Opus-proven).

## Opus final review: APPROVE
Catastrophic axis (brain auto-merging itself) structurally impossible + proven; fail-CLOSED per guardrail; air-gap zero-call; teardown-always; master-OFF byte-identical. Caught 2 Body-path findings (both FIXED `779a0f50`): (1) Medium — trivial-PR VOLUME could graduate under DEFAULT env (threshold floored flat while trust accrued linearly; ~43 trivial PRs); test masked it with non-default base → fixed via `JARVIS_TRUST_MIN_COMPLEXITY` demonstrated-non-trivial gate + default-env test; (2) Low — garbage/typo flag value failed OPEN for the jarvis hard-halt → fixed to explicit-falsy-only (`{0,false,no,off}` disable; else enabled). Neither touched prime/reactor. ~150 tests. $0 (G1/G3 pure; G2 ephemeral local Docker).

## Activation layer — MERGED PR #69679 (2026-06-23)
- **Master Arming Switch** (`cross_repo_master_flag.py`): `JARVIS_CROSS_REPO_MUTATION_ENABLED` is NOT a naive boolean — `cross_repo_mutation_enabled()` requires a startup handshake (explicit-truthy flag AND `docker info` alive AND egress-sinkhole configurable via G2's build_airgap_compose); garbage/dead-docker/unconfigurable → graceful-degrade to FALSE. Fail-CLOSED (only deliberate-arm + ready sandbox arms it). Default-OFF byte-identical (verified `cross_repo_mutation_enabled()`==False unset).
- **G1/G2 wired into orchestrator** (gated behind the master switch): G1 blast-radius context at GENERATE (`orchestrator.py:4116-4136`, new `cross_repo_blast_prompt` field on op_context, injected in providers.py ~2814); G2 air-gapped sandbox gate at APPLY (`_execute_saga_apply` ~12722-12764) → `verdict.fracture` routes into the EXISTING `compensate_after_verify_failure` rollback → terminal (`cross_repo_fracture`), never half-applied. Logic in `multi_repo/cross_repo_wiring.py` (minimal orchestrator diff).
- **Blast-Radius Visualizer** (`multi_repo/blast_radius_visualizer.py`): ASCII Body→Nerves dependency TREE printed to operator surface before any cross-repo PR (grouped by repo, `JARVIS_BLAST_TREE_MAX_NODES` cap). Authority-free.
- **FIRST SURGERY PROVEN** (`scripts/cross_repo_first_surgery.py`, `--run` PASSED live): chaos harness drove the REAL machinery — real `SagaApplyStrategy.execute` + `compensate_after_verify_failure` (preimage-restore on real git temp repos), real G2 gate + FRACTURE yield, real blast trace + visualizer, real `docker info` handshake (armed=True). Chaos = contract-break (`emit(metric,value)`→`emit_metric(metric)`: compiles but breaks the Body caller, deliberately chosen to hit the SANDBOX HANDSHAKE not the earlier compilation gate). Live result: blast tree printed (jarvis metrics_caller mapped to reactor TelemetryAdapter.emit) → chaos applied → `[SOVEREIGN YIELD: CROSS-REPO FRACTURE]` → **ROLLBACK VERIFIED both repos restored, organism intact**. HONEST CAVEAT: the air-gapped Docker broken-handshake is INJECTED via the gate's own runner seam (no 3-repo docker-compose exists on the dev Mac; the existing docker-compose.*.yml build ONLY jarvis) — the ROLLBACK is genuine, the Docker 3-container spin is the one simulated piece. 54 activation tests + the live PASS.

## Trinity Air-Gapped Sandbox Matrix — MERGED PR #69680 (2026-06-23) — eradicates the simulated G2 seam
Real infra to spin jarvis+prime+reactor air-gapped (replaces the injected fracture-runner). `saga/trinity_compose_generator.py` (dynamic 3-repo compose from .env, NO hardcoded paths, **mount-into-base** so NO Dockerfiles needed in sibling repos — adding them would be a cross-repo write) + `trinity_handshake_suite.py` (jarvis→mutated reactor/prime APIs; 404/500/schema-mismatch→FRACTURE) + `trinity_docker_runner.py` (`TrinityDockerRunner`: generate→sinkhole-assert→up→health-gate→handshake→`down -v` always; wired as the REAL production path for `run_trinity_sandbox_gate`). Compose: 3 services + egress-mock, **`internal: true` cryptographic sinkhole** (WAN blackholed, provider URLs→mock), **health-gated `depends_on: service_healthy`** (jarvis waits for prime+reactor /health — race eradication). `assert_sinkhole` fail-CLOSED. 55 tests.
**REAL BOOT PROVEN (`docker compose up` live on the dev Mac — better than happy-path):** (1) **sinkhole ENFORCED in reality** — prime pip-install FAILED with WAN name-resolution errors → `internal:true` genuinely blackholes egress (pip can't reach PyPI); (2) **reactor-core BOOTED air-gapped** (✅ ready :8090, Trinity mesh connected); (3) **health-gating WORKED** — jarvis correctly did NOT start when prime failed (`dependency prime failed to start`, no boot-race crash); (4) **teardown-always** clean. **Docker's own parser: DOCKER CONFIG VALID.** The generation+sinkhole+health-gating+runner are GENUINE (Docker-validated + partially boot-proven); the simulated CODE seam is gone.
**HONEST FINDING + the real next step:** the air-gap is so real it (correctly) BLOCKS pip-at-boot. The mount-into-base design assumed pip-install-at-container-start, but an air-gapped net has no PyPI. **FIX = pre-baked dependency images** (standard air-gapped-CI pattern: one-time `pip install` build OUTSIDE the air-gap → snapshot image → run air-gapped). Reactor proved a deps-present repo boots fine; prime needs its deps pre-baked. This is the prerequisite for a full unsimulated 3-container handshake. (Plus the 16GB M1 host-ceiling for 3 heavy containers — Linux/GCP is the real venue, same as the failover end-to-end.)

## Autonomous Pre-Flight Cache Manager — MERGED PR #69681 (2026-06-23) — closes the unsimulated-surgery gap
`saga/trinity_prebake_manager.py`: **hash check** (`dep_hash(repo)`=sha256 over requirements.txt+pyproject.toml = cache key; mutated dep→new hash→re-bake) → **fast path** (all 3 images cached via `docker image inspect <repo>:<hash>` → straight to air-gapped boot, no build) OR **autonomous pre-bake** (missing → WAN-connected `docker build -f deploy/sandbox/Dockerfile.<repo>.sandbox <repo-root>` bakes deps into the hash-tagged image, **strictly separated from + BEFORE** the internal:true air-gap is entered — never pip in the air-gap). **Fail-CLOSED**: missing/uninspectable image→needs-bake; bake-fail→runner FRACTUREs (never boots air-gapped with a missing image). **Dockerfile.{jarvis,prime,reactor}.sandbox live in JARVIS `deploy/sandbox/`** (used via `docker build -f` against the sibling-repo context — NOT in the sibling repos, which would be a cross-repo write). Wired into TrinityDockerRunner before `up` (added a per-service `images` map to generate_trinity_compose). **Default-OFF** (`JARVIS_TRINITY_PREBAKE_ENABLED`) verified inert: skipped=True, ZERO docker calls, generator byte-identical. 23+55 tests. GCP-ready, inert on the Mac.

## ARC COMPLETE — the Sovereign cross-repo architecture is structurally locked (PRs #69678→#69681)
Full chain merged: guardrails (G1 blast-context + G2 air-gap sandbox + G3 Immutable-Orange/Trust-Ledger) → activation (master arming switch + orchestrator wiring + visualizer + first-surgery rollback PROVEN) → real Trinity sandbox (compose generator + sinkhole + health-gating, boot-proven the air-gap is real) → Pre-Flight Cache Manager (dep-bake, closes the pip-vs-airgap gap). Everything default-OFF/inert on the Mac, GCP-deploy-ready. The one capability that could let O+V merge its own brain unwatched is structurally impossible (Immutable Orange, Opus-proven). The rollback safety is proven live.

## Sovereign IaC Hypervisor — MERGED PR #69682 (2026-06-23) — the cloud operating room
`scripts/sovereign_iac_hypervisor.py` (standalone, default-OFF, triple-gated `JARVIS_IAC_HYPERVISOR_ENABLED`+`--execute`+`--i-understand-this-spends-money`; `--dry-run` default). Projects the Trinity to GCP to run the UNSIMULATED 3-repo surgery (32GB Linux node — the 16GB M1 can't). 4 phases (~80% composition of session primitives — bake script structure + failover_deadman + gcp_vm_manager + the Trinity Sandbox Matrix as the remote payload): (1) **Cloud Projector** — e2-standard-8 (32GB) Spot Linux, `--instance-termination-action=DELETE --max-run-duration=3600s --scopes=cloud-platform`, startup-script installs Docker + the Dead-Man's Burn watchdog; (2) **Bidirectional Sync** — rsync/scp jarvis+prime+reactor → `/opt/trinity/*` when SSH warm (lean excludes); (3) **Remote Exec + Terminal Tunnel** — SSH-execs the trinity surgery with the env armed (prebake→WAN-bake→air-gap boot), STREAMING stdout line-by-line to the LOCAL terminal (watch the blast-viz + FRACTURE/PASS live); (4) **QUADRUPLE Dead-Man's Burn** (#1 invariant) — local `gcloud delete` in finally + remote SA-token-REST self-DELETE (fires on completion-sentinel, independent of local Mac/SSH) + Spot DELETE-on-preempt + max-run-duration. No orphaned 32GB node under PASS/FRACTURE/exception/SSH-drop/local-death. **Cost ~$0.02-0.10/surgery** (Spot ~$0.08/hr, minutes, hard-capped 1hr, zero dormant). 25 tests (burn-in-finally on PASS+FRACTURE+exception proven). `--execute` cmd: `JARVIS_IAC_HYPERVISOR_ENABLED=1 JARVIS_PRIME_REPO_PATH=... JARVIS_REACTOR_REPO_PATH=... python3 scripts/sovereign_iac_hypervisor.py --execute --i-understand-this-spends-money`.
**THE LIVE RUN IS UNPROVEN** — a real `--execute` is a ~30-40min real-money first-ever end-to-end (provision + Docker install + rsync 3 LARGE repos + prebake 3 images + air-gap boot + surgery + burn), many failure points. Operator-gated; not yet run. This is the venue for the fully autonomous unsimulated surgery.

## Sovereign IaC Hypervisor + LIVE CROSS-REPO ROLLBACK PROVEN (2026-06-23, MERGED #69682 + #69683)
`scripts/sovereign_iac_hypervisor.py` — projects the Trinity to GCP, runs the UNSIMULATED cross-repo surgery, streams it back, self-burns. Default-OFF, triple-gated (`JARVIS_IAC_HYPERVISOR_ENABLED`+`--execute`+`--i-understand-this-spends-money`). 4 phases: Cloud Projector (e2-standard-8 32GB, `--on-demand` STANDARD or Spot), Bidirectional Sync (tar-pipe), Remote Exec+Terminal Tunnel (streamed), Quadruple Dead-Man Burn. + autonomous checkpointing (`.hypervisor_state.json` resume-don't-restart) + full line-by-line streaming + keep-warm-on-resumable-failure.
**THE LIVE SURGERY HAPPENED (real 32GB GCP node `sovereign-sandbox-20260623-204114`):** ARMING handshake (armed=True, real docker) → BLAST-RADIUS visualizer (`reactor::TelemetryAdapter.emit ← [jarvis] metrics_caller.py::record_metric`) → CHAOS (contract-break emit→emit_metric via REAL SagaApplyStrategy.execute) → air-gapped gate handshake BROKEN → `[SOVEREIGN YIELD: CROSS-REPO FRACTURE]` → REAL SagaApplyStrategy.compensate_after_verify_failure → **reactor_restored=True jarvis_restored=True → ROLLBACK VERIFIED, organism intact, VERDICT: PASS.** Node burned after. Total cost across ALL ~9 node iterations ~$0.30, zero orphans.
**SIX LIVE-RUN ROOT-CAUSE FIXES (the first cloud run surfaced each; all committed):** (1) root-owned /opt/trinity→mkdir+chown prep; (2) `gcloud scp --recurse` IAP flakiness→**tar-pipe transport** (`tar czf -|ssh 'tar xzf -'`); (3) Spot preemption breaking the ~20min window→**--on-demand STANDARD**; (4) no pip on Debian slim→python3-pip at boot; (5) sentinel touched on EVERY exit→armed dead-man even on resumable failure→**verdict-conditional sentinel**; (6) **THE killer: dead-man (reused failover_deadman, Ollama-:11434-idle-based) deleted the node whenever ACTIVITY_FILE missing — and the IaC node has NO Ollama → fail-OPEN-to-delete at ~6min every time → fix=fail-SAFE skip on missing file; no-orphan now via max-run-duration(3600s)+verdict-sentinel+local-delete.** ALSO: the surgery harness imports the full ouroboros stack (oracle→engine→governance) → needs jarvis host deps; the bulk requirements.txt install chokes on a pinned pkg (masked by `-q`) → install the cascade incrementally (aiohttp, psutil, uuid6, +batch) — the engine __init__ is heavy. LESSON: a reused watchdog must fail-SAFE for the new node-type's assumptions (no-Ollama ≠ idle); verify the EFFECT (GCP ops log showed the Compute-SA self-delete, not Spot).

## Pending / next (operational, not architectural)
- **The fully autonomous unsimulated surgery** needs: (a) a GCP/Linux host (16GB M1 can't boot 3 heavy containers — same ceiling as the failover end-to-end), (b) flip `JARVIS_TRINITY_PREBAKE_ENABLED` + `JARVIS_CROSS_REPO_MUTATION_ENABLED` (the arming switch self-degrades if Docker/sinkhole not ready), (c) O+V generating the cross-repo candidate ITSELF via DW through a full GovernedLoopService boot (vs the controlled first-surgery harness). The ARCHITECTURE is complete; what remains is operational deployment to the right venue.
- **Master flag still default-OFF** (must be operator-armed — and the arm now requires a live Docker + configurable sinkhole, so it self-degrades if the env isn't ready). Nothing mutates cross-repo until armed.
- **The FULLY AUTONOMOUS first surgery** (O+V generates the cross-repo candidate ITSELF via DW through a full GovernedLoopService boot, not the controlled harness) is unproven — same class as the failover end-to-end: needs the full stack + a live generation provider + ideally a real Linux host (the 16GB M1 + no-3-repo-compose can't host it). The harness proves the FRACTURE→rollback machinery; the autonomous generation→surgery is the operator-gated production run.
- **No real 3-repo docker-compose exists** — building one (jarvis+prime+reactor as air-gapped services) is the prerequisite for a REAL (non-injected) G2 sandbox handshake. The compose files today build only jarvis.

See [[project_sovereign_failover_lifecycle]] (Phase 4 RSI flywheel — the DPO synthesizer #69677 + the J-Prime failover this cross-repo capability could one day improve).
