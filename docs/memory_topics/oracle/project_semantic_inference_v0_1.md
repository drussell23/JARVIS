---
title: Project Semantic Inference V0 1
modules: [backend/core/ouroboros/governance/semantic_index.py]
status: historical
source: project_semantic_inference_v0_1.md
---

**Fact (landed 2026-04-15 after ConversationBridge v1.1)**: SemanticIndex (`backend/core/ouroboros/governance/semantic_index.py`, ~550 lines) closes the "semantic goal inference" gap Derek identified — O+V now has a recency-weighted centroid over recent commits + active goals + conversation turns, and scores new intake signals by cosine similarity. Embedder: `fastembed` + `bge-small-en-v1.5` (local ONNX, optional install via `requirements-semantic.txt`, master-off means no import/no disk I/O/graceful disable). 27 unit tests + 4 existing bridge E2E + smoke-import green → 77 total tests across the two features. Corpus cap 50 items; commit halflife 14d, conversation halflife 3d (§12.4); POSTMORTEM excluded from centroid by default (§12.3 "failure-gravity" avoidance) but surfaces in separate "Recent friction / closures" prompt subsection. Boost clamped at `JARVIS_SEMANTIC_ALIGNMENT_BOOST_MAX=1` (strictly subordinate to `goal_alignment_boost=2`). Disk cache at `.jarvis/semantic_index.npz`.

**Why**: Derek approved GO v0.1 with explicit §12 answers: (1) fastembed optional install, master-off no-import contract; (2) boost_max=1 subordinate to goal=2; (3) POSTMORTEM excluded from centroid by default; (4) conversation halflife 3d separate env; (5) V1 refresh = boot + interval only (HEAD-change debounce V1.1). Five "beef" items all folded in: (1) determinism tested via pinned fake vectors not brittle floats; (2) pre-embed sanitizer runs on commit messages (they're not safe by nature — can leak secrets); (3) `semantic_index` imports `conversation_bridge`, never reverse (enforced by placing bridge-reader code in semantic_index only); (4) scoring = one embed + one cosine against precomputed centroid per signal, NEVER rebuild corpus per signal; (5) prompt shows "untrusted context" no raw scores. Two consumer surfaces wired: (A) `unified_intake_router.py:147` adds `- semantic_boost` to priority formula after `goal_boost`; (B) `orchestrator.py` CONTEXT_EXPANSION injects "## Recent Focus (semantic — untrusted prior)" section between ConversationBridge and Goals in the prompt ordering. Manifesto §1/§4/§5/§8-aligned. v5 Semantic Firewall Tier -1 reconciliation remains parked.

**How to apply**: **AUTHORITY INVARIANT — same pattern as ConversationBridge §9**. The score + centroid are consumed ONLY by the intake priority formula and StrategicDirection at CONTEXT_EXPANSION. If any future session / V0.2 / V1 proposes feeding `semantic_alignment` or centroid text into UrgencyRouter input, Iron Gate rulings (exploration, ASCII, multi-file coverage), risk-tier math, policy engine decisions, FORBIDDEN_PATH matching, ToolExecutor protected-path checks, or OperationLedger body — **STOP**. This requires a new plan round with Derek. V0.2 candidates (parked, not auto-green): k-means clustering (vs single centroid), HEAD-change debounced refresh, DW embedding endpoint as Tier 1 fallback, cross-repo corpus, intake WAL backlog as corpus source, GitHub issues as corpus source. Manifesto v5 Semantic Firewall reconciliation still its own track — do NOT bundle semantic_index with v5 concepts silently. Live battle-test verification (§8 INFO line grep) deferred to next healthy DW/Claude session, same footnote rhythm as ConversationBridge POSTMORTEM follow-up.
