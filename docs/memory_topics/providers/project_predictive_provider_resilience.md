---
title: Project Predictive Provider Resilience
modules: [scripts/run_provider_sonar.py]
status: historical
source: project_predictive_provider_resilience.md
---

**Predictive Provider Resilience arc** (operator-driven, started 2026-05-19 after element-web capability run was masked by Claude+DW provider TimeoutError storm). Goal: replace reactive `TimeoutError`→context-compaction with predictive TTFT forecasting + variance-aware dynamic timeouts.

**Slices built (all compose existing surfaces, no new standalone telemetry path, master flags default-FALSE):**
- **Slice 0 — Observability Seam** (`providers._emit_provider_latency` + `dw_ttft_observer.ProviderLatencySample`/`record_provider_latency`): server-exact `usage.input_tokens` (never len/4), bounded deque ring + `flock_append_line` JSONL dual sink, wired Claude success + timeout/cancel + DW-batch boundaries. Committed to main `3039b877f6` (LOCAL only — NOT pushed to origin/main; origin/main=c563965bb5). 19+ tests.
- **Slice 1 — TTFT Forecaster (SHADOW)** (`dw_ttft_observer.TtftForecaster`): EMA-weighted streaming-moment OLS (E[x],E[y],E[xx],E[xy] → slope/intercept), prequential predict-then-update, per-`(provider,route)` `_RegState`, warm-start from JSONL. **Variance-aware refinement**: EWMA residual-σ + `forecast_envelope()` → `threshold=pred+k·σ`, `JARVIS_PROVIDER_LATENCY_FORECAST_K` default 3.0. Synthetic proof PASSED (converges to known law; 95.8% envelope coverage on heavy-tailed synthetic). **Git: operator believes Slice 1 committed but verification showed it was NOT (HEAD had no TtftForecaster); currently STAGED-uncommitted in working tree. Flag this if asked.**
- **Autonomic Provider Sonar** (`provider_health_sonar.ProviderHealthSonar` + `scripts/run_provider_sonar.py`): active stratified TTFT probing, composes canonical `ClaudeProvider._ensure_client()` + `.env` auth + Slice-0 emit seam; severs stream at first `text_delta` (zero output cost); bounded burst. **Engineering SUCCESS: 9/9 probes, 7→16 fittable rows in ONE sub-minute burst** (vs passive soak hours-for-11). 12 tests. Staged-uncommitted.

**Cumulative regression: 152–197 green across slices; zero drift on existing TTFT/DW/flag spines.**

**THE CRITICAL FINDING (2026-05-19) — envelope proof NOT delivered, two root causes:**
1. **Route-key fragmentation (Sonar design flaw, mine):** forecaster fits a separate model per `(provider,route)`; Sonar tagged `route="sonar"`, fragmenting data (sonar=9, complex=2, immediate=4, standard=1) — no key converges. Synthetic route key shares no production traffic. Honest fix: probes must enrich a REAL route key (or forecaster pools a provider-level baseline) — NOT invent a key.
2. **Clean severed probes structurally under-represent the pathology:** Sonar TTFT n=9 range 1019–2676ms σ=494; real loaded-generation TTFT range 3168–**73512**ms σ=35172 (~70× more volatile). The 73s spike (the arc's entire reason) does NOT appear in clean isolated probes and cannot by construction. Sonar harvests a *baseline* distribution; the pathology lives in *loaded end-to-end* telemetry (passive harvest under tool-loop/queue pressure).

**Fix A + Fix C attempt (2026-05-19, after operator authorized Unified Keying + Phantom Sonar):**
- **Fix A (unified keying)**: `TtftForecaster._key` now keys on `provider` only, route excluded (signature kept, AST-pinned). 155 tests green. Correctly implemented BUT exposed a deeper flaw — pooling the real 73512ms high-leverage outlier with ~1000ms clean probes makes the naive moment-based EMA-OLS **numerically diverge**: pred=897350ms, sigma=894605, threshold=2,751,631ms, MAE=86458ms. The "92.3% envelope coverage" is DEGENERATE (predictions so huge they trivially exceed every actual) — NOT evidence the envelope works.
- **Fix C (Phantom burst)**: massive 70k-100k payloads fired concurrently → **10/10 ERROR, input_tokens=0, ttft=-1** (rejected at REQUEST SUBMISSION, not queued). Oversized single payloads get refused, NOT heavy-queued. Phantom mechanism is broken for the goal; induced ZERO pathology rows.
- **ROOT CAUSE (real, mathematical):** naive linear OLS on raw-millisecond TTFT is the WRONG estimator for heavy-tailed (~log-normal, 1.8-orders-of-magnitude) latency with high-leverage outliers. Needs log-TTFT-space fit and/or robust estimator (Huber/Theil-Sen/median-EMA). Single-client congestion induction may not reproduce provider-side 73s queueing at all — that pathology may ONLY be observable in real loaded production telemetry.
- Slice 1 NOW genuinely committed (HEAD 71a93674ad, TtftForecaster verified in HEAD). Sonar still NOT committed (provider_health_sonar.py absent from HEAD despite operator belief — flagged). Fix A/C edits staged-uncommitted.

**D3 attempt (2026-05-19): Log-Space + Huber + shrinkage + physical ceiling — and the FOUNDATIONAL FALSIFICATION.**
- Rolled back Phantom Sonar (3 files discarded, were uncommitted). Slice 1 confirmed committed HEAD 71a93674ad. Fix A unified keying kept (route excluded from key, AST-pinned).
- Rewrote `_RegState` fully in log-TTFT space + Huber-robust location (`_forecast_huber_c` default 1.345) + empirical-Bayes slope shrinkage (`_slope_shrinkage_tau` default 8) + physical ceiling (`_MAX_LOG_EXPONENT` 15.0 ≈ 54min, was absurd 30≈124days). 143 synthetic tests green incl. the D1 73s-divergence proof (location bounded under spike, envelope widens to cover it).
- **D2 real-data validation FALSIFIED THE CORE PREMISE.** Genuine passive fittable n=7. **Pearson r(input_tokens, ttft_ms) = 0.114** (≈0; 0.257 without spike). The 73512ms spike is at 7427 tokens; the LARGEST payload (14324 tok) was among the FASTEST (2745ms). Baseline TTFT ~flat (1442–3168ms) across 5× token range. **TTFT for this provider is NOT token-driven** — it is bimodal: tight token-independent baseline + rare queue/load spikes independent of payload size. `Forecasted_TTFT = base + tokens·coef` is FALSIFIED by the data (same class as the Phantom falsification). The regression explodes at low N precisely because there is no token→TTFT signal to fit; log-space/Huber/shrinkage/ceiling are band-aids on a mis-specified model.
- **Honest path (NOT auto-applied — operator decision):** the variance-aware ENVELOPE is sound; the TOKEN-SLOPE is wrong. Correct model = provider-keyed robust location-scale quantile estimator: predict ≈ EWMA-median baseline (no token slope), threshold = high robust quantile / median + k·robust-log-scale that spikes inflate. Drop the token coefficient entirely.
- Did NOT keep patching past the falsification. Multi-iteration pattern itself is the finding: every assumption of token→TTFT failed; the data now explains why.

**E1 implemented (2026-05-19): Robust Location-Scale (EWMA-Median + Log-MAD), token-slope BURNED.**
- Discarded all staged D3 edits (git restore to committed Slice-1 71a93674ad). Rewrote: `_RegState`(OLS)→`_RobustState` (Robbins-Monro EWMA-log-median location + EWMA-log-MAD scale, NO slope/tokens/OLS/shrinkage); `ForecastResult`→`EnvelopeResult` (baseline_ms/ceiling_ms/band_ms/enveloped, no predicted/MAE); `TtftForecaster`→`ProviderLatencyEnvelope` (token-independent baseline()/envelope(); back-compat aliases kept for import stability). Physical ceiling exp(15)≈3.27e6ms. Principled MAD→σ const 1.4826 (env-tunable, like Huber 1.345). Fix A unified key retained (route excluded). providers.py shadow callsite → `[TtftEnvelope]` baseline/band/ceiling/enveloped. register_flags +K +MAD_CONSISTENCY (7 total). proof harness reframed to envelope-coverage + `--exclude-route` (D2 genuine-passive filter). **135 tests green** incl. synthetic proofs: token-independence, no-baseline-divergence under 73512 spike (with adequate warm-up N), inflate/deflate, degenerate-skip, unified-key, slope-burned AST pin.
- **D2 real-data proof on genuine passive: NOT VALID (honest).** n=7 success rows, the 73512ms (~30× outlier) consumed during warm-up at n≈3-4. baseline reports 17k-50k ms vs actual ~2.5k ms; ceiling SATURATED at exp(15)=3.27e6 → "100% coverage" is DEGENERATE (a physical-max ceiling trivially covers everything). Robustness DID help vs OLS (bounded 3.27e6 not 1e13; baseline 50k not 897k) but **no location-scale estimator can characterize a bimodal dist from 7 points with one 30× spike.**
- **STABLE CONCLUSION — no more estimator pivots.** The math is now correct (slope falsified+burned, robust estimator proven on synthetic with adequate N). The blocker is NOT the estimator — it is **insufficient genuine passive telemetry (n=7)**. Honest path = passive accumulation over time (E2), not iteration. E1 staged-uncommitted. Per [[feedback-no-preresult-euphoria]]: the "100% coverage" is a saturation artifact, NOT a win.

**Status: proof of "k·σ envelope would have enveloped the real 73s spikes" is NOT empirically demonstrated. Token-regression FALSIFIED + burned; E1 robust estimator is correct but blocked by n=7 genuine-passive insufficiency.** Synthetic math is sound; real-data validation blocked by (1)+(2). Awaiting operator direction — likely needs: fix route-keying + reconcile that spike-envelope validation requires *loaded* telemetry, not clean probes (Sonar gives baseline; pathology needs passive/loaded data or a load-inducing probe variant). NO euphoria — per [[feedback-no-preresult-euphoria]]. Did NOT auto-fix-and-refire (would spend + risk claiming on a patch).
