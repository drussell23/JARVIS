---
title: Project Containerized Dw Ov Blocker Stack
modules: [backend/core/gcp_vm_manager.py, scripts/ignite_sovereign_cloud_node.py, tests/seed/test_seed_defect.py, backend/core/ouroboros/aegis/forwarding.py, backend/core/ouroboros/aegis/passthrough.py, backend/core/ouroboros/governance/model_pinning_heuristic.py, unified_supervisor.py, backend/core/ouroboros/governance/calibration_context.py, backend/core/ouroboros/governance/silent_boot.py, backend/core/ouroboros/governance/payload_transport_heuristic.py]
status: open
source: project_containerized_dw_ov_blocker_stack.md
---

Live containerized DW-primary O+V soak (2026-06-20, `jarvis-sovereign-prod` from the `discoroute` worktree, `docker-compose.prod.yml + docker-compose.desktop.yml`, Claude credits depleted so pure-DW autarky). Goal was first `state=applied` in the container. Bottom-up diagnosis peeled **7 distinct layers**; each fix revealed the next. `state=applied` was NOT achieved on the Mac — it's a host-resource floor (2 CPU too slow for Venom tool loop + DW streaming to converge within deadline). Intended venue is the Linux prod host (the whole T5 IaC: `docker-compose.prod.yml` + `scripts/migrate_to_host.sh`).

**Layers (each verified, not guessed):**
1. **Catalog hydration** — `dw_catalog_client.fetch()` sent raw `Bearer self._api_key` but aegis scrubs the key post-spawn → 401 → catalog never hydrated → `dw_models_for_route` empty → DW never dispatched. FIX = `_auth_headers()` using the aegis vault (`dw_session_auth_header`) + `_await_aegis_credential_ready()` handshake in `dw_discovery_runner.boot_discovery_once`. **PR #69588 MERGED** (proven live: catalog_models=26, dispatch firing, exhausted 6→0). Bundled the desktop-overlay constrained-host opt-outs in the same PR.
2. **Aegis DW-streaming rupture** *(real prod bug)* — `forwarding.py forward_request` releases the upstream `ClientResponse` ONLY on the guillotine branch; normal/client-disconnect/upstream-read-failed paths leak it → `aegis-daemon ERROR Unclosed connection` on session close. Aegis graduation soaks (2026-05-25) only tested Claude `/v1/messages`, never DW `/v1/chat/completions` streaming. FIX = release `upstream_resp` in the streaming `finally` on all paths (sync in aiohttp 3.x; guillotine's no-`await` is correct). **PR #69590 OPEN** — scoped honestly: fixes the leak; whether it fully kills the loop-side `live_transport` rupture needs a DW-streaming soak on a capable host. Passthrough routes (`/v1/models`,`/v1/files`,`/v1/batches`) use a DIFFERENT path (`passthrough.py`) and work fine (200/201).
3. **Stale `dw_surface_health.json`** — `.jarvis/` bind-mount persists the prior run's `transport_degraded` verdict across restarts (file is `root:root`; clear via `docker exec ... rm`, host-side rm works too since the mount maps ownership). Must clear between config changes or it poisons the fresh run.
4. **Event-loop starvation** — `posture_observer.run_one_cycle` blocks 14-45s, `semantic_index.SemanticIndex.build` + posture signals (`commit_ratios`,`postmortem_failure_rate`) run synchronously on the asyncio loop, starving the in-flight DW SSE read into a RuntimeError rupture. FIX (constrained-host) = `JARVIS_DIRECTION_INFERRER_ENABLED=false` + `JARVIS_SEMANTIC_INFERENCE_ENABLED=false` (both advisory CONTEXT_EXPANSION only).
5. **Heavy-probe false-degrade** — surface-health sweep (`dw_surface_probes.probe_direct_streaming` via `build_heavyprobe_adapter` on nvidia/Nemotron-550B) RuntimeErrors on the slow host → degrades the WHOLE `DIRECT_STREAMING` surface → WARM-BOOT armor forces batch, but Iron-Gate ops REQUIRE the RT tool loop and can't batch → skip → exhaust. FIX = `JARVIS_DW_SURFACE_HEALTH_ENABLED=false` (opt-out, `preflight_probe.is_surface_health_enabled`).
6. **DW autarky config-disable gap** *(real bug)* — Slice-225 full-runway grant keys off `_claude_breaker_open()` which reads only the breaker STATE; a CONFIG-disabled Claude (`JARVIS_PROVIDER_CLAUDE_DISABLED=true`) is never constructed and never trips the breaker → autarky NEVER engaged → sole-lane DW held to the 90s `_TIER0_MAX_WAIT_S` cap → `fsm_failure_mode=TIMEOUT`. FIX = `_claude_config_disabled()` ORed into `_fallback_dead` in `_call_primary`. **PR #69589 OPEN** (9 tests).
7. **Generation TIMEOUT (the floor)** — even with cap lifted (`OUROBOROS_TIER0_MAX_WAIT_S=600`, `JARVIS_GENERATION_TIMEOUT_S=900`), the 2-CPU host can't converge the Venom tool loop + heavy-model (550B) DW streaming within deadline (CPU-100% ~300s then TimeoutError, $0.00 billed). Host-resource floor. The sentinel walker (Slice 23) cascades all 16 models incl. the 550B, fragmenting budget — pinning a fast model (gpt-oss-120b, proven 200+SSE in-container) needs topology/sentinel rework (not just `DOUBLEWORD_MODEL` env; the walker overrides via ContextVar).

**Proven baseline:** DW itself is healthy from inside the container with the in-container key — `GET /v1/models`=200, `POST /v1/chat/completions`=200, `stream=true`=live SSE, key len=46. So none of the blockers were DW/network/key. See [[project_slice235_validation_sixth_layer.md]] for the related pre-existing tool_loop_deadline / heavy-GOAL closure issue.

**Ops:** desktop overlay now carries all 5 opt-outs with full rationale comments (merged in #69588). Container left idle ($0.00, CPU 0.5%). PR CI checks (`Mock Testing`, `Validate Database/Environment`, `Validate PR Title`, `Code Quality`) FAIL on ALL recent PRs incl. merged ones — pre-existing flaky non-required, don't block merge.

**FOLLOW-UP (2026-06-20, all merged/PR'd):** #69589 (autarky config-disable, layer 6) + #69590 (aegis stream leak, layer 2) **MERGED to main**. **#69591 OPEN** = Sovereign Context-Routing Override Matrix (`model_pinning_heuristic.py`): `JARVIS_DW_PRIMARY_OVERRIDE=<model>` promotes a model to Rank 1 across all routes, composed OVER `_fleet_guarded` at `provider_topology.dw_models_for_route`'s 3 sites (`_pin_and_fleet_guarded`). SOFT-LOCK = passively-observed dispatch outcomes (wired to `note_pin_outcome` next to the bandit `record_outcome` sites in candidate_generator) — NO active probe (deliberately, to avoid the layer-5 heavy-probe pathology); `JARVIS_DW_PIN_FAIL_THRESHOLD`(3)/`JARVIS_DW_PIN_COOLDOWN_S`(120) → yields to EWMA when tripped. 13 tests, 0 regressions. `deploy/ouroboros_linux_prod.env` now sets the pin + `JARVIS_PROVIDER_CLAUDE_DISABLED=true`. **Launch (after merging #69591):** `./scripts/migrate_to_host.sh <user@host> /opt/jarvis --launch` (packs main → scp artifact + .env out-of-band → provision → arm_and_launch; aegis default-ON engages natively; pin sourced from the prod env file).

**SOVEREIGN CLOUD IGNITION (2026-06-20, PR #69592 MERGED to main):** Reuses the EXISTING hybrid-cloud provisioner `backend/core/gcp_vm_manager.py` (10K-line, compute_v1, env-driven `VMManagerConfig`) — NOT a fresh gcloud script. Added: (1) `GCPVMManager.start_soak_vm()` — reuses `_build_instance_config` recipe (machine/disk/network/Spot+max_run_duration/custom startup_script_path) but BYPASSES offload-only gating (firewall health-check, active-VM reuse, component model) that `start_spot_vm` enforces; appends DW key + pin as instance metadata (the recipe's `metadata` param is vestigial); uses `self.instances_client`+`_initialize_gcp_clients()` NOT `_get_instances_client` (that's a DIFFERENT earlier class). (2) Preemption Shield `battle_test/graceful_preemption.py` — SYNCHRONOUS git-safety STASH (clears stale index.lock) + child-worker halt + GCP-preempt metadata detect, wired at TOP of harness `_handle_shutdown_signal` (the harness already had SIGTERM partial-summary; shield adds working-tree corruption safety for the 30s Spot window). Gated `JARVIS_PREEMPTION_SHIELD_ENABLED` default-on. (3) `deploy/gcp_ouroboros_startup.sh` (metadata-startup-script: Docker+clone main+`.env` from metadata+compose up), `docker-compose.gcp.yml` (8-vCPU overlay), `scripts/ignite_sovereign_cloud_node.py` (launcher). 26 tests. **Spot scheduling cost-safety (in `_build_instance_config`): `provisioning_model=SPOT`+`instance_termination_action=DELETE`+`max_run_duration` dead-man's-switch.** **GCP billing was CLOSED (AmEx ••1004 declined May+Jun, $146.68 owed) → operator paid → reopened.** **LIVE: `gcloud services enable compute.googleapis.com`, then ignited `jarvis-ouroboros-soak-20260619-221059` (us-central1-a, e2-custom-8-16384 SPOT, DELETE@3h, IP 34.56.107.17) — RUNNING, booting.** GCP project=jarvis-473803 (#888774109345), zone us-central1-a, ADC=djamesr23@gmail.com. **CRITICAL OPS: the `.claude/worktrees/discoroute` worktree got HARD-RESET mid-session (reflog `reset: moving to HEAD`, all untracked files wiped + tracked edits reverted) — recreate+commit+PUSH-to-origin ATOMICALLY; never trust uncommitted worktree state.**

**CLOUD SOAK DIAGNOSIS + EXCEPTION TAXONOMY (2026-06-20, PR #69593 MERGED):** Live 8-core GCP Spot soak DISPROVED the compute-floor theory — the SAME `live_transport RuntimeError` on all 16 models appeared on 8 cores with DW healthy (curl `/v1/chat/completions` gpt-oss-120b = 200 in 0.78s from the container). Dynamic in-container patch (docker exec python to un-swallow the log + docker restart, no rebuild) revealed the swallowed message: **`RuntimeError: 'all_providers_exhausted:fallback_skipped:no_fallback_configured'`** — i.e. an OUR-side FSM exhaustion (DW yields no candidate + no Claude fallback under autarky) was MISCLASSIFIED as `live_transport`, which SEVERED the whole DW lane (Slice 73/83) + corrupted surface-health → one op's no-candidate became a lane-wide outage. **FIX #69593:** `dw_fault_taxonomy.is_fsm_exhaustion()` + `FailureSource.FSM_EXHAUSTED` (weight 0.0, sever-immune like GENERATION_TIMEOUT) + classifier carve-out + un-swallow the exc message in the dispatch-FAILED WARNING. 12 tests + 115 regression. **DEEPER UNRESOLVED ROOT (the actual state=applied blocker):** DW yields no candidate BEFORE the HTTP call — `TTFT=0`, no `chat/completions`, `Primary sem` not acquired on the wire. Ruled OUT: `is_available` (True, key len 46 present, no scrub since aegis off), `s2_predictive_budget` (fail-open, just emits). Short-circuit is INSIDE `DoublewordProvider._dispatch_internal`'s RT path (`_zw_cached_or_generate`→`_dw_inner`→`_dispatch_internal`→RT/batch); pinpointing the exact pre-HTTP abort line needs RUNTIME INSTRUMENTATION (add logging at each early-return), not source reading (too many slice-gates: 36/175/189/194/226). Also: the OperationAdvisor correctly BLOCKS the risky ops the autonomous loop self-selects (0% coverage + 99K-line unified_supervisor.py + 2-6AM) — need a blast-radius-1 seed defect injected to test the dispatch path. Cloud node `jarvis-ouroboros-soak-20260619-221059` runs OLD code+in-container-patch, auto-DELETEs at 3h. **This session's 6 merged PRs: #69588 catalog, #69589 autarky, #69590 aegis-stream, #69591 override-matrix, #69592 cloud-ignition, #69593 fsm-taxonomy.**

**CALIBRATION + TELEMETRY RUN (2026-06-20, PRs #69594 + #69595 MERGED = 8 PRs total this session):** #69594: `calibration_context.py` (formal Advisor bypass — env `JARVIS_CALIBRATION_MODE_ENABLED` + basename-match of `JARVIS_CALIBRATION_TARGET_PATH`, fail-closed, auditable CALIBRATION_OVERRIDE reason; greenlights ONLY the seed) + `tests/seed/test_seed_defect.py` (blast-radius-1 `_add` a-b bug) + reuse existing `dispatch_profiler` (JARVIS_DISPATCH_PROFILER_ENABLED) as the telemetry mesh. #69595: `JARVIS_LOG_LEVEL` authoritative override + `.dockerignore !tests/seed/` (the broad tests/ exclude dropped the seed from the image). **FSM TAXONOMY FIX VALIDATED LIVE on the 2nd cloud node**: `source=fsm_exhausted` (not live_transport) on all 16 models, lane NOT severed, exc un-swallowed. **STILL BLOCKED on seeing the short-circuit trace**: container root log level stays WARNING despite JARVIS_LOG_LEVEL=INFO + the override code being in the image — something in `launch_linux_prod.sh`→harness boot resets root to WARNING AFTER main()'s logging setup (OR the entrypoint bypasses main's logging path). So the dispatch_profiler op_summary/STAGE_ trace (INFO) is suppressed → can't yet pinpoint the pre-HTTP short-circuit. NEXT: find the WARNING-setter in the prod boot path (harness or launch_linux_prod.sh), force INFO authoritatively there, redeploy, capture STAGE_ trace (STAGE_HTTP_DISPATCH presence = pre-HTTP vs parse abort), fix short-circuit. Node `jarvis-ouroboros-soak-20260619-230622` (2nd; 1st deleted), auto-DELETE 3h. state=applied NOT reached — realistically several more ~15min deploy-debug cycles, each snag-prone (proven pattern).

**🎉 STATE=APPLIED ACHIEVED (2026-06-20, cloud node jarvis-ouroboros-soak-20260619-230622):** First flawless 8-core, GCP-Spot, pure-DW-autarky `state=applied` generative repair. op-019ee3e6 autonomously bumped `anthropic==0.75.0`→`0.111.0` in requirements.txt (dep-staleness detection), generated via pinned gpt-oss-120b over RT streaming, validate→gate→APPLY(75%)→COMPLETE(100%), LEDGER_TERMINAL state=applied, "[COMPLETE] Applied successfully". **THE UNLOCK was the force-RT fix (#69596)**: Slice-36/41 forced the DW BATCH path for STANDARD+Claude-disabled, but the batch SUBMITS (~600ms) then NEVER completes within budget → 180s STAGE_PROVIDER_GENERATE TimeoutError → fsm_exhausted on all 16 models. RT streaming (proven 0.78s via curl) was being SKIPPED. `JARVIS_DW_FORCE_BATCH_STANDARD_COMPLEX=0` + `JARVIS_DW_FORCE_BATCH_ON_CLAUDE_BREAKER=0` → RT path → generation works → a naturally-low-blast-radius op (requirements.txt) flowed through APPLY (the calibration seed wasn't even needed; CALIB never fired — real generation carried it). KEY DIAGNOSIS ENABLER: silent_boot.py routes INFO→debug.log file, only WARNING→stdout, so `docker logs` hid the dispatch_profiler STAGE_ trace — read `/app/.ouroboros/sessions/*/debug.log` IN-CONTAINER, not docker logs. **11 PRs this session** (#69588 catalog, #69589 autarky, #69590 aegis-stream, #69591 override-matrix, #69592 cloud-ignition, #69593 fsm-taxonomy, #69594 calibration+mesh, #69595 log-level+seed, #69596 force-RT[THE UNLOCK], #69597 seed-targeting, #69598 pytest). Open follow-ups: DW batch-retrieve-hang (submit ok, never completes — separate defect); aegis DW-streaming rupture (#69590 fixed leak, rupture validation pending); the deeper "why earlier ops yielded no candidate" was actually the batch-hang, now resolved.

**SOVEREIGN TRANSPORT MATRIX (2026-06-20, 3 more PRs → 14 total this session):** After state=applied (#69596 force-RT workaround), operator demanded removing the workaround + fixing batch + re-arming aegis. **#69599 batch race fix:** `_await_batch_result` awaited the webhook future (registry.wait, 3600s) FIRST + only polled on its exception → on a cloud node (no public ingress for DW's `batch.completed` callback) Tier-1 blocked the whole budget while the working poll (Tier-2) was never reached. PROVED DW batch completes in 11s (direct curl probe). Fix: RACE webhook vs poll, first-non-None wins, cancel loser. 4 tests. **#69600 dynamic transport router:** `payload_transport_heuristic.py` (gated `JARVIS_DW_DYNAMIC_TRANSPORT_ENABLED`) replaces the force-RT hardcode — localized fix→RT, ≥3 files/≥400 lines/heavy→BATCH; wired into `_slice36_should_force_batch` ONLY for claude-unavailable+healthy-stream (rupture slices 170/172/179 still batch a degraded stream); reuses `_max_target_line_count`. 7 tests. **#69602 AEGIS RE-ARM FAILED VALIDATION:** soak with `JARVIS_AEGIS_ENABLED=true` → every op fsm_exhausted, ZERO state=applied (regression). So PR #69590 fixed the upstream-response LEAK ("Unclosed connection") but the aegis DW-SSE path STILL breaks generation under live load (the rupture, not the leak) — reverted aegis OFF (the proven config). **The deeper AegisForward DW-SSE rupture is the #1 open follow-up (needs dedicated fix + soak).** OPS LESSON: after `gh pr merge --squash`, the local feature branch stays checked out + `git checkout -b origin/main` ABORTS on uncommitted overlay edits → commits land on the wrong (merged) branch + push fails / PR gets squash-conflicts. Fix: `git checkout -B <newbranch> origin/main` AFTER stashing, make the edit FRESH off origin/main, never carry already-squashed commits into a new PR. PROVEN-WORKING CLOUD CONFIG: aegis OFF + dynamic router ON + pin gpt-oss-120b + posture/semantic/surface-health off + DW autarky.

**LANGUAGE-REWRITE VERDICT (operator asked twice):** NO Go/Rust/ARM64 rewrite. O+V is I/O-bound (waits on remote DW generation — language speed irrelevant); the GIL starvation was a wiring bug (CPU work on the event loop), already mitigated + properly solved with thread/process pools. Ouroboros is a Python organism that SELF-MODIFIES Python (Iron Gate, ast.parse validate, change_engine) — a Rust body makes the self-development target incoherent. CPython already runs native ARM64 on the M1 (no x86 emulation; ast/numpy/onnxruntime are arm64-native); Docker overhead is the Linux VM, not the ISA. Real levers: autarky fix (done) + native Python on real Linux cores. A rewrite = person-years to NOT fix an I/O-bound self-modifying-Python system.
