---
title: Project Dw Phantom Eradication
modules: []
status: historical
source: project_dw_phantom_eradication.md
---

**Slice 185 — The phantom eradication (MERGED main `54001ec6df`, 2026-06-09).** The single most important DW finding of the whole 168-185 arc. Extends [[project_dw_economic_sovereignty]] + [[project_artifact_janitor_hygiene]].

**THE PHANTOM.** Deep live-telemetry research (driven by the operator's "trace, don't guess" discipline) found that **~50% of measured "DoubleWord RT instability" was a self-inflicted bug, not the vendor.** `doubleword_provider.py:2061` (`_dispatch_internal`) referenced `_effective_model` — a variable defined ONLY in `submit_batch` + `_generate_realtime`, NEVER in `_dispatch_internal`. Introduced in **Slice 175** (per-model forecasting threaded `model_id=_effective_model` into the `_slice36_should_force_batch` call). It threw `NameError` on every RT op, was caught, run through the vendor failure-classifier, matched no HTTP/stream regex, and fell into the catch-all `else → FailureSource.LIVE_TRANSPORT` — **blaming DW's network for our logic bug AND recording it to the surface-health ledger**, inflating the learned rupture rate ~2×. Live split in one soak window: 3 real `RuntimeError` (DW TransferEncoding tears) vs 3 `NameError` (ours). **9 slices of resilience engineering partly fought a phantom.** Every unit test passed while this shipped — only live-log exception-type extraction caught it.

**THE FIX (4 phases, 13 tests, 79 DW regression green).** P1: resolve in scope (`model_id=self._resolve_effective_model(context)`). P2: **strict-type exception segregation** (`dw_fault_taxonomy.is_internal_fault`) — Python logical errors (NameError/TypeError/AttributeError/KeyError/IndexError/ImportError/AssertionError + ValueError-without-status_code) bypass the vendor lane, are NEVER recorded to the ledger, and are RE-RAISED to crash loud (before any LIVE_TRANSPORT classification, candidate_generator ~3461); `JSONDecodeError` + structured `status_code` errors stay vendor-side. **The AI can never again blame the vendor for its own logic flaw.** P3: **adaptive latency governor** (`_dw_latency_favors_batch`) — ingests live RT p95 (`DwLatencyTracker`) + elects batch ONLY when `rt_p95 > batch_ttft × mult` (default 3×, batch ttft default 8s); NO hardcoded preference — if DW fixes RT latency, p95 drops + the organism routes back to RT with zero config change. Wired into `_slice36_should_force_batch` alongside cold-start (184). P4: **ledger wipe** (`dw_ledger_wipe.wipe_corrupted_dw_ledgers`, opt-in `JARVIS_DW_LEDGER_WIPE_ON_BOOT`) — purges `dw_surface_health.json` + `dw_threshold_calibration_*.json` at GLS.start so the post-fix soak relearns clean. Masters: `JARVIS_DW_LATENCY_GOVERNOR_ENABLED` default-TRUE.

**CLEAN-SOAK VERDICT (first uncontaminated measurement, `54001ec6df`):** ledger wipe FIRED (purged 1 file), **0 NameError ruptures, 0 INTERNAL_FAULT, 0 RuntimeError ruptures, 0 exhaustions** with real RT activity (8 chat/completions, 1 batch). CAVEAT (honest): small/short window (~1 dispatch in 150s, slow boot) — NOT proof DW's RT is perfectly stable; the real `RuntimeError` ruptures were genuine, just not hit in 150s. **A long-window soak is needed for DW's TRUE residual rupture rate** (left running in background to gather it).

**DW STRATEGIC STATE (post-185, the honest scorecard).** The instability is DW's; the reliability is ours to manufacture (deliberate cost-sovereignty tradeoff: DW ~30× cheaper than Claude, $0.10/$0.40 vs $3/$15). (1) ~50% of "instability" was the phantom — gone. (2) Remaining real DW RT instability is platform-wide (ruptures hit all 6 models equally — SSE-proxy/load issue, not model-specific) + RT is ~10× slower than batch (v31: RT TTFT p50 66.8s vs batch 4-8s) → batch-primary is justified on LATENCY alone, independent of reliability. (3) The latency governor routes dynamically; the immortal layer (180) + cold-start seal (184) ensure no op is ever lost. **Open question for the long soak: DW's true residual RuntimeError rate — answers "RT-adaptive probe vs batch-primary permanent."** Cold-start best-practice settled: DETERMINISTIC fail-safe (batch-first boot window, Slice 184), NOT ML (no data to forecast at T=0); ML belongs in steady-state (172-176) + a future cheap cross-session EWMA meta-learner, never the cold-start.

**Side-observations flagged in the clean soak (→ Slice 186):** `ControlPlaneStarvation lag_ms=1190` (blocking sync op strangling the event loop) + `LONG-TERM-MEMORY Query failed: Expected embeddings to be a list of floats` (ChromaDB type-cast failure — numpy/tensor not flattened to python floats).
