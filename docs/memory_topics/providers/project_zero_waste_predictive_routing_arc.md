---
title: Project Zero Waste Predictive Routing Arc
modules: [scripts/ouroboros_battle_test.py, docs/architecture/ZERO_WASTE_PREDICTIVE_ROUTING.md]
status: merged
source: project_zero_waste_predictive_routing_arc.md
---

**Zero-Waste Substrate & Predictive Routing Arc — PRD DRAFTED 2026-05-19, NOT STARTED.** Root problem (from Phase-1 INCONCLUSIVE / $2.00 burn): autonomous sensors cannibalized the budget before the swe_bench fixture ran; redundant tokens paid for; host-sleep clock-skew killed the run. Architectural fix (NOT bespoke scripts / NOT hard-throttle workarounds). Stale 529MB `.jarvis/swe_bench_pro/repo_cache` PURGED (git-safe, untracked). Awaiting operator per-slice go; honor [[feedback-no-preresult-euphoria]].

**Duplication-avoidance grounding (verified):** `prompt_cache.py` (`PromptCache`: in-mem LRU + TTL + capacity bound, SHA-256 key, `get/put`, `get_prompt_cache()` singleton, env `JARVIS_PROMPT_CACHE_{TTL_S,MAX_ENTRIES}`) — caches *assembled prompt text*, NOT responses. `admission_gate.py` + `admission_estimator.py` (`WaitTimeEstimator` EWMA `estimator_alpha`, `RecentDecisionsRing`, budget-covers-projected-wait gate). `sensor_governor.py` (posture-weighted per-sensor caps, emergency brake at `cost_burn_normalized>0.9`, quarantine machinery). `semantic_index.py` (fastembed→stdlib-tfidf embedding + cosine — already present). `cross_process_jsonl.flock_append_line` (persistence primitive). Battle `WallClockWatchdog` lives in `scripts/ouroboros_battle_test.py` (uses `time.time()`; deliberately wall-cap-authoritative under skew for NTP-jump/hung safety). The arc EXTENDS these; zero new parallel router/cache/budget substrate.

**S1 STATUS — SUBSTRATE LANDED, GATE NOT WIRED (PR #43170 merged → main e9bcb242ec, 2026-05-19).** `provider_response_cache.py` + 32 spine tests + PRD `docs/architecture/ZERO_WASTE_PREDICTIVE_ROUTING.md` shipped DORMANT (master `JARVIS_PROVIDER_RESPONSE_CACHE_ENABLED` default-FALSE → byte-identical until graduated). Composes prompt_cache (reuses `PromptCache._make_key`, AST-pinned no-parallel) + cross_process_jsonl. Exact-only v1 (SEMANTIC_HIT reserved); MAX_BYTES=256MiB; repo digest=HEAD+SHA(git diff HEAD) fail-CLOSED on undeterminable; gate `cached_or_generate(prompt,model,route,repo_root,produce)` fail-OPEN/never-raises. Smoke proved $0 reuse (2nd identical req → exact_hit, provider skipped, cost_usd=0.0). **REMAINING S1 (not done — explicit, no euphoria, NOT end-to-end):** wire the gate into `ClaudeProvider.generate` (providers.py:6392, multi-return tool-loop) + `DoubleWordProvider.generate` (doubleword_provider.py:874; prompt local finalized ~988, success return ~2452). Deferred from the substrate commit to give the two most correctness-sensitive hot-path methods a FOCUSED, REVIEWED restructure (body→`produce` thunk; key on the assembled prompt) — not a rushed tail-of-turn edit. Dormant master ⇒ unwired = correct. Awaiting operator: wire now (focused) vs review approach first. S2/S3 remain design-only.

**S1 — Zero-Waste ProviderResponseCache (compute reuse, $0.00 on hit).** Extend the `prompt_cache` substrate (reuse its exact LRU+TTL+bound discipline) into a response/trajectory cache. Key = SHA-256 of (canonicalized prompt + model + repo-state digest [git HEAD + dirty-hash]). Seam = pre-call gate in `providers.ClaudeProvider.generate` + `doubleword_provider.generate`: exact-hash hit → return cached trajectory, skip API, cost $0.00; semantic-similar tier (compose `semantic_index` embed+cosine, env-tunable threshold, default conservative, advisory) → optional reuse. **Memory bound for 16GB M1: LRU eviction by serialized-BYTE budget (not entry count — trajectories are large), env `JARVIS_PROVIDER_CACHE_MAX_BYTES` default conservative.** Persist across sessions via `cross_process_jsonl` (survives restart — "persistent local"). Master default-FALSE + graduation. AST pins: composes prompt_cache (no parallel LRU), byte-bounded, NEVER raises (cache miss/fault → normal provider path — fail-open), no hardcoded threshold. ~25 spine tests (exact-hit→$0 / miss→passthrough / byte-LRU evict under M1 bound / semantic tier / repo-state-change invalidates / fail-open / persistence roundtrip).

**S2 — Predictive Budget Preemption & Dynamic Routing.** Extend `admission_gate` with a `Forecasted_Cost` dimension (compose `admission_estimator` EWMA + [[project-predictive-provider-resilience]] TTFT forecaster + a token→USD cost model). Before dispatch: if `current_spend + forecasted_cost` approaches `session_budget` OR a higher-priority op is queued (compose UnifiedIntakeRouter priority queue + `urgency_router`), the gate **drives the EXISTING `sensor_governor` quarantine** to dynamically starve low-priority sensors (OpportunityMiner/DocStaleness) — NOT hard-throttle, NOT a new router. Composes the governor's existing emergency-brake/quarantine; adds only the forecast-vs-budget admission dimension + the priority-preemption signal. Master default-FALSE + graduation. AST pins: no parallel router/governor, composes admission_estimator+sensor_governor, forecast env-tunable (no hardcode), authority-asymmetric. ~25 spine tests (forecast≈budget→low-prio quarantined / high-prio-queued→preempts / under-budget→no-op / governor composed not duplicated / never starves a high-urgency op).

**S3 — Monotonic-resilient WallClockWatchdog.** NOT a naive `s/time.time/monotonic/` (Darwin `CLOCK_MONOTONIC` pauses during suspend — that IS the desired soak behavior). Fix: the *budget/liveness deadline* becomes monotonic-authoritative (host sleep cleanly pauses the budget, not a premature kill) while a SEPARATE absolute wall ceiling stays as a runaway backstop; classify skew — monotonic-stalled-while-wall-jumps = benign host-sleep (resume) vs monotonic-advancing-past-budget = real runaway (kill). Master/behavior-flag default preserves current safety; graduation flips monotonic-authoritative. AST pins: dual-clock (both monotonic+wall referenced), skew-classification present, NEVER raises into the kill path. ~15 spine tests (host-sleep simulated → no premature kill / true runaway → killed / forward-NTP-jump → still safe / backstop honored).

**Order (operator-directed):** S1 → S2 → then the intelligently-routed + cache-warm system NATURALLY re-executes Phase 1 (sensors starved, redundant tokens $0) — that re-run is operator-spend-gated, NOT part of this PRD. S3 can land in parallel (independent). Each slice: own branch (NEW arc, not OCA Slice N — OCA/git-index/sovereignty/cursor-agent-ban CLOSED, untouched), default-FALSE master, Phase-9-cadence graduation contract (§41.6 evidence row), AST pins, spine tests, NO bespoke test scripts. Non-goals: no new cache/router/budget substrate (extend only); no hard-throttle workaround; no capability/euphoria claims; no Phase-1 re-run/spend without explicit operator approval.
