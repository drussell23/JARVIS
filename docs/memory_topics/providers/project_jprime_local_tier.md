---
title: Project Jprime Local Tier
modules: [backend/core/prime_client.py, backend/core/ouroboros/governance/, scripts/smoke_jprime_local.py, scripts/smoke_jprime_exhaustion.py]
status: historical
source: project_jprime_local_tier.md
---

Activated the dormant Tier-2 **J-Prime** provider seat in the Ouroboros GLS loop as a free/no-quota fallback (DW kept dying on `terminal_quota` + Claude no credits → `all_providers_exhausted`). Three arcs, all gated default-OFF (byte-identical legacy when off), all reuse-first (operator's standing "no duplication" mandate — verified each PR).

**CRITICAL distinction:** This is NOT the real `jarvis-prime` repo (~/Documents/repos/jarvis-prime). That repo = the real "Mind": 11 self-hosted specialist LLMs (~40GB, Qwen2.5-Coder-7B for code, DeepSeek-R1, etc.) on a GCP "Invincible Node" VM, reached via `JARVIS_PRIME_URL` → `backend/core/prime_client.py::get_prime_client()` → passed into GLS as `prime_client` ctor arg. Our work instead occupies the SAME `PrimeProvider` seat with a LOCAL Ollama backend (Qwen2.5-Coder-3B on the M1). They coexist; precedence respected.

**Key files (all `backend/core/ouroboros/governance/`):**
- `local_inference_director.py` (NEW) — `LocalConfig`, `LatencyProfiler` (bounded adaptive timeout, hard ceiling `JARVIS_LOCAL_INFERENCE_TIMEOUT_MS` so a wedged 3B still trips), `LocalPrimeClient` (aiohttp-pooled :11434, drop-in for PrimeClient: `generate→PrimeResponse`, `_check_health→PrimeStatus`, `aclose`; `attach_governor`), `LocalLatencyLockup`/`LocalMemoryCritical`, `LocalInferenceDirector` (`memory_guard` reuses shared `MemoryPressureGate.get_default_gate()`→CRITICAL evicts `keep_alive:0`+dual gc.collect+yield then raises→cascades up; `enforce_memory`; `stop`), `build_local_prime_client`.
- `tiered_prime_client.py` (NEW, Phase 3.2) — `TieredPrimeClient(heavy,light)` composite drop-in: heavy(GCP)→light(Ollama) routing + health hysteresis FSM (`HeavyState`, reuses `recovery_policy.get_recovery_params`) + non-blocking recovery probe + speculative hedging (accept-first, `_cancel_and_drain` laggard) + valve override. `build_tiered_prime_client`, `tiered_enabled`.
- `candidate_generator.py` — `classify_local_failure` (terminal_lag_lockup + local_memory_critical → PRIMARY_DEGRADED); concurrency already enforced by existing `_jprime_sem` (Semaphore(1) @ ~4221) — DID NOT duplicate. Cascade catch @ ~4949 reused.
- `governed_loop_service.py` — gated injection: tiered branch when `JARVIS_JPRIME_TIERED_ENABLED` else Phase 3.1 local-only when `prime_client is None`; `stop()` releases the local session.

**Flags (all default-OFF / inert):** `JARVIS_LOCAL_PRIME_ENABLED` (master), `JARVIS_JPRIME_TIERED_ENABLED` (compose heavy+light), `JARVIS_TIERED_HEDGE_ENABLED` + `JARVIS_TIERED_HEDGE_WINDOW_MS`, `JARVIS_LOCAL_MODEL_{NAME,BASE_URL,KEEP_ALIVE_SECONDS}`, `JARVIS_MEMORY_PRESSURE_GATE_ENABLED` (gate the valve honors).

**PRs (ALL MERGED to main):** Phase 3 #69571 (local tier), 3.1 #69572 (live memory governor), 3.2 #69573 (`tiered_prime_client.py` — GCP-heavy+local-light composite, health-FSM+hedging, gated `JARVIS_JPRIME_TIERED_ENABLED`), 3.3 #69574 (`exhaustion_interceptor.py` + `topological_file_pruner.py` — runtime `all_providers_exhausted` interceptor: gated `JARVIS_JPRIME_LASTRESORT_ENABLED`, routes to local with Oracle-degree-centrality file prune + `EXHAUSTION_HANDOFF_TRIGGERED` SSE beacon; reuses existing exhaustion catch + dw_transport_recovery/FSM, NO new probe loop). ADD/plan: `docs/superpowers/specs|plans/2026-06-18-jprime-local-cascade-phase3*`.

**Phase 3.4 (PR #69575, OPEN):** `local_daemon_governor.py` — `LocalDaemonGovernor`: autonomous zero-touch Ollama daemon lifecycle. JIT-boot (`brew services start ollama` + /api/tags health-verify) when local tier enabled; flush (keep_alive:0, reuses `_evict_model`) + STOP on idle/GLS.stop. SAFETY: gated `JARVIS_LOCAL_DAEMON_GOVERNOR_ENABLED` default-OFF + OWNERSHIP-TRACKED (`_owned` single-assignment — only stops a daemon IT started; operator-started Ollama flushed-not-killed) + Darwin-guarded + fail-soft + bounded (`JARVIS_LOCAL_HEALTH_TIMEOUT_S` default 5s). Wired into GLS boot+stop. ALSO: smoke scripts FORMALIZED into `chaos_simulation_fabric.py` (`ollama_available`, `run_local_prime_warm_standby_check`, `run_in_flight_exhaustion_handoff_check`) + `test_local_prime_chaos_hardware.py` @skipif(no Ollama) — root smoke scripts REMOVED (don't recreate them). `jprime_lifecycle_controller.py` is a health/restart-FSM (NOT OS-process mgmt) — didn't duplicate it.

**ANSWER to "DW exhausted → Ollama picks up?":** Pre-3.3 it was health/route-conditional (GLS:4229 static role alloc). Post-3.3 (flag on) it's UNCONDITIONAL on every route — the interceptor at the single exhaustion boundary (`candidate_generator.py::generate()` existing catch) routes to local before the panic raise. Loop can never die on upstream exhaustion.

**LIVE-VALIDATED (2026-06-18, native Ollama — Docker on M1 has NO Metal passthrough → CPU-only):** `scripts/smoke_jprime_local.py` (Phase 3/3.1): cold 14.6s / warm 2.1s, health AVAILABLE, memory_guard CRITICAL→evict+refuse ✓. `scripts/smoke_jprime_exhaustion.py` (Phase 3.3): induced all_providers_exhausted → topological prune kept central file only → live Ollama absorbed → beacon in real broker ✓ → unhealthy re-raises ORIGINAL exhaustion ✓. Both SMOKE PASS. (Both smoke scripts untracked — not committed.)

**To actually activate:** `brew services start ollama` + `ollama pull qwen2.5-coder:3b` + `JARVIS_LOCAL_PRIME_ENABLED=true` (+ `JARVIS_JPRIME_TIERED_ENABLED=true` + `JARVIS_PRIME_URL=<node>` for the GCP-heavy tier alongside). Reactor-Core CANNOT fix DW quota/raw-quality — see [[project_slice235_validation_sixth_layer]] for the DW selector history.
