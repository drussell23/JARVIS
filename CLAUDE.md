# CLAUDE.md -- JARVIS Trinity AI Ecosystem

**Repository**: [github.com/drussell23/JARVIS-AI-Agent](https://github.com/drussell23/JARVIS-AI-Agent)
**Author**: Derek J. Russell -- RSI/AGI Researcher & Trinity Architect

## What This Is

JARVIS is the Body of a tri-partite AGI OS. This repo contains the macOS integration layer (screen capture, voice, keyboard automation), the unified supervisor (102K-line monolithic kernel), and the Ouroboros self-development governance engine. All model references are resolved from `brain_selection_policy.yaml` -- zero hardcoded model names.

## Architecture at a Glance

```
JARVIS (Body) <--HTTP/WS--> J-Prime (Mind) <--sandbox--> Reactor Core (Soul)
     |
     +-- unified_supervisor.py (102K lines, Zones 0-7)
     +-- backend/core/ouroboros/ (autonomous self-development)
     +-- backend/vision/ (VLA pipeline, OCR, frame server)
     +-- backend/ghost_hands/ (focus-preserving UI automation)
     +-- backend/voice/ (voice I/O, wake word, TTS)
     +-- backend/core_contexts/ (5 execution contexts: Executor, Architect, Developer, Communicator, Observer)
     +-- backend/neural_mesh/ (22 legacy agents, Strangler Fig fallback)
```

## Key Conventions

- **Async-first**: All I/O uses `asyncio`. No blocking calls on the event loop.
- **No hardcoded models**: All model names come from `brain_selection_policy.yaml` or env vars.
- **Env-var driven config**: Every tunable parameter reads from environment variables with sensible defaults.
- **Python 3.9+**: No `asyncio.timeout` (3.11+) -- use `asyncio.wait_for` everywhere.
- **`from __future__ import annotations`**: Required in all files for forward reference support.

## Ouroboros Pipeline

The self-development engine runs an 11-phase governance pipeline:

```
CLASSIFY -> ROUTE -> [CONTEXT_EXPANSION] -> [PLAN] -> GENERATE -> VALIDATE -> GATE -> [APPROVE] -> APPLY -> VERIFY -> COMPLETE
```

### Provider Chain (3-Tier Failback)

| Tier | Provider | Cost | Notes |
|------|----------|------|-------|
| 0 | DoubleWord 397B (3-tier: RT SSE + webhook + adaptive poll) | $0.10/$0.40/M | 16384 max_tokens, RT default, preferred |
| 1 | Claude (Anthropic API) | $3/$15/M | Extended thinking + prompt caching, 60s fallback cap |
| 2 | J-Prime (GCP self-hosted) | VM cost only | When available |

### Urgency-Aware Provider Routing (Manifesto §5)

Deterministic routing based on signal urgency + source + task complexity. Stamped at ROUTE phase by `UrgencyRouter` (`urgency_router.py`). No LLM calls — pure code, <1ms.

| Route | Strategy | Cost | When |
|-------|----------|------|------|
| IMMEDIATE | Claude direct, skip DW | ~$0.03/op | Critical urgency, voice commands, test failures, runtime health |
| STANDARD | DW primary → Claude fallback | ~$0.005/op | Normal-priority moderate ops (default cascade) |
| COMPLEX | Claude plans → DW executes | ~$0.015/op | heavy_code, multi-file architectural changes |
| BACKGROUND | DW only, no Claude fallback | ~$0.002/op | OpportunityMiner, DocStaleness, TODOs, backlog |
| SPECULATIVE | DW batch fire-and-forget | ~$0.001/op | IntentDiscovery, DreamEngine pre-computation |

### Timeout Enforcement (Route-Aware)

- **IMMEDIATE generation**: 60s + 5s grace (fast reflex — accounts for Venom tool rounds)
- **STANDARD generation**: 120s + 5s grace
- **COMPLEX/BACKGROUND generation**: 180s + 5s grace
- **Fallback provider**: Hard cap at 60s (`_FALLBACK_MAX_TIMEOUT_S`)
- **Tier 1 reserve**: 25s minimum (not 45s -- reduced to avoid starving Tier 0)
- **DW poll interval**: 5s (not 15s)
- **pytest (TestWatcher)**: 30s timeout
- **IMMEDIATE extended thinking**: Disabled by default (env: `JARVIS_THINKING_BUDGET_IMMEDIATE`, default 0). Route check fires before complexity checks.
- **IMMEDIATE → STANDARD demotion**: After Claude exhausts retries on IMMEDIATE, demotes to STANDARD (DW primary) for one more attempt

### Worker Pool

- **BackgroundAgentPool**: 3 workers (env: `JARVIS_BG_POOL_SIZE`), PriorityQueue (16 slots)
- **Priority ordering**: IMMEDIATE(1) > STANDARD/COMPLEX(3) > BACKGROUND(5) > SPECULATIVE(7)
- **Venom tool loop**: Enabled for IMMEDIATE/STANDARD/COMPLEX routes; skipped for BACKGROUND/SPECULATIVE (cost optimization)

### 16 Autonomous Sensors

TestFailure, VoiceCommand, OpportunityMiner, CapabilityGap, Scheduled, Backlog, RuntimeHealth, WebIntelligence, PerformanceRegression, DocStaleness, GitHubIssue, ProactiveExploration, CrossRepoDrift, TodoScanner, CUExecution, IntentDiscovery.

All flow through `UnifiedIntakeRouter` with priority queuing, deduplication, and WAL persistence.

### Key Subsystems

- **PlanGenerator** (`plan_generator.py`): Model-reasoned implementation planning (PLAN phase) -- structured JSON plan (schema plan.1) with approach, ordered changes, risk factors, test strategy. Injected into GENERATE prompt.
- **SemanticTriage** (`semantic_triage.py`): Pre-generation filter -- classifies NO_OP/REDIRECT/ENRICH/GENERATE before expensive generation
- **CommProtocol** (`comm_protocol.py`): 5-phase observability -- INTENT -> PLAN -> HEARTBEAT -> DECISION -> POSTMORTEM
- **Phase B GENERAL Subagent — INFRASTRUCTURE GRADUATED 2026-04-20** (`agentic_general_subagent.py` + `semantic_firewall.py` + `subagent_orchestrator.py::dispatch_general`): The 4th and final Phase B subagent type, graduated in **infrastructure-only** mode per explicit architectural phasing — deterministic perimeter is production-active; LLM-driven execution body deferred to Step 2. **Semantic Firewall (Manifesto §5)**: 11 injection-pattern detectors (bracket/XML system tags, `<CRITICAL_SYSTEM_DIRECTIVE>`, `ignore previous instructions`, 5 credential shapes — sk-*, sk-ant-*, AKIA*, ghp_*/gho_*/ghs_*, xox[bp]-*, PEM) + length cap + sanitize-on-reject. 5 mandatory boundary conditions validated at dispatch (`operation_scope` concreteness — rejects `**`/`*`/`/`/absolute-paths-outside-tmp; `max_mutations` int ≥ 0, capped at `JARVIS_GENERAL_MAX_MUTATIONS_CEIL=10`; `allowed_tools` ⊂ 17-entry `_KNOWN_TOOLS` classification; `invocation_reason` non-empty ≤ 200 chars; `parent_op_risk_tier` ≥ `NOTIFY_APPLY` floor). Cross-field consistency: `max_mutations > 0` requires ≥ 1 mutating tool in allowed_tools AND parent ≥ NOTIFY_APPLY (double-pin). **Two-layer enforcement**: Layer 1 at `SubagentOrchestrator.dispatch_general()` (recursion ban + sanitize + boundary validate); Layer 2 at `AgenticGeneralSubagent.general()` runtime re-verification for callers that bypass dispatch_general and construct SubagentRequest directly. **Output quarantine fence**: every execution output wrapped in `<general_subagent_output untrusted="true" sub_id='X'>...</general_subagent_output>` — downstream consumers (orchestrator, ConversationBridge, memory) preserve the untrusted-trust boundary. **Recursion ban**: `object.__setattr__(parent_ctx, "_within_general_subagent", True)` stamp + `is_within_general_subagent()` detector prevents any nested GENERAL dispatch. **Hard-kill wrapper**: `asyncio.wait({task}, timeout=llm_budget_s+30.0)` pattern shipped ready for Step-2 LLM driver. **Execution body**: ships as deterministic STUB (`status=stub`, `NOT_IMPLEMENTED_NEEDS_LLM_WIRING` placeholder) when `llm_driver=None` (default factory). Elevated observability — every GENERAL exec emits an INFO line with sub_id / goal / scope / tools / max_mutations / parent_risk_tier. Regression spine: `tests/governance/test_general_subagent.py` (48 tests — firewall pattern rejection × 7, sanitization integrity × 1, boundary condition rejection × 19, valid-invocation pass-through × 1, dispatch integration × 4, executor behavior × 5, contract + factory × 3). **Step 2 scope-out** (documented in `memory/project_phase_b_step2_deferred.md`): wire a restricted Venom tool loop to the `llm_driver` seam; pressure the existing firewall + `allowed_tools` + `max_mutations` gates under real LLM load; graduate per a 3-consecutive-clean-session arc. Infrastructure graduation does **not** require model-driven tests — the perimeter is the substrate, the driver is the separate discipline.
- **Phase B — COMPLETE 2026-04-20** (all 4 subagent types graduated): EXPLORE (2026-04-18, Phase 1), REVIEW (2026-04-20, observer-only contract), PLAN (2026-04-20, shadow observer + parallel-edge exploit both default-on, 4× wall-clock speedup proven), GENERAL (2026-04-20, infrastructure-only with Semantic Firewall production-active + LLM-driver deferred to Step 2). **Combined Phase B test surface**: 28 REVIEW + 6 correlation + 38 PLAN + 18 exploit + 48 GENERAL = **138 Phase B regression tests**, all green. **Combined production mandate fulfilled**: EXPLORE cartography (Manifesto §1 Unified Organism boundary), REVIEW verdict (§6 Execution Validation), PLAN DAG + exploit fan-out (§2 DAG + §3 Disciplined Concurrency), GENERAL firewall (§5 Semantic Firewall). Step 2 / Phase C surface is now a clean sheet — the infrastructure layer is mathematically complete. Next deferred items enumerated in `memory/project_phase_b_step2_deferred.md` — primary target is GENERAL LLM driver; secondary are PLAN Step-2 LLM DAG refinement + sequential-DAG topological scheduling in plan_exploit + ExecutionGraphScheduler integration for in-repo DAGs + REVIEW mutation-runner activation.
- **Phase B PLAN Subagent — GRADUATED 2026-04-20** (`agentic_plan_subagent.py` + `dag_validator.py` + `plan_exploit.py` + `orchestrator.py::_run_plan_shadow`): Two-flag graduation — shadow observer (`JARVIS_PLAN_SUBAGENT_SHADOW`, default **`true`**) + parallel-edge exploit consumer (`JARVIS_PLAN_EXPLOIT_ENABLED`, default **`true`**). **Producer**: `AgenticPlanSubagent` deterministically partitions multi-file ops into independent DAG units (one unit per target file, no dependency edges, owned_paths disjoint by construction), validates via `dag_validator.validate_plan_dag` (6 invariants: unit_id uniqueness / dependency reference / acyclicity / reachability / owned-path disjointness / test-coverage-or-rationale), emits execution_graph 2d.1 tuple-of-tuple with deterministic sha256[:16] `graph_id`. **Shadow hook**: `_run_plan_shadow` runs post-legacy-PlanGenerator in the PLAN phase; stashes the DAG on `ctx.execution_graph` **without overwriting `ctx.implementation_plan`** (legacy flat plan remains authoritative); emits `[PLAN-SHADOW] op=X status=S dag_units=N edges=M roots=R parallel_pairs=P validation_valid=V graph_id=H duration_ms=D (observer — FSM proceeds regardless)`. **Exploit consumer** (`plan_exploit.try_parallel_generate`): first production consumer of `ctx.execution_graph`. Gate refuses: flag off / `is_read_only=True` / BG/SPEC route (unit-economics seal) / no execution_graph / single unit / `edges>0` (fully-parallel only this slice) / per-unit exception / per-unit is_noop / empty candidates / wall-clock timeout. On pass, fans out per-unit scoped generate() calls via `asyncio.gather` bounded by `asyncio.Semaphore(min(declared_concurrency, unit_count, hw_ceiling))` where `hw_ceiling` defaults to 4 (env-tunable via `JARVIS_PLAN_EXPLOIT_MAX_CONCURRENCY`). Merges per-unit `GenerationResult`s into single multi-file composite (`files:[...]` shape the existing multi-file APPLY path already consumes). Emits `[PLAN-EXPLOIT] op=X status=completed dag_units=N concurrent_streams=K serial_fallback=False merged_files=M total_cost_usd=C wall_ms=W per_unit_avg_ms=A (§3 Disciplined Concurrency — N files generated in parallel)` on success, or `status=fallback serial_fallback=True reason=<R>` on any guard failure. **Empirical graduation evidence** (2026-04-20): (1) Live session `bt-2026-04-20-214825` captured `[PLAN-SHADOW] dag_units=4 edges=0 roots=4 parallel_pairs=6 validation_valid=True graph_id=77b6e8d2d4e2f26c duration_ms=289` on the Phase B cartography op — 289ms deterministic DAG vs ~2m15s legacy PlanGenerator + Claude failure. (2) Synthetic harness (`/tmp/claude/plan_exploit_proof.py`) captured `[PLAN-EXPLOIT] status=completed dag_units=4 concurrent_streams=4 serial_fallback=False merged_files=4 wall_ms=1001 per_unit_avg_ms=1000` — **4× wall-clock speedup** proven on 4-unit parallel DAG. (3) Live session `bt-2026-04-20-222822` captured `[PlanExploit] skip reason=read_only_op` fallback trace on read-only cartography — safety gate empirically proven. **Regression spine**: 18 exploit tests + 38 plan_subagent tests = 56 green. **Manifesto §2 (DAG) + §3 (Disciplined Concurrency)** satisfied: the serialization bottleneck on multi-file ops is shattered; parallel GENERATE is the default behavior when conditions are safe, legacy single-stream is the fallback when they aren't. **Scope-outs parked** for next slice: sequential DAG scheduling (topological sort + wave execution on `edges>0` DAGs); LLM-driven dependency-edge derivation (Step-2 seam in AgenticPlanSubagent); ExecutionGraphScheduler integration for in-repo DAGs.
- **Phase B REVIEW Subagent — GRADUATED 2026-04-20** (`agentic_review_subagent.py` + `orchestrator.py::_run_review_shadow`): Post-VALIDATE observer hook that runs a parallel `AgenticReviewSubagent` over each candidate file, rendering a deterministic verdict (`approve` / `approve_with_reservations` / `reject`) via SemanticGuardian's 10-pattern AST battery + function-name loss signal + optional mutation-testing hook. Wire-up graduated from Slice 1a scaffolding through Slice 1b hardening (fix latent `_is_critical_path` ImportError, drop dead `hashlib` import, scope out CFG + exported-symbol-delta as future enhancements, clarify read-only contract as structural isolation not Rule 0d reuse, build 6-test correlation harness) to Session 1 live integration (25ms observer emission, FSM proceeds regardless) and Session 2 Path B synthetic REJECT proof (credential_shape_introduced poison → aggregate=REJECT telemetry). Flag `JARVIS_REVIEW_SUBAGENT_SHADOW` now defaults **`true`** — set to `false` to opt out. Observer contract remains strict: verdict emits telemetry only; FSM is NOT gated by REVIEW output (promoting REVIEW into authority-carrying gate logic is a separate slice with its own graduation arc). Latent case-mismatch bug surfaced during Session 2 Path B — `_run_review_shadow` aggregation had been comparing verdict values against uppercase literals while the actual constants from `subagent_contracts` are lowercase, silently reclassifying every REJECT as APPROVE in the aggregate log. Fixed via lazy-import of the `REVIEW_VERDICT_*` constants with 2 regression tests pinning `aggregate=REJECT` + `aggregate=APPROVE_WITH_RESERVATIONS` emission shapes. Regression spine: 22 tests in `tests/governance/test_review_subagent.py` + 6 tests in `tests/governance/test_review_subagent_correlation.py` — 28/28 green. Manifesto §6 (Execution Validation) satisfied without violating §3 (Disciplined Concurrency) — REVIEW runs in parallel to VALIDATE, never blocks the main generation thread. Next Phase B slice: PLAN subagent (§2 DAG) per `project_phase_b_subagent_roadmap.md`.
- **Gap #4 campaign — CLOSED 2026-04-20** (9 sensors migrated, 4 polling-by-design retained): Ouroboros intake layer now operates on true reflexive event streams (Manifesto §3) for every high-value signal source. **Migrated to event-primary** (in order): GitHubIssueSensor (webhook), TestFailureSensor (FS events), DocStalenessSensor (GitHub push webhook), CrossRepoDriftSensor (GitHub push webhook, first fan-out dispatcher), PerformanceRegressionSensor (CI webhook, first multi-surface), TodoScannerSensor (FS events, empirically graduated via `bt-2026-04-20-082519`), OpportunityMinerSensor (FS events + layered storm-guard: per-file debounce + global burst circuit breaker), BacklogSensor (FS events, tight filename filter), IntentDiscoverySensor (conversation-bus events + silence-window + inference cooldown + hourly cap — first non-filesystem event source). **Retained on polling — deterministic architectural choice** (no natural push source or timer-driven by design): RuntimeHealth (daily), WebIntelligence (daily external), ProactiveExploration (7200s speculation), Scheduled (IS the cron). **Already event-driven by construction** (no migration needed): CapabilityGap (GapSignalBus asyncio.Queue consumer), CUExecution (direct async callback from ActionDispatcher). The remaining 4 polling sensors are not a gap-#4 failure — forcing them into an artificial event architecture would violate Manifesto §1 (Tri-Partite Microkernel / Boundary Principle). All 9 migrated sensors are graduated (default `true`) and fail-safe (opt-out via `=false`).
- **EventChannelServer + webhook-primary GitHub intake** (`event_channel.py` + `intake/sensors/github_issue_sensor.py`): Closes gap #4 at the GitHub surface. When `JARVIS_GITHUB_WEBHOOK_ENABLED=true` (default `true`, graduated 2026-04-20 after 3-session arc; set to `false` to opt out), GitHub pushes `issues` webhooks to `POST /webhook/github` and `EventChannelServer._handle_github` short-circuits to `GitHubIssueSensor.ingest_webhook(payload)` — zero polling on the hot path. Ingestion produces an envelope shape-identical to the poll path (`source="github_issue"`, same evidence fields), runs through the same `_seen_issues` + `_issue_cooldown_active` gates, and is indistinguishable to downstream consumers. Poll loop demotes to 15-minute fallback (`JARVIS_GITHUB_ISSUE_FALLBACK_INTERVAL_S`, default 900s) — catches dropped webhooks rather than driving intake. `IntakeLayerService._maybe_start_event_channel_server()` is the authoritative activation point (runs after all sensors start, never crashes intake on failure). `/channel/health` exposes `github_issue_sensor.{wired, webhook_mode, last_webhook_at, webhooks_emitted, webhooks_ignored}` for operator-visible convergence. `sensor.ingest_webhook` runs inside `asyncio.wait_for(timeout=30s)` so no HTTP worker can hang. Regression spine: `tests/governance/intake/sensors/test_github_issue_sensor_webhook.py` (27 tests, Slice 1 mechanism) + `tests/governance/intake/test_event_channel_activation.py` (5 tests, Slice 2 HTTP round-trip). Master switch **graduated 2026-04-20** (default `true`) — all 4 gap-#4 sensor flags flipped together after a 3-session battle-test arc (S2 `bt-2026-04-20-064303` + S3 `bt-2026-04-20-065229` both delivered webhook-primary traffic with zero gap-#4 regressions; S1 proved boot-wiring).
- **FS-event-primary TestFailureSensor** (`intake/sensors/test_failure_sensor.py` + `intake/fs_event_bridge.py`): Closes gap #4 for the test-failure surface by routing signals through `TrinityEventBus` pub/sub instead of a 30-second pytest poll. When `JARVIS_TEST_FAILURE_FS_EVENTS_ENABLED=true` (default `true`, graduated 2026-04-20 after 3-session arc; set to `false` to opt out), `TestFailureSensor.subscribe_to_bus(bus)` registers an `fs.changed.*` handler — `.jarvis/test_results.json` changes trigger structured consumption via the ouroboros_pytest_plugin (no subprocess spawn), `*.py` changes trigger a debounced (2s) subprocess pytest run. Poll demotes to `JARVIS_TEST_FAILURE_FALLBACK_INTERVAL_S` (default 600s = 10min) whose job is to catch missed FS events. When the flag is off the sensor is a **pure-poll** sensor at the legacy 30s cadence and `subscribe_to_bus` is a logged no-op — zero silent regression. Manifesto §3 (Disciplined Concurrency — true parallel event streams): pytest only runs when code actually changes, eliminating the thermodynamic waste of running every 30s regardless. Counters: `_fs_events_handled` / `_fs_events_ignored` advance inside `_on_fs_event` for convergence tracking. Subscription failures are caught locally (intake boot never regresses). Regression spine: `tests/governance/intake/sensors/test_test_failure_sensor_fs_events.py` (10 tests) + `ov_smoke --check fs_event_routing` (real `TrinityEventBus` publish → handler end-to-end). This is the second sensor migrated (first was `GitHubIssueSensor`) and proves the gap #4 pattern generalizes across event-source types — HTTP webhook (GitHub) **and** in-process pub/sub (FS events).
- **GitHub push → DocStalenessSensor** (`intake/sensors/doc_staleness_sensor.py` + second `_handle_github` short-circuit in `event_channel.py`): Closes gap #4 for the documentation-staleness surface. When `JARVIS_DOC_STALENESS_WEBHOOK_ENABLED=true` (default `true`, graduated 2026-04-20 after 3-session arc; set to `false` to opt out), GitHub `push` webhooks arriving at `POST /webhook/github` short-circuit to `DocStalenessSensor.ingest_webhook(payload)` — additive to the existing FS-event path (unchanged). The sensor extracts `commits[].added+modified` files filtered to `.py` under watched scan paths; one or more matches triggers `scan_once()` (existing `_seen_findings` dedup handles overlap with FS + poll). Poll demotes to `JARVIS_DOC_STALENESS_FALLBACK_INTERVAL_S` (default 21600s = 6h) — lowest-priority sensor, so the fallback cadence is coarser than TestFailure's. The EventChannelServer activation in `IntakeLayerService._maybe_start_event_channel_server` now unions both flags (`JARVIS_GITHUB_WEBHOOK_ENABLED` OR `JARVIS_DOC_STALENESS_WEBHOOK_ENABLED`) and passes only the sensors whose flag is on — mixed-mode operation works (one sensor on, the other off). `/channel/health` exposes a parallel `doc_staleness_sensor.{wired, webhook_mode, last_push_at, pushes_emitted, pushes_ignored}` sub-object alongside the GitHub-issues counters. Regression spine: `tests/governance/intake/sensors/test_doc_staleness_sensor_webhook.py` (15 tests) + `ov_smoke --check doc_staleness_push_webhook` (real HTTP `push` round-trip). This is the third sensor migrated and proves the pattern scales to **multiple sensors on the same HTTP receiver with independent flags** — the EventChannelServer is now a dispatcher, not a per-sensor construct.
- **GitHub push → CrossRepoDriftSensor (fan-out dispatcher)** (`intake/sensors/cross_repo_drift_sensor.py` + `EventChannelServer._collect_active_push_handlers` / `_invoke_push_handler` in `event_channel.py`): Closes gap #4 for the contract-integrity surface AND upgrades the push dispatcher from short-circuit to **parallel fan-out**. When `JARVIS_CROSS_REPO_DRIFT_WEBHOOK_ENABLED=true` (default `true`, graduated 2026-04-20 after 3-session arc; set to `false` to opt out), `CrossRepoDriftSensor.ingest_webhook(payload)` intersects `commits[].added+modified` with `_WATCHED_PATHS` (derived from `_CONTRACT_FILES` + `_PROTOCOL_CHECKS` — `op_context.py`, `intent_envelope.py`, `mind_client.py`, `prime_client.py`, `docs/cross-repo-contract.md`). Any match triggers an immediate `scan_once`; a push that touches no watched file is a pure no-op (drift is by definition a function of watched-contract changes). Poll demotes to `JARVIS_CROSS_REPO_DRIFT_FALLBACK_INTERVAL_S` (default 14400s = 4h). **Architectural change**: `_handle_github`'s `push` branch was rewritten from an `if/elif` short-circuit (first match wins, subsequent handlers starved) to a fan-out dispatcher — `_collect_active_push_handlers()` returns `(name, sensor)` tuples for every wired sensor whose flag is currently on, and `_invoke_push_handler()` runs each under a 30s `asyncio.wait_for` inside `asyncio.gather`, so one slow sensor cannot starve the others. Both DocStaleness and CrossRepoDrift fire on a single push when both flags are on. Per-request flag re-check means runtime env flips work without restart. Stable telemetry: `[EventChannel] github/push fan-out ref=R fired=[A,B] emitted=[A] duration_ms=N`. `/channel/health` gains a parallel `cross_repo_drift_sensor` sub-object. Regression spine: `tests/governance/intake/sensors/test_cross_repo_drift_sensor_webhook.py` (11 tests) + two new fan-out integration tests in `test_event_channel_activation.py` (independent-flags + both-flags-on) + `ov_smoke --check push_fan_out`. This is the fourth sensor migrated and proves the pattern supports **multiple sensors per event** — the dispatcher is now a true N:1 dispatcher, not a first-match short-circuit.
- **FS-event-primary TodoScannerSensor** (`intake/sensors/todo_scanner_sensor.py` + `intake/fs_event_bridge.py`): Closes gap #4 for the unfinished-work detection surface. When `JARVIS_TODO_FS_EVENTS_ENABLED=true` (default `true`, graduated 2026-04-20 via behavioral proof — `[TodoScanner] Found 1 markers: TODO=1 (1 emitted, 1 trigger-tagged)` fired 8s after probe `.py` creation in session `bt-2026-04-20-082519` once the `FileWatchGuard` narrow-scope transport fix landed; set to `false` to opt out), `TodoScannerSensor.subscribe_to_bus(bus)` registers an `fs.changed.*` handler on `TrinityEventBus` — each `.py` file change triggers a **single-file** `scan_file` instead of waiting for the 24-hour whole-tree sweep. Events outside watched extensions, deletions, and files under `_SKIP_DIRS` (`venv`/`__pycache__`/`.worktrees`/...) bump `_fs_events_ignored`; eligible `.py` changes bump `_fs_events_handled`. Poll demotes to `JARVIS_TODO_FALLBACK_INTERVAL_S` (default 21600s = 6h — safety-net cadence matching DocStaleness) when flag is on. When flag is off the sensor is a **pure-poll** sensor at the legacy 24h cadence and `subscribe_to_bus` is a logged no-op — zero silent regression. Subscription failures are caught locally. Reuses the same `TrinityEventBus` pub/sub transport that TestFailureSensor + DocStalenessSensor + CrossRepoDriftSensor already rely on; this slice does not add any new infrastructure, just wires a new sensor into the existing event spine. Regression spine: `tests/governance/intake/sensors/test_todo_scanner_fs_events.py` (11 tests: flag helper, init mode capture, subscribe gate both directions + failure case, 4 event-routing cases + counters, interval gate). Master switch **graduated 2026-04-20** (default `true`) after the `FileWatchGuard` narrow-scope transport fix proved end-to-end OS→bus→sensor delivery empirically; the 3-session bureaucratic arc was skipped per Manifesto §6 (no redundant exploration when physics is proven).
- **Conversation-event-primary IntentDiscoverySensor** (`intake/sensors/intent_discovery_sensor.py` + `conversation_bridge.py` turn-observer hook): Closes gap #4 for the human↔autonomous-action surface. First gap-#4 sensor whose event source is **not** `fs.changed.*` — intent comes from the conversation bus, so ConversationBridge gained a lightweight fire-and-forget observer registry (`register_turn_observer` / `unregister_turn_observer`, invoked from `record_turn` after admit with exception-swallowing). Bridge authority invariant is preserved — observers are advisory fan-out, not authority-carrying consumers. When `JARVIS_INTENT_DISCOVERY_EVENTS_ENABLED=true` (default **`true`**, graduated 2026-04-20 as part of the gap-#4 campaign close-out; set to `false` to opt out), `IntentDiscoverySensor.subscribe_to_bridge(bridge)` registers `_on_turn` on the bridge; each turn bumps a monotonic `_last_turn_ts` and the `_events_received` counter. **Inference is NOT fired in the observer** — that's the job of the async `_silence_window_loop`, which wakes every `JARVIS_INTENT_DISCOVERY_CHECK_INTERVAL_S` (default 5s) and evaluates four storm-guard constraints (first-failure-wins, each bumps its own `_sw_*` counter): **(1) no_turn_yet** — at least one turn must have arrived; **(2) silence_not_met** — `now - _last_turn_ts >= JARVIS_INTENT_DISCOVERY_SILENCE_S` (default 30s) ensures we never synthesize intent mid-sentence; **(3) cooldown_active** — `now - _last_inference_ts >= JARVIS_INTENT_DISCOVERY_COOLDOWN_S` (default 300s = 5min) is the hard floor between two DW inference calls regardless of how chatty the human is; **(4) hourly_cap_hit** — a rolling 3600s window (`_hourly_fires` list, pruned per-evaluation) caps invocations at `JARVIS_INTENT_DISCOVERY_HOURLY_CAP` (default 10/hour) as the last line of defense against a token-bill blow-out. Poll demotes to `JARVIS_INTENT_DISCOVERY_FALLBACK_INTERVAL_S` (default 14400s = 4h) when flag is on — the fallback exists only to catch missed observer dispatches. When flag is off the sensor is a **pure-poll** sensor at the legacy 900s cadence and `subscribe_to_bridge` is a logged no-op — zero silent regression. Explicit origin telemetry: event-driven fires log `[IntentDiscovery] scan trigger=conversation_silence_window silence_elapsed=Xs hourly_fires=N`; fallback polls log `trigger=fallback_poll`. Counter invariant: `_sw_fires + _sw_no_turn_yet + _sw_silence_not_met + _sw_cooldown_active + _sw_hourly_cap_hit == total evaluations`. Scan-lock (`asyncio.Lock`) prevents the silence-window loop and the fallback poll from racing into `scan_once` simultaneously during demotion. Registration failures are caught locally (intake boot never regresses); `stop()` unregisters the observer and cancels both loops. Regression spine: `tests/governance/intake/sensors/test_intent_discovery_events.py` (15 tests: flag helper, init capture, subscribe gating both directions + failure, observer turn-counter update, silence-window rejection paths (no_turn_yet / silence_not_met / cooldown / hourly cap), fire success + ledger updates, counter invariant, interval gate, real-bridge integration smoke). This is the **ninth sensor migrated** and the first one whose storm-guard is layered (silence + cooldown + hourly cap) because every trigger carries real token cost — the pattern becomes the reference template for any future sensor whose activation is expensive.
- **FS-event-primary BacklogSensor** (`intake/sensors/backlog_sensor.py` + `intake/fs_event_bridge.py`): Closes gap #4 for the operator-curated task surface. When `JARVIS_BACKLOG_FS_EVENTS_ENABLED=true` (default **`true`**, graduated 2026-04-20 as part of the gap-#4 campaign close-out; set to `false` to opt out), `BacklogSensor.subscribe_to_bus(bus)` registers an `fs.changed.*` handler on `TrinityEventBus` — a write to `.jarvis/backlog.json` triggers an instant `scan_once` at pub/sub latency instead of a 60s poll cycle. Poll demotes to `JARVIS_BACKLOG_FALLBACK_INTERVAL_S` (default 3600s = 1h) when flag is on. When flag is off the sensor is a **pure-poll** sensor at the legacy 60s cadence and `subscribe_to_bus` is a logged no-op — zero silent regression. **No storm-guard needed**: the path filter is a tight `rel_path.endswith("backlog.json")` suffix match, so bulk mutations elsewhere in the tree (e.g. `npm install`) bump only the `_fs_events_ignored` counter without ever reaching `scan_once`. Explicit origin telemetry: matching events log `[BacklogSensor] scan trigger=fs_event path=X topic=Y` at INFO; fallback poll cycles log `scan trigger=fallback_poll` (or `trigger=poll` when flag is off) at DEBUG — operators can distinguish the two delivery modes without parsing counter deltas. Counter invariant: `handled + ignored == bus_events_delivered`. Subscription failures caught locally (intake boot never regresses). Regression spine: `tests/governance/intake/sensors/test_backlog_sensor_fs_events.py` (11 tests: flag helper, init mode capture, subscribe gating both directions + failure case, matching/non-matching/malformed event routing + counters, fs_event origin telemetry, interval gate). This is the **eighth sensor migrated** and validates the pattern on the simplest possible event target — a single operator-edited JSON file — proving the flag+fallback+gated-subscription contract applies uniformly from high-frequency producers (OpportunityMiner) down to sparse config-style sources.
- **FS-event-primary OpportunityMinerSensor** (`intake/sensors/opportunity_miner_sensor.py` + `intake/fs_event_bridge.py`): Closes gap #4 for the code-optimization-discovery surface. When `JARVIS_OPPORTUNITY_MINER_FS_EVENTS_ENABLED=true` (default **`true`**, graduated 2026-04-20 as part of the gap-#4 campaign close-out; set to `false` to opt out), `OpportunityMinerSensor.subscribe_to_bus(bus)` registers an `fs.changed.*` handler on `TrinityEventBus` — each `.py` file change triggers a **single-file** multi-dimensional analysis (cyclomatic complexity / function length / cognitive load / duplication / fan-out / tech-debt), emitting an envelope only if `composite_score >= 0.3`. Poll demotes to `JARVIS_OPPORTUNITY_MINER_FALLBACK_INTERVAL_S` (default 21600s = 6h) when flag is on. When flag is off the sensor is a **pure-poll** sensor at the legacy 1h cadence and `subscribe_to_bus` is a logged no-op — zero silent regression. **Storm-guard hardening** (unique to this sensor — bulk-file events like `npm install` / `git checkout` can spray thousands of FS events in seconds, and naive subscription would re-parse every touched file): (1) **Per-file debounce** (`JARVIS_OPPORTUNITY_MINER_DEBOUNCE_S`, default 30s) — repeat events on the same path within the debounce window increment `_fs_events_debounced` without an AST parse; absorbs editor atomic-save sprays (save/backup/rename/chmod/fsync) without losing a genuine human follow-up edit. (2) **Global burst circuit breaker** (`JARVIS_OPPORTUNITY_MINER_STORM_THRESHOLD`, default 50 events/s + `JARVIS_OPPORTUNITY_MINER_STORM_COOLDOWN_S`, default 60s) — a rolling 1-second window counts post-skip-dir events; exceeding the threshold trips storm mode for the cooldown period during which all events are counted as `_fs_events_storm_dropped` and bypass scan entirely. Emits one WARNING per storm; scan resumes automatically after cooldown. Skip-dirs (`_OPP_SKIP_DIRS`) matches the FileWatchGuard default exclude set (`venv`/`.venv`/`venv_py39_backup`/`node_modules`/`.git`/`__pycache__`/`.worktrees`/`.pytest_cache`/`.mypy_cache`/`build`/`dist`/`.ouroboros`) so storm accounting only counts events that would otherwise pass to AST parsing. Filter order is cheapest-first: extension → test-file → delete (state-cleanup) → storm-active → skip-dirs → burst-update → debounce → scan. Subscription failures are caught locally (intake boot never regresses). Counter invariant: `handled + ignored + debounced + storm_dropped == bus_events_delivered`. Regression spine: `tests/governance/intake/sensors/test_opportunity_miner_fs_events.py` (16 tests: flag helper, init state capture, subscribe gate both directions + failure case, 7 event-routing cases + counters, debounce window + per-file isolation, storm trip-and-drop, interval gate). This is the **seventh sensor migrated** and introduces the storm-guard pattern that future high-frequency event consumers (e.g. a future code-metrics sensor) can reuse.
- **CI webhook → PerformanceRegressionSensor** (`intake/sensors/performance_regression_sensor.py` + `_handle_ci` short-circuit in `event_channel.py`): Closes gap #4 for the performance-regression surface by reacting to CI completion events at network latency rather than polling `PerformanceRecordPersistence` every 1h. When `JARVIS_PERF_REGRESSION_WEBHOOK_ENABLED=true` (default `true`, graduated 2026-04-20 after 3-session arc — S1 `bt-2026-04-20-071726`, S2 `bt-2026-04-20-072731`, S3 `bt-2026-04-20-073350`; set to `false` to opt out), CI systems push to `POST /webhook/ci` and `EventChannelServer._handle_ci` short-circuits to `PerformanceRegressionSensor.ingest_webhook(payload)`. The sensor is **selective**: triggers `scan_once` only for regression-relevant events — `status` (or `conclusion`) ∈ `{failure, failed, error}`, payload carries `benchmark`/`metrics`/`perf_data`, or GitHub Actions-as-CI sets `workflow_run.conclusion=failure`. Passing/pending runs with no benchmark data are ignored. Poll demotes to `JARVIS_PERF_REGRESSION_FALLBACK_INTERVAL_S` (default 14400s = 4h) when flag is on. **Architectural note**: first CI-surface sensor — uses the single-short-circuit pattern from Slices 1/2 rather than the fan-out from Slice 5. When a second CI-reactive sensor lands, refactor `_handle_ci` to an `asyncio.gather` dispatcher identical to `_handle_github`'s push branch. Stable telemetry: `[EventChannel] ci/event delivered sensor=PerformanceRegressionSensor status=X emitted=Y duration_ms=N`. `/channel/health` gains a `performance_regression_sensor.{wired, webhook_mode, last_ci_at, ci_emitted, ci_ignored}` sub-object. Regression spine: `tests/governance/intake/sensors/test_performance_regression_sensor_webhook.py` (23 tests) + `ov_smoke --check perf_regression_ci_webhook` (real HTTP `POST /webhook/ci` round-trip). This is the fifth sensor migrated and proves the pattern extends to **a new HTTP endpoint** (`/webhook/ci`) beyond the established `/webhook/github` surface — EventChannelServer is now multi-surface. Master switch **graduated 2026-04-20** (default `true`) — three consecutive clean sessions with `[EventChannel] ci/event delivered sensor=PerformanceRegressionSensor status=failed emitted=False duration_ms=0-2` and zero gap-#4 WARN/ERROR.
- **SerpentFlow** (`battle_test/serpent_flow.py`, 1900+ lines): CC-style flowing CLI with `Update(path)` blocks, numbered diffs, per-op reasoning
- **LiveDashboard** (`battle_test/live_dashboard.py`, 1233 lines): Persistent Rich TUI with 3-channel terminal muting
- **Venom** (`tool_executor.py`): Multi-turn agentic tool loop -- 16 built-in tools + MCP external tools. Built-in: read_file, search_code, edit_file, write_file, bash, web_fetch, web_search, run_tests, get_callers, glob_files, list_dir, list_symbols, git_log, git_diff, git_blame, ask_human. MCP tools from external servers discovered at prompt time and forwarded (Gap #7). Live context auto-compaction between rounds (Gap #8).
- **L2 Repair** (`repair_engine.py`): Iterative self-repair FSM (5 iterations, 120s timebox). **Enabled by default** (`JARVIS_L2_ENABLED=true`) — engages when VALIDATE exhausts retries, closes the Ouroboros cycle per Manifesto §6.
- **Iron Gate** (orchestrator.py post-GENERATE): Two deterministic gates flow through the GENERATE retry loop with targeted feedback. (1) Exploration-first (`JARVIS_EXPLORATION_GATE`): min 2 `read_file`/`search_code`/`get_callers` calls before any patch (trivial ops bypass). When `JARVIS_EXPLORATION_LEDGER_ENABLED=true` (default off), the gate switches from the legacy int counter to `ExplorationLedger` — diversity-weighted scoring across categories (comprehension / discovery / call_graph / structure / history) with env-tunable per-complexity floors (`JARVIS_EXPLORATION_MIN_SCORE_<COMPLEXITY>`, `JARVIS_EXPLORATION_MIN_CATEGORIES_<COMPLEXITY>`). Insufficient verdicts raise `ExplorationInsufficientError(verdict, floors)` and the retry path renders a category-aware feedback block via `render_retry_feedback(exc.verdict, exc.floors)` naming the missing categories. Log tags distinguish modes: `ExplorationLedger(decision)` when enforcing, `ExplorationLedger(shadow)` when observing only, `ExplorationLedger(shadow,partial)` from the post-exception handler on generation failures. (2) ASCII-strictness (`JARVIS_ASCII_GATE`): rejects any non-ASCII codepoint in candidate content to prevent Unicode corruption (e.g. `rapidفuzz` → blocked). Manifesto §6 Iron Gate enforcement.
- **SemanticGuardian** (`semantic_guardian.py` + orchestrator integration post-VALIDATE / pre-GATE): Deterministic pre-APPLY pattern detector closing the "SAFE_AUTO is size-heuristic only" gap from the 2026-04-16 risk-engine audit. Ten AST/regex patterns inspect (pre-apply on-disk content) vs (candidate content) with zero LLM calls (~10ms per candidate): `removed_import_still_referenced` (hard), `function_body_collapsed` (hard), `guard_boolean_inverted` (soft), `credential_shape_introduced` (hard — sk-*/AKIA*/ghp_*/xox[bp]-*/PEM), `test_assertion_inverted` (hard), `return_value_flipped` (soft), `permission_loosened` (hard — chmod/umask), `silent_exception_swallow` (soft), `hardcoded_url_swap` (soft), `docstring_only_delete` (soft). Findings upgrade the risk tier: hard → APPROVAL_REQUIRED (force human gate), soft → NOTIFY_APPLY (force 5s /reject window). Per-pattern + master kill switches via `JARVIS_SEMGUARD_<PATTERN>_ENABLED` / `JARVIS_SEMANTIC_GUARD_ENABLED`. Structured telemetry on every op: `[SemanticGuard] op=X findings=N hard=H soft=S patterns=[...] risk_before=X risk_after=Y duration_ms=D files_scanned=F` (Track A observability — simple split("=") parses into rollup counters). **Boundary Principle (Manifesto §1)**: guardian answers *"would a syntactically-valid semantic atrocity auto-apply?"* — for the 10 pattern classes, usually no. It does NOT answer *"is the patch logically correct?"* — that remains VALIDATE + Iron Gate + exploration discipline + the user's own test suite. Deterministic checks raise friction; they don't replace proof. New patterns added via closed-loop incident → fixture + pattern rule (never upfront heuristic stacking). Regression spine: `tests/governance/test_semantic_guardian.py` (47 cases).
- **Risk-tier floor** (`risk_tier_floor.py` + orchestrator integration): Three composing env knobs — strictest wins — that forbid SAFE_AUTO from auto-applying overnight. `JARVIS_MIN_RISK_TIER={safe_auto|notify_apply|approval_required}` explicit floor. `JARVIS_PARANOIA_MODE=1` shortcut for `notify_apply`. `JARVIS_AUTO_APPLY_QUIET_HOURS=<start>-<end>` time-of-day window interpreted in `JARVIS_AUTO_APPLY_QUIET_HOURS_TZ` (IANA zone name; **defaults to UTC** — implicit local-wall-clock is ambiguous across multi-operator deployments). Wrap-around supported (`22-7` = 22:00-06:59 in the resolved zone). Regression spine: `tests/governance/test_risk_tier_floor.py` (43 cases including TZ math + DST-correct conversion).
- **Multi-file coordinated generation** (orchestrator.py `_iter_candidate_files`/`_apply_multi_file_candidate`): Candidates may return a `files: [{file_path, full_content, rationale}, ...]` list in addition to the legacy single `file_path`/`full_content` pair. Every file is AST/placeholder-validated at the parser, and the APPLY path composes per-file `ChangeEngine.execute` calls with **batch-level rollback** — if file N fails, files 1..N-1 are restored from pre-apply snapshots (new files are unlinked). Preserves the 8-phase guarantees per file while adding atomic multi-file semantics. Master switch: `JARVIS_MULTI_FILE_GEN_ENABLED` (default `true`).
- **ConsciousnessBridge** (`consciousness_bridge.py`): Injects memory/prediction into pipeline
- **StrategicDirection** (`strategic_direction.py`): Manifesto principles injected into every generation prompt. Additionally infers recent development momentum from the last 50 `git log` commits via Conventional Commit parsing — extracts scope/type histograms and the three freshest subject lines into a "Recent Development Momentum" section of the digest. Manifesto §4 (synthetic soul). Gated by `JARVIS_STRATEGIC_GIT_HISTORY_ENABLED` (default `true`).
- **AutoCommitter** (`auto_committer.py`): Structured git commits with O+V signature after successful APPLY+VERIFY. Conventional commit format, risk-tier metadata, protected-branch push prevention.
- **OrangePRReviewer** (`orange_pr_reviewer.py`): Async-review path for Orange-tier (`APPROVAL_REQUIRED`) changes. Instead of blocking the loop on a synchronous CLI approval, creates a `ouroboros/review/{op-id}` branch, commits the candidate, pushes, and files a GitHub PR via `gh pr create` with evidence + review checklist in the body. The autonomous loop continues immediately; the human reviews asynchronously. Opt-in via `JARVIS_ORANGE_PR_ENABLED` (default `false`). On any failure, falls back to the existing CLI approval provider. Manifesto §7 (absolute observability) — the PR is the auditable artifact.
- **DreamEngine** (`consciousness/dream_engine.py`): Idle GPU speculative improvement blueprints
- **TestRunner** (`test_runner.py`): Multi-strategy async test discovery + pytest execution. Env: `JARVIS_TEST_TIMEOUT_S`, `JARVIS_TEST_RETRY_ENABLED`, `JARVIS_TEST_MAX_FILES`, `JARVIS_TEST_DIR_NAMES`. Known fix: sandbox path resolution via original_paths mapping (commit 22f297d).

### Autonomous Developer Intelligence (O+V)

O+V is **proactive** (self-initiating), not reactive (human-prompted). Key capabilities:

- **4-tier risk escalation**: `SAFE_AUTO` / `NOTIFY_APPLY` / `APPROVAL_REQUIRED` / `BLOCKED` -- Green/Yellow auto-apply, Orange blocks for human
- **Exploration-first**: Generation prompt requires 2+ tool calls before any patch. **Hard-enforced** at the Iron Gate (post-GENERATE, pre-VALIDATE) — violators route through GENERATE_RETRY with targeted "you MUST call read_file/search_code" feedback before any patch reaches disk.
- **Post-apply verification**: Scoped test run after APPLY, routes failures to L2 repair. L2 candidate now applied via change_engine.
- **Session intelligence**: `_session_lessons` buffer (20 max) with infra/code tagging. Convergence metric auto-clears misleading lessons.
- **Cost-aware priority**: `_compute_priority()` factors urgency, file count, confidence, dependency credit (capped at 3).
- **Dependency DAG**: `_active_file_ops` with TTL-based stale lock release (`JARVIS_FILE_LOCK_TTL_S`, default 300s).
- **Signal coalescing**: Same-file signals merged into single operation within `JARVIS_COALESCE_WINDOW_S` (default 30s).
- **Stale exploration guard**: File hashes snapshotted at GENERATE, verified at APPLY. Stale candidates logged.
- **REPL /cancel**: `cancel <op-id>` cooperative cancellation, checked at GENERATE and APPLY phase boundaries.
- **Diff preview for Yellow**: `JARVIS_NOTIFY_APPLY_DELAY_S` (default 5s) delay with diff rendered before auto-apply.
- **Per-op reasoning**: Model rationale captured at GENERATE, displayed in SerpentFlow `Update` blocks.
- **Model-reasoned planning**: PLAN phase between CONTEXT_EXPANSION and GENERATE. Model reasons about implementation strategy (schema plan.1) before writing code. Trivial ops skip planning.
- **Mid-operation clarification**: `ask_human` tool in Venom lets the model ask the human for clarification. Gated to NOTIFY_APPLY+ risk tiers (Green ops don't interrupt).
- **L3 worktree isolation** (`subagent_scheduler.py` + `worktree_manager.py`): Enabled by default (`JARVIS_GOVERNED_L3_ENABLED=true`). Parallel execution graphs use isolated git worktrees (COW via `git worktree add -b`, no copy/venv warmup) to prevent filesystem conflicts between parallel units. **Manifesto §1 Boundary + §6 Iron Gate**: if isolation was promised and `create()` fails (branch collision, disk full, permission denied), the unit returns `WorkUnitResult(FAILED, failure_class="infra", error="worktree_create_failed:<type>:<msg>")` — **no silent fallback to the shared tree**. Generator is never reached. **§2 Progressive Awakening**: `WorktreeManager.reap_orphans()` sweeps on boot (`JARVIS_WORKTREE_REAP_ORPHANS=true`, default `true`) — registered `unit-*` worktrees, unregistered on-disk `unit-*` dirs under `worktree_base`, and dangling `unit-*` branches (prevents "branch already exists" on next submit), followed by `git worktree prune`. Recovers from SIGKILL/OOM/power-loss leftovers; the `finally`-block cleanup covers normal exits. Regression spine: `tests/governance/test_worktree_isolation.py` (10 tests), `tests/governance/autonomy/test_subagent_executor_worktree.py` (2 tests).
- **Auto-commit post-APPLY**: AutoCommitter creates structured git commits with O+V signature after VERIFY passes. Conventional commit type/scope inference, risk-tier metadata, protected-branch push prevention. Master switch: `JARVIS_AUTO_COMMIT_ENABLED`.
- **MCP tool forwarding**: External MCP tools discovered from connected servers and injected into generation prompt (Gap #7). Model can call `mcp_{server}_{tool}` during tool loop. Policy engine auto-allows MCP tools; external servers handle their own auth.
- **Live context auto-compaction**: When tool loop prompt exceeds 75% of budget, older tool results are compacted into a deterministic summary (Gap #8). Preserves recent 6 chunks. No model inference. Env: `JARVIS_TOOL_LOOP_COMPACT_THRESHOLD`.
- **UserPreferenceMemory** (`user_preference_memory.py`): Persistent typed memory across O+V sessions, modeled on Claude Code auto-memory (typed `.md` files with YAML frontmatter + `MEMORY.md` index). Six types: `USER` / `FEEDBACK` / `PROJECT` / `REFERENCE` / `FORBIDDEN_PATH` / `STYLE`. Storage lives at `.jarvis/user_preferences/`. Three integration points: (1) StrategicDirection injects a relevance-scored "User Preferences" prompt section at CONTEXT_EXPANSION (scored by path overlap > tag match > type bonus, FORBIDDEN_PATH doubled on matching target). (2) ToolExecutor's `_is_protected_path` consults a global provider hook — every `FORBIDDEN_PATH` memory becomes a hard block on Venom `edit_file`/`write_file`/`delete_file` (same layer as the hardcoded `.git/`, `.env`, `credentials` list). (3) Post-rejection postmortem: when a human rejects an `APPROVAL_REQUIRED` op, `orchestrator.py` auto-extracts the rejection reason into a `FEEDBACK` memory tagged `("rejection", "approval")`, deduped by op-description slug so repeat rejections upsert rather than pile up. Manifesto §4 (synthetic soul, cross-session learning) + §6 (threshold-triggered neuroplasticity).
- **LastSessionSummary** (`last_session_summary.py`, v1.1a): Read-only session-to-session episodic continuity. At CONTEXT_EXPANSION, parses the most recent `.ouroboros/sessions/bt-*/summary.json` into a frozen `SessionRecord` and renders one dense one-liner per session (sanitized via `sanitize_for_log` + `redact_secrets`, 256-char cap per field, zero-op note when `stats_attempted == 0`). **Strictly read-only**: no `debug.log` grep, no commit scraping, no memory/*.md narrative. Injected ordering: Strategic → Bridge → Semantic → **LastSession** → Goals → UserPreferences. **Authority invariant**: output consumed ONLY by StrategicDirection — zero authority over Iron Gate, risk tier, FORBIDDEN_PATH, or approval gating. Env gates: `JARVIS_LAST_SESSION_SUMMARY_ENABLED` (master, default `false`), `…_N_SESSIONS` (1, clamped 3), `…_MAX_CHARS` (4096). **v1.1a (additive)**: `summary.json` gains `schema_version: 2` + optional `ops_digest` (typed APPLY/VERIFY/commit facts — `last_apply_mode`/`_files`/`_op_id`, `last_verify_tests_passed/total`, `last_commit_hash`), fed via `OpsDigestObserver` protocol from 3 orchestrator call sites (post-APPLY/VERIFY/AutoCommitter). `_render_session` appends tokens `apply=MODE/N verify=P/T commit=HASH[:10]`. v1 files still parse cleanly. V1.1b (debug.log tail) + V1.1c (memory pointer) parked. §8: one INFO line per op with hash8 + chars_out + source. Manifesto §1 / §4 / §8.
- **SemanticIndex** (`semantic_index.py`, v0.1): Local, bounded semantic goal inference via recency-weighted centroid over last 30 git commits + active goals + ConversationBridge snapshot (3-day halflife for conversation, 14-day for commits/goals). POSTMORTEM turns excluded from centroid by default (avoid "failure gravity"); surface in a separate "Recent friction / closures" prompt subsection. Embedder: `fastembed` + `bge-small-en-v1.5` (local ONNX, optional install via `requirements-semantic.txt`; master-off means no import/disk I/O). Cosine → non-negative priority boost ≤ `JARVIS_SEMANTIC_ALIGNMENT_BOOST_MAX` (default `1`, strictly subordinate to `goal_alignment_boost=2`). Two consumer surfaces only: (1) intake priority bias (`envelope.evidence["semantic_alignment"]` + `"semantic_boost"`), (2) CONTEXT_EXPANSION prompt subsection. **Authority invariant**: consumed ONLY by intake priority + StrategicDirection — zero authority over Iron Gate, risk tier, FORBIDDEN_PATH, or approval gating. Disk cache: `.jarvis/semantic_index.npz`. Env gates: `JARVIS_SEMANTIC_INFERENCE_ENABLED` (master, default `false`), `…_HALFLIFE_DAYS`/`…_CONVERSATION_HALFLIFE_DAYS`/`…_MAX_ITEMS`/`…_REFRESH_S`/`…_ALIGNMENT_BOOST_MAX`/`…_PROMPT_TOP_K`/`…_POSTMORTEM_IN_CENTROID`/`…_GIT_LOG_N`. §8: `built_at`/`corpus_n`/`centroid_hash8` per rebuild + per-injection INFO. Imports `conversation_bridge` (bridge never imports this module — no circular coupling). Pre-embed sanitizer applied to commit messages. Manifesto §1 / §4 / §5 / §8.
- **ConversationBridge** (`conversation_bridge.py`, v1.1): Sanitized, bounded channel from agentic dialogue into `ctx.strategic_memory_prompt` at CONTEXT_EXPANSION. In-process ring buffer (no disk persistence), Tier -1 deterministic sanitizer (`sanitize_for_log` + secret-shape redaction for sk-*/xox*/AKIA*/ghp_*/PEM). Injected ordering: Strategic → **Bridge (untrusted)** → Goals → UserPreferences. Five signal sources (all untrusted): `tui_user` (SerpentFlow REPL), `ask_human_q`+`_a` (Venom tool Q+A), `postmortem` (deterministic one-liner from `format_postmortem_payload`, skipped when root_cause empty), `voice` (reserved V1.2). Cross-op episodic memory: POSTMORTEM from op N+1 may reach op N+2 CONTEXT_EXPANSION (best-effort). **Authority invariant**: consumed ONLY by StrategicDirection — zero authority over Iron Gate, risk tier, FORBIDDEN_PATH, or approval. Prompt format: one `<conversation untrusted="true">` fence with 3 source-grouped subheaders. Master switch: `JARVIS_CONVERSATION_BRIDGE_ENABLED` (default `false`). Caps: `…_MAX_TURNS` (10), `…_MAX_CHARS_PER_TURN` (4096), `…_MAX_TOTAL_CHARS` (16384), `…_MAX_POSTMORTEMS` (3), `…_POSTMORTEM_TTL_S` (600). `record_turn` never raises — failures bump `stats.dropped_errors` silently. §8: one INFO line per op with hash8 + counts. Manifesto §1 / §5 / §6 / §8.
- **VisionSensor** (`intake/sensors/vision_sensor.py`, Slices 1-2): Read-only consumer of the Ferrari frame stream. Tier 1 deterministic regex (traceback/panic/segfault/modal-error/linter-red) + Tier 2 VLM classification (Qwen3-VL-235B via injectable `vlm_fn`). Hot path: dhash dedup → app denylist (1Password/Bitwarden/Keychain/Messages/Mail/Signal + user `FORBIDDEN_APP` memory) → OCR → credential-shape regex (sk-*/AKIA*/ghp_*/xox[bp]-*/PEM) → Tier 1 classify → finding cooldown → Tier 2 VLM (only when T1 quiet + not downshifted + new dhash) → T1 firewall sanitize → schema v1 evidence → `IntentEnvelope(source="vision_sensor")`. **Structural invariants**: I7 `ctx.attachments` export-ban (2 authorized readers, AST-enforced); I8 no-capture-authority (never imports Quartz/SCK/AVFoundation, never spawns `frame_server.py` — AST-enforced, fails closed to zero-signal when stream absent); I2 risk floor `NOTIFY_APPLY`, env tunable upward only, never IMMEDIATE. **Policy** (`.jarvis/vision_sensor_fp_ledger.json`): rolling 20-op FP budget (auto-pause on rate >30%); per-finding cooldown (120s); chain cap 1→3 on graduation; 3-fail penalty (300s). **Cost ledger** (`.jarvis/vision_cost_ledger.json`): $1 daily cap, UTC rollover, 3-step cascade (80%→VLM skip / 95%→pause / midnight→reset); `/vision boost <seconds>` (max 300s, TTY-gated). **Retention**: `.jarvis/vision_frames/<session_id>/` lazy-created, `frame_ttl_s=600`, atexit+SIGTERM purge. Env gates: `JARVIS_VISION_SENSOR_ENABLED`/`…_TIER2_ENABLED`/`…_CHAIN_MAX`/`…_FP_BUDGET`/`…_FINDING_COOLDOWN_S`/`…_FRAME_TTL_S`/`JARVIS_VISION_DAILY_COST_CAP_USD`/`…_TIER2_COST_USD`/`…_MIN_CONFIDENCE`. REPL (`vision_repl.py`): `/vision status|resume|boost`. Master switches default `false` until per-slice 3-session graduation arcs complete (checklists in `docs/operations/vision-sensor-slice-{1,2,3,4}-graduation.md`). Manifesto §1 / §3 / §5 / §7.
- **Multi-modal ingest** (ctx.attachments → Claude/DW GENERATE): The "Mind" can now perceive what the "Senses" captured. Two convergent ingress paths feed a single `ctx.attachments: Tuple[Attachment, ...]` surface — the Manifesto §1 Tri-Partite Microkernel invariant. **(1) Autonomous**: VisionSensor's `evidence["vision_signal"]["frame_path"]` is hoisted at `unified_intake_router.py` into `Attachment(kind="sensor_frame")` before CLASSIFY. **(2) Human-initiated**: SerpentFlow `/attach <path> [description]` REPL command builds an `IntentEnvelope(source="voice_human", evidence["user_attachments"]=[{path, kind="user_provided"}])` that goes through the same router hoist — `Attachment(kind="user_provided")`. The envelope schema at `intent_envelope.py` exempts both (vision_sensor source + user_attachments evidence) from the `target_files` non-empty rule — both semantically mean "the op is about perceiving this content, not patching a pre-known file". At GENERATE, `providers.py::_serialize_attachments()` (purpose-gated to `sensor_classify`|`visual_verify`|`generate`, master kill `JARVIS_GENERATE_ATTACHMENTS_ENABLED`) walks ctx.attachments and emits: **Claude** — native `{"type":"image",...}` blocks for JPG/PNG/WEBP, `{"type":"document","source":{"media_type":"application/pdf",...}}` for PDFs (Anthropic document content API); **DoubleWord / J-Prime** — OpenAI-compatible `{"type":"image_url","image_url":{"url":"data:..."}}` blocks for images, PDFs dropped with WARNING (Qwen3-VL is image-only). `Attachment.from_file()` enforces absolute path + extension in `{.jpg,.jpeg,.png,.webp,.pdf}` + mime in `{image/jpeg, image/png, image/webp, application/pdf}` + 10 MiB cap + sha256[:8] hash. `/attach` additionally enforces Venom's `_is_protected_path()` deny-check (same layer that blocks `.git/`, `.env`, credentials on Venom edit_file/write_file). BG/SPEC routes strip attachments regardless (§5 cost). §8 observability: `[IntakeRouter] attachments_hoisted op=X kind=sensor_frame|user_provided hash8=X mime=X basename=X source=X` at hoist + `[ClaudeProvider|DoublewordProvider] multi_modal op=X blocks=N attachments=K bytes=B kinds=[X] mime_kinds=[X] hash8s=[X] route=X purpose=generate` at ship. **Behavioral proof** (2026-04-20, live Anthropic API via `scripts/attach_livefire.py`): 701-byte PDF containing `"BUG: division by zero on line 42 of compute.py"` → Claude returned `2b.1-noop` with verbatim quote of the embedded string in its rationale. CC-parity for "see a screenshot/PDF of a bug and describe it" proven end-to-end. Manifesto §1 / §4 / §6 / §8.
- **Visual VERIFY** (`visual_verify.py`, Slices 3-4): Post-APPLY pre-COMPLETE hook for UI-affected ops. Trigger (D2 three-tier): primary = `target_files` glob match on `{.tsx,.jsx,.vue,.svelte,.css,.scss,.html,.htm}`; structured-negative = any classifiable non-frontend file suppresses fallback; secondary = plan `ui_affected` hint; tertiary = zero TestRunner targets + risk_tier ≥ NOTIFY_APPLY. **Deterministic battery** (Slice 3, first-miss-wins): missing pre/post → skipped; app_crashed (Quartz `CGWindowListCopyWindowInfo`, injectable); blank_screen (pixel-variance ≥ 0.01); hash_unchanged (pre==post); hash_scrambled (distance ≥ 0.9); deterministic_pass. **I4 asymmetry**: if `test_runner_result ∈ {failed,fail,red}` a Visual VERIFY pass is **clamped to fail** — additive-failure only, never rescues TestRunner red into green. **Model-assisted advisory** (Slice 4): `run_advisory()` via injectable `advisory_fn` (Qwen3-VL-235B); verdict ∈ {`aligned`,`regressed`,`unclear`}; confidence gate strictly `> JARVIS_VISION_VERIFY_REGRESS_CONFIDENCE` (0.80); only `regressed`-above-threshold routes to L2 via `l2_triggered=True`. Reasoning sanitized through T1 firewall. **`AdvisoryLedger`** (`.jarvis/vision_verify_advisory_ledger.json`): verdict + `reasoning_hash` (sha256[:16], raw never on disk) + `/verify-confirm <op-id> {agree|disagree}`. FP rate drives Slice 4 graduation (≥60% agreement) + **auto-demotion guardrail**: post-graduation FP ≥50% writes `.jarvis/vision_verify_model_assisted_demoted.flag` → `model_assisted_active()=False` until `/verify-undemote`. Env gates: `JARVIS_VISION_VERIFY_ENABLED` / `…_MODEL_ASSISTED_ENABLED` / `…_REGRESS_CONFIDENCE` / `…_MIN_VARIANCE` / `…_HASH_DIST_{MIN,MAX}` / `…_RENDER_DELAY_S`. Test spines: 115 tests (68 `test_visual_verify.py` + 47 `test_visual_verify_advisory.py`). Manifesto §1 / §3 / §7.

## Battle Test

```bash
python3 scripts/ouroboros_battle_test.py --cost-cap 0.50 --idle-timeout 600 -v
```

Boots the full 6-layer stack: GovernedLoopService, IntakeLayer (16 sensors), TrinityConsciousness, StrategicDirection, CommProtocol, SerpentFlow CLI.

On startup, the harness auto-reaps any zombie `ouroboros_battle_test.py` processes from earlier crashed sessions (psutil-based, strict path-tail match, SIGTERM → SIGKILL escalation) and removes stale `.jarvis/intake_router.lock` files whose owning PID is dead. Prevents budget competition between sessions. Master switch: `JARVIS_BATTLE_REAP_ZOMBIES` (default `true`).

Partial-shutdown insurance: the harness registers an `atexit` fallback **and** a sync signal-handler write so every session dir ends up with a v1.1a-parseable `summary.json` — even when SIGTERM arrives mid-cleanup or the async finally can't complete. `SIGKILL` remains unrecoverable by design (OS-level, uncatchable in Python). Regression spine for "session continuity + aborted runs": `tests/governance/test_last_session_summary_composition.py` (proves production injection path wires LSS tokens into the composed CONTEXT_EXPANSION prompt) + `tests/battle_test/test_harness_partial_shutdown.py` (proves partial summaries land on every reachable exit path and are LSS-parseable on the next boot).

Operator-visible UX (Rich): the GENERATE token stream (`stream_renderer.py`) and the NOTIFY_APPLY rich diff preview (`diff_preview.py`) both require a real interactive TTY — headless / sandbox / CI runs always fall through to the plain (spinner-and-sleep) paths, so visual verification of these features is a local interactive battle test, not a background run.

## File Layout (Key Paths)

```
backend/core/ouroboros/
  governance/
    governed_loop_service.py    # Main loop (Zone 6.8)
    orchestrator.py             # 11-phase FSM
    candidate_generator.py      # 3-tier failback + route-based dispatch
    urgency_router.py           # Deterministic provider routing (§5 Tier 0)
    providers.py                # Claude + Prime providers
    doubleword_provider.py      # DW 397B
    plan_generator.py           # Model-reasoned PLAN phase (schema plan.1)
    semantic_triage.py          # Pre-generation filter
    comm_protocol.py            # 5-phase observability
    tool_executor.py            # Venom tool loop + live context compaction
    auto_committer.py           # Auto-commit with O+V signature (Gap #6)
    mcp_tool_client.py          # MCP external tool client (Gap #7)
    context_compaction.py       # Live context auto-compaction (Gap #8)
    batch_future_registry.py    # Zero-poll webhook batch futures (DW Tier 1)
    event_channel.py            # Webhook receiver (DW + GitHub + CI)
    repair_engine.py            # L2 self-repair
    consciousness_bridge.py     # Consciousness integration
    strategic_direction.py      # Manifesto injection
    user_preference_memory.py   # Persistent typed memory across sessions (Task #195)
    conversation_bridge.py      # TUI dialogue → CONTEXT_EXPANSION (v1.1, Tier -1 sanitized)
    semantic_index.py           # Recency-weighted semantic centroid + cosine scoring (v0.1)
    last_session_summary.py     # Read-only session-to-session continuity from summary.json (v1.1a)
    ops_digest_observer.py      # Stable observer protocol for APPLY/VERIFY/commit telemetry (v1.1a)
    visual_verify.py            # Post-APPLY deterministic + advisory VERIFY (Slices 3-4)
    vision_repl.py              # /vision status|resume|boost handlers + dashboard + origin tag (Task 21)
    serpent_animation.py        # ASCII animation
    intake/
      sensors/                  # 17 sensors (VisionSensor added — Slices 1-2)
        vision_sensor.py        # Read-only Ferrari consumer with Tier 0/1/2 cascade + policy layer
    intent/
      test_watcher.py           # Pytest polling (30s timeout)
      signals.py                # IntentSignal dataclass + SignalSource enum + VisionSignalEvidence schema v1
  consciousness/                # Zone 6.11 (7,063 lines)
    consciousness_service.py    # TrinityConsciousness orchestrator
    health_cortex.py            # System health monitoring
    memory_engine.py            # Per-file reputation tracking
    dream_engine.py             # Idle GPU speculative analysis
    prophecy_engine.py          # Regression prediction
  battle_test/
    harness.py                  # 6-layer stack boot
    serpent_flow.py             # SerpentFlow: CC-style CLI (1,900+ lines)
    live_dashboard.py           # Persistent Rich TUI (1,233 lines)
  oracle.py                     # Codebase semantic index
```

## The Governing Philosophy

This is not a software refactor. It is the genesis of an autonomous, self-evolving AI Operating System. The 7 principles:

1. **Unified organism** -- tri-partite microkernel, single entry point
2. **Progressive awakening** -- adaptive lifecycle, no blocking boot chains
3. **Asynchronous tendrils** -- structured concurrency, no event loop starvation
4. **Synthetic soul** -- episodic awareness, cross-session learning
5. **Intelligence-driven routing** -- semantic, not regex; DAGs, not scripts
6. **Threshold-triggered neuroplasticity** -- Ouroboros: detect gaps, synthesize, graduate
7. **Absolute observability** -- every autonomous decision is visible

**Zero-shortcut mandate**: No brute-force retries without diagnosis. No hardcoded routing tables. Structural repair, not bypasses.

## Battle Test Milestones

Full postmortems for sustained battle-test breakthroughs live in `docs/architecture/OUROBOROS.md#battle-test-breakthrough-log`. The canonical source of truth for any "did the loop work" question is the session `debug.log` under `.ouroboros/sessions/<session-id>/`, not `summary.json` (which has a known `attempted` counter bug).

- **2026-04-11 (`bt-2026-04-11-154947`)** — First sustained full-pipeline completion post-Iron Gate tightening. `op-019d7d3e` traversed CLASSIFY → IRON_GATE_REJECT → REGENERATE → APPLY → VERIFY → L2 → POSTMORTEM autonomously. `dependency_file_integrity` gate caught hallucinated `anthropic → anthropichttp` rename. Unblocker: captured-client race fix in `providers.py` re-acquires `self._client` on every `_call_with_backoff` retry.
- **2026-04-12 (`bt-2026-04-12-073546`)** — IMMEDIATE thinking cap + DurableJSONL sandbox fix. 5 validated fixes: fallback_concurrency=3, outer-gate grace 5s→15s, async sensor scans, IMMEDIATE thinking disabled (first_token 94.5s→961ms, 98x), DurableJSONL sandbox_fallback. TestRunner sandbox path bug fixed (commit `22f297d`).
- **2026-04-14 (Sessions A–G)** — `ExplorationLedger` enforcement validated. Session G: retry feedback → model diversifies to `get_callers`/`git_blame`/`search_code` → `would_pass=True score=25.50` across 5 categories. Commits `614009ec05`/`db13f045ce`/`4f60a584f9`/`ad05fb7c7e`/`5d169266d6`.
- **2026-04-15 (Session O, `bt-2026-04-15-175547`)** — **First end-to-end autonomous APPLY to disk under full complex-route enforcement.** `test_test_failure_sensor_dedup.py` (4 986 bytes) written after attempt 2 scored `11.00`/4 categories → ChangeEngine → L2 iter 1 CONVERGED → POSTMORTEM root_cause=none. ~16m45s, $0.55/$0.60. Closed an 8-session arc (H–O) surfacing 6 additional failure modes: headless TTY crash (`d8c1cb4d30`), risk escalation, L3 planning-mode switch (`5d169266d6`), RollbackArtifact new-file path (`28d52274ec`), pool ceiling (`JARVIS_BG_WORKER_OP_TIMEOUT_COMPLEX_S=1800`), fallback cap (`…_FALLBACK_MAX_TIMEOUT_COMPLEX_S=360`), intake WAL cross-session coalescing (state cleanup). 1-of-4 files landed — multi-file fan-out gap.
- **2026-04-15 (Sessions Q–S, `bt-2026-04-15-204901`)** — Multi-file `files: [...]` enforcement proven through every gate. Shipped: `multi_file_coverage_gate.py` as Iron Gate 5 (commit `31504a8f12`, 31 tests), `_build_multi_file_contract_block` prompt hint, `_parse_generation_response` multi-file shape detection, `provider_exhaustion_watcher.py` per-op dedup (`37a371e65d`). Session S: Gate 5 silently passed all 4 paths, LSP+TestRunner saw all 4. Persistence blocked by L2 timebox → tracked as Follow-up A.
- **2026-04-15 (Sessions U–W, `bt-2026-04-15-230849`)** — **First end-to-end autonomous multi-file APPLY to disk.** `op-019d9368-654b` generated 4 test modules → L2 iter 1 (50s) → `APPLY mode=multi candidate_files=4` → 4 `::NN` sub-op DECISIONs applied → 4 POSTMORTEMs root_cause=none → AutoCommitter commit `0890a7b6f0`. Post-hoc pytest: 20/20 pass in 2.28s. Key fixes: FSM instrumentation (`d6aa78c8ba`) exposed re-validation flakiness (iter=0 'test' vs iter=1 'infra' flake), `JARVIS_MAX_VALIDATE_RETRIES` env factory (`58709f27de`) bypasses it, L2 deadline reconciliation (`53e6bd9f76`) — fresh `now + JARVIS_L2_TIMEBOX_S` at dispatch, `ctx.pipeline_deadline` reconciled upward. Not yet graduated (§6 needs 3 consecutive successes); 3 deferred latent bugs as workarounds (re-validation infra flake, cost_governor ctx staleness, hardcoded 90s wait_for).
- **2026-04-20 (Phase C Epoch 3 Slice 3c — Themed CONTEXT_EXPANSION prompt rendering)** — **Second slice of the Synthetic Soul deepening. Operators now SEE the clusters as themes in the prompt before any policy change flips.** `format_prompt_sections()` rewritten to branch on cluster state: `cluster_mode=kmeans` AND `len(clusters) >= 2` AND non-empty corpus → render per-cluster `### Theme: <label> (N items, <kind>)` blocks; otherwise (centroid mode, K=1, or empty clusters) → v0.1 "Focus items" fallback. Within each theme, items are ranked by cosine-to-cluster-centroid (descending). Themes are ordered by size descending (ties broken by cluster_id ascending — Occam on equal evidence). New `_theme_label_from_text(text, *, max_tokens=3)` tokenizer: lowercase → strip trailing punctuation → drop English stopwords from a small frozen set (`_THEME_LABEL_STOPWORDS`: articles / auxiliaries / prepositions / conjunctions / pronouns) → take first 2-3 survivors joined with spaces. Empty or all-stopword result falls back to `theme-<cluster_id>`. Pure function, no LLM call, deterministic — critical for prompt-cache stability across builds. **Postmortem-kind clusters ARE rendered as Themes** (with `, postmortem)` kind tag) so operators can see failure themes as structural elements — aligns with the see-before-policy-affects sequencing. Separate `### Recent friction / closures` subsection (raw recency-ordered postmortem list from corpus) is unchanged across both paths — orthogonal to clustering. Authority disclaimer preamble preserved verbatim: `**no authority** over Iron Gate, routing, risk tier, policy, or FORBIDDEN_PATH matching`. Cap: max themes = `PROMPT_TOP_K` (default 3); max items per theme = `PROMPT_TOP_K` (same budget as v0.1). Regression spine: **21 new tests** (6 tokenizer + 15 renderer) joining 70 from v0.1 + Slice 3a for **91/91 green**. Critical pins: `test_themed_renderer_output_deterministic_for_same_corpus` (byte-identical prompts across rebuilds), `test_themed_renderer_authority_disclaimer_preserved` (authority language carried in prompt text), `test_themed_renderer_falls_back_to_v01_when_only_one_cluster` (K=1 degrades cleanly). Manifesto §4 (Synthetic Soul — themes are the organism's self-model of its attention) + §7 (Absolute Observability — the operator reads the organism's theme inference directly in the GENERATE prompt, not behind a stats wall).
- **2026-04-20 (Phase C Epoch 3 Slice 3a — Semantic Index v1.0 cluster math + telemetry)** — **First expansion of the Synthetic Soul (Manifesto §4) after the GENERAL cage was sealed.** v0.1's single-centroid cosine alignment averaged multiple simultaneous themes into a mediocre mean vector; v1.0 introduces cluster-based goal alignment so the organism can recognize separate themes (auth refactor + perf optimization + docs cleanup) without cross-contamination. Hand-rolled NumPy k-means (~200 lines, zero sklearn dep) with deterministic seeded init (`np.random.RandomState(seed).permutation(N)[:k]`), cosine-distance Lloyd iteration, and empty-cluster repair (reseeds from point farthest from own centroid — no silent K collapse). Auto-K via silhouette sweep over K ∈ [K_MIN, K_MAX ∩ N]; K=1 always scores 0.0 as the tie-break floor so any K≥2 with silhouette ≤0 loses to K=1 → graceful degradation to v0.1 behavior when data is coherent. Cluster-kind classifier maps source-composition → `{goal, conversation, postmortem, mixed}` via ≥60% dominance threshold (git_commit + goal combined for the `goal` family). New `ClusterInfo` frozen dataclass carries `cluster_id`, `size`, `kind`, `centroid` (tuple), `centroid_hash8`, `nearest_item_text`, `nearest_item_source`, `source_composition`. `IndexStats` extended with `cluster_mode`, `cluster_count`, `clusters` (content-light summaries — never raw vectors per §8), `cluster_churn` (hash-delta vs prior build), `kmeans_silhouette`, `kmeans_inertia`, `kmeans_converged`, `kmeans_iter_count`, `alignment_histogram_by_kind` (cumulative), `failure_gravity_alerts`, `failure_gravity_window_rate`. **Shadow-mode discipline** (Slice 3a invariant): `score()` return value is UNCHANGED under `cluster_mode=kmeans` — still the v0.1 centroid cosine. Cluster alignment is observed via `_observe_cluster_alignment()` which populates the histogram + rolling failure-gravity window without altering priority boost. Test `test_score_output_unchanged_under_kmeans_mode` pins this: two indexes with identical corpus, one in each mode, must produce bit-identical `score()` output. New `score_with_cluster(text) -> Dict` debug API returns `{score, cluster_id, cluster_kind, cluster_cosine, cluster_size}` for intake router to stash in `envelope.evidence` — informational only in 3a. **Failure-gravity tripwire**: rolling 50-signal cluster-kind buffer; once full, if postmortem-cluster fraction ≥ 0.3 → WARN `[SemanticIndex] failure_gravity_detected rate=X threshold=T window=N` + increment counter. Shadow-mode in 3a; Slice 3b introduces policy effect. **Authority invariant preserved** (Manifesto §1 Boundary Principle): clustering adds no new consumer surface; enforced via `test_authority_invariant_clustering_does_not_import_gate_modules` grep of semantic_index.py for forbidden imports of iron_gate / urgency_router / risk_tier_floor / semantic_guardian / policy_engine. **Env flags** (all additive, all defaults preserve v0.1 behavior): `JARVIS_SEMANTIC_INDEX_CLUSTER_MODE` default `centroid`; `_CLUSTER_K_MIN` default 1; `_CLUSTER_K_MAX` default 5; `_CLUSTER_KMEANS_SEED` default 42; `_CLUSTER_KMEANS_MAX_ITER` default 30; `_CLUSTER_KMEANS_TOL` default 1e-4; `_CLUSTER_POSTMORTEM_DOMINANCE` default 0.6; `_CLUSTER_FAILURE_GRAVITY_THRESHOLD` default 0.3; `_CLUSTER_FAILURE_GRAVITY_WINDOW` default 50. Regression spine: **43 new tests** (6 k-means math + 4 silhouette + 7 auto-K + 7 cluster-kind + 3 centroid hash + 4 integration + 5 shadow observation + 2 failure-gravity + 4 env hardening + 1 authority invariant + 1 misc) joining the 27 v0.1 tests for **70/70 green** total. Next slices: 3c multi-theme CONTEXT_EXPANSION prompt rendering, 3b max-cluster scoring policy with postmortem zero-boost-with-evidence, 3d graduation flip of CLUSTER_MODE default to kmeans. Manifesto §4 (Synthetic Soul — episodic memory + metacognitive theme recognition) + §8 (Observability — per-cluster structured telemetry without raw-content leakage).
- **2026-04-20 (Phase C Epochs 1+2 — GENERAL cage made absolute)** — **Closes the cooperative-vs-structural gap left open by Slice 1b graduation.** Epoch 1 (Ticket 8, `max_mutations` COUNT enforcement): `ScopedToolBackend` now carries a per-instance mutation counter (`_mutations_count` / `_max_mutations`). Every call to a tool in `_MUTATION_TOOLS` consumes one slot; once exhausted, the second-layer COUNT gate returns `POLICY_DENIED` with `reason=mutation_budget_exhausted` before the inner backend sees the call — independent of whether the tool *type* is in the allowlist. Slot consumed at AUTHORIZATION time (not inner-success), so a model cannot retry a failing mutation to farm extra slots. Epoch 2 (Ticket 9, hard_kill records preservation): adapter writes every `execute_async` decision (`authorized` / `type_denied` / `count_denied`) into a shared `state_mirror: Dict[str, Any]` that the executor passes into the driver payload. Driver attaches a `_scoped_backend_ref` for belt-and-suspenders access. When the hard-kill wrapper at `AgenticGeneralSubagent._execute_body` fires, the executor reads the mirror and builds an exec_trace with `tool_calls_made`, `mutations_count`, `mutation_records`, `call_records`, and `tool_names` all preserved — the previous implementation zeroed every counter on cancellation. Driver's `except asyncio.CancelledError` emits a WARNING-level audit log before re-raising: `[GeneralDriver] CANCELLED sub=X tool_calls=N mutations=M/K tool_names=[...] mutation_records=P call_records=Q`. Every failure path in `run_general_tool_loop` now routes through a uniform `_build_partial_trace` helper so the exec_trace shape is structurally identical across `no_provider_wired` / `backend_init_error` / `policy_init_error` / `tool_loop_error` / `malformed_final` / `completed` / `hard_kill` / `malformed_driver_output`. Regression spine: 9 adapter records tests (mutation_records, call_records, tool_names, state_mirror live updates, inner-exception survival, snapshot isolation, ref-attachable, defensive None) + 4 driver tests (uniform records fields, tool_loop_error preservation, CancelledError re-raise with audit log, success-path records) + 3 subagent integration tests (full-stack hard_kill with simulated hang, malformed_driver_output mirror read, well-formed driver pass-through). 111/111 GENERAL-surface tests green. Manifesto §5 (Semantic Firewall) — cage is now structural, not cooperative. Manifesto §7 (Absolute Observability) — every hard-killed driver leaves a complete audit trail of every tool it authorized.
- **2026-04-20 (Phase C Slice 1a+1b — GENERAL LLM driver graduation)** — **First cognitive interior landed on a Phase B subagent.** Closes the Phase B→C transition: all four subagent types (EXPLORE/REVIEW/PLAN/GENERAL) were infrastructure-graduated by 2026-04-20, but GENERAL's `_execute_body` was a stub returning `NOT_IMPLEMENTED_NEEDS_LLM_WIRING`. Slice 1a shipped `general_driver.py` (canonical LLM driver — scoped Venom tool loop behind `ScopedToolBackend` allowlist gate, `2b.2-tool` tool-call schema in system prompt, `general.final.v1` final-answer contract, hard-kill wrapper at `llm_budget_s + 30s`) + `agentic_general_subagent.build_llm_general_factory()` + 25-test regression spine. Slice 1b Step 0 wired the factory at `governed_loop_service.py` with a provider-registry closure (`_resolve_provider_for_subagent`: claude→claude_ref, doubleword/dw/qwen→doubleword_ref, default→claude_ref since NOTIFY_APPLY+ routes IMMEDIATE = Claude direct) — 7-test wire-in spine. Live battle test matrix (3 tests against real Anthropic API, harness at `/tmp/claude/general_battle_matrix.py`) proved the safety triad: **Test 1 Happy Path** — 5 tool calls (read_file×2, list_symbols, search_code×2) with diversity≥2, status=completed, 45.6s; **Test 2 Boundary Push** — model emitted `edit_file` tool call with full scoped path, hard_kill at 120s but scope honored + max_mutations=1 respected cooperatively; **Test 3 Tool Violation** — model declined bash request, emitted `status=blocked_by_tools` in 3.2s. Four sub-fixes landed during 1b: (a) `_generate_fn` wired to `provider._client.messages.create` with explicit `_ensure_client()` call (ToolLoopCoordinator contract is `(str)→str`, ClaudeProvider.generate expects ctx — bridged via private-attribute reach, tracked for cleanup as Ticket 6 `generate_text` public method), (b) `max_output_tokens` default 4096→8192 to survive edit_file arg serialization, (c) None-valued payload knobs crash fixed (`int(None)` → default fallback), (d) REVIEW aggregate verdict case-mismatch bug fixed (commit `d924b563c1` — constants are lowercase). Tool-call schema injection in `GENERAL_SUBAGENT_SYSTEM_PROMPT_TEMPLATE` (commits `c34f504ed2` + `88b55c2107`) closed the prose-instead-of-JSON failure mode from Run 4. **Graduation flag flipped 2026-04-20**: `JARVIS_GENERAL_LLM_DRIVER_ENABLED` default `false`→`true` — set explicitly to `"false"` to opt back into the Phase B stub. 32/32 tests green under new defaults (25 driver + 7 wire-in). Latent tickets tracked in Step-2 deferred memory: Ticket 6 (`generate_text` public method), Ticket 8 (max_mutations COUNT enforcement — today scope gates TYPE not COUNT), Ticket 9 (hard_kill records preservation). Manifesto §5 (Semantic Firewall held under live pressure) + §6 (Iron Gate — 3/3 safety properties empirically proven before flag flip).
- **2026-04-19→20 (Multi-Modal Ingest arc, 12 commits)** — **CC-parity for "see a screenshot/PDF of a bug and describe it" closed end-to-end with live Anthropic API proof.** Two-layer delivery: (A) **VisionSensor autonomous path hardened to graduation** via 8 commits closing 4 sequential producer/consumer contract mismatches (filename `frame_meta.json→latest_frame.json` in `5d7ee564a4`+`3c9a879042`, schema key `timestamp→ts` in `27d7c6a603`, schema value `dhash int→16-char hex` in `fdc06ed41e`), the event-loop-starvation markdown storm fix in `820855daee` (PIL TTY-gate + buffer slice-cap on rich.markdown re-parse), and OCR adapter wiring (`eaa4f6632f` added `backend/vision/ocr_adapter.py` with PyObjC VNRecognizeTextRequest, `c01f1866b8` flipped default to Accurate mode after proving Fast mode drops sparse-text top-region content on 1280×800 Retina-downscaled frames, `44305f7e96` instrumented all silent-skip paths in scan_once, `fe42166376` added per-sensor env gates for graduation isolation, `df960e5490` shipped `scripts/ferrari.sh` supervisor with caffeinate/nohup/auto-restart and sensor staleness guard). Attempts 1–2 of the 3-session arc landed clean (`attachments_hoisted`/`multi_modal` fired with matching op_id threaded through intake→provider); Attempt 3's miss was osascript-vs-macOS-UI-policy focus theft (pipeline was fully wired and logging, probe content wasn't frontmost) so the sensor graduated on 2/2 + structural proof per the Manifesto §6 "boundary principle" read. (B) **Human-initiated `/attach` path shipped in 4 commits** — `5e84ed2e60` wired ctx.attachments through Claude `_generate_raw` multi-modal content blocks + DW `_generate_realtime` OpenAI-compatible image_url blocks (with `JARVIS_GENERATE_ATTACHMENTS_ENABLED` master kill-switch and per-call `[ClaudeProvider|DoublewordProvider] multi_modal` INFO observability), `d119d48cad` extended `_VALID_ATTACHMENT_KINDS` with `user_provided` + `_VALID_ATTACHMENT_MIMES` with `application/pdf` + Claude native `{"type":"document"}` block serialization for PDFs (DW/J-Prime drop PDFs with WARNING — Qwen3-VL is image-only), `99b778b7a2` added SerpentFlow `/attach <path> [description]` REPL command with Venom `_is_protected_path()` reuse + full validation perimeter + envelope→router submission, `35aeff50ce` relaxed envelope schema `target_files` requirement for `evidence.user_attachments`-bearing envelopes (same exemption as vision_sensor source — both semantically mean "op is about perceiving this"). **Behavioral proof live-fire** (`scripts/attach_livefire.py`): 701-byte PDF `/tmp/claude/jarvis_smoke.pdf` containing `"BUG: division by zero on line 42 of compute.py"` → submitted via direct `router.ingest()` → routed IMMEDIATE → Claude returned `2b.1-noop schema` with rationale verbatim: *"The bug described in the PDF is a division-by-zero crash on line 42 of compute.py, quoted exactly as: 'BUG: division by zero on line 42 of compute.py' — no file patch is needed or appropriate here."* 5002+100 tokens, $0.0165, 3.8s, first_token 1705ms. Observability lineage proven: single `op-019da893-d0aa-7191-b15d-443476691668-cau` threaded cleanly through `attachments_hoisted` → `multi_modal` → Claude API → generation stream. Full session scorecard: 12 commits, 1 new launcher (`scripts/ferrari.sh`), 1 new regression driver (`scripts/attach_livefire.py`), 1 new adapter (`backend/vision/ocr_adapter.py`), 4 schema contract fixes, 1 event-loop-starvation fix, 1 envelope-validation relaxation. Manifesto §1 Unified Organism — Mind perceives what Senses captured, regardless of ingress.
